RESISTANCE SHEET tndiff [6.82 0]
RESISTANCE SHEET tpdiff [7.76 0]
RESISTANCE SHEET poly [7.9  0]
RESISTANCE SHEET metal6 [0.0178 0]
RESISTANCE SHEET ctm [0.307 0]
RESISTANCE SHEET metal5 [0.078 0]
RESISTANCE SHEET metal4 [0.078 0]
RESISTANCE SHEET metal3 [0.078 0]
RESISTANCE SHEET metal2 [0.078 0]
RESISTANCE SHEET metal1 [0.078 0]
PEX RESISTANCE PARAMETERS metal6 TC1 3.60e-3 TC2 -1.16e-7
PEX RESISTANCE PARAMETERS metal5 TC1 3.43e-3 TC2 -1.08e-6
PEX RESISTANCE PARAMETERS metal4 TC1 3.43e-3 TC2 -1.08e-6
PEX RESISTANCE PARAMETERS metal3 TC1 3.43e-3 TC2 -1.08e-6
PEX RESISTANCE PARAMETERS metal2 TC1 3.43e-3 TC2 -1.08e-6
PEX RESISTANCE PARAMETERS metal1 TC1 3.43e-3 TC2 -1.08e-6
PEX RESISTANCE PARAMETERS poly TC1 2.88e-3 TC2 5.01e-7
PEX RESISTANCE PARAMETERS tndiff TC1 3.35e-3 TC2 4.31e-7
PEX RESISTANCE PARAMETERS tpdiff TC1 3.44e-3 TC2 5.02e-7
CAPACITANCE ORDER tndiff tpdiff gate poly metal1 metal2 metal3 metal4 metal5 mim_block ctm metal6 
RESISTANCE CONNECTION metal1 tpdiff [0.484 0]
RESISTANCE CONNECTION metal1 tndiff [0.5324 0]
RESISTANCE CONNECTION metal1 poly [0.37752 0]
RESISTANCE CONNECTION metal2 metal1 [0.43264  0]
RESISTANCE CONNECTION metal3 metal2 [0.43264  0]
RESISTANCE CONNECTION metal4 metal3 [0.43264  0]
RESISTANCE CONNECTION metal5 metal4 [0.43264  0]
RESISTANCE CONNECTION metal6 metal5 [0.329184  0]
RESISTANCE CONNECTION metal6 ctm [0.329184  0]

// To specify density window size 
//
// Generated on Thu Jun 12 19:20:18 2008 by xCalibrate v2007.4_21.19 Thu Dec  6 22:10:39 PST 2007
//

// Comments: 

//----- [# USER CONTROL OPTIONS] -----

// UNIT CAPACITANCE                                ff
// UNIT INDUCTANCE                                 ph

PEX VIA CAPACITANCE                                YES // 'YES' (or omitting statement) turns on extraction of all vias and contacts - some test structures require 'NO'

// LAYER INFORMATION                               use 'PEX ALIAS cal_layer lvs_layer' to map names, where cal_layer is from this calibration and lvs_layer is layer from LVS connectivity extraction
//                                                 Also when mapping need to provide connectivity for all aliased cal_layers
// poly                                            conductor layer must have connectivity and be in CAPACITANCE ORDER statement
// metal1                                          conductor layer must have connectivity and be in CAPACITANCE ORDER statement
// metal2                                          conductor layer must have connectivity and be in CAPACITANCE ORDER statement
// metal3                                          conductor layer must have connectivity and be in CAPACITANCE ORDER statement
// metal4                                          conductor layer must have connectivity and be in CAPACITANCE ORDER statement
// metal5                                          conductor layer must have connectivity and be in CAPACITANCE ORDER statement
// ctm                                             conductor layer must have connectivity and be in CAPACITANCE ORDER statement
// metal6                                          conductor layer must have connectivity and be in CAPACITANCE ORDER statement

// PROFILE INFORMATION                            
// #define $XCAL_EXCL_active                       // define this to remove diffusion to metal coupling or set XCAL_EXCL_active environment variable beforeband

// #define $XCAL_EXCL_INDIE                        // uncomment this line to switch off indie variation (widths, thicknesses, etc)
                                                   // or set XCAL_EXCL_INDIE environment variable before invoking calibre

// MAX WINDOW SETTINGS                             uncomment 1 of the following definitions or set XCAL_MAXD_MODE environment variable beforehand
// #define $XCAL_MAXD_MODE smart                   // for typical layouts (default)
// #define $XCAL_MAXD_MODE maximum                 // for test structures
// #define $XCAL_MAXD_MODE classic                 // for backwards compatibility (not recommended)

//----- [# END USER CONTROL OPTIONS] -----
//

//
// Technology name: cr018g_1p6m_typical
// Precision: 1000
// DBU Size: 1e-09
// DB Magnification: 1
// Capacitance Units: ff
// Inductance Units: ph
// Background Dielectric: 1
//
#DEFINE $XCAL_USE_PRODUCTION_VIA 
#ifdef $XCALIBRATE_INCLUDE_OLD_VIA
    #define $XCAL_USE_PRODUCTION_VIA
#endif // ifdef $XCALIBRATE_INCLUDE_OLD_VIA
#ifdef $XCALIBRATE_MAXD_MODE
    #ifdef $XCAL_MAXD_MODE
        SVRF MESSAGE "Note: $XCAL_MAXD_MODE overrides $XCALIBRATE_MAXD_MODE; ignoring $XCALIBRATE_MAXD_MODE setting"
    #else
        #ifdef $XCALIBRATE_MAXD_MODE smart
        #define $XCAL_MAXD_MODE smart
        #endif
        #ifdef $XCALIBRATE_MAXD_MODE classic
        #define $XCAL_MAXD_MODE classic
        #endif
        #ifdef $XCALIBRATE_MAXD_MODE maximum
        #define $XCAL_MAXD_MODE maximum
        #endif
        #ifdef $XCALIBRATE_MAXD_MODE ASIC_smart
        #define $XCAL_MAXD_MODE ASIC_smart
        #endif
        #ifdef $XCALIBRATE_MAXD_MODE ASIC_fill
        #define $XCAL_MAXD_MODE ASIC_fill
        #endif
    #endif // if $XCAL_MAXD_MODE
#endif // ifdef $XCALIBRATE_MAXD_MODE
#ifndef $XCAL_MAXD_MODE
#define $XCAL_MAXD_MODE smart // set max_distance type unless already set from shell - smart, classic or maximum
#endif // ifndef $XCAL_MAXD_MODE
#ifdef $XCAL_MAXD_MODE smart
    SVRF MESSAGE "Note: Using capacitance max distance mode 'smart'"
    VARIABLE MAXDISTANCE_poly 1 // Warning; true two-pitch value smaller than recommended limit (1u)
    VARIABLE MAXWIDTH_poly 1 // Warning; true two-pitch value smaller than recommended limit (1u)
    VARIABLE MAXENCLOSE_poly 1 // Warning; true two-pitch value smaller than recommended limit (1u)
    VARIABLE MAXRADIUS_poly 500

    VARIABLE MAXDISTANCE_metal1 1 // Warning; true two-pitch value smaller than recommended limit (1u)
    VARIABLE MAXWIDTH_metal1 1 // Warning; true two-pitch value smaller than recommended limit (1u)
    VARIABLE MAXENCLOSE_metal1 1 // Warning; true two-pitch value smaller than recommended limit (1u)
    VARIABLE MAXRADIUS_metal1 500

    VARIABLE MAXDISTANCE_metal2 1.12
    VARIABLE MAXWIDTH_metal2 1.12
    VARIABLE MAXENCLOSE_metal2 1.12
    VARIABLE MAXRADIUS_metal2 500

    VARIABLE MAXDISTANCE_metal3 1.12
    VARIABLE MAXWIDTH_metal3 1.12
    VARIABLE MAXENCLOSE_metal3 1.12
    VARIABLE MAXRADIUS_metal3 500

    VARIABLE MAXDISTANCE_metal4 1.12
    VARIABLE MAXWIDTH_metal4 1.12
    VARIABLE MAXENCLOSE_metal4 1.12
    VARIABLE MAXRADIUS_metal4 500

    VARIABLE MAXDISTANCE_metal5 1.12
    VARIABLE MAXWIDTH_metal5 1.12
    VARIABLE MAXENCLOSE_metal5 1.12
    VARIABLE MAXRADIUS_metal5 500

    VARIABLE MAXDISTANCE_ctm 5.2 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXWIDTH_ctm 5.2 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXENCLOSE_ctm 5.2 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXRADIUS_ctm 420

    VARIABLE MAXDISTANCE_metal6 3 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXWIDTH_metal6 3 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXENCLOSE_metal6 3 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXRADIUS_metal6 450

#endif // ifdef $XCAL_MAXD_MODE smart
#ifdef $XCAL_MAXD_MODE classic
    SVRF MESSAGE "Note: Using capacitance max distance mode 'classic'"
    VARIABLE MAXDISTANCE_poly 3
    VARIABLE MAXWIDTH_poly 3
    VARIABLE MAXENCLOSE_poly 3
    VARIABLE MAXRADIUS_poly 500

    VARIABLE MAXDISTANCE_metal1 3
    VARIABLE MAXWIDTH_metal1 3
    VARIABLE MAXENCLOSE_metal1 3
    VARIABLE MAXRADIUS_metal1 500

    VARIABLE MAXDISTANCE_metal2 3
    VARIABLE MAXWIDTH_metal2 3
    VARIABLE MAXENCLOSE_metal2 3
    VARIABLE MAXRADIUS_metal2 500

    VARIABLE MAXDISTANCE_metal3 3
    VARIABLE MAXWIDTH_metal3 3
    VARIABLE MAXENCLOSE_metal3 3
    VARIABLE MAXRADIUS_metal3 500

    VARIABLE MAXDISTANCE_metal4 3
    VARIABLE MAXWIDTH_metal4 3
    VARIABLE MAXENCLOSE_metal4 3
    VARIABLE MAXRADIUS_metal4 500

    VARIABLE MAXDISTANCE_metal5 3
    VARIABLE MAXWIDTH_metal5 3
    VARIABLE MAXENCLOSE_metal5 3
    VARIABLE MAXRADIUS_metal5 500

    VARIABLE MAXDISTANCE_ctm 3
    VARIABLE MAXWIDTH_ctm 3
    VARIABLE MAXENCLOSE_ctm 3
    VARIABLE MAXRADIUS_ctm 420

    VARIABLE MAXDISTANCE_metal6 3
    VARIABLE MAXWIDTH_metal6 3
    VARIABLE MAXENCLOSE_metal6 3
    VARIABLE MAXRADIUS_metal6 450

#endif // ifdef $XCAL_MAXD_MODE classic
#ifdef $XCAL_MAXD_MODE maximum
    SVRF MESSAGE "Note: Using capacitance max distance mode 'maximum'"
    VARIABLE MAXDISTANCE_poly 5
    VARIABLE MAXWIDTH_poly 0.64
    VARIABLE MAXENCLOSE_poly 5
    VARIABLE MAXRADIUS_poly 500

    VARIABLE MAXDISTANCE_metal1 5
    VARIABLE MAXWIDTH_metal1 5
    VARIABLE MAXENCLOSE_metal1 5
    VARIABLE MAXRADIUS_metal1 500

    VARIABLE MAXDISTANCE_metal2 5
    VARIABLE MAXWIDTH_metal2 5
    VARIABLE MAXENCLOSE_metal2 5
    VARIABLE MAXRADIUS_metal2 500

    VARIABLE MAXDISTANCE_metal3 5
    VARIABLE MAXWIDTH_metal3 5
    VARIABLE MAXENCLOSE_metal3 5
    VARIABLE MAXRADIUS_metal3 500

    VARIABLE MAXDISTANCE_metal4 5
    VARIABLE MAXWIDTH_metal4 5
    VARIABLE MAXENCLOSE_metal4 5
    VARIABLE MAXRADIUS_metal4 500

    VARIABLE MAXDISTANCE_metal5 5
    VARIABLE MAXWIDTH_metal5 5
    VARIABLE MAXENCLOSE_metal5 5
    VARIABLE MAXRADIUS_metal5 500

    VARIABLE MAXDISTANCE_ctm 4.2
    VARIABLE MAXWIDTH_ctm 4.2
    VARIABLE MAXENCLOSE_ctm 4.2
    VARIABLE MAXRADIUS_ctm 420

    VARIABLE MAXDISTANCE_metal6 4.5
    VARIABLE MAXWIDTH_metal6 4.5
    VARIABLE MAXENCLOSE_metal6 4.5
    VARIABLE MAXRADIUS_metal6 450

#endif // ifdef $XCAL_MAXD_MODE maximum
#ifdef $XCAL_MAXD_MODE ASIC_smart
    SVRF MESSAGE "Note: Using capacitance max distance mode 'ASIC_smart'"
    VARIABLE MAXDISTANCE_poly 0.86
    VARIABLE MAXWIDTH_poly 0.86
    VARIABLE MAXENCLOSE_poly 0.86
    VARIABLE MAXRADIUS_poly 500

    VARIABLE MAXDISTANCE_metal1 0.92
    VARIABLE MAXWIDTH_metal1 0.92
    VARIABLE MAXENCLOSE_metal1 0.92
    VARIABLE MAXRADIUS_metal1 500

    VARIABLE MAXDISTANCE_metal2 1.12
    VARIABLE MAXWIDTH_metal2 1.12
    VARIABLE MAXENCLOSE_metal2 1.12
    VARIABLE MAXRADIUS_metal2 500

    VARIABLE MAXDISTANCE_metal3 1.12
    VARIABLE MAXWIDTH_metal3 1.12
    VARIABLE MAXENCLOSE_metal3 1.12
    VARIABLE MAXRADIUS_metal3 500

    VARIABLE MAXDISTANCE_metal4 1.12
    VARIABLE MAXWIDTH_metal4 1.12
    VARIABLE MAXENCLOSE_metal4 1.12
    VARIABLE MAXRADIUS_metal4 500

    VARIABLE MAXDISTANCE_metal5 1.12
    VARIABLE MAXWIDTH_metal5 1.12
    VARIABLE MAXENCLOSE_metal5 1.12
    VARIABLE MAXRADIUS_metal5 500

    VARIABLE MAXDISTANCE_ctm 5.2 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXWIDTH_ctm 5.2 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXENCLOSE_ctm 5.2 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXRADIUS_ctm 420

    VARIABLE MAXDISTANCE_metal6 3 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXWIDTH_metal6 3 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXENCLOSE_metal6 3 // Warning; value less than true two-pitch (exceeded limits)
    VARIABLE MAXRADIUS_metal6 450

#endif // ifdef $XCAL_MAXD_MODE ASIC_smart
#ifdef $XCAL_MAXD_MODE ASIC_fill
    SVRF MESSAGE "Note: Using capacitance max distance mode 'ASIC_fill'"
    VARIABLE MAXDISTANCE_poly 0.4
    VARIABLE MAXWIDTH_poly 0.4
    VARIABLE MAXENCLOSE_poly 0.4
    VARIABLE MAXRADIUS_poly 500

    VARIABLE MAXDISTANCE_metal1 1.06
    VARIABLE MAXWIDTH_metal1 1.06
    VARIABLE MAXENCLOSE_metal1 1.06
    VARIABLE MAXRADIUS_metal1 500

    VARIABLE MAXDISTANCE_metal2 1.06
    VARIABLE MAXWIDTH_metal2 1.06
    VARIABLE MAXENCLOSE_metal2 1.06
    VARIABLE MAXRADIUS_metal2 500

    VARIABLE MAXDISTANCE_metal3 1.06
    VARIABLE MAXWIDTH_metal3 1.06
    VARIABLE MAXENCLOSE_metal3 1.06
    VARIABLE MAXRADIUS_metal3 500

    VARIABLE MAXDISTANCE_metal4 1.06
    VARIABLE MAXWIDTH_metal4 1.06
    VARIABLE MAXENCLOSE_metal4 1.06
    VARIABLE MAXRADIUS_metal4 500

    VARIABLE MAXDISTANCE_metal5 1.06
    VARIABLE MAXWIDTH_metal5 1.06
    VARIABLE MAXENCLOSE_metal5 1.06
    VARIABLE MAXRADIUS_metal5 500

    VARIABLE MAXDISTANCE_ctm 0.38
    VARIABLE MAXWIDTH_ctm 0.38
    VARIABLE MAXENCLOSE_ctm 0.38
    VARIABLE MAXRADIUS_ctm 420

    VARIABLE MAXDISTANCE_metal6 4.68
    VARIABLE MAXWIDTH_metal6 4.68
    VARIABLE MAXENCLOSE_metal6 4.68
    VARIABLE MAXRADIUS_metal6 450

#endif // ifdef $XCAL_MAXD_MODE ASIC_fill
#ifndef $XCAL_MAXD_MODE
SVRF ERROR "Missing $XCAL_MAXD_MODE setting"
#endif // ifndef $XCAL_MAXD_MODE

// Following line(s) should be in your rulesfile
// UNIT CAPACITANCE ff
// UNIT INDUCTANCE ph
// Above line(s) should be in your rulesfile

VARIABLE EPS_ENC 0.01
VARIABLE m34RS 1
VARIABLE m36RS 0
VARIABLE m37RS 1
VARIABLE m33RS 0
VARIABLE m33NB_COEF 1
VARIABLE m33IN_COEF 1
VARIABLE m33IN0_COEF 1
VARIABLE m34NB_COEF 1
VARIABLE m34IN_COEF 1
VARIABLE m34IN0_COEF 1
VARIABLE m36NB_COEF 1
VARIABLE m36IN_COEF 1
VARIABLE m36IN0_COEF 1
VARIABLE m37NB_COEF 1
VARIABLE m37IN_COEF 1
VARIABLE m37IN0_COEF 1
// 
// Profile = field
// 
// Layers =  (field_base field_base_diel poly poly_diel poly_diel2 metal1 metal1_diel metal1_diel2 metal2 metal2_diel metal2_diel2 metal3 metal3_diel metal3_diel2 metal4 metal4_diel metal4_diel2 metal5 metal5_diel ctm ctm_diel ctm_diel2 metal6 metal6_diel metal6_diel2 metal6_diel3)
// 
//     Z-COORD              NAME TYPE THICKNESS   DIEL   WIDTH   SPACE      WIDTH   SPACE    BIAS ENC      NONPLANAR DIELECTRIC PARMS
//                                                 ACTUAL  ACTUAL     DRAWN   DRAWN                   TYPE HEIGHT SWTHK   SWSLOPE  GAP  MIN   TYPE
//                                                                                                                                 TYPE GAP  PARMS
//  --- 13.7400 ---------------------------------------------------------------------------------------------------------------------------------------------
//  |   11.4000     metal6_diel3   D    0.6000   7.9000                                                C  2.9400   0.6000  90.0000                                               
//  --- 10.8000 ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 13.1400 ---------------------------------------------------------------------------------------------------------------------------------------------
//  |   10.8000     metal6_diel2   D    0.1500   4.2000                                                C  2.4900   0.1500  90.0000                                               
//  --- 10.6500 ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 12.9900 ---------------------------------------------------------------------------------------------------------------------------------------------
//  |   10.6500      metal6_diel   D    2.5000   4.2000                                                C  4.8400   2.5000  90.0000                                               
//  --- 8.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 10.4900 ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal6   C    2.3400           1.5000  1.5000 
//  --- 8.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                  ctm_diel2   D    0.3500   4.2000      
//  --- 7.8000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                   ctm_diel   D    0.4410   3.7000      
//  --- 7.3590  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                        ctm   C    0.1900           4.0000  1.2000 
//  --- 7.1690  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal5_diel   D    0.0190   3.7000      
//  --- 7.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal5   C    0.5300           0.2800  0.2800 
//  --- 6.6200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel2   D    0.2000   4.2000      
//  --- 6.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal4_diel   D    0.6500   3.7000      
//  --- 5.7700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal4   C    0.5300           0.2800  0.2800 
//  --- 5.2400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel2   D    0.2000   4.2000      
//  --- 5.0400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal3_diel   D    0.6500   3.7000      
//  --- 4.3900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal3   C    0.5300           0.2800  0.2800 
//  --- 3.8600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel2   D    0.2000   4.2000      
//  --- 3.6600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal2_diel   D    0.6500   3.7000      
//  --- 3.0100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal2   C    0.5300           0.2800  0.2800 
//  --- 2.4800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel2   D    0.2000   4.2000      
//  --- 2.2800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal1_diel   D    0.6500   3.7000      
//  --- 1.6300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal1   C    0.5300           0.2300  0.2300 
//  --- 1.1000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                 poly_diel2   D    0.5500   4.0000      
//  --- 0.5500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                  poly_diel   D    0.0000   4.0000      
//  --- 0.5500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       poly   C    0.2000           0.1600  0.2700     0.1800  0.2500   -0.0200
//  --- 0.3500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |            field_base_diel   D    0.3500   3.9000      
//  --- 0.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                 field_base   B    1.0000
//  --- -1.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  ===========================
//  Non-planar Z coordinate map
//  ===========================
// 
//  --- 13.7400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)
//  --- 13.1400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)    metal6_diel2 (4.2000)
//  --- 12.9900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)    metal6_diel2 (4.2000)     metal6_diel (4.2000)
//  --- 11.4000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)    metal6_diel2 (4.2000)     metal6_diel (4.2000)
//  --- 10.8000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                                         metal6_diel2 (4.2000)     metal6_diel (4.2000)
//  --- 10.6500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                                                                   metal6_diel (4.2000)
//  --- 10.4900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                               metal6                              metal6_diel (4.2000)
//  --- 8.1500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   ctm_diel2 (4.2000)                                                  
//  --- 7.8000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                    ctm_diel (3.7000)                                                  
//  --- 7.3590 ---------------------------------------------------------------------------------------------------------------------------------------------
//                    ctm_diel (3.7000)                      ctm                         
//  --- 7.1690 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                                                  
//  --- 7.1500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                   metal5                         
//  --- 6.6200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel2 (4.2000)                                                  
//  --- 6.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                                                  
//  --- 5.7700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                   metal4                         
//  --- 5.2400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel2 (4.2000)                                                  
//  --- 5.0400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                                                  
//  --- 4.3900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                   metal3                         
//  --- 3.8600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel2 (4.2000)                                                  
//  --- 3.6600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                                                  
//  --- 3.0100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                   metal2                         
//  --- 2.4800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel2 (4.2000)                                                  
//  --- 2.2800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (3.7000)                                                  
//  --- 1.6300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (3.7000)                   metal1                         
//  --- 1.1000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                  poly_diel2 (4.0000)                                                  
//  --- 0.5500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   poly_diel (4.0000)                     poly                         
//  --- 0.3500 ---------------------------------------------------------------------------------------------------------------------------------------------
//             field_base_diel (3.9000)                                                  
//  --- 0.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                           field_base                                                  
// 
// Profile = active
// Auxiliary layers = tndiff tpdiff
// 
// Layers =  (active_base active_base_diel metal1 metal1_diel metal1_diel2 metal2 metal2_diel metal2_diel2 metal3 metal3_diel metal3_diel2 metal4 metal4_diel metal4_diel2 metal5 metal5_diel ctm ctm_diel ctm_diel2 metal6 metal6_diel metal6_diel2 metal6_diel3)
// 
//     Z-COORD              NAME TYPE THICKNESS   DIEL   WIDTH   SPACE  ENC      NONPLANAR DIELECTRIC PARMS
//                                                                          TYPE HEIGHT SWTHK   SWSLOPE  GAP  MIN   TYPE
//                                                                                                       TYPE GAP  PARMS
//  --- 13.4400 ------------------------------------------------------------------------------------------------------------------
//  |   11.1000     metal6_diel3   D    0.6000   7.9000                      C  2.9400   0.6000  90.0000                                               
//  --- 10.5000 ------------------------------------------------------------------------------------------------------------------
// 
//  --- 12.8400 ------------------------------------------------------------------------------------------------------------------
//  |   10.5000     metal6_diel2   D    0.1500   4.2000                      C  2.4900   0.1500  90.0000                                               
//  --- 10.3500 ------------------------------------------------------------------------------------------------------------------
// 
//  --- 12.6900 ------------------------------------------------------------------------------------------------------------------
//  |   10.3500      metal6_diel   D    2.5000   4.2000                      C  4.8400   2.5000  90.0000                                               
//  --- 7.8500  ------------------------------------------------------------------------------------------------------------------
// 
//  --- 10.1900 ------------------------------------------------------------------------------------------------------------------
//  |                     metal6   C    2.3400           1.5000  1.5000 
//  --- 7.8500  ------------------------------------------------------------------------------------------------------------------
//  |                  ctm_diel2   D    0.3500   4.2000      
//  --- 7.5000  ------------------------------------------------------------------------------------------------------------------
//  |                   ctm_diel   D    0.4410   3.7000      
//  --- 7.0590  ------------------------------------------------------------------------------------------------------------------
//  |                        ctm   C    0.1900           4.0000  1.2000 
//  --- 6.8690  ------------------------------------------------------------------------------------------------------------------
//  |                metal5_diel   D    0.0190   3.7000      
//  --- 6.8500  ------------------------------------------------------------------------------------------------------------------
//  |                     metal5   C    0.5300           0.2800  0.2800 
//  --- 6.3200  ------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel2   D    0.2000   4.2000      
//  --- 6.1200  ------------------------------------------------------------------------------------------------------------------
//  |                metal4_diel   D    0.6500   3.7000      
//  --- 5.4700  ------------------------------------------------------------------------------------------------------------------
//  |                     metal4   C    0.5300           0.2800  0.2800 
//  --- 4.9400  ------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel2   D    0.2000   4.2000      
//  --- 4.7400  ------------------------------------------------------------------------------------------------------------------
//  |                metal3_diel   D    0.6500   3.7000      
//  --- 4.0900  ------------------------------------------------------------------------------------------------------------------
//  |                     metal3   C    0.5300           0.2800  0.2800 
//  --- 3.5600  ------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel2   D    0.2000   4.2000      
//  --- 3.3600  ------------------------------------------------------------------------------------------------------------------
//  |                metal2_diel   D    0.6500   3.7000      
//  --- 2.7100  ------------------------------------------------------------------------------------------------------------------
//  |                     metal2   C    0.5300           0.2800  0.2800 
//  --- 2.1800  ------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel2   D    0.2000   4.2000      
//  --- 1.9800  ------------------------------------------------------------------------------------------------------------------
//  |                metal1_diel   D    0.6500   3.7000      
//  --- 1.3300  ------------------------------------------------------------------------------------------------------------------
//  |                     metal1   C    0.5300           0.2300  0.2300 
//  --- 0.8000  ------------------------------------------------------------------------------------------------------------------
//  |           active_base_diel   D    0.8000   4.0000      
//  --- 0.0000  ------------------------------------------------------------------------------------------------------------------
//  |                active_base   B    1.0000
//  --- -1.0000 ------------------------------------------------------------------------------------------------------------------
// 
//  ===========================
//  Non-planar Z coordinate map
//  ===========================
// 
//  --- 13.4400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)
//  --- 12.8400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)    metal6_diel2 (4.2000)
//  --- 12.6900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)    metal6_diel2 (4.2000)     metal6_diel (4.2000)
//  --- 11.1000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (7.9000)    metal6_diel2 (4.2000)     metal6_diel (4.2000)
//  --- 10.5000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                                         metal6_diel2 (4.2000)     metal6_diel (4.2000)
//  --- 10.3500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                                                                   metal6_diel (4.2000)
//  --- 10.1900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                               metal6                              metal6_diel (4.2000)
//  --- 7.8500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   ctm_diel2 (4.2000)                                                  
//  --- 7.5000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                    ctm_diel (3.7000)                                                  
//  --- 7.0590 ---------------------------------------------------------------------------------------------------------------------------------------------
//                    ctm_diel (3.7000)                      ctm                         
//  --- 6.8690 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                                                  
//  --- 6.8500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                   metal5                         
//  --- 6.3200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel2 (4.2000)                                                  
//  --- 6.1200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                                                  
//  --- 5.4700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                   metal4                         
//  --- 4.9400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel2 (4.2000)                                                  
//  --- 4.7400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                                                  
//  --- 4.0900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                   metal3                         
//  --- 3.5600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel2 (4.2000)                                                  
//  --- 3.3600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                                                  
//  --- 2.7100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                   metal2                         
//  --- 2.1800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel2 (4.2000)                                                  
//  --- 1.9800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (3.7000)                                                  
//  --- 1.3300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (3.7000)                   metal1                         
//  --- 0.8000 ---------------------------------------------------------------------------------------------------------------------------------------------
//             active_base_diel (4.0000)                                                  
//  --- 0.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                          active_base                                                  

VARIABLE default_profileid 0
PEX PROFILE default default_profileid
[
    poly CONDUCTOR 0.35 0.55
    metal1 CONDUCTOR 1.1 1.63
    metal2 CONDUCTOR 2.48 3.01
    metal3 CONDUCTOR 3.86 4.39
    metal4 CONDUCTOR 5.24 5.77
    metal5 CONDUCTOR 6.62 7.15
    ctm CONDUCTOR 7.169 7.359
    metal6 CONDUCTOR 8.15 10.49
    field_base_diel PLANAR 0 0.35 3.9
    poly_diel PLANAR 0.35 0.55 4
    poly_diel2 PLANAR 0.55 1.1 4
    metal1_diel PLANAR 1.1 2.28 3.7
    metal1_diel2 PLANAR 2.28 2.48 4.2
    metal2_diel PLANAR 2.48 3.66 3.7
    metal2_diel2 PLANAR 3.66 3.86 4.2
    metal3_diel PLANAR 3.86 5.04 3.7
    metal3_diel2 PLANAR 5.04 5.24 4.2
    metal4_diel PLANAR 5.24 6.42 3.7
    metal4_diel2 PLANAR 6.42 6.62 4.2
    metal5_diel PLANAR 6.62 7.169 3.7
    ctm_diel PLANAR 7.169 7.8 3.7
    ctm_diel2 PLANAR 7.8 8.15 4.2
    metal6_diel CONFORMAL metal6 8.15 10.49 2.5 2.5 2.5 4.2
    metal6_diel2 CONFORMAL metal6_diel 8.15 12.99 0.15 0.15 0.15 4.2
    metal6_diel3 CONFORMAL metal6_diel2 10.65 13.14 0.6 0.6 0.6 7.9
]


VARIABLE field_profileid 1
PEX PROFILE field field_profileid
[
    poly CONDUCTOR 0.35 0.55
    metal1 CONDUCTOR 1.1 1.63
    metal2 CONDUCTOR 2.48 3.01
    metal3 CONDUCTOR 3.86 4.39
    metal4 CONDUCTOR 5.24 5.77
    metal5 CONDUCTOR 6.62 7.15
    ctm CONDUCTOR 7.169 7.359
    metal6 CONDUCTOR 8.15 10.49
    field_base_diel PLANAR 0 0.35 3.9
    poly_diel PLANAR 0.35 0.55 4
    poly_diel2 PLANAR 0.55 1.1 4
    metal1_diel PLANAR 1.1 2.28 3.7
    metal1_diel2 PLANAR 2.28 2.48 4.2
    metal2_diel PLANAR 2.48 3.66 3.7
    metal2_diel2 PLANAR 3.66 3.86 4.2
    metal3_diel PLANAR 3.86 5.04 3.7
    metal3_diel2 PLANAR 5.04 5.24 4.2
    metal4_diel PLANAR 5.24 6.42 3.7
    metal4_diel2 PLANAR 6.42 6.62 4.2
    metal5_diel PLANAR 6.62 7.169 3.7
    ctm_diel PLANAR 7.169 7.8 3.7
    ctm_diel2 PLANAR 7.8 8.15 4.2
    metal6_diel CONFORMAL metal6 8.15 10.49 2.5 2.5 2.5 4.2
    metal6_diel2 CONFORMAL metal6_diel 8.15 12.99 0.15 0.15 0.15 4.2
    metal6_diel3 CONFORMAL metal6_diel2 10.65 13.14 0.6 0.6 0.6 7.9
]


VARIABLE active_profileid 2
PEX PROFILE active active_profileid
[
    metal1 CONDUCTOR 0.8 1.33
    metal2 CONDUCTOR 2.18 2.71
    metal3 CONDUCTOR 3.56 4.09
    metal4 CONDUCTOR 4.94 5.47
    metal5 CONDUCTOR 6.32 6.85
    ctm CONDUCTOR 6.869 7.059
    metal6 CONDUCTOR 7.85 10.19
    active_base_diel PLANAR 0 0.8 4
    metal1_diel PLANAR 0.8 1.98 3.7
    metal1_diel2 PLANAR 1.98 2.18 4.2
    metal2_diel PLANAR 2.18 3.36 3.7
    metal2_diel2 PLANAR 3.36 3.56 4.2
    metal3_diel PLANAR 3.56 4.74 3.7
    metal3_diel2 PLANAR 4.74 4.94 4.2
    metal4_diel PLANAR 4.94 6.12 3.7
    metal4_diel2 PLANAR 6.12 6.32 4.2
    metal5_diel PLANAR 6.32 6.869 3.7
    ctm_diel PLANAR 6.869 7.5 3.7
    ctm_diel2 PLANAR 7.5 7.85 4.2
    metal6_diel CONFORMAL metal6 7.85 10.19 2.5 2.5 2.5 4.2
    metal6_diel2 CONFORMAL metal6_diel 7.85 12.69 0.15 0.15 0.15 4.2
    metal6_diel3 CONFORMAL metal6_diel2 10.35 12.84 0.6 0.6 0.6 7.9
]


PEX MINIMUM PATH WIDTH 0.175 // = 0.18 - .005 DO NOT ADJUST

PEX XCALIBRATE VERSION CLASSIC v2007.4_21.19
PEX TABLE PIECEWISE DELTA_poly
[
 // CORNERS(1) = { typical }
    CORNERS(1) = { 0 }
    WIDTHS(2) = { 0.18 0.66 }
    SPACINGS(2) = { 0.25 4.98 }
    VALUE(4) = { -0.02 -0.02 -0.02 -0.02 }
]

PEX THICKNESS poly NOMINAL 0.200000

PEX THICKNESS metal1 NOMINAL 0.530000

PEX THICKNESS metal2 NOMINAL 0.530000

PEX THICKNESS metal3 NOMINAL 0.530000

PEX THICKNESS metal4 NOMINAL 0.530000

PEX THICKNESS metal5 NOMINAL 0.530000

PEX THICKNESS ctm NOMINAL 0.190000

PEX THICKNESS metal6 NOMINAL 2.340000

//
// Generated on Thu Jun 12 19:20:18 2008 by xCalibrate v2007.4_21.19 Thu Dec  6 22:10:39 PST 2007
//
// Resistance rules 

#ifdef $XCALIBRATE_EXCLUDE_PROFILE_field
    #define $XCAL_EXCL_PROF_field
#endif // ifdef $XCALIBRATE_EXCLUDE_PROFILE_field
#ifdef $XCALIBRATE_INCLUDE_PROFILE_field
    #define $XCAL_INCL_PROF_field
#endif // ifdef $XCALIBRATE_INCLUDE_PROFILE_field
#ifndef $XCAL_EXCL_PROF_field
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,poly
//
CAPACITANCE INTRINSIC PLATE poly MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0988951 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal1
//
CAPACITANCE INTRINSIC PLATE metal1 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0319826 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal2
//
CAPACITANCE INTRINSIC PLATE metal2 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0137589 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal3
//
CAPACITANCE INTRINSIC PLATE metal3 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00876735 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal4
//
CAPACITANCE INTRINSIC PLATE metal4 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0064334 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal5
//
CAPACITANCE INTRINSIC PLATE metal5 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00508081 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,ctm
//
CAPACITANCE INTRINSIC PLATE ctm MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00468424 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal6
//
CAPACITANCE INTRINSIC PLATE metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0041283 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER PLATE poly metal1 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0645633 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER PLATE poly metal2 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0175938 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER PLATE poly metal3 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0101864 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER PLATE poly metal4 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00716836 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER PLATE poly metal5 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00552994 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE CROSSOVER PLATE poly ctm MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0050614 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER PLATE poly metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00442114 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER PLATE metal1 metal2 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0397126 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER PLATE metal1 metal3 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.015023 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER PLATE metal1 metal4 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0092654 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER PLATE metal1 metal5 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00669827 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE CROSSOVER PLATE metal1 ctm MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0060234 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER PLATE metal1 metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00513699 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER PLATE metal2 metal3 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0397126 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER PLATE metal2 metal4 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.015023 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER PLATE metal2 metal5 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0092654 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE CROSSOVER PLATE metal2 ctm MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00802181 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER PLATE metal2 metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00652286 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER PLATE metal3 metal4 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0397126 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER PLATE metal3 metal5 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.015023 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE CROSSOVER PLATE metal3 ctm MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0120047 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER PLATE metal3 metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.00893277 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER PLATE metal4 metal5 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0397126 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE CROSSOVER PLATE metal4 ctm MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0238582 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER PLATE metal4 metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0141668 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE CROSSOVER PLATE metal5 ctm MASK
[
    PROPERTY C
    profile = field_profileid
    C = 1.72423 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER PLATE metal5 metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.034238 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE CROSSOVER PLATE ctm metal6 MASK
[
    PROPERTY C
    profile = field_profileid
    C = 0.0437868 * area()
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,poly
//
CAPACITANCE INTRINSIC FRINGE poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.119845 * (1 - exp(-1.31842 * (distance() + 0.103046))) * pow(width() , 0.00117584 * distance() + 0.0413752) * (0.225145 * thickness() + 0.35594) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0449802 * (1 - exp(-1.29869 * (distance() + 0.061821))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.18641 * pow(width() , 0.0813883) * (0.257768 * thickness() + 0.22474) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0491268 * pow(width() , 0.04756) * (1 - 0.456031 * exp(-0.512616 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,poly
//
CAPACITANCE NEARBODY poly WITH SHIELD poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.35097 - 1.57879 * distance()) + 0.0701288 / pow(distance() , 1.14454)) * 3.01824 * pow((width1() + width2()) / 2 , 0.144214) * (0.10961 * (thickness1() + thickness2()) / 2 + 0.0203993) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,metal1
//
CAPACITANCE INTRINSIC FRINGE metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.139243 * (1 - exp(-0.509137 * (distance() + 0.214663))) * pow(width() , 0.0108362 * distance() + -0.00670801) * (0.0416194 * thickness() + 0.274636) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.038447 * (1 - exp(-0.548742 * (distance() + 0.0826065))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.154654 * pow(width() , 0.0706474) * (0.0954824 * thickness() + 0.23222) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.1))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.1)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.042069 * pow(width() , 0.025151) * (1 - 0.458846 * exp(-0.227538 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,metal1
//
CAPACITANCE NEARBODY metal1 WITH SHIELD metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.951266 - 0.823046 * distance()) + 0.290124 / pow(distance() , 0.993291)) * 3.07186 * pow((width1() + width2()) / 2 , 0.0942054) * (0.0309033 * (thickness1() + thickness2()) / 2 + 0.00995159) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.1))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.1)))
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,metal2
//
CAPACITANCE INTRINSIC FRINGE metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.123064 * (1 - exp(-0.290545 * (distance() + 0.514558))) * pow(width() , 0.0148877 * distance() + -0.0359266) * (0.0177197 * thickness() + 0.254677) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0299407 * (1 - exp(-0.31802 * (distance() + 0.192586))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.141837 * pow(width() , 0.0798222) * (0.0683053 * thickness() + 0.210907) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.48))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.48)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0339493 * pow(width() , 0.0195834) * (1 - 0.536351 * exp(-0.141453 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,metal2
//
CAPACITANCE NEARBODY metal2 WITH SHIELD metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.1622 - 0.450324 * distance()) + 0.508329 / pow(distance() , 0.86947)) * 3.97239 * pow((width1() + width2()) / 2 , 0.120474) * (0.0165866 * (thickness1() + thickness2()) / 2 + 0.00730178) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.48))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.48)))
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,metal3
//
CAPACITANCE INTRINSIC FRINGE metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0998504 * (1 - exp(-0.249753 * (distance() + 0.749804))) * pow(width() , 0.0133686 * distance() + -0.0425848) * (0.00704473 * thickness() + 0.258037) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0248717 * (1 - exp(-0.251329 * (distance() + 0.278091))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.13561 * pow(width() , 0.0800783) * (0.0562186 * thickness() + 0.198867) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.86))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.86)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0302412 * pow(width() , 0.0131622) * (1 - 0.560984 * exp(-0.09941 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,metal3
//
CAPACITANCE NEARBODY metal3 WITH SHIELD metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.323891 - 0.280957 * distance()) + 1.5564 / pow(distance() , 0.835562)) * 4.01426 * pow((width1() + width2()) / 2 , 0.128674) * (0.00570754 * (thickness1() + thickness2()) / 2 + 0.00267216) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.86))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.86)))
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,metal4
//
CAPACITANCE INTRINSIC FRINGE metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0782718 * (1 - exp(-0.25905 * (distance() + 0.896952))) * pow(width() , 0.0100116 * distance() + -0.0409672) * (-0.00077122 * thickness() + 0.265608) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0210445 * (1 - exp(-0.223042 * (distance() + 0.340995))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.13158 * pow(width() , 0.0783565) * (0.0488227 * thickness() + 0.189506) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 5.24))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 5.24)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0280201 * pow(width() , 0.00680141) * (1 - 0.578868 * exp(-0.0740974 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,metal4
//
CAPACITANCE NEARBODY metal4 WITH SHIELD metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(0.523024 - 0.19159 * distance()) + 3.94908 / pow(distance() , 0.824385)) * 4.1022 * pow((width1() + width2()) / 2 , 0.132689) * (0.002239 * (thickness1() + thickness2()) / 2 + 0.00107973) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 5.24))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 5.24)))
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,metal5
//
CAPACITANCE INTRINSIC FRINGE metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0624422 * (1 - exp(-0.280701 * (distance() + 1.01054))) * pow(width() , 0.00716061 * distance() + -0.0418965) * (-0.00187453 * thickness() + 0.27002) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.62))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.62)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0182244 * (1 - exp(-0.203882 * (distance() + 0.393003))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.62))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.62)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.128416 * pow(width() , 0.0757464) * (0.0429699 * thickness() + 0.182273) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.62))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.62)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0256909 * pow(width() , 0) * (1 - 0.642456 * exp(-0.0855783 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,metal5
//
CAPACITANCE NEARBODY metal5 WITH SHIELD metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(1.13233 - 0.142665 * distance()) + 6.82725 / pow(distance() , 0.827487)) * 4.17976 * pow((width1() + width2()) / 2 , 0.136651) * (0.00125124 * (thickness1() + thickness2()) / 2 + 0.00061776) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.62))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.62)))
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,ctm
//
CAPACITANCE INTRINSIC FRINGE ctm MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    max_distance = MAXDISTANCE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.112043 * (1 - exp(-0.969202 * (distance() + 0.325144))) * pow(width() , 0.100218 * distance() + -0.543782) * (0.00391516 * thickness() + 0.112419) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.169))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.169)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0425026 * (1 - exp(-0.0548937 * (distance() + 0.344357))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.169))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.169)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.144157 * pow(width() , 0.0500674) * (0.0292162 * thickness() + 0.174867) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 7.169))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 7.169)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0298547 * pow(width() , 0.0149914) * (1 - 0.876917 * exp(-0.0625048 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,ctm
//
CAPACITANCE NEARBODY ctm WITH SHIELD ctm MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.70974 - 0.229376 * distance()) + 0.288381 / pow(distance() , 0.526244)) * 2.62264 * pow((width1() + width2()) / 2 , 0.127335) * (0.02073 * (thickness1() + thickness2()) / 2 + 0.0398879) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 7.169))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 7.169)))
]
//
//  model: 34  target: intrinsicns  profile: field  layers: field_base,metal6
//
CAPACITANCE INTRINSIC FRINGE metal6 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0660452 * (1 - exp(-0.174488 * (distance() + 1.17991))) * pow(width() , 0.0530136 * distance() + -0.340333) * (0.00259988 * thickness() + 0.258894) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 8.15))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 8.15)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0465004 * (1 - exp(-0.0455416 * (distance() + 0.241344))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 8.15))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 8.15)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0886249 * pow(width() , 0.0307712) * (0.0242045 * thickness() + 0.274205) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 8.15))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 8.15)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0298547 * pow(width() , 0.0149914) * (1 - 0.876917 * exp(-0.0625048 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: field  layers: field_base,metal6
//
CAPACITANCE NEARBODY metal6 WITH SHIELD metal6 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-2.20995 - 0.214691 * distance()) + 1.57289 / pow(distance() , 0.771673)) * 2.72685 * pow((width1() + width2()) / 2 , 0.0997285) * (0.0066097 * (thickness1() + thickness2()) / 2 + 0.0122778) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 8.15))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 8.15)))
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.015049 * exp(-0.914354 * distance())) / pow(distance() , 0.113615) * pow((width1() + width2()) / 2 , 0.102656) * pow(distance2() , 0.250256)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.015049 * exp(-0.914354 * distance())) / pow(distance() , 0.113615) * pow((width1() + width2()) / 2 , 0.102656) * 1.49597
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.015049 * exp(-0.914354 * distance())) / pow(distance() , 0.113615) * pow((width1() + width2()) / 2 , 0.102656) * pow(distance2() , 0.250256)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.015049 * exp(-0.914354 * distance())) / pow(distance() , 0.113615) * pow((width1() + width2()) / 2 , 0.102656) * 1.49597
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00903895 * exp(-0.384963 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.27544) * pow(distance2() , 0.170952)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00903895 * exp(-0.384963 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.27544) * 1.31671
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00903895 * exp(-0.384963 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.27544) * pow(distance2() , 0.170952)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00903895 * exp(-0.384963 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.27544) * 1.31671
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00575025 * exp(-0.196614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.331927) * pow(distance2() , 0.140915)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00575025 * exp(-0.196614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.331927) * 1.25457
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00575025 * exp(-0.196614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.331927) * pow(distance2() , 0.140915)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00575025 * exp(-0.196614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.331927) * 1.25457
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00407676 * exp(-0.107629 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.351484) * pow(distance2() , 0.127867)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00407676 * exp(-0.107629 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.351484) * 1.2285
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00407676 * exp(-0.107629 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.351484) * pow(distance2() , 0.127867)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00407676 * exp(-0.107629 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.351484) * 1.2285
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00313587 * exp(-0.0603614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.360891) * pow(distance2() , 0.120926)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00313587 * exp(-0.0603614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.360891) * 1.21485
    }
]
CAPACITANCE NEARBODY metal5 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00313587 * exp(-0.0603614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.360891) * pow(distance2() , 0.120926)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00313587 * exp(-0.0603614 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.360891) * 1.21485
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE NEARBODY poly ctm INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00303663 * exp(-0.0888537 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.354573) * pow(distance2() , 0.122119)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00303663 * exp(-0.0888537 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.354573) * 1.21719
    }
]
CAPACITANCE NEARBODY ctm poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00303663 * exp(-0.0888537 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.354573) * pow(distance2() , 0.122119)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00303663 * exp(-0.0888537 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.354573) * 1.21719
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00278904 * exp(-0.0761118 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305711) * pow(distance2() , 0.123961)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00278904 * exp(-0.0761118 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305711) * 1.2208
    }
]
CAPACITANCE NEARBODY metal6 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00278904 * exp(-0.0761118 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305711) * pow(distance2() , 0.123961)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00278904 * exp(-0.0761118 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305711) * 1.2208
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0158239 * exp(-0.566594 * distance())) / pow(distance() , 0.0237353) * pow((width1() + width2()) / 2 , 0.10964) * pow(distance2() , 0.40566)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0158239 * exp(-0.566594 * distance())) / pow(distance() , 0.0237353) * pow((width1() + width2()) / 2 , 0.10964) * 1.92107
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0158239 * exp(-0.566594 * distance())) / pow(distance() , 0.0237353) * pow((width1() + width2()) / 2 , 0.10964) * pow(distance2() , 0.40566)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0158239 * exp(-0.566594 * distance())) / pow(distance() , 0.0237353) * pow((width1() + width2()) / 2 , 0.10964) * 1.92107
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101198 * exp(-0.286993 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.200678) * pow(distance2() , 0.271045)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101198 * exp(-0.286993 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.200678) * 1.54686
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101198 * exp(-0.286993 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.200678) * pow(distance2() , 0.271045)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101198 * exp(-0.286993 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.200678) * 1.54686
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00719889 * exp(-0.161519 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.243651) * pow(distance2() , 0.215917)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00719889 * exp(-0.161519 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.243651) * 1.41553
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00719889 * exp(-0.161519 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.243651) * pow(distance2() , 0.215917)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00719889 * exp(-0.161519 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.243651) * 1.41553
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00552189 * exp(-0.0954511 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.26301) * pow(distance2() , 0.189473)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00552189 * exp(-0.0954511 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.26301) * 1.35655
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00552189 * exp(-0.0954511 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.26301) * pow(distance2() , 0.189473)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00552189 * exp(-0.0954511 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.26301) * 1.35655
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE NEARBODY metal1 ctm INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0053215 * exp(-0.115153 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.251986) * pow(distance2() , 0.188116)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0053215 * exp(-0.115153 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.251986) * 1.35359
    }
]
CAPACITANCE NEARBODY ctm metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0053215 * exp(-0.115153 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.251986) * pow(distance2() , 0.188116)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0053215 * exp(-0.115153 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.251986) * 1.35359
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00477044 * exp(-0.0957368 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.203883) * pow(distance2() , 0.18943)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00477044 * exp(-0.0957368 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.203883) * 1.35646
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00477044 * exp(-0.0957368 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.203883) * pow(distance2() , 0.18943)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00477044 * exp(-0.0957368 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.203883) * 1.35646
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162484 * exp(-0.468786 * distance())) / pow(distance() , 0.0419642) * pow((width1() + width2()) / 2 , 0.103172) * pow(distance2() , 0.443172)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162484 * exp(-0.468786 * distance())) / pow(distance() , 0.0419642) * pow((width1() + width2()) / 2 , 0.103172) * 2.04063
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162484 * exp(-0.468786 * distance())) / pow(distance() , 0.0419642) * pow((width1() + width2()) / 2 , 0.103172) * pow(distance2() , 0.443172)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162484 * exp(-0.468786 * distance())) / pow(distance() , 0.0419642) * pow((width1() + width2()) / 2 , 0.103172) * 2.04063
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110345 * exp(-0.262436 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.166911) * pow(distance2() , 0.29523)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110345 * exp(-0.262436 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.166911) * 1.60826
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110345 * exp(-0.262436 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.166911) * pow(distance2() , 0.29523)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110345 * exp(-0.262436 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.166911) * 1.60826
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00819556 * exp(-0.154762 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.202391) * pow(distance2() , 0.226716)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00819556 * exp(-0.154762 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.202391) * 1.44035
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00819556 * exp(-0.154762 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.202391) * pow(distance2() , 0.226716)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00819556 * exp(-0.154762 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.202391) * 1.44035
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE NEARBODY metal2 ctm INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00775959 * exp(-0.159709 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.181411) * pow(distance2() , 0.218278)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00775959 * exp(-0.159709 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.181411) * 1.42092
    }
]
CAPACITANCE NEARBODY ctm metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00775959 * exp(-0.159709 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.181411) * pow(distance2() , 0.218278)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00775959 * exp(-0.159709 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.181411) * 1.42092
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00671691 * exp(-0.128556 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.137392) * pow(distance2() , 0.21166)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00671691 * exp(-0.128556 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.137392) * 1.40587
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00671691 * exp(-0.128556 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.137392) * pow(distance2() , 0.21166)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00671691 * exp(-0.128556 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.137392) * 1.40587
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164758 * exp(-0.403494 * distance())) / pow(distance() , 0.0568567) * pow((width1() + width2()) / 2 , 0.102731) * pow(distance2() , 0.433924)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164758 * exp(-0.403494 * distance())) / pow(distance() , 0.0568567) * pow((width1() + width2()) / 2 , 0.102731) * 2.01048
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164758 * exp(-0.403494 * distance())) / pow(distance() , 0.0568567) * pow((width1() + width2()) / 2 , 0.102731) * pow(distance2() , 0.433924)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164758 * exp(-0.403494 * distance())) / pow(distance() , 0.0568567) * pow((width1() + width2()) / 2 , 0.102731) * 2.01048
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0116637 * exp(-0.240833 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.148618) * pow(distance2() , 0.283161)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0116637 * exp(-0.240833 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.148618) * 1.57732
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0116637 * exp(-0.240833 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.148618) * pow(distance2() , 0.283161)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0116637 * exp(-0.240833 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.148618) * 1.57732
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0104657 * exp(-0.216852 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.116065) * pow(distance2() , 0.261859)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0104657 * exp(-0.216852 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.116065) * 1.52416
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0104657 * exp(-0.216852 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.116065) * pow(distance2() , 0.261859)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0104657 * exp(-0.216852 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.116065) * 1.52416
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00870358 * exp(-0.168259 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0767625) * pow(distance2() , 0.236829)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00870358 * exp(-0.168259 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0767625) * 1.46398
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00870358 * exp(-0.168259 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0767625) * pow(distance2() , 0.236829)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00870358 * exp(-0.168259 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0767625) * 1.46398
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016665 * exp(-0.360738 * distance())) / pow(distance() , 0.0682989) * pow((width1() + width2()) / 2 , 0.101275) * pow(distance2() , 0.42146)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016665 * exp(-0.360738 * distance())) / pow(distance() , 0.0682989) * pow((width1() + width2()) / 2 , 0.101275) * 1.97055
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016665 * exp(-0.360738 * distance())) / pow(distance() , 0.0682989) * pow((width1() + width2()) / 2 , 0.101275) * pow(distance2() , 0.42146)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016665 * exp(-0.360738 * distance())) / pow(distance() , 0.0682989) * pow((width1() + width2()) / 2 , 0.101275) * 1.97055
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0128227 * exp(-0.199594 * distance())) / pow(distance() , 0.247788) * pow((width1() + width2()) / 2 , 0.0606428) * pow(distance2() , 0.38055)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0128227 * exp(-0.199594 * distance())) / pow(distance() , 0.247788) * pow((width1() + width2()) / 2 , 0.0606428) * 1.84499
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0128227 * exp(-0.199594 * distance())) / pow(distance() , 0.247788) * pow((width1() + width2()) / 2 , 0.0606428) * pow(distance2() , 0.38055)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0128227 * exp(-0.199594 * distance())) / pow(distance() , 0.247788) * pow((width1() + width2()) / 2 , 0.0606428) * 1.84499
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0108808 * exp(-0.190382 * distance())) / pow(distance() , 0.0965296) * pow((width1() + width2()) / 2 , 0.0241041) * pow(distance2() , 0.305773)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0108808 * exp(-0.190382 * distance())) / pow(distance() , 0.0965296) * pow((width1() + width2()) / 2 , 0.0241041) * 1.63578
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0108808 * exp(-0.190382 * distance())) / pow(distance() , 0.0965296) * pow((width1() + width2()) / 2 , 0.0241041) * pow(distance2() , 0.305773)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0108808 * exp(-0.190382 * distance())) / pow(distance() , 0.0965296) * pow((width1() + width2()) / 2 , 0.0241041) * 1.63578
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141945 * exp(-0.18447 * distance())) / pow(distance() , 0.530585) * pow((width1() + width2()) / 2 , 0.0673497) * pow(distance2() , 0.598529)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141945 * exp(-0.18447 * distance())) / pow(distance() , 0.530585) * pow((width1() + width2()) / 2 , 0.0673497) * 2.62032
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141945 * exp(-0.18447 * distance())) / pow(distance() , 0.530585) * pow((width1() + width2()) / 2 , 0.0673497) * pow(distance2() , 0.598529)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141945 * exp(-0.18447 * distance())) / pow(distance() , 0.530585) * pow((width1() + width2()) / 2 , 0.0673497) * 2.62032
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0126939 * exp(-0.170736 * distance())) / pow(distance() , 0.367809) * pow((width1() + width2()) / 2 , -0.00367346) * pow(distance2() , 0.478379)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0126939 * exp(-0.170736 * distance())) / pow(distance() , 0.367809) * pow((width1() + width2()) / 2 , -0.00367346) * 2.1596
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0126939 * exp(-0.170736 * distance())) / pow(distance() , 0.367809) * pow((width1() + width2()) / 2 , -0.00367346) * pow(distance2() , 0.478379)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0126939 * exp(-0.170736 * distance())) / pow(distance() , 0.367809) * pow((width1() + width2()) / 2 , -0.00367346) * 2.1596
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00969834 * exp(-0.345359 * distance())) / pow(distance() , 0.269964) * pow((width1() + width2()) / 2 , -0.0217819) * pow(distance2() , 0.937101)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00969834 * exp(-0.345359 * distance())) / pow(distance() , 0.269964) * pow((width1() + width2()) / 2 , -0.0217819) * 3.83749
    }
]
CAPACITANCE NEARBODY metal6 ctm INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00969834 * exp(-0.345359 * distance())) / pow(distance() , 0.269964) * pow((width1() + width2()) / 2 , -0.0217819) * pow(distance2() , 0.937101)
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00969834 * exp(-0.345359 * distance())) / pow(distance() , 0.269964) * pow((width1() + width2()) / 2 , -0.0217819) * 3.83749
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    C = length() * (exp(-1.34292 - 2.85756 * distance()) + 0.00197127 / pow(distance() , 2.17638)) * 3.39518 * pow((width1() + width2()) / 2 , 0.0450533) * (0.308906 * (thickness1() + thickness2()) / 2 + 0.0261259) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.37536 - 2.61579 * distance()) + 0.00126334 / pow(distance() , 1.61463)) * 1.73757 * pow((width1() + width2()) / 2 , 0.0423442) * pow(radius_up() , 0.041584)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER FRINGE poly metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.067734 * (1 - exp(-1.87778 * (distance()))) * pow(width() , 0.00647531 * distance() + -0.0135173) * (-0.6837 * thickness() + 0.444361)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0200338 * (1 - exp(-2.33288 * (distance() + -0.0551253)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.140366 * pow(width() , 0.00615571) * (-0.0815306 * thickness() + 0.159623)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00671664 * distance() + 0.565295) * (0.0727512 - exp(-(-0.668154 * distance() + 0.373712) * (enclose() + 4.13017 * distance() + 4.353) - (0.00278514 * distance() + 0.136349) * (enclose() + 4.08042 * distance() + 2.3214) * (enclose() + 4.08042 * distance() + 2.3214)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.067734 * (1 - exp(-1.87778 * (distance()))) * pow(width() , 0.00647531 * distance() + -0.0135173) * (-0.6837 * thickness() + 0.444361)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00831564 * (1 - exp(-3.09603 * (distance() + 0.151999))) * pow(width() , 0.00093372 * distance() + -0.0103528) * pow(radius_up() , 0.191511)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.265856 * (0.0727512 - exp(2.96706 * (enclose() + 25.0038) - 0.150275 * (enclose() + 22.7235) * (enclose() + 22.7235)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.132305 * (1 - exp(-2.00474 * (distance()))) * pow(width() , 0.00464207 * distance() + -0.00210068) * (0.120961 * thickness() + 0.197595) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0283372 * (1 - exp(-2.38062 * (distance() + -0.0471262))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0327233 * (1 - exp(-2.05527 * (distance() + 0.339909))) * pow(width() , -0.00021963 * distance() + 0.00310948) * pow(radius_up() , 0.0233086)
    }
    if (distance() <= 0.0) {
        C = length() * 0.139352 * pow(width() , 0.0118712) * (0.340164 * thickness() + 0.137388) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    C = length() * (exp(-1.37932 - 1.77696 * distance()) + 0.0177597 / pow(distance() , 1.49601)) * 3.41023 * pow((width1() + width2()) / 2 , 0.118129) * (0.162554 * (thickness1() + thickness2()) / 2 + 0.0279905) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.06183 - 1.7895 * distance()) + 0.00476311 / pow(distance() , 1.38029)) * 1.01791 * pow((width1() + width2()) / 2 , 0.113781) * pow(radius_up() , 0.0142342)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER FRINGE poly metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0328594 * (1 - exp(-1.25334 * (distance() + 0.0523667))) * pow(width() , 0.00881179 * distance() + -0.0007971) * (0.167462 * thickness() + 0.228759)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00815043 * (1 - exp(-1.44261 * (distance() + -0.00961327)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0857791 * pow(width() , 0.0535709) * (0.111662 * thickness() + 0.0796314)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00103494 * distance() + 0.031402) * (0.581298 - exp(-(0.0886721 * distance() + 0.27367) * (enclose() + 1.51055 * distance() + 1.37949) - (0.00103367 * distance() + -0.0354825) * (enclose() + -2.51409 * distance() + -0.600519) * (enclose() + -2.51409 * distance() + -0.600519)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0328594 * (1 - exp(-1.25334 * (distance() + 0.0523667))) * pow(width() , 0.00881179 * distance() + -0.0007971) * (0.167462 * thickness() + 0.228759)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00095154 * (1 - exp(-1.25822 * (distance() + 0.326359))) * pow(width() , 0.0242566 * distance() + -0.228982) * pow(radius_up() , 0.811593)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0131136 * (0.581298 - exp(-0.717031 * (enclose() + 8.93224) - -0.0303141 * (enclose() + -13.171) * (enclose() + -13.171)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.145877 * (1 - exp(-1.41489 * (distance() + 0.0491115))) * pow(width() , 0.00468313 * distance() + 0.00261565) * (0.172867 * thickness() + 0.229092) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0372555 * (1 - exp(-1.52126 * (distance() + 0.00581959))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0411626 * (1 - exp(-1.48108 * (distance() + 0.501726))) * pow(width() , 0.00242455 * distance() + 0.0254698) * pow(radius_up() , 0.0100328)
    }
    if (distance() <= 0.0) {
        C = length() * 0.159669 * pow(width() , 0.0406457) * (0.246814 * thickness() + 0.197122) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    C = length() * (exp(-1.43458 - 1.5889 * distance()) + 0.0309804 / pow(distance() , 1.34087)) * 3.47499 * pow((width1() + width2()) / 2 , 0.132834) * (0.135678 * (thickness1() + thickness2()) / 2 + 0.0246513) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.24981 - 1.66817 * distance()) + 0.00610936 / pow(distance() , 1.26824)) * 1.08162 * pow((width1() + width2()) / 2 , 0.131688) * pow(radius_up() , 0.00757333)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER FRINGE poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0226841 * (1 - exp(-1.14755 * (distance() + 0.089986))) * pow(width() , 0.00849897 * distance() + 0.0088204) * (0.232336 * thickness() + 0.197658)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00517389 * (1 - exp(-1.30063 * (distance() + 0.016438)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0662255 * pow(width() , 0.0693873) * (0.122233 * thickness() + 0.0617322)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0008066 * distance() + 0.0204198) * (0.586331 - exp(-(0.09597 * distance() + 0.195561) * (enclose() + 0.38719 * distance() + 2.05403) - (0.00025748 * distance() + -0.00942074) * (enclose() + -2.74197 * distance() + -0.598629) * (enclose() + -2.74197 * distance() + -0.598629)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0226841 * (1 - exp(-1.14755 * (distance() + 0.089986))) * pow(width() , 0.00849897 * distance() + 0.0088204) * (0.232336 * thickness() + 0.197658)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00430913 * (1 - exp(-0.00503257 * (distance() + 0.918238))) * pow(width() , 0.151858 * distance() + -0.708102) * pow(radius_up() , 1.74627)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0081934 * (0.586331 - exp(-0.675411 * (enclose() + 3.98998) - -0.00813334 * (enclose() + -14.3085) * (enclose() + -14.3085)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.104541 * (1 - exp(-1.31751 * (distance() + 0.0785453))) * pow(width() , 0.00379585 * distance() + 0.0108817) * (0.234206 * thickness() + 0.350108) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0399516 * (1 - exp(-1.37909 * (distance() + 0.0303941))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.044622 * (1 - exp(-1.40819 * (distance() + 0.54088))) * pow(width() , 0.00242199 * distance() + 0.0370256) * pow(radius_up() , -0.00371839)
    }
    if (distance() <= 0.0) {
        C = length() * 0.162472 * pow(width() , 0.0533799) * (0.249466 * thickness() + 0.216069) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    C = length() * (exp(-1.46833 - 1.54223 * distance()) + 0.0393978 / pow(distance() , 1.27164)) * 3.54819 * pow((width1() + width2()) / 2 , 0.137736) * (0.124046 * (thickness1() + thickness2()) / 2 + 0.0228114) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.30265 - 1.64868 * distance()) + 0.00704033 / pow(distance() , 1.21709)) * 1.07127 * pow((width1() + width2()) / 2 , 0.137897) * pow(radius_up() , 0.00567749)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER FRINGE poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.017038 * (1 - exp(-1.13062 * (distance() + 0.10357))) * pow(width() , 0.00776674 * distance() + 0.016358) * (0.26113 * thickness() + 0.187299)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00379068 * (1 - exp(-1.25778 * (distance() + 0.0289081)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0569248 * pow(width() , 0.0774421) * (0.116387 * thickness() + 0.0513657)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00091052 * distance() + 0.0170154) * (0.665417 - exp(-(0.0651737 * distance() + 0.131157) * (enclose() + -0.162396 * distance() + 2.41526) - (-0.00672309 * distance() + -0.00537786) * (enclose() + 0.0673705 * distance() + -0.677614) * (enclose() + 0.0673705 * distance() + -0.677614)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.017038 * (1 - exp(-1.13062 * (distance() + 0.10357))) * pow(width() , 0.00776674 * distance() + 0.016358) * (0.26113 * thickness() + 0.187299)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00099142 * (1 - exp(-0.0009314 * (distance() + 0.664991))) * pow(width() , 0.169678 * distance() + -0.953184) * pow(radius_up() , 2.93551)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0062314 * (0.665417 - exp(-0.457025 * (enclose() + 1.60328) - -0.0389933 * (enclose() + -0.340762) * (enclose() + -0.340762)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.107313 * (1 - exp(-1.30009 * (distance() + 0.088895))) * pow(width() , 0.00312812 * distance() + 0.0168547) * (0.228098 * thickness() + 0.355049) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0412225 * (1 - exp(-1.33521 * (distance() + 0.0415466))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0469665 * (1 - exp(-1.39511 * (distance() + 0.551605))) * pow(width() , 0.00223454 * distance() + 0.0435894) * pow(radius_up() , -0.0155855)
    }
    if (distance() <= 0.0) {
        C = length() * 0.164551 * pow(width() , 0.0601552) * (0.253253 * thickness() + 0.224126) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    C = length() * (exp(-1.50005 - 1.5448 * distance()) + 0.0441982 / pow(distance() , 1.23151)) * 3.58478 * pow((width1() + width2()) / 2 , 0.140009) * (0.120468 * (thickness1() + thickness2()) / 2 + 0.0221845) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.35533 - 1.6484 * distance()) + 0.0073617 / pow(distance() , 1.19122)) * 1.09115 * pow((width1() + width2()) / 2 , 0.140615) * pow(radius_up() , 0.00585618)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER FRINGE poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0137111 * (1 - exp(-1.13131 * (distance() + 0.109262))) * pow(width() , 0.00716534 * distance() + 0.0218084) * (0.275827 * thickness() + 0.180577)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0029914 * (1 - exp(-1.24173 * (distance() + 0.0353794)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0488102 * pow(width() , 0.0823129) * (0.114245 * thickness() + 0.0465451)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00060369 * distance() + 0.0123264) * (0.653541 - exp(-(0.0179298 * distance() + 0.155563) * (enclose() + 0.542659 * distance() + 2.10596) - (0.00203264 * distance() + -0.0234549) * (enclose() + -1.22142 * distance() + 0.404753) * (enclose() + -1.22142 * distance() + 0.404753)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0137111 * (1 - exp(-1.13131 * (distance() + 0.109262))) * pow(width() , 0.00716534 * distance() + 0.0218084) * (0.275827 * thickness() + 0.180577)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00015397 * (1 - exp(-0.00013421 * (distance() + 0.534722))) * pow(width() , 0.17903 * distance() + -1.15125) * pow(radius_up() , 4.30595)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00465397 * (0.653541 - exp(-0.245212 * (enclose() + 4.81926) - -0.0132917 * (enclose() + -5.70235) * (enclose() + -5.70235)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.109044 * (1 - exp(-1.29834 * (distance() + 0.0932946))) * pow(width() , 0.00270318 * distance() + 0.0209861) * (0.225782 * thickness() + 0.357535) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0419593 * (1 - exp(-1.31858 * (distance() + 0.0472244))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0487608 * (1 - exp(-1.39411 * (distance() + 0.555712))) * pow(width() , 0.00199411 * distance() + 0.0477546) * pow(radius_up() , -0.0245341)
    }
    if (distance() <= 0.0) {
        C = length() * 0.165561 * pow(width() , 0.0643298) * (0.257131 * thickness() + 0.229087) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE NEARBODY poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    C = length() * (exp(-1.50894 - 1.53432 * distance()) + 0.0454061 / pow(distance() , 1.22353)) * 3.59333 * pow((width1() + width2()) / 2 , 0.140499) * (0.119006 * (thickness1() + thickness2()) / 2 + 0.022058) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.36555 - 1.63408 * distance()) + 0.00747541 / pow(distance() , 1.18745)) * 1.09598 * pow((width1() + width2()) / 2 , 0.141261) * pow(radius_up() , 0.00212322)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE CROSSOVER FRINGE poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0127604 * (1 - exp(-1.13279 * (distance() + 0.110674))) * pow(width() , 0.00696391 * distance() + 0.0236186) * (0.277697 * thickness() + 0.178168)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00275644 * (1 - exp(-1.23795 * (distance() + 0.037393)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0460641 * pow(width() , 0.0837982) * (0.113127 * thickness() + 0.0453448)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00043954 * distance() + 0.0109864) * (0.657206 - exp(-(0.00708671 * distance() + 0.152147) * (enclose() + 0.84633 * distance() + 2.10979) - (0.00299662 * distance() + -0.0316803) * (enclose() + -1.12251 * distance() + 0.0839282) * (enclose() + -1.12251 * distance() + 0.0839282)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0127604 * (1 - exp(-1.13279 * (distance() + 0.110674))) * pow(width() , 0.00696391 * distance() + 0.0236186) * (0.277697 * thickness() + 0.178168)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00063887 * (1 - exp(-0.00057159 * (distance() + 0.514414))) * pow(width() , 0.17948 * distance() + -1.12546) * pow(radius_up() , 2.59832)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00439435 * (0.657206 - exp(-0.187581 * (enclose() + 6.34144) - -0.0166972 * (enclose() + -5.52862) * (enclose() + -5.52862)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.109611 * (1 - exp(-1.29885 * (distance() + 0.0943676))) * pow(width() , 0.0025748 * distance() + 0.0223123) * (0.225224 * thickness() + 0.358115) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0421785 * (1 - exp(-1.31484 * (distance() + 0.0487346))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0472747 * (1 - exp(-1.3852 * (distance() + 0.558368))) * pow(width() , 0.00202218 * distance() + 0.0485474) * pow(radius_up() , -0.00358629)
    }
    if (distance() <= 0.0) {
        C = length() * 0.165822 * pow(width() , 0.0655911) * (0.258528 * thickness() + 0.230604) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    C = length() * (exp(-1.51546 - 1.53697 * distance()) + 0.047464 / pow(distance() , 1.20957)) * 3.60117 * pow((width1() + width2()) / 2 , 0.141197) * (0.117305 * (thickness1() + thickness2()) / 2 + 0.0217713) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.35))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.35)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.38985 - 1.62925 * distance()) + 0.00745327 / pow(distance() , 1.18344)) * 1.12016 * pow((width1() + width2()) / 2 , 0.142042) * pow(radius_up() , -0.00104486)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER FRINGE poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0112708 * (1 - exp(-1.13573 * (distance() + 0.112339))) * pow(width() , 0.00668007 * distance() + 0.0261651) * (0.285134 * thickness() + 0.176624)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00242997 * (1 - exp(-1.23335 * (distance() + 0.0399487)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0410644 * pow(width() , 0.0858495) * (0.114594 * thickness() + 0.0445839)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00021759 * distance() + 0.00804625) * (0.642581 - exp(-(0.0277111 * distance() + 0.198346) * (enclose() + 0.0787077 * distance() + 1.95761) - (0.00555085 * distance() + -0.014882) * (enclose() + -0.670129 * distance() + 2.23091) * (enclose() + -0.670129 * distance() + 2.23091)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0112708 * (1 - exp(-1.13573 * (distance() + 0.112339))) * pow(width() , 0.00668007 * distance() + 0.0261651) * (0.285134 * thickness() + 0.176624)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00040271 * (1 - exp(-0.00052909 * (distance() + 0.998618))) * pow(width() , 0.146236 * distance() + -0.787234) * pow(radius_up() , 2.93527)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00347915 * (0.642581 - exp(-0.336901 * (enclose() + 2.35115) - 0.0128723 * (enclose() + -1.11973) * (enclose() + -1.11973)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    profile = field_profileid
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.110309 * (1 - exp(-1.30015 * (distance() + 0.0957122))) * pow(width() , 0.00239864 * distance() + 0.0242325) * (0.224963 * thickness() + 0.35923) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0424865 * (1 - exp(-1.3105 * (distance() + 0.0507122))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.35)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0484582 * (1 - exp(-1.382 * (distance() + 0.560368))) * pow(width() , 0.00200382 * distance() + 0.0502328) * pow(radius_up() , -0.0145938)
    }
    if (distance() <= 0.0) {
        C = length() * 0.16615 * pow(width() , 0.0673792) * (0.260836 * thickness() + 0.23274) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.35))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.35)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.07512 - 1.47074 * distance()) + 0.0284559 / pow(distance() , 1.48636)) * 3.7355 * pow((width1() + width2()) / 2 , 0.0500754) * (0.0790844 * (thickness1() + thickness2()) / 2 + 0.0201274) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.1))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.1)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.06464 - 1.3931 * distance()) + 0.00795322 / pow(distance() , 1.27751)) * 1.3455 * pow((width1() + width2()) / 2 , 0.0362585) * pow(radius_up() , 0.0185087)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0999097 * (1 - exp(-0.827321 * (distance()))) * pow(width() , 0.014613 * distance() + -0.0399849) * (0.0739972 * thickness() + 0.234139)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0261685 * (1 - exp(-0.970539 * (distance() + -0.0433475)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123766 * pow(width() , 0.0175707) * (0.0974724 * thickness() + 0.160341)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00100533 * distance() + 0.621207) * (0.0809706 - exp(-(-0.0428809 * distance() + 0.0377705) * (enclose() + 6.71021 * distance() + 19.0198) - (0.0121072 * distance() + 0.0224462) * (enclose() + 0.751863 * distance() + 8.49415) * (enclose() + 0.751863 * distance() + 8.49415)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0999097 * (1 - exp(-0.827321 * (distance()))) * pow(width() , 0.014613 * distance() + -0.0399849) * (0.0739972 * thickness() + 0.234139)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00914297 * (1 - exp(-1.48702 * (distance() + 0.428304))) * pow(width() , 0.0018414 * distance() + 0.00569567) * pow(radius_up() , 0.258859)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.30809 * (0.0809706 - exp(0.176634 * (enclose() + 52.5709) - 0.0829822 * (enclose() + 12.2535) * (enclose() + 12.2535)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0871133 * (1 - exp(-0.796197 * (distance()))) * pow(width() , 0.0164006 * distance() + -0.0444165) * (0.0889463 * thickness() + 0.221341) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0222837 * (1 - exp(-0.94668 * (distance() + -0.0473069))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0274188 * (1 - exp(-0.788648 * (distance() + 0.962765))) * pow(width() , 0.00080776 * distance() + 9.04775e-05) * pow(radius_up() , 0.01766)
    }
    if (distance() <= 0.0) {
        C = length() * 0.116879 * pow(width() , 0.0210771) * (0.0996055 * thickness() + 0.138748) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.1)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.04605 - 1.058 * distance()) + 0.0623246 / pow(distance() , 1.29717)) * 3.74751 * pow((width1() + width2()) / 2 , 0.0768235) * (0.0567415 * (thickness1() + thickness2()) / 2 + 0.0171063) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.1))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.1)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.35671 - 1.07894 * distance()) + 0.00999692 / pow(distance() , 1.15173)) * 1.51862 * pow((width1() + width2()) / 2 , 0.0605459) * pow(radius_up() , 0.00882764)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0631358 * (1 - exp(-0.509146 * (distance() + 0.0876584))) * pow(width() , 0.0227223 * distance() + -0.0683674) * (0.0888403 * thickness() + 0.209811)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0149353 * (1 - exp(-0.646315 * (distance() + -0.0169663)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.100822 * pow(width() , 0.0379965) * (0.0790804 * thickness() + 0.108269)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00010014 * distance() + 0.0356762) * (0.817085 - exp(-(0.306798 * distance() + 0.225296) * (enclose() + 0.221933 * distance() + 0.699796) - (-0.00941013 * distance() + -0.0299884) * (enclose() + 0.844741 * distance() + 1.41859) * (enclose() + 0.844741 * distance() + 1.41859)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0631358 * (1 - exp(-0.509146 * (distance() + 0.0876584))) * pow(width() , 0.0227223 * distance() + -0.0683674) * (0.0888403 * thickness() + 0.209811)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0020601 * (1 - exp(-0.689838 * (distance() + 0.678317))) * pow(width() , 0.0274131 * distance() + -0.125366) * pow(radius_up() , 0.787871)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0175877 * (0.817085 - exp(-1.75929 * (enclose() + 1.80946) - -0.077039 * (enclose() + 5.64229) * (enclose() + 5.64229)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.110225 * (1 - exp(-0.552228 * (distance() + 0.0755316))) * pow(width() , 0.0201608 * distance() + -0.0620006) * (0.0611537 * thickness() + 0.243127) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0284075 * (1 - exp(-0.679002 * (distance() + -0.012627))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0327829 * (1 - exp(-0.634242 * (distance() + 1.15328))) * pow(width() , 0.00242129 * distance() + 0.0108316) * pow(radius_up() , -0.00036898)
    }
    if (distance() <= 0.0) {
        C = length() * 0.130336 * pow(width() , 0.0319107) * (0.0822715 * thickness() + 0.177299) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.1)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.12896 - 0.930294 * distance()) + 0.0872616 / pow(distance() , 1.19843)) * 4.12633 * pow((width1() + width2()) / 2 , 0.0850581) * (0.0467302 * (thickness1() + thickness2()) / 2 + 0.0145802) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.1))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.1)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.35782 - 0.984074 * distance()) + 0.0136505 / pow(distance() , 1.08451)) * 1.33706 * pow((width1() + width2()) / 2 , 0.0709841) * pow(radius_up() , 0.00354451)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0455498 * (1 - exp(-0.440371 * (distance() + 0.15108))) * pow(width() , 0.0236576 * distance() + -0.0698818) * (0.0931652 * thickness() + 0.207286)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0106208 * (1 - exp(-0.562127 * (distance() + 0.0111825)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0899383 * pow(width() , 0.0491225) * (0.0685153 * thickness() + 0.0853656)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0002129 * distance() + 0.0245256) * (0.880313 - exp(-(0.193366 * distance() + 0.176316) * (enclose() + 0.345288 * distance() + 0.755857) - (-0.00372695 * distance() + -0.017583) * (enclose() + 1.13653 * distance() + 2.23167) * (enclose() + 1.13653 * distance() + 2.23167)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0455498 * (1 - exp(-0.440371 * (distance() + 0.15108))) * pow(width() , 0.0236576 * distance() + -0.0698818) * (0.0931652 * thickness() + 0.207286)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0003588 * (1 - exp(-0.426482 * (distance() + 0.810313))) * pow(width() , 0.052354 * distance() + -0.270063) * pow(radius_up() , 1.51222)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0117306 * (0.880313 - exp(-1.14315 * (enclose() + 2.4823) - -0.0362178 * (enclose() + 7.91432) * (enclose() + 7.91432)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.121449 * (1 - exp(-0.490375 * (distance() + 0.124714))) * pow(width() , 0.0211915 * distance() + -0.0652389) * (0.0504945 * thickness() + 0.250775) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0312522 * (1 - exp(-0.603116 * (distance() + 0.0126985))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0363686 * (1 - exp(-0.597187 * (distance() + 1.23188))) * pow(width() , 0.00230176 * distance() + 0.0201174) * pow(radius_up() , -0.0237248)
    }
    if (distance() <= 0.0) {
        C = length() * 0.13686 * pow(width() , 0.0401439) * (0.0797706 * thickness() + 0.193021) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.1)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.16886 - 0.879428 * distance()) + 0.109761 / pow(distance() , 1.1378)) * 4.26386 * pow((width1() + width2()) / 2 , 0.0884031) * (0.0415382 * (thickness1() + thickness2()) / 2 + 0.0131356) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.1))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.1)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.52429 - 0.947254 * distance()) + 0.0138843 / pow(distance() , 1.04789)) * 1.45393 * pow((width1() + width2()) / 2 , 0.075741) * pow(radius_up() , 0.00051644)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.035571 * (1 - exp(-0.426348 * (distance() + 0.184132))) * pow(width() , 0.0224376 * distance() + -0.0628383) * (0.0950249 * thickness() + 0.204048)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00822844 * (1 - exp(-0.531768 * (distance() + 0.0299982)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.083287 * pow(width() , 0.0560442) * (0.0605189 * thickness() + 0.0714925)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00063577 * distance() + 0.0219652) * (0.902768 - exp(-(0.126697 * distance() + 0.164301) * (enclose() + 0.371802 * distance() + 1.02771) - (-0.0015786 * distance() + -0.0150822) * (enclose() + 1.20432 * distance() + 2.94123) * (enclose() + 1.20432 * distance() + 2.94123)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.035571 * (1 - exp(-0.426348 * (distance() + 0.184132))) * pow(width() , 0.0224376 * distance() + -0.0628383) * (0.0950249 * thickness() + 0.204048)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00219376 * (1 - exp(-0.0028932 * (distance() + 1.52588))) * pow(width() , 0.107563 * distance() + -0.473401) * pow(radius_up() , 2.398)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00939318 * (0.902768 - exp(-0.797786 * (enclose() + 2.88672) - -0.0229752 * (enclose() + 8.96283) * (enclose() + 8.96283)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.126651 * (1 - exp(-0.476286 * (distance() + 0.150452))) * pow(width() , 0.0204695 * distance() + -0.0611604) * (0.045689 * thickness() + 0.254909) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0327878 * (1 - exp(-0.574964 * (distance() + 0.0285586))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0395334 * (1 - exp(-0.587107 * (distance() + 1.26701))) * pow(width() , 0.00182359 * distance() + 0.0272463) * pow(radius_up() , -0.0471479)
    }
    if (distance() <= 0.0) {
        C = length() * 0.140689 * pow(width() , 0.0457176) * (0.0800871 * thickness() + 0.201699) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.1)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE NEARBODY metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.18302 - 0.867776 * distance()) + 0.117209 / pow(distance() , 1.12065)) * 4.29369 * pow((width1() + width2()) / 2 , 0.0892133) * (0.0402467 * (thickness1() + thickness2()) / 2 + 0.0127633) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.1))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.1)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.55319 - 0.932698 * distance()) + 0.0140119 / pow(distance() , 1.04169)) * 1.45667 * pow((width1() + width2()) / 2 , 0.0770754) * pow(radius_up() , 0.00456891)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE CROSSOVER FRINGE metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0326027 * (1 - exp(-0.425561 * (distance() + 0.19298))) * pow(width() , 0.0217905 * distance() + -0.05954) * (0.0959539 * thickness() + 0.203016)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00752451 * (1 - exp(-0.525309 * (distance() + 0.0324245)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0814104 * pow(width() , 0.0584944) * (0.0568053 * thickness() + 0.0679383)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.000903 * distance() + 0.0221646) * (0.887519 - exp(-(0.117224 * distance() + 0.174802) * (enclose() + 0.32283 * distance() + 1.15613) - (-0.00087361 * distance() + -0.018297) * (enclose() + 1.17722 * distance() + 2.75342) * (enclose() + 1.17722 * distance() + 2.75342)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0326027 * (1 - exp(-0.425561 * (distance() + 0.19298))) * pow(width() , 0.0217905 * distance() + -0.05954) * (0.0959539 * thickness() + 0.203016)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00272598 * (1 - exp(-0.00866561 * (distance() + 1.47261))) * pow(width() , 0.106563 * distance() + -0.443628) * pow(radius_up() , 1.48704)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0088248 * (0.887519 - exp(-0.760922 * (enclose() + 2.77028) - -0.022665 * (enclose() + 8.63952) * (enclose() + 8.63952)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.127983 * (1 - exp(-0.475021 * (distance() + 0.15728))) * pow(width() , 0.020042 * distance() + -0.0589602) * (0.0444851 * thickness() + 0.256072) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0332208 * (1 - exp(-0.568905 * (distance() + 0.0332438))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0373072 * (1 - exp(-0.585488 * (distance() + 1.27451))) * pow(width() , 0.00161166 * distance() + 0.0291526) * pow(radius_up() , -0.00424544)
    }
    if (distance() <= 0.0) {
        C = length() * 0.141821 * pow(width() , 0.0474686) * (0.0804931 * thickness() + 0.204182) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.1)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.20179 - 0.854317 * distance()) + 0.128295 / pow(distance() , 1.09783)) * 4.32738 * pow((width1() + width2()) / 2 , 0.0902153) * (0.0385328 * (thickness1() + thickness2()) / 2 + 0.0122616) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.1))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.1)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.52739 - 0.923625 * distance()) + 0.0148358 / pow(distance() , 1.03615)) * 1.40469 * pow((width1() + width2()) / 2 , 0.0785109) * pow(radius_up() , 0.00169792)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0281297 * (1 - exp(-0.426797 * (distance() + 0.20444))) * pow(width() , 0.020673 * distance() + -0.0538856) * (0.0984763 * thickness() + 0.204319)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00658969 * (1 - exp(-0.518195 * (distance() + 0.0436208)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0780427 * pow(width() , 0.0611364) * (0.0542115 * thickness() + 0.0611893)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0005677 * distance() + 0.0175474) * (0.959306 - exp(-(0.0819382 * distance() + 0.127165) * (enclose() + 0.500791 * distance() + 1.34737) - (-0.00093534 * distance() + -0.00641408) * (enclose() + 1.38387 * distance() + 5.44475) * (enclose() + 1.38387 * distance() + 5.44475)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0281297 * (1 - exp(-0.426797 * (distance() + 0.20444))) * pow(width() , 0.020673 * distance() + -0.0538856) * (0.0984763 * thickness() + 0.204319)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 7.01286e-05 * (1 - exp(-0.377481 * (distance() + 1.05025))) * pow(width() , 0.0458346 * distance() + -0.266341) * pow(radius_up() , 1.77379)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00735445 * (0.959306 - exp(-0.536856 * (enclose() + 3.85133) - -0.0110908 * (enclose() + 12.3641) * (enclose() + 12.3641)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.129137 * (1 - exp(-0.475557 * (distance() + 0.165515))) * pow(width() , 0.0192832 * distance() + -0.0549638) * (0.0426691 * thickness() + 0.259048) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0338107 * (1 - exp(-0.562078 * (distance() + 0.0396826))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.1)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0389543 * (1 - exp(-0.581894 * (distance() + 1.2782))) * pow(width() , 0.00155946 * distance() + 0.034502) * pow(radius_up() , -0.0259941)
    }
    if (distance() <= 0.0) {
        C = length() * 0.143405 * pow(width() , 0.0499859) * (0.0812139 * thickness() + 0.207622) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.1))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.1)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.21041 - 1.0806 * distance()) + 0.0482544 / pow(distance() , 1.35795)) * 3.64594 * pow((width1() + width2()) / 2 , 0.0712021) * (0.0669367 * (thickness1() + thickness2()) / 2 + 0.0218923) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.48))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.48)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.64815 - 1.04014 * distance()) + 0.0103992 / pow(distance() , 1.10016)) * 1.69265 * pow((width1() + width2()) / 2 , 0.0522238) * pow(radius_up() , 0.0238683)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.118049 * (1 - exp(-0.645575 * (distance() + 0.0443729))) * pow(width() , 0.0167904 * distance() + -0.0512216) * (0.0567323 * thickness() + 0.25058)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0317714 * (1 - exp(-0.743668 * (distance() + -0.0192845)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.134458 * pow(width() , 0.0266893) * (0.0841917 * thickness() + 0.194154)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00799132 * distance() + 0.387409) * (0.138963 - exp(-(-0.172054 * distance() + 0.372976) * (enclose() + 1.34176 * distance() + 4.3206) - (0.0241682 * distance() + 0.00568208) * (enclose() + 0.451767 * distance() + 5.68077) * (enclose() + 0.451767 * distance() + 5.68077)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.118049 * (1 - exp(-0.645575 * (distance() + 0.0443729))) * pow(width() , 0.0167904 * distance() + -0.0512216) * (0.0567323 * thickness() + 0.25058)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0106921 * (1 - exp(-1.34615 * (distance() + 0.5223))) * pow(width() , -0.00186932 * distance() + 0.0311712) * pow(radius_up() , 0.250757)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.213683 * (0.138963 - exp(0.487294 * (enclose() + 11.0294) - 0.126523 * (enclose() + 7.93961) * (enclose() + 7.93961)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.054914 * (1 - exp(-0.570246 * (distance() + 0.0551534))) * pow(width() , 0.021661 * distance() + -0.0640772) * (0.0993484 * thickness() + 0.203723) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0132396 * (1 - exp(-0.688914 * (distance() + -0.0322422))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0188495 * (1 - exp(-0.556393 * (distance() + 1.563))) * pow(width() , -0.00043018 * distance() + 0.00236843) * pow(radius_up() , 0.0116248)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0960358 * pow(width() , 0.0370811) * (0.081207 * thickness() + 0.0963829) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.48)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.09506 - 0.750486 * distance()) + 0.0922507 / pow(distance() , 1.21079)) * 3.59765 * pow((width1() + width2()) / 2 , 0.0977367) * (0.0472857 * (thickness1() + thickness2()) / 2 + 0.0188808) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.48))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.48)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.6449 - 0.76871 * distance()) + 0.0142987 / pow(distance() , 1.01642)) * 1.53877 * pow((width1() + width2()) / 2 , 0.0731657) * pow(radius_up() , 0.011276)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0887524 * (1 - exp(-0.325605 * (distance() + 0.187732))) * pow(width() , 0.0305641 * distance() + -0.109197) * (0.0518647 * thickness() + 0.230138)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0201946 * (1 - exp(-0.443015 * (distance() + 0.0144625)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.113708 * pow(width() , 0.041913) * (0.0638162 * thickness() + 0.14166)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00088366 * distance() + 0.0395386) * (0.845067 - exp(-(0.280355 * distance() + 0.392499) * (enclose() + 0.310182 * distance() + 1.25048) - (-0.00590646 * distance() + -0.0343391) * (enclose() + 0.943834 * distance() + 3.53885) * (enclose() + 0.943834 * distance() + 3.53885)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0887524 * (1 - exp(-0.325605 * (distance() + 0.187732))) * pow(width() , 0.0305641 * distance() + -0.109197) * (0.0518647 * thickness() + 0.230138)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00292802 * (1 - exp(-0.638591 * (distance() + 0.872066))) * pow(width() , 0.0163292 * distance() + -0.0543297) * pow(radius_up() , 0.72129)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0219785 * (0.845067 - exp(-1.79427 * (enclose() + 2.80139) - -0.0638714 * (enclose() + 8.25802) * (enclose() + 8.25802)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0831243 * (1 - exp(-0.322779 * (distance() + 0.188446))) * pow(width() , 0.03075 * distance() + -0.10979) * (0.0551111 * thickness() + 0.226984) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0187706 * (1 - exp(-0.440668 * (distance() + 0.0132683))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0234236 * (1 - exp(-0.418621 * (distance() + 1.89005))) * pow(width() , 0.00103939 * distance() + 0.0106751) * pow(radius_up() , -0.0115894)
    }
    if (distance() <= 0.0) {
        C = length() * 0.110327 * pow(width() , 0.0423466) * (0.0639192 * thickness() + 0.134083) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.48)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.06517 - 0.635732 * distance()) + 0.142806 / pow(distance() , 1.10903)) * 3.60721 * pow((width1() + width2()) / 2 , 0.10766) * (0.0378421 * (thickness1() + thickness2()) / 2 + 0.0158378) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.48))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.48)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.55912 - 0.6719 * distance()) + 0.0199205 / pow(distance() , 0.96595)) * 1.2691 * pow((width1() + width2()) / 2 , 0.0850853) * pow(radius_up() , 0.00377463)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0710938 * (1 - exp(-0.250868 * (distance() + 0.302268))) * pow(width() , 0.0335851 * distance() + -0.123792) * (0.0500207 * thickness() + 0.226573)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.015226 * (1 - exp(-0.361006 * (distance() + 0.0536904)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.102299 * pow(width() , 0.0509896) * (0.0552011 * thickness() + 0.117682)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00028133 * distance() + 0.0305519) * (0.876178 - exp(-(0.170792 * distance() + 0.209589) * (enclose() + 0.415067 * distance() + 0.919915) - (-0.00216125 * distance() + -0.0225883) * (enclose() + 1.28582 * distance() + 2.46137) * (enclose() + 1.28582 * distance() + 2.46137)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0710938 * (1 - exp(-0.250868 * (distance() + 0.302268))) * pow(width() , 0.0335851 * distance() + -0.123792) * (0.0500207 * thickness() + 0.226573)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00064731 * (1 - exp(-0.403487 * (distance() + 1.12612))) * pow(width() , 0.0336128 * distance() + -0.150967) * pow(radius_up() , 1.35346)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0159793 * (0.876178 - exp(-1.06355 * (enclose() + 2.99525) - -0.0333945 * (enclose() + 8.89047) * (enclose() + 8.89047)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0999347 * (1 - exp(-0.258038 * (distance() + 0.28721))) * pow(width() , 0.033471 * distance() + -0.124518) * (0.0397103 * thickness() + 0.23555) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0218485 * (1 - exp(-0.367684 * (distance() + 0.0507183))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0270155 * (1 - exp(-0.379021 * (distance() + 2.05089))) * pow(width() , 0.00110209 * distance() + 0.0193598) * pow(radius_up() , -0.0446008)
    }
    if (distance() <= 0.0) {
        C = length() * 0.11779 * pow(width() , 0.0486813) * (0.0596033 * thickness() + 0.151981) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.48)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE NEARBODY metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.06266 - 0.60901 * distance()) + 0.163141 / pow(distance() , 1.07895)) * 3.63164 * pow((width1() + width2()) / 2 , 0.109795) * (0.03513 * (thickness1() + thickness2()) / 2 + 0.0148382) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.48))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.48)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.60372 - 0.645692 * distance()) + 0.0198725 / pow(distance() , 0.957783)) * 1.28859 * pow((width1() + width2()) / 2 , 0.0883679) * pow(radius_up() , 0.00998499)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE CROSSOVER FRINGE metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.064979 * (1 - exp(-0.240678 * (distance() + 0.334228))) * pow(width() , 0.0333314 * distance() + -0.12323) * (0.0495858 * thickness() + 0.225822)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0138328 * (1 - exp(-0.345142 * (distance() + 0.0664987)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0987505 * pow(width() , 0.0536955) * (0.0527862 * thickness() + 0.110943)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00037116 * distance() + 0.0309088) * (0.88727 - exp(-(0.15592 * distance() + 0.217275) * (enclose() + 0.432098 * distance() + 1.06087) - (-0.00105418 * distance() + -0.0247036) * (enclose() + 1.38878 * distance() + 2.63718) * (enclose() + 1.38878 * distance() + 2.63718)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.064979 * (1 - exp(-0.240678 * (distance() + 0.334228))) * pow(width() , 0.0333314 * distance() + -0.12323) * (0.0495858 * thickness() + 0.225822)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00107963 * (1 - exp(-0.342164 * (distance() + 1.19527))) * pow(width() , 0.0386216 * distance() + -0.142581) * pow(radius_up() , 0.870084)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0145265 * (0.88727 - exp(-0.996875 * (enclose() + 3.22136) - -0.0299745 * (enclose() + 9.58108) * (enclose() + 9.58108)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.103839 * (1 - exp(-0.248951 * (distance() + 0.314672))) * pow(width() , 0.0333572 * distance() + -0.124807) * (0.0359381 * thickness() + 0.237631) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0226758 * (1 - exp(-0.353335 * (distance() + 0.0625396))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.026054 * (1 - exp(-0.373585 * (distance() + 2.08704))) * pow(width() , 0.00076243 * distance() + 0.0226545) * pow(radius_up() , 0.00174488)
    }
    if (distance() <= 0.0) {
        C = length() * 0.119917 * pow(width() , 0.0508296) * (0.0588618 * thickness() + 0.156972) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.48)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.06745 - 0.576517 * distance()) + 0.194985 / pow(distance() , 1.03928)) * 3.6658 * pow((width1() + width2()) / 2 , 0.112298) * (0.031774 * (thickness1() + thickness2()) / 2 + 0.013566) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.48))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.48)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.61549 - 0.625707 * distance()) + 0.0204043 / pow(distance() , 0.950909)) * 1.29093 * pow((width1() + width2()) / 2 , 0.0922032) * pow(radius_up() , 0.00459176)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0559525 * (1 - exp(-0.233645 * (distance() + 0.375994))) * pow(width() , 0.0321177 * distance() + -0.118734) * (0.0495835 * thickness() + 0.224766)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.011936 * (1 - exp(-0.328214 * (distance() + 0.0849249)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0941216 * pow(width() , 0.0575218) * (0.0491598 * thickness() + 0.101061)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00014491 * distance() + 0.0248797) * (0.939132 - exp(-(0.101203 * distance() + 0.158028) * (enclose() + 0.567632 * distance() + 1.1978) - (-0.00090562 * distance() + -0.0107447) * (enclose() + 1.57872 * distance() + 4.20367) * (enclose() + 1.57872 * distance() + 4.20367)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0559525 * (1 - exp(-0.233645 * (distance() + 0.375994))) * pow(width() , 0.0321177 * distance() + -0.118734) * (0.0495835 * thickness() + 0.224766)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00064544 * (1 - exp(-0.371365 * (distance() + 1.45004))) * pow(width() , 0.0247053 * distance() + -0.117509) * pow(radius_up() , 1.08595)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0120776 * (0.939132 - exp(-0.664043 * (enclose() + 4.03596) - -0.0152728 * (enclose() + 12.0973) * (enclose() + 12.0973)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.107974 * (1 - exp(-0.242723 * (distance() + 0.350064))) * pow(width() , 0.0324629 * distance() + -0.12189) * (0.0314213 * thickness() + 0.240372) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0237425 * (1 - exp(-0.337955 * (distance() + 0.0792897))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.48)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0276301 * (1 - exp(-0.362995 * (distance() + 2.1155))) * pow(width() , 0.00079089 * distance() + 0.0333549) * pow(radius_up() , -0.0299834)
    }
    if (distance() <= 0.0) {
        C = length() * 0.122817 * pow(width() , 0.0539675) * (0.0583068 * thickness() + 0.163714) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.48))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.48)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.23995 - 0.965587 * distance()) + 0.0723636 / pow(distance() , 1.24539)) * 3.69092 * pow((width1() + width2()) / 2 , 0.0814775) * (0.0569129 * (thickness1() + thickness2()) / 2 + 0.0192754) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.86))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.86)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.95076 - 0.95164 * distance()) + 0.0130402 / pow(distance() , 0.995106)) * 1.7548 * pow((width1() + width2()) / 2 , 0.0614485) * pow(radius_up() , 0.0228827)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.128102 * (1 - exp(-0.585138 * (distance() + 0.0863765))) * pow(width() , 0.018044 * distance() + -0.0545288) * (0.0476831 * thickness() + 0.255875)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0343577 * (1 - exp(-0.671806 * (distance() + 0.00509206)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.139911 * pow(width() , 0.0360078) * (0.0828551 * thickness() + 0.208482)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0137674 * distance() + 0.408893) * (0.132198 - exp(-(-0.14198 * distance() + 0.291017) * (enclose() + 1.64105 * distance() + 4.80995) - (0.018804 * distance() + 0.0121933) * (enclose() + 0.534326 * distance() + 6.05727) * (enclose() + 0.534326 * distance() + 6.05727)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.128102 * (1 - exp(-0.585138 * (distance() + 0.0863765))) * pow(width() , 0.018044 * distance() + -0.0545288) * (0.0476831 * thickness() + 0.255875)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0114365 * (1 - exp(-1.29053 * (distance() + 0.570044))) * pow(width() , -0.00304852 * distance() + 0.0419365) * pow(radius_up() , 0.24574)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.238865 * (0.132198 - exp(0.418883 * (enclose() + 13.0152) - 0.106213 * (enclose() + 8.7289) * (enclose() + 8.7289)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0404768 * (1 - exp(-0.505067 * (distance() + 0.109968))) * pow(width() , 0.0231047 * distance() + -0.0667516) * (0.102421 * thickness() + 0.197595) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00949487 * (1 - exp(-0.611947 * (distance() + -0.00558953))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.015057 * (1 - exp(-0.529549 * (distance() + 1.859))) * pow(width() , -0.00233302 * distance() + 0.00883618) * pow(radius_up() , 0.00282749)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0862715 * pow(width() , 0.0492694) * (0.070117 * thickness() + 0.0760435) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.86)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.07984 - 0.648384 * distance()) + 0.138861 / pow(distance() , 1.11227)) * 3.60875 * pow((width1() + width2()) / 2 , 0.10648) * (0.038684 * (thickness1() + thickness2()) / 2 + 0.016071) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.86))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.86)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.98646 - 0.668824 * distance()) + 0.0158112 / pow(distance() , 0.930884)) * 1.72457 * pow((width1() + width2()) / 2 , 0.0801834) * pow(radius_up() , 0.0096607)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.103343 * (1 - exp(-0.271708 * (distance() + 0.273662))) * pow(width() , 0.0325206 * distance() + -0.11992) * (0.038636 * thickness() + 0.237962)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0230291 * (1 - exp(-0.379488 * (distance() + 0.0488602)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.12056 * pow(width() , 0.048289) * (0.0603478 * thickness() + 0.158001)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00217744 * distance() + 0.0432457) * (0.687819 - exp(-(0.12643 * distance() + 0.187121) * (enclose() + 0.0493636 * distance() + 1.43952) - (-3.94227e-05 * distance() + -4.30287e-05) * (enclose() + -12.4276 * distance() + 18.1857) * (enclose() + -12.4276 * distance() + 18.1857)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.103343 * (1 - exp(-0.271708 * (distance() + 0.273662))) * pow(width() , 0.0325206 * distance() + -0.11992) * (0.038636 * thickness() + 0.237962)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00346893 * (1 - exp(-0.609888 * (distance() + 0.997462))) * pow(width() , 0.0117346 * distance() + -0.0214012) * pow(radius_up() , 0.681555)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0270664 * (0.687819 - exp(-0.819271 * (enclose() + 1.68634) - -0.000240142 * (enclose() + -43.9523) * (enclose() + -43.9523)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0658792 * (1 - exp(-0.261441 * (distance() + 0.288461))) * pow(width() , 0.0328157 * distance() + -0.119833) * (0.0537663 * thickness() + 0.224048) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.014217 * (1 - exp(-0.370006 * (distance() + 0.0497315))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0188989 * (1 - exp(-0.384533 * (distance() + 2.27515))) * pow(width() , -0.00103371 * distance() + 0.0155156) * pow(radius_up() , -0.0234804)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0995722 * pow(width() , 0.0510284) * (0.0557601 * thickness() + 0.111908) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.86)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE NEARBODY metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.0384 - 0.590051 * distance()) + 0.170965 / pow(distance() , 1.07381)) * 3.62807 * pow((width1() + width2()) / 2 , 0.111355) * (0.0338652 * (thickness1() + thickness2()) / 2 + 0.014451) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.86))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.86)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.82697 - 0.607282 * distance()) + 0.0189966 / pow(distance() , 0.923503)) * 1.45628 * pow((width1() + width2()) / 2 , 0.0865504) * pow(radius_up() , 0.0154555)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE metal3 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0971654 * (1 - exp(-0.229533 * (distance() + 0.343073))) * pow(width() , 0.0346603 * distance() + -0.131233) * (0.0363838 * thickness() + 0.235612)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0206569 * (1 - exp(-0.336457 * (distance() + 0.068992)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.115256 * pow(width() , 0.0514998) * (0.0561509 * thickness() + 0.147262)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00080135 * distance() + 0.0375113) * (0.866028 - exp(-(0.216773 * distance() + 0.36983) * (enclose() + 0.317124 * distance() + 1.31826) - (-0.00307022 * distance() + -0.0343452) * (enclose() + 1.01853 * distance() + 3.51102) * (enclose() + 1.01853 * distance() + 3.51102)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0971654 * (1 - exp(-0.229533 * (distance() + 0.343073))) * pow(width() , 0.0346603 * distance() + -0.131233) * (0.0363838 * thickness() + 0.235612)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00369987 * (1 - exp(-0.466316 * (distance() + 1.12434))) * pow(width() , 0.0202606 * distance() + -0.0257382) * pow(radius_up() , 0.452381)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.020759 * (0.866028 - exp(-1.45369 * (enclose() + 2.90388) - -0.0496963 * (enclose() + 8.60367) * (enclose() + 8.60367)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0743111 * (1 - exp(-0.225252 * (distance() + 0.350885))) * pow(width() , 0.0346339 * distance() + -0.130505) * (0.0447889 * thickness() + 0.228498) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0155677 * (1 - exp(-0.331956 * (distance() + 0.0694157))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.019306 * (1 - exp(-0.365611 * (distance() + 2.36278))) * pow(width() , -0.00116875 * distance() + 0.0189299) * pow(radius_up() , 0.0105577)
    }
    if (distance() <= 0.0) {
        C = length() * 0.103184 * pow(width() , 0.0528124) * (0.0533562 * thickness() + 0.120347) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.86)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-0.981851 - 0.523714 * distance()) + 0.230916 / pow(distance() , 1.02169)) * 3.66818 * pow((width1() + width2()) / 2 , 0.116456) * (0.0277348 * (thickness1() + thickness2()) / 2 + 0.0121508) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.86))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.86)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.70377 - 0.564673 * distance()) + 0.021882 / pow(distance() , 0.918905)) * 1.2987 * pow((width1() + width2()) / 2 , 0.0938397) * pow(radius_up() , 0.00776662)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0855862 * (1 - exp(-0.195734 * (distance() + 0.436136))) * pow(width() , 0.0353716 * distance() + -0.137256) * (0.0339435 * thickness() + 0.234102)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0175643 * (1 - exp(-0.294522 * (distance() + 0.0989927)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.108257 * pow(width() , 0.0558313) * (0.0511183 * thickness() + 0.133006)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00112014 * distance() + 0.0283003) * (0.874758 - exp(-(0.141619 * distance() + 0.169192) * (enclose() + 0.466138 * distance() + 0.777669) - (-0.00213421 * distance() + -0.0145392) * (enclose() + 1.36484 * distance() + 2.32853) * (enclose() + 1.36484 * distance() + 2.32853)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0855862 * (1 - exp(-0.195734 * (distance() + 0.436136))) * pow(width() , 0.0353716 * distance() + -0.137256) * (0.0339435 * thickness() + 0.234102)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00257739 * (1 - exp(-0.431289 * (distance() + 1.47501))) * pow(width() , 0.0134426 * distance() + -0.0365898) * pow(radius_up() , 0.631065)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0169505 * (0.874758 - exp(-0.877287 * (enclose() + 3.10836) - -0.0252103 * (enclose() + 9.15273) * (enclose() + 9.15273)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.084455 * (1 - exp(-0.195716 * (distance() + 0.433996))) * pow(width() , 0.035389 * distance() + -0.137436) * (0.0346845 * thickness() + 0.233444) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.017316 * (1 - exp(-0.294378 * (distance() + 0.0979293))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.86)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0205344 * (1 - exp(-0.336613 * (distance() + 2.45528))) * pow(width() , -0.00082889 * distance() + 0.0349025) * pow(radius_up() , -0.023101)
    }
    if (distance() <= 0.0) {
        C = length() * 0.1077 * pow(width() , 0.0557138) * (0.0514838 * thickness() + 0.131298) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.86))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.86)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.27862 - 0.921655 * distance()) + 0.0915291 / pow(distance() , 1.17741)) * 3.73087 * pow((width1() + width2()) / 2 , 0.085958) * (0.0518459 * (thickness1() + thickness2()) / 2 + 0.0177786) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 5.24))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 5.24)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-4.2246 - 0.923577 * distance()) + 0.013947 / pow(distance() , 0.938877)) * 1.8907 * pow((width1() + width2()) / 2 , 0.0659609) * pow(radius_up() , 0.0218441)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.133036 * (1 - exp(-0.569318 * (distance() + 0.108939))) * pow(width() , 0.0175896 * distance() + -0.0511899) * (0.0436632 * thickness() + 0.258879)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0357765 * (1 - exp(-0.644197 * (distance() + 0.0201766)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143374 * pow(width() , 0.0423556) * (0.0835111 * thickness() + 0.216172)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0138386 * distance() + 0.317336) * (0.16846 - exp(-(-0.080066 * distance() + 0.133749) * (enclose() + 2.88566 * distance() + 5.46884) - (0.012628 * distance() + 0.0269167) * (enclose() + 0.711322 * distance() + 5.69013) * (enclose() + 0.711322 * distance() + 5.69013)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.133036 * (1 - exp(-0.569318 * (distance() + 0.108939))) * pow(width() , 0.0175896 * distance() + -0.0511899) * (0.0436632 * thickness() + 0.258879)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0119127 * (1 - exp(-1.26159 * (distance() + 0.600573))) * pow(width() , -0.00381421 * distance() + 0.0492242) * pow(radius_up() , 0.242382)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.193265 * (0.16846 - exp(0.266581 * (enclose() + 19.8971) - 0.0900567 * (enclose() + 9.24674) * (enclose() + 9.24674)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0320161 * (1 - exp(-0.490193 * (distance() + 0.13982))) * pow(width() , 0.0221065 * distance() + -0.060345) * (0.103999 * thickness() + 0.192825) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00739238 * (1 - exp(-0.583083 * (distance() + 0.0125229))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.012794 * (1 - exp(-0.550918 * (distance() + 1.96781))) * pow(width() , -0.00399164 * distance() + 0.0146597) * pow(radius_up() , -0.00459936)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0800683 * pow(width() , 0.0570745) * (0.0621813 * thickness() + 0.0635618) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 5.24)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE NEARBODY metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.21502 - 0.743777 * distance()) + 0.12273 / pow(distance() , 1.11317)) * 3.70341 * pow((width1() + width2()) / 2 , 0.0984603) * (0.0433945 * (thickness1() + thickness2()) / 2 + 0.0168525) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 5.24))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 5.24)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-4.02358 - 0.709324 * distance()) + 0.0165918 / pow(distance() , 0.922839)) * 1.64695 * pow((width1() + width2()) / 2 , 0.0756216) * pow(radius_up() , 0.022805)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.119315 * (1 - exp(-0.385891 * (distance() + 0.190281))) * pow(width() , 0.0249671 * distance() + -0.0842888) * (0.0380433 * thickness() + 0.249608)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0296328 * (1 - exp(-0.473063 * (distance() + 0.0371172)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133895 * pow(width() , 0.0476478) * (0.0701889 * thickness() + 0.190844)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00134431 * distance() + 0.0831535) * (0.700295 - exp(-(0.044157 * distance() + 0.69975) * (enclose() + -0.0143179 * distance() + 0.985039) - (0.0104659 * distance() + -0.130694) * (enclose() + -0.370097 * distance() + 1.8327) * (enclose() + -0.370097 * distance() + 1.8327)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.119315 * (1 - exp(-0.385891 * (distance() + 0.190281))) * pow(width() , 0.0249671 * distance() + -0.0842888) * (0.0380433 * thickness() + 0.249608)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00915231 * (1 - exp(-0.801288 * (distance() + 0.749537))) * pow(width() , 0.00602916 * distance() + 0.0446898) * pow(radius_up() , 0.176107)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.038216 * (0.700295 - exp(-0.920535 * (enclose() + 0.91345) - -0.0783645 * (enclose() + -0.017785) * (enclose() + -0.017785)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0402593 * (1 - exp(-0.352564 * (distance() + 0.22281))) * pow(width() , 0.0264086 * distance() + -0.0857541) * (0.0767431 * thickness() + 0.211973) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0092098 * (1 - exp(-0.444762 * (distance() + 0.0386604))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0143594 * (1 - exp(-0.469148 * (distance() + 2.13809))) * pow(width() , -0.00268819 * distance() + 0.0130353) * pow(radius_up() , 0.0131091)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0863562 * pow(width() , 0.0558463) * (0.0553187 * thickness() + 0.080132) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 5.24)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.07589 - 0.582204 * distance()) + 0.19169 / pow(distance() , 1.04117)) * 3.66784 * pow((width1() + width2()) / 2 , 0.11173) * (0.0321883 * (thickness1() + thickness2()) / 2 + 0.0136998) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 5.24))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 5.24)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.81757 - 0.604457 * distance()) + 0.0190256 / pow(distance() , 0.925676)) * 1.45751 * pow((width1() + width2()) / 2 , 0.0888479) * pow(radius_up() , 0.0119822)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.108582 * (1 - exp(-0.247531 * (distance() + 0.344252))) * pow(width() , 0.0321329 * distance() + -0.120184) * (0.031081 * thickness() + 0.241265)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0240399 * (1 - exp(-0.342401 * (distance() + 0.0785278)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123536 * pow(width() , 0.0538001) * (0.0583474 * thickness() + 0.165338)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00327586 * distance() + 0.0353576) * (0.772119 - exp(-(0.230674 * distance() + 0.232334) * (enclose() + 0.352602 * distance() + 1.07476) - (-0.00700751 * distance() + -0.0091417) * (enclose() + 0.96566 * distance() + 3.51601) * (enclose() + 0.96566 * distance() + 3.51601)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.108582 * (1 - exp(-0.247531 * (distance() + 0.344252))) * pow(width() , 0.0321329 * distance() + -0.120184) * (0.031081 * thickness() + 0.241265)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00679169 * (1 - exp(-0.578363 * (distance() + 1.20917))) * pow(width() , 0.00654715 * distance() + 0.0081188) * pow(radius_up() , 0.325141)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0258685 * (0.772119 - exp(-1.3857 * (enclose() + 2.83777) - -0.0441793 * (enclose() + 8.34431) * (enclose() + 8.34431)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0547086 * (1 - exp(-0.237748 * (distance() + 0.366559))) * pow(width() , 0.0318765 * distance() + -0.117544) * (0.051032 * thickness() + 0.223785) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0117024 * (1 - exp(-0.331992 * (distance() + 0.0818112))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.24)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0151972 * (1 - exp(-0.385547 * (distance() + 2.37133))) * pow(width() , -0.00269243 * distance() + 0.0387486) * pow(radius_up() , -0.0136055)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0934713 * pow(width() , 0.057209) * (0.0495669 * thickness() + 0.0993621) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 5.24))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 5.24)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE NEARBODY metal5 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    C = length() * (exp(-1.5221 - 1.45717 * distance()) + 0.0698022 / pow(distance() , 1.30523)) * 3.66232 * pow((width1() + width2()) / 2 , 0.106768) * (0.0688282 * (thickness1() + thickness2()) / 2 + 0.00502984) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.62))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.62)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-4.20131 - 0.84443 * distance()) + 0.0148351 / pow(distance() , 0.94235)) * 1.48937 * pow((width1() + width2()) / 2 , 0.0697605) * pow(radius_up() , 0.0386121)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0435114 * (1 - exp(-1.83965 * (distance() + 0.350114))) * pow(width() , -0.00346427 * distance() + 0.0108166) * (0.275755 * thickness() + 2.39098)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.108628 * (1 - exp(-1.59524 * (distance() + 0.445486)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.243135 * pow(width() , 0.0183469) * (0.311766 * thickness() + 0.317186)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0088491 * distance() + 0.120054) * (1.27154 - exp(-(-66.973 * distance() + -36.8752) * (enclose() + 35.6167 * distance() + 100) - (97.7516 * distance() + 46.4638) * (enclose() + -45.1099 * distance() + -32.6502) * (enclose() + -45.1099 * distance() + -32.6502)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0435114 * (1 - exp(-1.83965 * (distance() + 0.350114))) * pow(width() , -0.00346427 * distance() + 0.0108166) * (0.275755 * thickness() + 2.39098)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0608865 * (1 - exp(-1.98331 * (distance() + 0.805675))) * pow(width() , -0.00069464 * distance() + 0.0247572) * pow(radius_up() , 0.00719235)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0821497 * (1.27154 - exp(371.74 * (enclose() + 278.084) - 535.222 * (enclose() + -258.2) * (enclose() + -258.2)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE INTRINSIC FRINGE metal5 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0140556 * (1 - exp(-1.00474 * (distance() + 0.084529))) * pow(width() , 0.0142549 * distance() + -0.00601038) * (0.247932 * thickness() + 0.106753) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.62))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.62)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00313947 * (1 - exp(-1.15537 * (distance() + 0.0104715))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.62))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.62)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0103168 * (1 - exp(-1.00858 * (distance() + 1.04922))) * pow(width() , -0.00421569 * distance() + 0.0108399) * pow(radius_up() , 0.00965337)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0642473 * pow(width() , 0.0808495) * (0.0765305 * thickness() + 0.0125968) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.62))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.62)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    C = length() * (exp(-1.36202 - 0.849336 * distance()) + 0.106293 / pow(distance() , 1.11838)) * 3.98201 * pow((width1() + width2()) / 2 , 0.0915054) * (0.0471316 * (thickness1() + thickness2()) / 2 + 0.0169294) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.62))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.62)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.84175 - 0.767886 * distance()) + 0.0150389 / pow(distance() , 0.985245)) * 1.57777 * pow((width1() + width2()) / 2 , 0.0768542) * pow(radius_up() , 0.0164099)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.131386 * (1 - exp(-0.510295 * (distance() + 0.140408))) * pow(width() , 0.0185765 * distance() + -0.0544418) * (0.0398829 * thickness() + 0.258356)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0348023 * (1 - exp(-0.578925 * (distance() + 0.0331854)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143059 * pow(width() , 0.0482602) * (0.0803239 * thickness() + 0.213969)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00023241 * distance() + 0.065412) * (0.999993 - exp(-(-0.116055 * distance() + 2.10681) * (enclose() + -0.160036 * distance() + 1.83393) - (0.0239534 * distance() + -0.197211) * (enclose() + -0.240188 * distance() + 4.5018) * (enclose() + -0.240188 * distance() + 4.5018)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.131386 * (1 - exp(-0.510295 * (distance() + 0.140408))) * pow(width() , 0.0185765 * distance() + -0.0544418) * (0.0398829 * thickness() + 0.258356)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0155794 * (1 - exp(-0.959897 * (distance() + 0.745266))) * pow(width() , 0.00134615 * distance() + 0.0281441) * pow(radius_up() , 0.123189)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.032125 * (0.999993 - exp(-1.52653 * (enclose() + 1.03375) - -0.077444 * (enclose() + 3.30086) * (enclose() + 3.30086)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE INTRINSIC FRINGE metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0275961 * (1 - exp(-0.448577 * (distance() + 0.177544))) * pow(width() , 0.0215538 * distance() + -0.0584236) * (0.0987459 * thickness() + 0.198898) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.62))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.62)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0064403 * (1 - exp(-0.528848 * (distance() + 0.0315774))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.62))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.62)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0107638 * (1 - exp(-0.561699 * (distance() + 1.84105))) * pow(width() , -0.00342671 * distance() + 0.0438325) * pow(radius_up() , -0.00858895)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0771117 * pow(width() , 0.0617351) * (0.0544148 * thickness() + 0.0592818) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.62))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.62)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE NEARBODY ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    C = length() * (exp(-1.89364 - 0.587353 * distance()) + 0.0698981 / pow(distance() , 1.17819)) * 2.74568 * pow((width1() + width2()) / 2 , 0.0602292) * (0.0960952 * (thickness1() + thickness2()) / 2 + 0.0465141) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 7.169))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 7.169)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.25793 - 0.514893 * distance()) + 0.0098699 / pow(distance() , 1.28253)) * 0.844766 * pow((width1() + width2()) / 2 , 0.0459729) * pow(radius_up() , 0.128821)
    }
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0611449 * (1 - exp(-0.705519 * (distance()))) * pow(width() , 0.0153166 * distance() + -0.126789) * (-4.06734 * thickness() + 1.44413)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0385908 * (1 - exp(-0.594062 * (distance() + 0.100639)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.155281 * pow(width() , 0) * (0.329074 * thickness() + 0.171846)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00196842 * distance() + 0.0711384) * (0.866626 - exp(-(-0.0702513 * distance() + 1.34118) * (enclose() + 0.146123 * distance() + 0.262315) - (0.0903467 * distance() + -0.435682) * (enclose() + -0.317559 * distance() + 0.942715) * (enclose() + -0.317559 * distance() + 0.942715)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0611449 * (1 - exp(-0.705519 * (distance()))) * pow(width() , 0.0153166 * distance() + -0.126789) * (-4.06734 * thickness() + 1.44413)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0168107 * (1 - exp(-1.17851 * (distance() + 0.624582))) * pow(width() , 0.00017953 * distance() + 0.0146028) * pow(radius_up() , 0.106701)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0397029 * (0.866626 - exp(-1.04612 * (enclose() + 0.876032) - -0.0562259 * (enclose() + -0.391033) * (enclose() + -0.391033)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE INTRINSIC FRINGE ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0229451 * (1 - exp(-0.67829 * (distance()))) * pow(width() , 0.0118207 * distance() + -0.0452347) * (0.196778 * thickness() + 0.15677) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.169))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.169)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00461069 * (1 - exp(-0.579298 * (distance() + 0.013947))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.169))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.169)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00840708 * (1 - exp(-0.560927 * (distance() + 2.02626))) * pow(width() , 0.00159872 * distance() + 0.00733295) * pow(radius_up() , 0.0204865)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0592737 * pow(width() , 0.0190292) * (0.126868 * thickness() + 0.0538298) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 7.169))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 7.169)))
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.44051 - 1.17826 * distance()) + 0.121522 / pow(distance() , 1.07725)) * 4.41022 * pow((width1() + width2()) / 2 , 0.0795497) * (0.0441794 * (thickness1() + thickness2()) / 2 + 0.0115007) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.55))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.55)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER FRINGE metal1 poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.201621 * (1 - exp(-0.801836 * (distance() + 0.112595))) * pow(width() , 0.00532486 * distance() + 0.0134391) * (0.0423911 * thickness() + 0.22348) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.201621 * (1 - exp(-0.801836 * (distance() + 0.112595))) * pow(width() , 0.00532486 * distance() + 0.0134391) * (0.0423911 * thickness() + 0.22348) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0471612 * (1 - exp(-0.820146 * (distance() + 0.0533592))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.164846 * pow(width() , 0.0625405) * (0.116846 * thickness() + 0.256938) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0187671 * distance() + 0.911726) * (0.0810313 - exp(-(-0.259172 * distance() + 0.265407) * (enclose() + 4.28613 * distance() + 7.07427) - (0.00110167 * distance() + 0.0439228) * (enclose() + 4.4275 * distance() + 3.36641) * (enclose() + 4.4275 * distance() + 3.36641))) + 0.5 * 0.520277 * exp(-0.802162 * enclose()) * (0.114396 - 0.12703 * exp(-0.503089 * distance()) * pow(distance() , 0.102204))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0187671 * distance() + 0.911726) * (0.0810313 - exp(-(-0.259172 * distance() + 0.265407) * (enclose() + 4.28613 * distance() + 7.07427) - (0.00110167 * distance() + 0.0439228) * (enclose() + 4.4275 * distance() + 3.36641) * (enclose() + 4.4275 * distance() + 3.36641))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0463039 * (1 - exp(-1.10299 * (enclose() + 0.276344))) * pow(width() , -0.00796805 * enclose() + 0.0625251) + 0.5 * (0.0383332 * exp(-1.25158 * enclose()) + 0.0259946 * exp(-0.226774 * enclose())) * pow(width() , 0.0352235 * enclose() + 0.0699773)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0463039 * (1 - exp(-1.10299 * (enclose() + 0.276344))) * pow(width() , -0.00796805 * enclose() + 0.0625251) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.25264 - 0.557616 * distance()) + 0.358874 / pow(distance() , 0.904273)) * 3.84003 * pow((width1() + width2()) / 2 , 0.114676) * (0.0225951 * (thickness1() + thickness2()) / 2 + 0.00959893) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.93))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.93)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER FRINGE metal2 poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.128002 * (1 - exp(-0.338456 * (distance() + 0.399449))) * pow(width() , 0.0142334 * distance() + -0.0287854) * (0.0241449 * thickness() + 0.261073) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.128002 * (1 - exp(-0.338456 * (distance() + 0.399449))) * pow(width() , 0.0142334 * distance() + -0.0287854) * (0.0241449 * thickness() + 0.261073) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0324326 * (1 - exp(-0.369821 * (distance() + 0.151754))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.145507 * pow(width() , 0.0782752) * (0.0754086 * thickness() + 0.216785) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00056411 * distance() + 0.0764305) * (0.603925 - exp(-(0.0361103 * distance() + 0.416712) * (enclose() + -0.0671255 * distance() + 1.44163) - (0.017512 * distance() + -0.0851977) * (enclose() + -0.498591 * distance() + 1.54032) * (enclose() + -0.498591 * distance() + 1.54032))) + 0.5 * 0.490364 * exp(-0.395787 * enclose()) * (0.129001 - 0.118494 * exp(-0.198056 * distance()) * pow(distance() , 0.0413276))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00056411 * distance() + 0.0764305) * (0.603925 - exp(-(0.0361103 * distance() + 0.416712) * (enclose() + -0.0671255 * distance() + 1.44163) - (0.017512 * distance() + -0.0851977) * (enclose() + -0.498591 * distance() + 1.54032) * (enclose() + -0.498591 * distance() + 1.54032))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0336609 * (1 - exp(-0.410996 * (enclose() + 0.337608))) * pow(width() , 0.00695871 * enclose() + 0.0298083) + 0.5 * (0.0261223 * exp(-0.170556 * enclose()) + 0.0262992 * exp(-0.555498 * enclose())) * pow(width() , 0.0137521 * enclose() + 0.0662121)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0336609 * (1 - exp(-0.410996 * (enclose() + 0.337608))) * pow(width() , 0.00695871 * enclose() + 0.0298083) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.624885 - 0.332251 * distance()) + 1.06909 / pow(distance() , 0.844499)) * 3.86848 * pow((width1() + width2()) / 2 , 0.126372) * (0.00848499 * (thickness1() + thickness2()) / 2 + 0.00390643) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.31))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.31)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.1085 * (1 - exp(-0.256469 * (distance() + 0.671124))) * pow(width() , 0.0144794 * distance() + -0.0434346) * (0.0101817 * thickness() + 0.256137) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.1085 * (1 - exp(-0.256469 * (distance() + 0.671124))) * pow(width() , 0.0144794 * distance() + -0.0434346) * (0.0101817 * thickness() + 0.256137) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0265632 * (1 - exp(-0.269115 * (distance() + 0.248075))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.137239 * pow(width() , 0.0799349) * (0.0604054 * thickness() + 0.20311) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0019828 * distance() + 0.0561432) * (0.846754 - exp(-(0.0396239 * distance() + 0.0947602) * (enclose() + -0.00644313 * distance() + 1.26761) - (0.00145893 * distance() + -0.0203295) * (enclose() + -0.692376 * distance() + 0.712162) * (enclose() + -0.692376 * distance() + 0.712162))) + 0.5 * 0.678274 * exp(-0.266725 * enclose()) * (0.130769 - 0.115774 * exp(-0.0628752 * distance()) * pow(distance() , -0.00478094))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0019828 * distance() + 0.0561432) * (0.846754 - exp(-(0.0396239 * distance() + 0.0947602) * (enclose() + -0.00644313 * distance() + 1.26761) - (0.00145893 * distance() + -0.0203295) * (enclose() + -0.692376 * distance() + 0.712162) * (enclose() + -0.692376 * distance() + 0.712162))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0307745 * (1 - exp(-0.254549 * (enclose() + 0.373784))) * pow(width() , 0.00958028 * enclose() + 0.0194719) + 0.5 * (0.0217088 * exp(-0.139094 * enclose()) + 0.0217214 * exp(-0.311698 * enclose())) * pow(width() , 0.0082468 * enclose() + 0.0634552)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0307745 * (1 - exp(-0.254549 * (enclose() + 0.373784))) * pow(width() , 0.00958028 * enclose() + 0.0194719) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.326567 - 0.216017 * distance()) + 1.66998 / pow(distance() , 0.826645)) * 4.06158 * pow((width1() + width2()) / 2 , 0.131536) * (0.0053385 * (thickness1() + thickness2()) / 2 + 0.00254873) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.69))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.69)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0855174 * (1 - exp(-0.253959 * (distance() + 0.84876))) * pow(width() , 0.0114403 * distance() + -0.0438414) * (0.00205287 * thickness() + 0.261374) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0855174 * (1 - exp(-0.253959 * (distance() + 0.84876))) * pow(width() , 0.0114403 * distance() + -0.0438414) * (0.00205287 * thickness() + 0.261374) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0223211 * (1 - exp(-0.230777 * (distance() + 0.318669))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.132903 * pow(width() , 0.0787069) * (0.0513135 * thickness() + 0.192554) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00094257 * distance() + 0.0405736) * (0.87379 - exp(-(0.0350848 * distance() + 0.0687184) * (enclose() + -0.061604 * distance() + 1.29548) - (0.00118762 * distance() + -0.0100145) * (enclose() + -0.714628 * distance() + 0.761752) * (enclose() + -0.714628 * distance() + 0.761752))) + 0.5 * 0.672589 * exp(-0.201945 * enclose()) * (0.124835 - 0.108095 * exp(-0.0416466 * distance()) * pow(distance() , -0.0170054))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00094257 * distance() + 0.0405736) * (0.87379 - exp(-(0.0350848 * distance() + 0.0687184) * (enclose() + -0.061604 * distance() + 1.29548) - (0.00118762 * distance() + -0.0100145) * (enclose() + -0.714628 * distance() + 0.761752) * (enclose() + -0.714628 * distance() + 0.761752))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.029444 * (1 - exp(-0.186038 * (enclose() + 0.419492))) * pow(width() , 0.0105673 * enclose() + 0.0122982) + 0.5 * (0.0182871 * exp(-0.156456 * enclose()) + 0.0182872 * exp(-0.160046 * enclose())) * pow(width() , 0.00690671 * enclose() + 0.058362)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.029444 * (1 - exp(-0.186038 * (enclose() + 0.419492))) * pow(width() , 0.0105673 * enclose() + 0.0122982) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0 - 0.0947232 * distance()) + 8.36181 / pow(distance() , 0.72679)) * 4.31361 * pow((width1() + width2()) / 2 , 0.135965) * (0.00124151 * (thickness1() + thickness2()) / 2 + 0.00060355) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.07))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.07)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0666119 * (1 - exp(-0.273663 * (distance() + 0.976551))) * pow(width() , 0.00844908 * distance() + -0.045072) * (-0.00506661 * thickness() + 0.271557) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0666119 * (1 - exp(-0.273663 * (distance() + 0.976551))) * pow(width() , 0.00844908 * distance() + -0.045072) * (-0.00506661 * thickness() + 0.271557) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0192098 * (1 - exp(-0.207641 * (distance() + 0.376397))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.129458 * pow(width() , 0.0762973) * (0.0447983 * thickness() + 0.184794) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00035955 * distance() + 0.0270233) * (0.83061 - exp(-(0.0343925 * distance() + 0.0761183) * (enclose() + -0.160953 * distance() + 1.72125) - (0.00245688 * distance() + -0.00772777) * (enclose() + -0.647043 * distance() + 0.975942) * (enclose() + -0.647043 * distance() + 0.975942))) + 0.5 * 0.462551 * exp(-0.164784 * enclose()) * (0.124536 - 0.0999136 * exp(-0.0464266 * distance()) * pow(distance() , -0.033751))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00035955 * distance() + 0.0270233) * (0.83061 - exp(-(0.0343925 * distance() + 0.0761183) * (enclose() + -0.160953 * distance() + 1.72125) - (0.00245688 * distance() + -0.00772777) * (enclose() + -0.647043 * distance() + 0.975942) * (enclose() + -0.647043 * distance() + 0.975942))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0279263 * (1 - exp(-0.154296 * (enclose() + 0.459512))) * pow(width() , 0.0104047 * enclose() + 0.0092508) + 0.5 * (0.0157477 * exp(-0.124286 * enclose()) + 0.0157477 * exp(-0.124456 * enclose())) * pow(width() , 0.00662278 * enclose() + 0.0527023)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0279263 * (1 - exp(-0.154296 * (enclose() + 0.459512))) * pow(width() , 0.0104047 * enclose() + 0.0092508) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE NEARBODY ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.75892 - 0.23373 * distance()) + 0.263499 / pow(distance() , 0.535801)) * 2.74463 * pow((width1() + width2()) / 2 , 0.126372) * (0.0214998 * (thickness1() + thickness2()) / 2 + 0.0407662) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.619))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.619)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE CROSSOVER FRINGE ctm poly MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.119016 * (1 - exp(-0.881961 * (distance() + 0.346499))) * pow(width() , 0.0969137 * distance() + -0.520774) * (0.0019545 * thickness() + 0.111371) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.119016 * (1 - exp(-0.881961 * (distance() + 0.346499))) * pow(width() , 0.0969137 * distance() + -0.520774) * (0.0019545 * thickness() + 0.111371) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0441645 * (1 - exp(-0.0566031 * (distance() + 0.340551))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.14638 * pow(width() , 0.0503362) * (0.0279672 * thickness() + 0.176332) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00062905 * distance() + 0.0465403) * (0.601577 - exp(-(0.00854565 * distance() + 0.146014) * (enclose() + 0.0452915 * distance() + 1.99453) - (3.25891e-05 * distance() + -0.00556074) * (enclose() + -1.15994 * distance() + 4.5864) * (enclose() + -1.15994 * distance() + 4.5864))) + 0.5 * 0.471648 * exp(-0.188557 * enclose()) * (0.115925 - 0.10974 * exp(-0.0942752 * distance()) * pow(distance() , 0.0664117))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00062905 * distance() + 0.0465403) * (0.601577 - exp(-(0.00854565 * distance() + 0.146014) * (enclose() + 0.0452915 * distance() + 1.99453) - (3.25891e-05 * distance() + -0.00556074) * (enclose() + -1.15994 * distance() + 4.5864) * (enclose() + -1.15994 * distance() + 4.5864))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0460661 * (1 - exp(-0.0784277 * (enclose() + 0.346769))) * pow(width() , -0.010167 * enclose() + 0.0602667) + 0.5 * (0.0153971 * exp(-0.112322 * enclose()) + 0.0153971 * exp(-0.112322 * enclose())) * pow(width() , 0.00434932 * enclose() + 0.0222436)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.619)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0460661 * (1 - exp(-0.0784277 * (enclose() + 0.346769))) * pow(width() , -0.010167 * enclose() + 0.0602667) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.619))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.619)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-2.16434 - 0.197195 * distance()) + 1.30253 / pow(distance() , 0.788811)) * 2.22684 * pow((width1() + width2()) / 2 , 0.0981994) * (0.00967469 * (thickness1() + thickness2()) / 2 + 0.0177747) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 7.6))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 7.6)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 poly MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_calenclose = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0699834 * (1 - exp(-0.175081 * (distance() + 1.09735))) * pow(width() , 0.0527844 * distance() + -0.333625) * (0.00276127 * thickness() + 0.256678) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.6)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0699834 * (1 - exp(-0.175081 * (distance() + 1.09735))) * pow(width() , 0.0527844 * distance() + -0.333625) * (0.00276127 * thickness() + 0.256678) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0480381 * (1 - exp(-0.046953 * (distance() + 0.230056))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.6)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0899269 * pow(width() , 0.030112) * (0.0250681 * thickness() + 0.276104) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 7.6)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00207793 * distance() + 0.0260258) * (0.616557 - exp(-(0.01277 * distance() + 0.120967) * (enclose() + -0.0270123 * distance() + 2.49304) - (0.00150354 * distance() + -0.00403719) * (enclose() + -0.589142 * distance() + 5.15593) * (enclose() + -0.589142 * distance() + 5.15593))) + 0.5 * 0.351548 * exp(-0.172202 * enclose()) * (0.123749 - 0.108345 * exp(-0.112441 * distance()) * pow(distance() , 0.098025))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.6)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00207793 * distance() + 0.0260258) * (0.616557 - exp(-(0.01277 * distance() + 0.120967) * (enclose() + -0.0270123 * distance() + 2.49304) - (0.00150354 * distance() + -0.00403719) * (enclose() + -0.589142 * distance() + 5.15593) * (enclose() + -0.589142 * distance() + 5.15593))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.6)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0306121 * (1 - exp(-0.131938 * (enclose() + 0.483792))) * pow(width() , 0.0125536 * enclose() + -0.0387112) + 0.5 * (0.0146443 * exp(-0.0812364 * enclose()) + 0.0146443 * exp(-0.0808542 * enclose())) * pow(width() , 0.00245629 * enclose() + 0.0232058)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 7.6)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0306121 * (1 - exp(-0.131938 * (enclose() + 0.483792))) * pow(width() , 0.0125536 * enclose() + -0.0387112) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 7.6))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 7.6)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.49869 - 0.905563 * distance()) + 0.153385 / pow(distance() , 1.01674)) * 4.10931 * pow((width1() + width2()) / 2 , 0.094608) * (0.0402851 * (thickness1() + thickness2()) / 2 + 0.0140958) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.143974 * (1 - exp(-0.595593 * (distance() + 0.164569))) * pow(width() , 0.0089451 * distance() + 0.00092299) * (0.043141 * thickness() + 0.279965) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.143974 * (1 - exp(-0.595593 * (distance() + 0.164569))) * pow(width() , 0.0089451 * distance() + 0.00092299) * (0.043141 * thickness() + 0.279965) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0413457 * (1 - exp(-0.61809 * (distance() + 0.0692833))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.158434 * pow(width() , 0.0713297) * (0.0981588 * thickness() + 0.240877) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00845215 * distance() + 0.178232) * (0.312544 - exp(-(-0.105873 * distance() + 0.106655) * (enclose() + 3.64526 * distance() + 4.72528) - (0.00194475 * distance() + 0.0457652) * (enclose() + 2.20164 * distance() + 3.2499) * (enclose() + 2.20164 * distance() + 3.2499))) + 0.5 * 0.613431 * exp(-0.465025 * enclose()) * (0.1094 - 0.101523 * exp(-0.0974985 * distance()) * pow(distance() , 0.0129642))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00845215 * distance() + 0.178232) * (0.312544 - exp(-(-0.105873 * distance() + 0.106655) * (enclose() + 3.64526 * distance() + 4.72528) - (0.00194475 * distance() + 0.0457652) * (enclose() + 2.20164 * distance() + 3.2499) * (enclose() + 2.20164 * distance() + 3.2499))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0421571 * (1 - exp(-0.790358 * (enclose() + 0.562043))) * pow(width() , 0.00097485 * enclose() + 0.0462647) + 0.5 * (0.0237365 * exp(-0.831962 * enclose()) + 0.0162687 * exp(-0.178486 * enclose())) * pow(width() , 0.0300295 * enclose() + 0.0650595)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0421571 * (1 - exp(-0.790358 * (enclose() + 0.562043))) * pow(width() , 0.00097485 * enclose() + 0.0462647) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.16374 - 0.488838 * distance()) + 0.46365 / pow(distance() , 0.881278)) * 3.84605 * pow((width1() + width2()) / 2 , 0.118627) * (0.0183083 * (thickness1() + thickness2()) / 2 + 0.00798795) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.123778 * (1 - exp(-0.30594 * (distance() + 0.471946))) * pow(width() , 0.0149573 * distance() + -0.0360885) * (0.0201407 * thickness() + 0.257966) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.123778 * (1 - exp(-0.30594 * (distance() + 0.471946))) * pow(width() , 0.0149573 * distance() + -0.0360885) * (0.0201407 * thickness() + 0.257966) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0307132 * (1 - exp(-0.33451 * (distance() + 0.176908))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143315 * pow(width() , 0.0788111) * (0.0699712 * thickness() + 0.212661) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00497847 * distance() + 0.0833976) * (0.941921 - exp(-(0.0584914 * distance() + 0.284935) * (enclose() + -0.144036 * distance() + 1.52088) - (-0.00287555 * distance() + -0.024541) * (enclose() + -0.441034 * distance() + 4.17023) * (enclose() + -0.441034 * distance() + 4.17023))) + 0.5 * 1.21625 * exp(-0.291675 * enclose()) * (0.113266 - 0.107124 * exp(-0.0262672 * distance()) * pow(distance() , 0.00304246))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00497847 * distance() + 0.0833976) * (0.941921 - exp(-(0.0584914 * distance() + 0.284935) * (enclose() + -0.144036 * distance() + 1.52088) - (-0.00287555 * distance() + -0.024541) * (enclose() + -0.441034 * distance() + 4.17023) * (enclose() + -0.441034 * distance() + 4.17023))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0329513 * (1 - exp(-0.392519 * (enclose() + 0.767732))) * pow(width() , 0.00595021 * enclose() + 0.0373033) + 0.5 * (0.0172594 * exp(-0.151377 * enclose()) + 0.017432 * exp(-0.463128 * enclose())) * pow(width() , 0.0150207 * enclose() + 0.0554076)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0329513 * (1 - exp(-0.392519 * (enclose() + 0.767732))) * pow(width() , 0.00595021 * enclose() + 0.0373033) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.378973 - 0.290895 * distance()) + 1.48501 / pow(distance() , 0.83563)) * 3.98849 * pow((width1() + width2()) / 2 , 0.128128) * (0.00603854 * (thickness1() + thickness2()) / 2 + 0.00280654) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.61))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.61)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.101099 * (1 - exp(-0.253627 * (distance() + 0.72214))) * pow(width() , 0.0140161 * distance() + -0.0467161) * (0.0087923 * thickness() + 0.25802) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.101099 * (1 - exp(-0.253627 * (distance() + 0.72214))) * pow(width() , 0.0140161 * distance() + -0.0467161) * (0.0087923 * thickness() + 0.25802) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0253388 * (1 - exp(-0.255709 * (distance() + 0.267074))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.136123 * pow(width() , 0.0792277) * (0.0575823 * thickness() + 0.199854) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00059215 * distance() + 0.0409155) * (0.870568 - exp(-(0.104963 * distance() + 0.149532) * (enclose() + 0.301397 * distance() + 1.22807) - (-0.00131842 * distance() + -0.0116766) * (enclose() + 1.19673 * distance() + 3.05448) * (enclose() + 1.19673 * distance() + 3.05448))) + 0.5 * 1.56678 * exp(-0.218816 * enclose()) * (0.116969 - 0.111092 * exp(-0.0112364 * distance()) * pow(distance() , -0.00284275))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00059215 * distance() + 0.0409155) * (0.870568 - exp(-(0.104963 * distance() + 0.149532) * (enclose() + 0.301397 * distance() + 1.22807) - (-0.00131842 * distance() + -0.0116766) * (enclose() + 1.19673 * distance() + 3.05448) * (enclose() + 1.19673 * distance() + 3.05448))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.029663 * (1 - exp(-0.269752 * (enclose() + 0.909543))) * pow(width() , 0.00656784 * enclose() + 0.0338534) + 0.5 * (0.0148401 * exp(-0.122825 * enclose()) + 0.0148251 * exp(-0.293355 * enclose())) * pow(width() , 0.0109943 * enclose() + 0.0505963)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.029663 * (1 - exp(-0.269752 * (enclose() + 0.909543))) * pow(width() , 0.00656784 * enclose() + 0.0338534) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0 - 0.17392 * distance()) + 4.24094 / pow(distance() , 0.768722)) * 4.17548 * pow((width1() + width2()) / 2 , 0.134181) * (0.00232043 * (thickness1() + thickness2()) / 2 + 0.00111063) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.99))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.99)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0778845 * (1 - exp(-0.259103 * (distance() + 0.897772))) * pow(width() , 0.0112067 * distance() + -0.0501964) * (-0.00084666 * thickness() + 0.267595) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0778845 * (1 - exp(-0.259103 * (distance() + 0.897772))) * pow(width() , 0.0112067 * distance() + -0.0501964) * (-0.00084666 * thickness() + 0.267595) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0215108 * (1 - exp(-0.219091 * (distance() + 0.338286))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.131972 * pow(width() , 0.0774681) * (0.0490873 * thickness() + 0.190403) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0001467 * distance() + 0.0366503) * (0.909517 - exp(-(0.0637231 * distance() + 0.111622) * (enclose() + 0.295349 * distance() + 1.49134) - (-0.00071561 * distance() + -0.00618832) * (enclose() + 1.16543 * distance() + 4.40287) * (enclose() + 1.16543 * distance() + 4.40287))) + 0.5 * 1.056 * exp(-0.179824 * enclose()) * (0.118236 - 0.108762 * exp(-0.00966136 * distance()) * pow(distance() , -0.00949348))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0001467 * distance() + 0.0366503) * (0.909517 - exp(-(0.0637231 * distance() + 0.111622) * (enclose() + 0.295349 * distance() + 1.49134) - (-0.00071561 * distance() + -0.00618832) * (enclose() + 1.16543 * distance() + 4.40287) * (enclose() + 1.16543 * distance() + 4.40287))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0274859 * (1 - exp(-0.214878 * (enclose() + 1.016))) * pow(width() , 0.00623223 * enclose() + 0.0322433) + 0.5 * (0.0127648 * exp(-0.149543 * enclose()) + 0.0127649 * exp(-0.150565 * enclose())) * pow(width() , 0.00979862 * enclose() + 0.0453604)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0274859 * (1 - exp(-0.214878 * (enclose() + 1.016))) * pow(width() , 0.00623223 * enclose() + 0.0322433) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE NEARBODY ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.7439 - 0.246053 * distance()) + 0.247389 / pow(distance() , 0.556248)) * 2.67987 * pow((width1() + width2()) / 2 , 0.125721) * (0.023056 * (thickness1() + thickness2()) / 2 + 0.0422189) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 5.539))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 5.539)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE CROSSOVER FRINGE ctm metal1 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.12973 * (1 - exp(-0.710903 * (distance() + 0.38785))) * pow(width() , 0.0865792 * distance() + -0.457427) * (0.0054542 * thickness() + 0.114543) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.12973 * (1 - exp(-0.710903 * (distance() + 0.38785))) * pow(width() , 0.0865792 * distance() + -0.457427) * (0.0054542 * thickness() + 0.114543) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0494967 * (1 - exp(-0.0585109 * (distance() + 0.355401))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.148208 * pow(width() , 0.0523917) * (0.0338026 * thickness() + 0.181539) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00021638 * distance() + 0.062168) * (0.622905 - exp(-(0.00073383 * distance() + 0.162358) * (enclose() + 0.276053 * distance() + 1.85946) - (-0.00038876 * distance() + -0.00931585) * (enclose() + -0.978241 * distance() + 3.44044) * (enclose() + -0.978241 * distance() + 3.44044))) + 0.5 * 0.356784 * exp(-0.182768 * enclose()) * (0.111728 - 0.106312 * exp(-0.106288 * distance()) * pow(distance() , 0.128197))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00021638 * distance() + 0.062168) * (0.622905 - exp(-(0.00073383 * distance() + 0.162358) * (enclose() + 0.276053 * distance() + 1.85946) - (-0.00038876 * distance() + -0.00931585) * (enclose() + -0.978241 * distance() + 3.44044) * (enclose() + -0.978241 * distance() + 3.44044))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0282289 * (1 - exp(-0.181078 * (enclose() + 1.0523))) * pow(width() , 0.00327466 * enclose() + 0.0247158) + 0.5 * (0.0123156 * exp(-0.133985 * enclose()) + 0.0123156 * exp(-0.133985 * enclose())) * pow(width() , 0.00630369 * enclose() + 0.0177832)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 5.539)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0282289 * (1 - exp(-0.181078 * (enclose() + 1.0523))) * pow(width() , 0.00327466 * enclose() + 0.0247158) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 5.539))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 5.539)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.92879 - 0.233081 * distance()) + 1.40218 / pow(distance() , 0.797514)) * 2.24058 * pow((width1() + width2()) / 2 , 0.094764) * (0.00891867 * (thickness1() + thickness2()) / 2 + 0.015969) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.52))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.52)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal1 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_calenclose = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0791103 * (1 - exp(-0.172265 * (distance() + 0.940591))) * pow(width() , 0.0500938 * distance() + -0.307415) * (0.00316793 * thickness() + 0.257326) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.52)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0791103 * (1 - exp(-0.172265 * (distance() + 0.940591))) * pow(width() , 0.0500938 * distance() + -0.307415) * (0.00316793 * thickness() + 0.257326) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.52)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0509105 * (1 - exp(-0.0510895 * (distance() + 0.215129))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.52)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0908735 * pow(width() , 0.0294053) * (0.0274609 * thickness() + 0.286696) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 6.52)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0016766 * distance() + 0.0350194) * (0.681824 - exp(-(0.0138258 * distance() + 0.147458) * (enclose() + -0.0370985 * distance() + 2.3969) - (3.5671e-05 * distance() + -0.00536252) * (enclose() + -1.16146 * distance() + 6.43388) * (enclose() + -1.16146 * distance() + 6.43388))) + 0.5 * 0.293132 * exp(-0.181503 * enclose()) * (0.118643 - 0.104072 * exp(-0.121231 * distance()) * pow(distance() , 0.175801))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.52)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0016766 * distance() + 0.0350194) * (0.681824 - exp(-(0.0138258 * distance() + 0.147458) * (enclose() + -0.0370985 * distance() + 2.3969) - (3.5671e-05 * distance() + -0.00536252) * (enclose() + -1.16146 * distance() + 6.43388) * (enclose() + -1.16146 * distance() + 6.43388))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.52)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0338439 * (1 - exp(-0.144768 * (enclose() + 1.1732))) * pow(width() , 0.00477967 * enclose() + -0.0032971) + 0.5 * (0.0119275 * exp(-0.0976564 * enclose()) + 0.0119275 * exp(-0.0978106 * enclose())) * pow(width() , 0.00445506 * enclose() + 0.013055)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 6.52)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0338439 * (1 - exp(-0.144768 * (enclose() + 1.1732))) * pow(width() , 0.00477967 * enclose() + -0.0032971) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 6.52))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 6.52)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.50176 - 0.907081 * distance()) + 0.156039 / pow(distance() , 1.01299)) * 4.11214 * pow((width1() + width2()) / 2 , 0.0947895) * (0.0399139 * (thickness1() + thickness2()) / 2 + 0.0139713) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.143545 * (1 - exp(-0.595976 * (distance() + 0.164514))) * pow(width() , 0.00901205 * distance() + -0.00024011) * (0.0428246 * thickness() + 0.280206) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.143545 * (1 - exp(-0.595976 * (distance() + 0.164514))) * pow(width() , 0.00901205 * distance() + -0.00024011) * (0.0428246 * thickness() + 0.280206) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0412643 * (1 - exp(-0.617687 * (distance() + 0.0693011))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.158367 * pow(width() , 0.0710577) * (0.0979673 * thickness() + 0.240873) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0196801 * distance() + 0.348168) * (0.155666 - exp(-(-0.1014 * distance() + 0.071717) * (enclose() + 5.60349 * distance() + 10.7223) - (0.00062169 * distance() + 0.0318492) * (enclose() + 3.61889 * distance() + 5.35938) * (enclose() + 3.61889 * distance() + 5.35938))) + 0.5 * 0.68981 * exp(-0.402627 * enclose()) * (0.110183 - 0.101346 * exp(-0.0373017 * distance()) * pow(distance() , -0.0059966))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0196801 * distance() + 0.348168) * (0.155666 - exp(-(-0.1014 * distance() + 0.071717) * (enclose() + 5.60349 * distance() + 10.7223) - (0.00062169 * distance() + 0.0318492) * (enclose() + 3.61889 * distance() + 5.35938) * (enclose() + 3.61889 * distance() + 5.35938))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0429069 * (1 - exp(-0.853648 * (enclose() + 0.65998))) * pow(width() , 0.00090071 * enclose() + 0.0493512) + 0.5 * (0.01208 * exp(-0.182743 * enclose()) + 0.0163772 * exp(-0.83921 * enclose())) * pow(width() , 0.0344019 * enclose() + 0.0538393)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0429069 * (1 - exp(-0.853648 * (enclose() + 0.65998))) * pow(width() , 0.00090071 * enclose() + 0.0493512) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.16165 - 0.483984 * distance()) + 0.488544 / pow(distance() , 0.875625)) * 3.8516 * pow((width1() + width2()) / 2 , 0.118903) * (0.017609 * (thickness1() + thickness2()) / 2 + 0.00767088) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.1224 * (1 - exp(-0.307738 * (distance() + 0.472106))) * pow(width() , 0.0151172 * distance() + -0.0389806) * (0.0199046 * thickness() + 0.258197) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.1224 * (1 - exp(-0.307738 * (distance() + 0.472106))) * pow(width() , 0.0151172 * distance() + -0.0389806) * (0.0199046 * thickness() + 0.258197) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0305524 * (1 - exp(-0.3332 * (distance() + 0.177265))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143223 * pow(width() , 0.0782049) * (0.0700269 * thickness() + 0.212221) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00296788 * distance() + 0.0471558) * (0.682025 - exp(-(0.0783607 * distance() + 0.201531) * (enclose() + -0.201467 * distance() + 1.81984) - (0.00229549 * distance() + -0.00208193) * (enclose() + -0.221132 * distance() + 7.19647) * (enclose() + -0.221132 * distance() + 7.19647))) + 0.5 * 2.96349 * exp(-0.263682 * enclose()) * (0.110968 - 0.108479 * exp(-0.0050303 * distance()) * pow(distance() , -0.00083903))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00296788 * distance() + 0.0471558) * (0.682025 - exp(-(0.0783607 * distance() + 0.201531) * (enclose() + -0.201467 * distance() + 1.81984) - (0.00229549 * distance() + -0.00208193) * (enclose() + -0.221132 * distance() + 7.19647) * (enclose() + -0.221132 * distance() + 7.19647))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0332533 * (1 - exp(-0.428695 * (enclose() + 1.01195))) * pow(width() , 0.00482363 * enclose() + 0.0434166) + 0.5 * (0.0124173 * exp(-0.470485 * enclose()) + 0.0123686 * exp(-0.146458 * enclose())) * pow(width() , 0.0187143 * enclose() + 0.0443966)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0332533 * (1 - exp(-0.428695 * (enclose() + 1.01195))) * pow(width() , 0.00482363 * enclose() + 0.0434166) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.0326357 - 0.269166 * distance()) + 2.07265 / pow(distance() , 0.837664)) * 3.99198 * pow((width1() + width2()) / 2 , 0.130126) * (0.00430468 * (thickness1() + thickness2()) / 2 + 0.00200661) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.61))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.61)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0986678 * (1 - exp(-0.25488 * (distance() + 0.73733))) * pow(width() , 0.0147394 * distance() + -0.0543941) * (0.00648673 * thickness() + 0.260025) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0986678 * (1 - exp(-0.25488 * (distance() + 0.73733))) * pow(width() , 0.0147394 * distance() + -0.0543941) * (0.00648673 * thickness() + 0.260025) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0253074 * (1 - exp(-0.249475 * (distance() + 0.272406))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.136101 * pow(width() , 0.0784339) * (0.056832 * thickness() + 0.19984) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00111605 * distance() + 0.0381614) * (0.868313 - exp(-(0.114258 * distance() + 0.175748) * (enclose() + 0.384906 * distance() + 1.19998) - (-0.00131363 * distance() + -0.013537) * (enclose() + 1.31935 * distance() + 3.14063) * (enclose() + 1.31935 * distance() + 3.14063))) + 0.5 * 0.299419 * exp(-0.208426 * enclose()) * (0.123345 - 0.0950819 * exp(-0.0314726 * distance()) * pow(distance() , -0.0256635))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00111605 * distance() + 0.0381614) * (0.868313 - exp(-(0.114258 * distance() + 0.175748) * (enclose() + 0.384906 * distance() + 1.19998) - (-0.00131363 * distance() + -0.013537) * (enclose() + 1.31935 * distance() + 3.14063) * (enclose() + 1.31935 * distance() + 3.14063))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0295782 * (1 - exp(-0.302254 * (enclose() + 1.24252))) * pow(width() , 0.00491875 * enclose() + 0.042139) + 0.5 * (0.0107077 * exp(-0.314157 * enclose()) + 0.0107029 * exp(-0.112752 * enclose())) * pow(width() , 0.0143229 * enclose() + 0.039568)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0295782 * (1 - exp(-0.302254 * (enclose() + 1.24252))) * pow(width() , 0.00491875 * enclose() + 0.042139) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE NEARBODY ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.77183 - 0.265625 * distance()) + 0.21175 / pow(distance() , 0.595283)) * 2.6453 * pow((width1() + width2()) / 2 , 0.129167) * (0.0263451 * (thickness1() + thickness2()) / 2 + 0.0451129) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.159))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.159)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE CROSSOVER FRINGE ctm metal2 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.144132 * (1 - exp(-0.595967 * (distance() + 0.339457))) * pow(width() , 0.0728827 * distance() + -0.376769) * (0.00771563 * thickness() + 0.126181) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.144132 * (1 - exp(-0.595967 * (distance() + 0.339457))) * pow(width() , 0.0728827 * distance() + -0.376769) * (0.00771563 * thickness() + 0.126181) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0512218 * (1 - exp(-0.0735296 * (distance() + 0.357429))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.149693 * pow(width() , 0.0557258) * (0.0404695 * thickness() + 0.192394) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00784269 * distance() + 0.0204284) * (0.549544 - exp(-(-0.0209663 * distance() + 0.486069) * (enclose() + 0.18948 * distance() + 1.25897) - (0.00205665 * distance() + -0.0105985) * (enclose() + -1.66314 * distance() + 8.89407) * (enclose() + -1.66314 * distance() + 8.89407))) + 0.5 * 0.433815 * exp(-0.194668 * enclose()) * (0.10955 - 0.104115 * exp(-0.0567901 * distance()) * pow(distance() , 0.0715898))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00784269 * distance() + 0.0204284) * (0.549544 - exp(-(-0.0209663 * distance() + 0.486069) * (enclose() + 0.18948 * distance() + 1.25897) - (0.00205665 * distance() + -0.0105985) * (enclose() + -1.66314 * distance() + 8.89407) * (enclose() + -1.66314 * distance() + 8.89407))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0294823 * (1 - exp(-0.245945 * (enclose() + 1.38396))) * pow(width() , 0.00282885 * enclose() + 0.0297447) + 0.5 * (0.0100922 * exp(-0.173286 * enclose()) + 0.0100922 * exp(-0.168559 * enclose())) * pow(width() , 0.00890414 * enclose() + 0.0177466)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 4.159)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0294823 * (1 - exp(-0.245945 * (enclose() + 1.38396))) * pow(width() , 0.00282885 * enclose() + 0.0297447) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 4.159))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 4.159)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.82596 - 0.268689 * distance()) + 1.1477 / pow(distance() , 0.820914)) * 2.28311 * pow((width1() + width2()) / 2 , 0.0896749) * (0.0105974 * (thickness1() + thickness2()) / 2 + 0.01808) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 5.14))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 5.14)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal2 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_calenclose = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0853743 * (1 - exp(-0.169355 * (distance() + 0.721394))) * pow(width() , 0.0435945 * distance() + -0.260185) * (0.00360294 * thickness() + 0.292211) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.14)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0853743 * (1 - exp(-0.169355 * (distance() + 0.721394))) * pow(width() , 0.0435945 * distance() + -0.260185) * (0.00360294 * thickness() + 0.292211) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.14)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0569698 * (1 - exp(-0.0567195 * (distance() + 0.21003))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.14)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0926494 * pow(width() , 0.0281209) * (0.0306551 * thickness() + 0.304357) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 5.14)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00620734 * distance() + 0.0171848) * (0.592636 - exp(-(-0.0093871 * distance() + 0.31376) * (enclose() + 0.299751 * distance() + 1.11857) - (0.00367018 * distance() + -0.00810113) * (enclose() + -0.85492 * distance() + 7.16615) * (enclose() + -0.85492 * distance() + 7.16615))) + 0.5 * 0.20357 * exp(-0.196086 * enclose()) * (0.114679 - 0.097772 * exp(-0.151435 * distance()) * pow(distance() , 0.246036))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.14)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00620734 * distance() + 0.0171848) * (0.592636 - exp(-(-0.0093871 * distance() + 0.31376) * (enclose() + 0.299751 * distance() + 1.11857) - (0.00367018 * distance() + -0.00810113) * (enclose() + -0.85492 * distance() + 7.16615) * (enclose() + -0.85492 * distance() + 7.16615))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.14)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0363238 * (1 - exp(-0.180409 * (enclose() + 1.54456))) * pow(width() , 0.0029983 * enclose() + 0.00549838) + 0.5 * (0.00999145 * exp(-0.121478 * enclose()) + 0.00999145 * exp(-0.121482 * enclose())) * pow(width() , 0.00555506 * enclose() + 0.00727435)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 5.14)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0363238 * (1 - exp(-0.180409 * (enclose() + 1.54456))) * pow(width() , 0.0029983 * enclose() + 0.00549838) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 5.14))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 5.14)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.5126 - 0.912918 * distance()) + 0.16115 / pow(distance() , 1.00516)) * 4.12179 * pow((width1() + width2()) / 2 , 0.095024) * (0.0394215 * (thickness1() + thickness2()) / 2 + 0.0136572) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.142883 * (1 - exp(-0.597718 * (distance() + 0.164393))) * pow(width() , 0.00904505 * distance() + -0.00160142) * (0.0426573 * thickness() + 0.280387) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.142883 * (1 - exp(-0.597718 * (distance() + 0.164393))) * pow(width() , 0.00904505 * distance() + -0.00160142) * (0.0426573 * thickness() + 0.280387) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0411524 * (1 - exp(-0.617078 * (distance() + 0.069767))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.158279 * pow(width() , 0.0707962) * (0.0982795 * thickness() + 0.240661) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0217909 * distance() + 0.361389) * (0.148303 - exp(-(-0.0979603 * distance() + 0.0648687) * (enclose() + 5.84256 * distance() + 11.4327) - (0.00059398 * distance() + 0.0317397) * (enclose() + 3.65005 * distance() + 5.58597) * (enclose() + 3.65005 * distance() + 5.58597))) + 0.5 * 0.321373 * exp(-0.385503 * enclose()) * (0.114516 - 0.0963542 * exp(-0.0564528 * distance()) * pow(distance() , -0.0198359))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0217909 * distance() + 0.361389) * (0.148303 - exp(-(-0.0979603 * distance() + 0.0648687) * (enclose() + 5.84256 * distance() + 11.4327) - (0.00059398 * distance() + 0.0317397) * (enclose() + 3.65005 * distance() + 5.58597) * (enclose() + 3.65005 * distance() + 5.58597))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0433411 * (1 - exp(-0.911864 * (enclose() + 0.701294))) * pow(width() , 0.00014165 * enclose() + 0.053139) + 0.5 * (0.0121123 * exp(-0.882852 * enclose()) + 0.00989529 * exp(-0.193711 * enclose())) * pow(width() , 0.0382071 * enclose() + 0.0437904)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0433411 * (1 - exp(-0.911864 * (enclose() + 0.701294))) * pow(width() , 0.00014165 * enclose() + 0.053139) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.10394 - 0.457066 * distance()) + 0.55854 / pow(distance() , 0.868729)) * 3.85634 * pow((width1() + width2()) / 2 , 0.120996) * (0.0156745 * (thickness1() + thickness2()) / 2 + 0.00684084) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.121352 * (1 - exp(-0.307677 * (distance() + 0.48372))) * pow(width() , 0.0158349 * distance() + -0.0453407) * (0.018039 * thickness() + 0.258141) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.121352 * (1 - exp(-0.307677 * (distance() + 0.48372))) * pow(width() , 0.0158349 * distance() + -0.0453407) * (0.018039 * thickness() + 0.258141) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0304903 * (1 - exp(-0.327573 * (distance() + 0.182388))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143362 * pow(width() , 0.0779082) * (0.0692079 * thickness() + 0.21254) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0036524 * distance() + 0.0451681) * (0.669116 - exp(-(0.0617462 * distance() + 0.215943) * (enclose() + -0.213112 * distance() + 1.69178) - (0.00385407 * distance() + -0.00198797) * (enclose() + -0.219208 * distance() + 6.791) * (enclose() + -0.219208 * distance() + 6.791))) + 0.5 * 0.797522 * exp(-0.26133 * enclose()) * (0.115876 - 0.107153 * exp(-0.0107271 * distance()) * pow(distance() , -0.00555135))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0036524 * distance() + 0.0451681) * (0.669116 - exp(-(0.0617462 * distance() + 0.215943) * (enclose() + -0.213112 * distance() + 1.69178) - (0.00385407 * distance() + -0.00198797) * (enclose() + -0.219208 * distance() + 6.791) * (enclose() + -0.219208 * distance() + 6.791))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0336095 * (1 - exp(-0.45945 * (enclose() + 1.1313))) * pow(width() , 0.00410071 * enclose() + 0.0479557) + 0.5 * (0.00429728 * exp(-0.765794 * enclose()) + 0.0149188 * exp(-0.204891 * enclose())) * pow(width() , 0.0216314 * enclose() + 0.0354834)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0336095 * (1 - exp(-0.45945 * (enclose() + 1.1313))) * pow(width() , 0.00410071 * enclose() + 0.0479557) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE NEARBODY ctm INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.70078 - 0.299003 * distance()) + 0.198479 / pow(distance() , 0.649195)) * 2.24867 * pow((width1() + width2()) / 2 , 0.141782) * (0.0312791 * (thickness1() + thickness2()) / 2 + 0.048602) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.779))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.779)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE ctm metal3 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.156879 * (1 - exp(-0.576436 * (distance() + 0.223165))) * pow(width() , 0.0596376 * distance() + -0.296831) * (0.0119356 * thickness() + 0.148724) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.156879 * (1 - exp(-0.576436 * (distance() + 0.223165))) * pow(width() , 0.0596376 * distance() + -0.296831) * (0.0119356 * thickness() + 0.148724) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0515033 * (1 - exp(-0.104298 * (distance() + 0.384729))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.155992 * pow(width() , 0.0612721) * (0.0498891 * thickness() + 0.201863) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0113508 * distance() + 0.0204752) * (0.542991 - exp(-(-0.0398206 * distance() + 0.623816) * (enclose() + 0.412144 * distance() + 0.399608) - (0.0102834 * distance() + -0.013775) * (enclose() + -1.23509 * distance() + 6.24455) * (enclose() + -1.23509 * distance() + 6.24455))) + 0.5 * 0.298679 * exp(-0.235013 * enclose()) * (0.109999 - 0.101593 * exp(-0.069828 * distance()) * pow(distance() , 0.0855207))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0113508 * distance() + 0.0204752) * (0.542991 - exp(-(-0.0398206 * distance() + 0.623816) * (enclose() + 0.412144 * distance() + 0.399608) - (0.0102834 * distance() + -0.013775) * (enclose() + -1.23509 * distance() + 6.24455) * (enclose() + -1.23509 * distance() + 6.24455))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0368338 * (1 - exp(-0.259698 * (enclose() + 1.47154))) * pow(width() , -0.00817784 * enclose() + 0.0598972) + 0.5 * (0.00878692 * exp(-0.227708 * enclose()) + 0.00878692 * exp(-0.227709 * enclose())) * pow(width() , 0.0130423 * enclose() + 0.0225402)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 2.779)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0368338 * (1 - exp(-0.259698 * (enclose() + 1.47154))) * pow(width() , -0.00817784 * enclose() + 0.0598972) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 2.779))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 2.779)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.74507 - 0.287715 * distance()) + 0.916188 / pow(distance() , 0.863766)) * 2.375 * pow((width1() + width2()) / 2 , 0.0847408) * (0.0126208 * (thickness1() + thickness2()) / 2 + 0.0198903) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.76))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.76)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal3 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_calenclose = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0957193 * (1 - exp(-0.160834 * (distance() + 0.549186))) * pow(width() , 0.032507 * distance() + -0.195398) * (0.00362785 * thickness() + 0.34739) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.76)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0957193 * (1 - exp(-0.160834 * (distance() + 0.549186))) * pow(width() , 0.032507 * distance() + -0.195398) * (0.00362785 * thickness() + 0.34739) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.76)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0561793 * (1 - exp(-0.0785352 * (distance() + 0.181302))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.76)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0954371 * pow(width() , 0.0268072) * (0.0347409 * thickness() + 0.327337) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 3.76)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00958695 * distance() + 0.0152898) * (0.568331 - exp(-(-0.0318038 * distance() + 0.489596) * (enclose() + 0.490293 * distance() + 0.438957) - (0.0117917 * distance() + -0.0192282) * (enclose() + -0.812922 * distance() + 4.57962) * (enclose() + -0.812922 * distance() + 4.57962))) + 0.5 * 0.438135 * exp(-0.218418 * enclose()) * (0.107046 - 0.10093 * exp(-0.0510131 * distance()) * pow(distance() , 0.0759243))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.76)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00958695 * distance() + 0.0152898) * (0.568331 - exp(-(-0.0318038 * distance() + 0.489596) * (enclose() + 0.490293 * distance() + 0.438957) - (0.0117917 * distance() + -0.0192282) * (enclose() + -0.812922 * distance() + 4.57962) * (enclose() + -0.812922 * distance() + 4.57962))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.76)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0395201 * (1 - exp(-0.238233 * (enclose() + 1.57779))) * pow(width() , 0.00192487 * enclose() + 0.00996578) + 0.5 * (0.00902492 * exp(-0.154626 * enclose()) + 0.00902492 * exp(-0.155483 * enclose())) * pow(width() , 0.00710174 * enclose() + 0.00204668)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 3.76)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0395201 * (1 - exp(-0.238233 * (enclose() + 1.57779))) * pow(width() , 0.00192487 * enclose() + 0.00996578) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 3.76))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 3.76)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.51637 - 0.901584 * distance()) + 0.177549 / pow(distance() , 0.991569)) * 4.11456 * pow((width1() + width2()) / 2 , 0.0971904) * (0.0372013 * (thickness1() + thickness2()) / 2 + 0.012863) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.141792 * (1 - exp(-0.597009 * (distance() + 0.170344))) * pow(width() , 0.00944226 * distance() + -0.00481822) * (0.0414463 * thickness() + 0.282194) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.141792 * (1 - exp(-0.597009 * (distance() + 0.170344))) * pow(width() , 0.00944226 * distance() + -0.00481822) * (0.0414463 * thickness() + 0.282194) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0411115 * (1 - exp(-0.611676 * (distance() + 0.0742693))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.15859 * pow(width() , 0.0714374) * (0.0975982 * thickness() + 0.241399) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.011084 * distance() + 0.173267) * (0.306021 - exp(-(-0.0665759 * distance() + 0.0798555) * (enclose() + 4.01176 * distance() + 4.62974) - (0.0034489 * distance() + 0.0503358) * (enclose() + 1.46232 * distance() + 3.55344) * (enclose() + 1.46232 * distance() + 3.55344))) + 0.5 * 0.260738 * exp(-0.392395 * enclose()) * (0.117484 - 0.0955249 * exp(-0.0432057 * distance()) * pow(distance() , -0.0305653))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.011084 * distance() + 0.173267) * (0.306021 - exp(-(-0.0665759 * distance() + 0.0798555) * (enclose() + 4.01176 * distance() + 4.62974) - (0.0034489 * distance() + 0.0503358) * (enclose() + 1.46232 * distance() + 3.55344) * (enclose() + 1.46232 * distance() + 3.55344))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0439011 * (1 - exp(-0.947666 * (enclose() + 0.723994))) * pow(width() , 7.58407e-05 * enclose() + 0.055733) + 0.5 * (0.00921908 * exp(-0.934612 * enclose()) + 0.00877948 * exp(-0.213024 * enclose())) * pow(width() , 0.0405454 * enclose() + 0.0372044)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0439011 * (1 - exp(-0.947666 * (enclose() + 0.723994))) * pow(width() , 7.58407e-05 * enclose() + 0.055733) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE NEARBODY ctm INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-2.02221 - 0.356584 * distance()) + 0.13413 / pow(distance() , 0.741559)) * 2.51726 * pow((width1() + width2()) / 2 , 0.17477) * (0.0394246 * (thickness1() + thickness2()) / 2 + 0.0504959) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.399))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.399)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE ctm metal4 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0840536 * (1 - exp(-0.719981 * (distance() + 0.0562238))) * pow(width() , 0.0460448 * distance() + -0.209543) * (0.0503082 * thickness() + 0.377337) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0840536 * (1 - exp(-0.719981 * (distance() + 0.0562238))) * pow(width() , 0.0460448 * distance() + -0.209543) * (0.0503082 * thickness() + 0.377337) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0698107 * (1 - exp(-0.11068 * (distance() + 0.802757))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.171248 * pow(width() , 0.0669651) * (0.0675851 * thickness() + 0.213679) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00756703 * distance() + 0.0533345) * (0.657557 - exp(-(-0.103368 * distance() + 1.17186) * (enclose() + 0.208939 * distance() + 0.428107) - (0.0824163 * distance() + -0.297092) * (enclose() + -0.353878 * distance() + 1.23663) * (enclose() + -0.353878 * distance() + 1.23663))) + 0.5 * 0.219142 * exp(-0.325805 * enclose()) * (0.109946 - 0.0975262 * exp(-0.0966354 * distance()) * pow(distance() , 0.116059))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00756703 * distance() + 0.0533345) * (0.657557 - exp(-(-0.103368 * distance() + 1.17186) * (enclose() + 0.208939 * distance() + 0.428107) - (0.0824163 * distance() + -0.297092) * (enclose() + -0.353878 * distance() + 1.23663) * (enclose() + -0.353878 * distance() + 1.23663))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0361245 * (1 - exp(-0.665968 * (enclose() + 1.00992))) * pow(width() , 0.00756359 * enclose() + 0.0298674) + 0.5 * (0.00792823 * exp(-0.329043 * enclose()) + 0.00792824 * exp(-0.328978 * enclose())) * pow(width() , 0.0223064 * enclose() + 0.0328712)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 1.399)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0361245 * (1 - exp(-0.665968 * (enclose() + 1.00992))) * pow(width() , 0.00756359 * enclose() + 0.0298674) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 1.399))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 1.399)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.79511 - 0.336347 * distance()) + 0.653984 / pow(distance() , 0.914727)) * 2.53712 * pow((width1() + width2()) / 2 , 0.0828734) * (0.0164134 * (thickness1() + thickness2()) / 2 + 0.0223963) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.38))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.38)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal4 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_calenclose = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0978537 * (1 - exp(-0.214155 * (distance() + 0.276482))) * pow(width() , 0.0220212 * distance() + -0.133272) * (0.00416093 * thickness() + 0.408145) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.38)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0978537 * (1 - exp(-0.214155 * (distance() + 0.276482))) * pow(width() , 0.0220212 * distance() + -0.133272) * (0.00416093 * thickness() + 0.408145) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.38)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0508238 * (1 - exp(-0.143352 * (distance() + 0.097525))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.38)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.103622 * pow(width() , 0.0257688) * (0.0382235 * thickness() + 0.352157) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 2.38)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0129778 * distance() + 0.0200364) * (0.606756 - exp(-(-0.0636104 * distance() + 0.790047) * (enclose() + 0.326593 * distance() + 0.494028) - (0.0226066 * distance() + -0.040892) * (enclose() + -0.892993 * distance() + 4.0363) * (enclose() + -0.892993 * distance() + 4.0363))) + 0.5 * 0.395864 * exp(-0.261149 * enclose()) * (0.105799 - 0.101034 * exp(-0.054503 * distance()) * pow(distance() , 0.0674303))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.38)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0129778 * distance() + 0.0200364) * (0.606756 - exp(-(-0.0636104 * distance() + 0.790047) * (enclose() + 0.326593 * distance() + 0.494028) - (0.0226066 * distance() + -0.040892) * (enclose() + -0.892993 * distance() + 4.0363) * (enclose() + -0.892993 * distance() + 4.0363))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.38)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0450145 * (1 - exp(-0.344043 * (enclose() + 1.3584))) * pow(width() , 0.00089635 * enclose() + 0.0136356) + 0.5 * (0.00872197 * exp(-0.208259 * enclose()) + 0.00872198 * exp(-0.207739 * enclose())) * pow(width() , 0.0108809 * enclose() + -0.00431754)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 2.38)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0450145 * (1 - exp(-0.344043 * (enclose() + 1.3584))) * pow(width() , 0.00089635 * enclose() + 0.0136356) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 2.38))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 2.38)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE NEARBODY ctm INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-2.64562 - 0.412092 * distance()) + 0.0669663 / pow(distance() , 0.847208)) * 1.56359 * pow((width1() + width2()) / 2 , 0.333913) * (0.0949187 * (thickness1() + thickness2()) / 2 + 0.0555934) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.019))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.019)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE ctm metal5 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.1 * (1 - exp(-0.1 * (distance() + 0.1))) * pow(width() , -0.180478 * distance() + 0.354027) * (3.81218 * thickness() + 5.21381) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.1 * (1 - exp(-0.1 * (distance() + 0.1))) * pow(width() , -0.180478 * distance() + 0.354027) * (3.81218 * thickness() + 5.21381) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.142414 * (1 - exp(-0.187148 * (distance() + 6.47372))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.169361 * pow(width() , 0.409421) * (0.516065 * thickness() + 0.183206) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00422913 * distance() + 0.0705571) * (2.21507 - exp(-(-18.7678 * distance() + -5.58239) * (enclose() + -100 * distance() + 100) - (33.2888 * distance() + 75.6249) * (enclose() + -64.4736 * distance() + -100) * (enclose() + -64.4736 * distance() + -100))) + 0.5 * 0.204388 * exp(-0.608374 * enclose()) * (0.111331 - 0.0965022 * exp(-0.102027 * distance()) * pow(distance() , 0.125815))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00422913 * distance() + 0.0705571) * (2.21507 - exp(-(-18.7678 * distance() + -5.58239) * (enclose() + -100 * distance() + 100) - (33.2888 * distance() + 75.6249) * (enclose() + -64.4736 * distance() + -100) * (enclose() + -64.4736 * distance() + -100))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.160603 * (1 - exp(-0.3394 * (enclose() + 1.40188))) * pow(width() , -0.0633685 * enclose() + 0.145835) + 0.5 * (0.0066128 * exp(-0.634508 * enclose()) + 0.00661281 * exp(-0.634726 * enclose())) * pow(width() , 0.075351 * enclose() + 0.057096)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 0.019)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.160603 * (1 - exp(-0.3394 * (enclose() + 1.40188))) * pow(width() , -0.0633685 * enclose() + 0.145835) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 0.019))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 0.019)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.85743 - 0.403409 * distance()) + 0.486767 / pow(distance() , 1.00294)) * 2.63704 * pow((width1() + width2()) / 2 , 0.0917012) * (0.0213933 * (thickness1() + thickness2()) / 2 + 0.0204338) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal5 MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_calenclose = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.10286 * (1 - exp(-0.340858 * (distance() + 0.185522))) * pow(width() , 0.0127543 * distance() + -0.078912) * (0.0040068 * thickness() + 0.528355) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.10286 * (1 - exp(-0.340858 * (distance() + 0.185522))) * pow(width() , 0.0127543 * distance() + -0.078912) * (0.0040068 * thickness() + 0.528355) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0588744 * (1 - exp(-0.272911 * (distance() + 0.155474))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.11912 * pow(width() , 0.0240967) * (0.0426314 * thickness() + 0.412964) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00558686 * distance() + 0.0821389) * (0.802104 - exp(-(-0.0520151 * distance() + 1.008) * (enclose() + 0.108943 * distance() + 0.47021) - (0.0506676 * distance() + -0.272072) * (enclose() + -0.340457 * distance() + 1.23083) * (enclose() + -0.340457 * distance() + 1.23083))) + 0.5 * 0.198197 * exp(-0.362645 * enclose()) * (0.105558 - 0.0990107 * exp(-0.148176 * distance()) * pow(distance() , 0.161658))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00558686 * distance() + 0.0821389) * (0.802104 - exp(-(-0.0520151 * distance() + 1.008) * (enclose() + 0.108943 * distance() + 0.47021) - (0.0506676 * distance() + -0.272072) * (enclose() + -0.340457 * distance() + 1.23083) * (enclose() + -0.340457 * distance() + 1.23083))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0583518 * (1 - exp(-0.61689 * (enclose() + 0.914135))) * pow(width() , 0.00214325 * enclose() + 0.0101294) + 0.5 * (0.00899652 * exp(-0.430933 * enclose()) + 0.00899195 * exp(-0.213419 * enclose())) * pow(width() , 0.0180077 * enclose() + -0.00966846)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0583518 * (1 - exp(-0.61689 * (enclose() + 0.914135))) * pow(width() , 0.00214325 * enclose() + 0.0101294) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.91079 - 0.399777 * distance()) + 0.465097 / pow(distance() , 1.03248)) * 2.62786 * pow((width1() + width2()) / 2 , 0.0950336) * (0.0227069 * (thickness1() + thickness2()) / 2 + 0.0192698) * m37NB_COEF * (1 - m37RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.791))) * (1 - m37RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.791)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 ctm MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_calenclose = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    profile = field_profileid
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.106397 * (1 - exp(-0.375855 * (distance() + 0.219594))) * pow(width() , 0.0112756 * distance() + -0.0696384) * (0.00393733 * thickness() + 0.553143) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.791)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.106397 * (1 - exp(-0.375855 * (distance() + 0.219594))) * pow(width() , 0.0112756 * distance() + -0.0696384) * (0.00393733 * thickness() + 0.553143) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.791)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0615125 * (1 - exp(-0.324657 * (distance() + 0.16357))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.791)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.114432 * pow(width() , 0.0235378) * (0.0464878 * thickness() + 0.466919) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 0.791)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0206193 * distance() + 0.0354664) * (0.801346 - exp(-(-0.0290099 * distance() + 1.4471) * (enclose() + 0.26377 * distance() + -0.0298393) - (-0.0333086 * distance() + 0.0708804) * (enclose() + 0.0486147 * distance() + 3.09283) * (enclose() + 0.0486147 * distance() + 3.09283))) + 0.5 * 0.34906 * exp(-0.462665 * enclose()) * (0.10362 - 0.0991722 * exp(-0.085206 * distance()) * pow(distance() , 0.0602574))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.791)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0206193 * distance() + 0.0354664) * (0.801346 - exp(-(-0.0290099 * distance() + 1.4471) * (enclose() + 0.26377 * distance() + -0.0298393) - (-0.0333086 * distance() + 0.0708804) * (enclose() + 0.0486147 * distance() + 3.09283) * (enclose() + 0.0486147 * distance() + 3.09283))) * m37IN_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.791)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0624065 * (1 - exp(-0.710964 * (enclose() + 0.723902))) * pow(width() , 0.0024968 * enclose() + 0.0101292) + 0.5 * (0.0102587 * exp(-0.217063 * enclose()) + 0.0102487 * exp(-0.509654 * enclose())) * pow(width() , 0.0209495 * enclose() + -0.0147257)) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 0.791)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0624065 * (1 - exp(-0.710964 * (enclose() + 0.723902))) * pow(width() , 0.0024968 * enclose() + 0.0101292) * m37IN0_COEF * (1 - m37RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 0.791))) * (1 - m37RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 0.791)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.11261 - 1.84174 * distance()) + 0.0154742 / pow(distance() , 1.65344)) * 3.89809 * pow((width1() + width2()) / 2 , 0.0303423) * (0.0961904 * (thickness1() + thickness2()) / 2 + 0.0194931) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.55))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.55)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0932384 * (1 - exp(-0.978501 * (distance()))) * pow(width() , 0.0155757 * distance() + -0.040686) * (0.0300913 * thickness() + 0.235052)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.02217 * (1 - exp(-1.221 * (distance() + -0.0557765)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.114323 * pow(width() , 0.0130903) * (0.116888 * thickness() + 0.131791)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0019735 * distance() + 0.216519) * (0.206071 - exp(-(-0.0805472 * distance() + 0.00739298) * (enclose() + 14.2451 * distance() + 19.586) - (0.0005411 * distance() + 0.0393069) * (enclose() + 4.90972 * distance() + 5.59462) * (enclose() + 4.90972 * distance() + 5.59462)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0932384 * (1 - exp(-0.978501 * (distance()))) * pow(width() , 0.0155757 * distance() + -0.040686) * (0.0300913 * thickness() + 0.235052)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.103326 * (0.206071 - exp(0.395343 * (enclose() + 90.8115) - 0.0420124 * (enclose() + 30.1432) * (enclose() + 30.1432)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.122169 * (1 - exp(-1.09663 * (distance()))) * pow(width() , 0.00941325 * distance() + -0.0222542) * (0.0286626 * thickness() + 0.255548) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.122169 * (1 - exp(-1.09663 * (distance()))) * pow(width() , 0.00941325 * distance() + -0.0222542) * (0.0286626 * thickness() + 0.255548) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0318943 * (1 - exp(-1.28209 * (distance() + -0.0433721))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.134431 * pow(width() , 0.0092303) * (0.112932 * thickness() + 0.17685) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0145238 * distance() + 1.21225) * (0.053786 - exp(-(-0.31373 * distance() + 0.211881) * (enclose() + 6.14688 * distance() + 9.14603) - (0.00092324 * distance() + 0.0516472) * (enclose() + 5.59001 * distance() + 3.98591) * (enclose() + 5.59001 * distance() + 3.98591))) + 0.5 * 0.4486 * exp(-2.12125 * enclose()) * (0.0715388 - 0.136627 * exp(-0.994623 * distance()) * pow(distance() , 0.31513))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0145238 * distance() + 1.21225) * (0.053786 - exp(-(-0.31373 * distance() + 0.211881) * (enclose() + 6.14688 * distance() + 9.14603) - (0.00092324 * distance() + 0.0516472) * (enclose() + 5.59001 * distance() + 3.98591) * (enclose() + 5.59001 * distance() + 3.98591))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0318675 * (1 - exp(-1.74145 * (enclose() + 0.211995))) * pow(width() , -0.00047405 * enclose() + 0.0113786) + 0.5 * (0.0147595 * exp(-1.62191 * enclose()) + 0.0148286 * exp(-1.66334 * enclose())) * pow(width() , 0.00476737 * enclose() + 0.0122188)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0318675 * (1 - exp(-1.74145 * (enclose() + 0.211995))) * pow(width() , -0.00047405 * enclose() + 0.0113786) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.13915 - 1.36671 * distance()) + 0.0442747 / pow(distance() , 1.36465)) * 3.81813 * pow((width1() + width2()) / 2 , 0.0611149) * (0.069128 * (thickness1() + thickness2()) / 2 + 0.0169715) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.55))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.55)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0502738 * (1 - exp(-0.707141 * (distance() + 0.0430994))) * pow(width() , 0.0191538 * distance() + -0.048424) * (0.124803 * thickness() + 0.185596)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0118828 * (1 - exp(-0.85938 * (distance() + -0.0307876)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.092023 * pow(width() , 0.0382683) * (0.0939947 * thickness() + 0.0826833)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00092479 * distance() + 0.037497) * (0.66797 - exp(-(0.182829 * distance() + 0.187775) * (enclose() + -0.1106 * distance() + 1.48799) - (0.00473605 * distance() + -0.00648505) * (enclose() + -0.127393 * distance() + 1.41233) * (enclose() + -0.127393 * distance() + 1.41233)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0502738 * (1 - exp(-0.707141 * (distance() + 0.0430994))) * pow(width() , 0.0191538 * distance() + -0.048424) * (0.124803 * thickness() + 0.185596)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0164365 * (0.66797 - exp(-1.10192 * (enclose() + 0.93499) - 0.0171952 * (enclose() + 0.775365) * (enclose() + 0.775365)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.134819 * (1 - exp(-0.865819 * (distance() + 0.0347051))) * pow(width() , 0.0110453 * distance() + -0.0285371) * (0.0624827 * thickness() + 0.259204) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.134819 * (1 - exp(-0.865819 * (distance() + 0.0347051))) * pow(width() , 0.0110453 * distance() + -0.0285371) * (0.0624827 * thickness() + 0.259204) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0381527 * (1 - exp(-0.970069 * (distance() + -0.0097383))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.148851 * pow(width() , 0.0234241) * (0.0957424 * thickness() + 0.210499) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0019375 * distance() + 1.52634) * (0.0472046 - exp(-(-0.251727 * distance() + 0.200789) * (enclose() + 6.13574 * distance() + 10.4405) - (0.00072539 * distance() + 0.0392475) * (enclose() + 5.70091 * distance() + 4.55324) * (enclose() + 5.70091 * distance() + 4.55324))) + 0.5 * 0.527776 * exp(-1.62213 * enclose()) * (0.0842977 - 0.121533 * exp(-0.643642 * distance()) * pow(distance() , 0.198778))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0019375 * distance() + 1.52634) * (0.0472046 - exp(-(-0.251727 * distance() + 0.200789) * (enclose() + 6.13574 * distance() + 10.4405) - (0.00072539 * distance() + 0.0392475) * (enclose() + 5.70091 * distance() + 4.55324) * (enclose() + 5.70091 * distance() + 4.55324))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0383073 * (1 - exp(-1.4047 * (enclose() + 0.237871))) * pow(width() , -0.00366488 * enclose() + 0.0317501) + 0.5 * (0.024642 * exp(-1.53693 * enclose()) + 0.0162727 * exp(-0.818859 * enclose())) * pow(width() , 0.021689 * enclose() + 0.029351)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0383073 * (1 - exp(-1.4047 * (enclose() + 0.237871))) * pow(width() , -0.00366488 * enclose() + 0.0317501) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.17878 - 1.22887 * distance()) + 0.0668971 / pow(distance() , 1.26074)) * 3.87057 * pow((width1() + width2()) / 2 , 0.0701598) * (0.0586225 * (thickness1() + thickness2()) / 2 + 0.0148738) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.55))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.55)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0351082 * (1 - exp(-0.636544 * (distance() + 0.088058))) * pow(width() , 0.0195825 * distance() + -0.0455012) * (0.132918 * thickness() + 0.180852)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00820896 * (1 - exp(-0.769444 * (distance() + -0.00710603)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0815641 * pow(width() , 0.0513987) * (0.080834 * thickness() + 0.06264)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00025713 * distance() + 0.0206288) * (0.829173 - exp(-(0.199307 * distance() + 0.117081) * (enclose() + 0.216776 * distance() + 0.745255) - (-0.00718515 * distance() + -0.00173526) * (enclose() + 0.793483 * distance() + 2.12815) * (enclose() + 0.793483 * distance() + 2.12815)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0351082 * (1 - exp(-0.636544 * (distance() + 0.088058))) * pow(width() , 0.0195825 * distance() + -0.0455012) * (0.132918 * thickness() + 0.180852)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00967157 * (0.829173 - exp(-1.11362 * (enclose() + 1.82913) - -0.037661 * (enclose() + 6.09557) * (enclose() + 6.09557)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.143841 * (1 - exp(-0.798912 * (distance() + 0.0659349))) * pow(width() , 0.0111911 * distance() + -0.0267911) * (0.054839 * thickness() + 0.264917) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.143841 * (1 - exp(-0.798912 * (distance() + 0.0659349))) * pow(width() , 0.0111911 * distance() + -0.0267911) * (0.054839 * thickness() + 0.264917) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0407898 * (1 - exp(-0.884866 * (distance() + 0.0106616))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.155309 * pow(width() , 0.0327395) * (0.0950819 * thickness() + 0.222327) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0141751 * distance() + 1.61058) * (0.0455292 - exp(-(-0.249724 * distance() + 0.215645) * (enclose() + 5.79046 * distance() + 10.1324) - (0.0007084 * distance() + 0.0373119) * (enclose() + 5.63935 * distance() + 4.52785) * (enclose() + 5.63935 * distance() + 4.52785))) + 0.5 * 0.560971 * exp(-1.40303 * enclose()) * (0.0884768 - 0.115847 * exp(-0.554167 * distance()) * pow(distance() , 0.160042))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0141751 * distance() + 1.61058) * (0.0455292 - exp(-(-0.249724 * distance() + 0.215645) * (enclose() + 5.79046 * distance() + 10.1324) - (0.0007084 * distance() + 0.0373119) * (enclose() + 5.63935 * distance() + 4.52785) * (enclose() + 5.63935 * distance() + 4.52785))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0409902 * (1 - exp(-1.28735 * (enclose() + 0.250523))) * pow(width() , -0.00563195 * enclose() + 0.0435939) + 0.5 * (0.0215535 * exp(-0.62571 * enclose()) + 0.0257089 * exp(-1.52505 * enclose())) * pow(width() , 0.0287233 * enclose() + 0.0416031)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0409902 * (1 - exp(-1.28735 * (enclose() + 0.250523))) * pow(width() , -0.00563195 * enclose() + 0.0435939) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.27848 - 1.18279 * distance()) + 0.0781389 / pow(distance() , 1.20353)) * 4.14915 * pow((width1() + width2()) / 2 , 0.0737971) * (0.053931 * (thickness1() + thickness2()) / 2 + 0.0137731) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.55))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.55)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0274977 * (1 - exp(-0.62246 * (distance() + 0.110444))) * pow(width() , 0.0184658 * distance() + -0.0378891) * (0.134856 * thickness() + 0.173876)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00626749 * (1 - exp(-0.737784 * (distance() + 0.00773098)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0750142 * pow(width() , 0.0592624) * (0.0710995 * thickness() + 0.0513578)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00046764 * distance() + 0.0173442) * (0.840913 - exp(-(0.126619 * distance() + 0.12002) * (enclose() + 0.190315 * distance() + 0.917741) - (-0.00339543 * distance() + -0.00714306) * (enclose() + 0.802775 * distance() + 1.98133) * (enclose() + 0.802775 * distance() + 1.98133)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0274977 * (1 - exp(-0.62246 * (distance() + 0.110444))) * pow(width() , 0.0184658 * distance() + -0.0378891) * (0.134856 * thickness() + 0.173876)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.007503 * (0.840913 - exp(-0.753115 * (enclose() + 1.86932) - -0.0241202 * (enclose() + 5.99521) * (enclose() + 5.99521)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.148232 * (1 - exp(-0.781956 * (distance() + 0.080913))) * pow(width() , 0.0106799 * distance() + -0.0227276) * (0.0518598 * thickness() + 0.267995) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.148232 * (1 - exp(-0.781956 * (distance() + 0.080913))) * pow(width() , 0.0106799 * distance() + -0.0227276) * (0.0518598 * thickness() + 0.267995) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0421803 * (1 - exp(-0.853787 * (distance() + 0.0218312))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.161079 * pow(width() , 0.0387091) * (0.0941195 * thickness() + 0.225987) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0216932 * distance() + 1.70394) * (0.0432477 - exp(-(-0.250801 * distance() + 0.223016) * (enclose() + 5.67485 * distance() + 10.0723) - (0.00068934 * distance() + 0.0362486) * (enclose() + 5.67364 * distance() + 4.56751) * (enclose() + 5.67364 * distance() + 4.56751))) + 0.5 * 0.573688 * exp(-1.27844 * enclose()) * (0.0906664 - 0.113521 * exp(-0.524561 * distance()) * pow(distance() , 0.1429))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0216932 * distance() + 1.70394) * (0.0432477 - exp(-(-0.250801 * distance() + 0.223016) * (enclose() + 5.67485 * distance() + 10.0723) - (0.00068934 * distance() + 0.0362486) * (enclose() + 5.67364 * distance() + 4.56751) * (enclose() + 5.67364 * distance() + 4.56751))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0423285 * (1 - exp(-1.23055 * (enclose() + 0.258008))) * pow(width() , -0.00660455 * enclose() + 0.0493664) + 0.5 * (0.0283215 * exp(-1.4748 * enclose()) + 0.0230093 * exp(-0.509339 * enclose())) * pow(width() , 0.0315977 * enclose() + 0.0498127)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0423285 * (1 - exp(-1.23055 * (enclose() + 0.258008))) * pow(width() , -0.00660455 * enclose() + 0.0493664) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,ctm
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.29417 - 1.17354 * distance()) + 0.082615 / pow(distance() , 1.18816)) * 4.17397 * pow((width1() + width2()) / 2 , 0.0746009) * (0.0526254 * (thickness1() + thickness2()) / 2 + 0.0135106) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.55))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.55)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,ctm
//
CAPACITANCE CROSSOVER FRINGE metal1 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0247315 * (1 - exp(-0.621551 * (distance() + 0.11618))) * pow(width() , 0.0179566 * distance() + -0.0348328) * (0.13842 * thickness() + 0.175585)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00571906 * (1 - exp(-0.73118 * (distance() + 0.0120163)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0729211 * pow(width() , 0.0615829) * (0.0679357 * thickness() + 0.0480319)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00099247 * distance() + 0.018568) * (0.793744 - exp(-(0.064125 * distance() + 0.157151) * (enclose() + -0.00929293 * distance() + 1.24248) - (0.00168949 * distance() + -0.0224025) * (enclose() + -0.300602 * distance() + 1.42078) * (enclose() + -0.300602 * distance() + 1.42078)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0247315 * (1 - exp(-0.621551 * (distance() + 0.11618))) * pow(width() , 0.0179566 * distance() + -0.0348328) * (0.13842 * thickness() + 0.175585)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00680283 * (0.793744 - exp(-0.477776 * (enclose() + 1.19602) - -0.013955 * (enclose() + -0.08223) * (enclose() + -0.08223)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,ctm
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.149397 * (1 - exp(-0.779755 * (distance() + 0.0846984))) * pow(width() , 0.0104289 * distance() + -0.0210045) * (0.0512115 * thickness() + 0.268905) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.149397 * (1 - exp(-0.779755 * (distance() + 0.0846984))) * pow(width() , 0.0104289 * distance() + -0.0210045) * (0.0512115 * thickness() + 0.268905) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0425701 * (1 - exp(-0.847002 * (distance() + 0.0251652))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162104 * pow(width() , 0.0405284) * (0.0947989 * thickness() + 0.227712) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.025972 * distance() + 1.89505) * (0.0389518 - exp(-(-0.249039 * distance() + 0.219319) * (enclose() + 5.81737 * distance() + 10.5077) - (0.00064784 * distance() + 0.0349147) * (enclose() + 5.84389 * distance() + 4.79615) * (enclose() + 5.84389 * distance() + 4.79615))) + 0.5 * 0.576775 * exp(-1.24147 * enclose()) * (0.0912494 - 0.112916 * exp(-0.518722 * distance()) * pow(distance() , 0.138567))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.025972 * distance() + 1.89505) * (0.0389518 - exp(-(-0.249039 * distance() + 0.219319) * (enclose() + 5.81737 * distance() + 10.5077) - (0.00064784 * distance() + 0.0349147) * (enclose() + 5.84389 * distance() + 4.79615) * (enclose() + 5.84389 * distance() + 4.79615))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0426835 * (1 - exp(-1.21693 * (enclose() + 0.259887))) * pow(width() , -0.00694448 * enclose() + 0.05127) + 0.5 * (0.0233135 * exp(-0.475824 * enclose()) + 0.029277 * exp(-1.45529 * enclose())) * pow(width() , 0.0322956 * enclose() + 0.0523685)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0426835 * (1 - exp(-1.21693 * (enclose() + 0.259887))) * pow(width() , -0.00694448 * enclose() + 0.05127) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.31474 - 1.16648 * distance()) + 0.0890326 / pow(distance() , 1.16724)) * 4.20703 * pow((width1() + width2()) / 2 , 0.0756893) * (0.0509958 * (thickness1() + thickness2()) / 2 + 0.0130955) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.55))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.55)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0216369 * (1 - exp(-0.62257 * (distance() + 0.123503))) * pow(width() , 0.0170903 * distance() + -0.0298135) * (0.140021 * thickness() + 0.173283)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00497446 * (1 - exp(-0.723872 * (distance() + 0.0179365)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0697958 * pow(width() , 0.0648644) * (0.0635014 * thickness() + 0.0433156)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00037618 * distance() + 0.0139024) * (0.863624 - exp(-(0.0773721 * distance() + 0.0707656) * (enclose() + 0.245166 * distance() + 0.834679) - (-0.00235723 * distance() + -8.55934e-06) * (enclose() + 0.888524 * distance() + 1.74023) * (enclose() + 0.888524 * distance() + 1.74023)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0216369 * (1 - exp(-0.62257 * (distance() + 0.123503))) * pow(width() , 0.0170903 * distance() + -0.0298135) * (0.140021 * thickness() + 0.173283)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00601075 * (0.863624 - exp(-0.457626 * (enclose() + 2.06051) - -0.0117947 * (enclose() + 6.18285) * (enclose() + 6.18285)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = field_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.150927 * (1 - exp(-0.778665 * (distance() + 0.0894559))) * pow(width() , 0.00998725 * distance() + -0.0181457) * (0.0505162 * thickness() + 0.270169) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.150927 * (1 - exp(-0.778665 * (distance() + 0.0894559))) * pow(width() , 0.00998725 * distance() + -0.0181457) * (0.0505162 * thickness() + 0.270169) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0431047 * (1 - exp(-0.839395 * (distance() + 0.0293395))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.163488 * pow(width() , 0.0430937) * (0.0959673 * thickness() + 0.230068) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.026988 * distance() + 1.78055) * (0.0414551 - exp(-(-0.256799 * distance() + 0.239666) * (enclose() + 5.56807 * distance() + 9.81985) - (0.00067473 * distance() + 0.0360193) * (enclose() + 5.71514 * distance() + 4.38257) * (enclose() + 5.71514 * distance() + 4.38257))) + 0.5 * 0.580972 * exp(-1.18962 * enclose()) * (0.0919689 - 0.112079 * exp(-0.512676 * distance()) * pow(distance() , 0.133077))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.026988 * distance() + 1.78055) * (0.0414551 - exp(-(-0.256799 * distance() + 0.239666) * (enclose() + 5.56807 * distance() + 9.81985) - (0.00067473 * distance() + 0.0360193) * (enclose() + 5.71514 * distance() + 4.38257) * (enclose() + 5.71514 * distance() + 4.38257))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0431666 * (1 - exp(-1.19866 * (enclose() + 0.262355))) * pow(width() , -0.00735827 * enclose() + 0.0540675) + 0.5 * (0.0237633 * exp(-0.431154 * enclose()) + 0.0306301 * exp(-1.42837 * enclose())) * pow(width() , 0.0330679 * enclose() + 0.0560535)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0431666 * (1 - exp(-1.19866 * (enclose() + 0.262355))) * pow(width() , -0.00735827 * enclose() + 0.0540675) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.55))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.20326 - 1.16728 * distance()) + 0.0383529 / pow(distance() , 1.42156)) * 3.6208 * pow((width1() + width2()) / 2 , 0.0636113) * (0.0734587 * (thickness1() + thickness2()) / 2 + 0.0232543) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.93))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.93)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.111703 * (1 - exp(-0.69956 * (distance() + 0.0200828))) * pow(width() , 0.0154179 * distance() + -0.0463006) * (0.0632272 * thickness() + 0.246966)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0301771 * (1 - exp(-0.800455 * (distance() + -0.0321819)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.131663 * pow(width() , 0.0220069) * (0.0859035 * thickness() + 0.1847)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0025637 * distance() + 0.171792) * (0.307844 - exp(-(-0.160204 * distance() + 0.343653) * (enclose() + 1.27347 * distance() + 2.32779) - (0.0327839 * distance() + 0.0134098) * (enclose() + 0.380994 * distance() + 3.80859) * (enclose() + 0.380994 * distance() + 3.80859)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.111703 * (1 - exp(-0.69956 * (distance() + 0.0200828))) * pow(width() , 0.0154179 * distance() + -0.0463006) * (0.0632272 * thickness() + 0.246966)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0923053 * (0.307844 - exp(0.457367 * (enclose() + 8.69514) - 0.177329 * (enclose() + 5.71356) * (enclose() + 5.71356)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0633677 * (1 - exp(-0.631053 * (distance() + 0.0244101))) * pow(width() , 0.0196966 * distance() + -0.0579306) * (0.0976411 * thickness() + 0.207729) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0633677 * (1 - exp(-0.631053 * (distance() + 0.0244101))) * pow(width() , 0.0196966 * distance() + -0.0579306) * (0.0976411 * thickness() + 0.207729) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0156006 * (1 - exp(-0.75126 * (distance() + -0.0453832))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.101725 * pow(width() , 0.0302514) * (0.0868929 * thickness() + 0.108115) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-3.23555e-05 * distance() + 0.0561145) * (0.517388 - exp(-(0.0562773 * distance() + 0.544607) * (enclose() + -0.0840307 * distance() + 1.42698) - (0.0502991 * distance() + -0.103218) * (enclose() + -0.419788 * distance() + 1.67522) * (enclose() + -0.419788 * distance() + 1.67522))) + 0.5 * 0.27655 * exp(-0.908212 * enclose()) * (0.0905466 - 0.113128 * exp(-0.482256 * distance()) * pow(distance() , 0.191923))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-3.23555e-05 * distance() + 0.0561145) * (0.517388 - exp(-(0.0562773 * distance() + 0.544607) * (enclose() + -0.0840307 * distance() + 1.42698) - (0.0502991 * distance() + -0.103218) * (enclose() + -0.419788 * distance() + 1.67522) * (enclose() + -0.419788 * distance() + 1.67522))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0162035 * (1 - exp(-0.711134 * (enclose() + 0.211175))) * pow(width() , 0.0114024 * enclose() + -0.0118692) + 0.5 * (0.0115709 * exp(-0.82821 * enclose()) + 0.0115766 * exp(-0.800351 * enclose())) * pow(width() , -0.00719256 * enclose() + 0.0704844)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0162035 * (1 - exp(-0.711134 * (enclose() + 0.211175))) * pow(width() , 0.0114024 * enclose() + -0.0118692) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.10558 - 0.821256 * distance()) + 0.0755079 / pow(distance() , 1.26119)) * 3.57966 * pow((width1() + width2()) / 2 , 0.0914178) * (0.0522328 * (thickness1() + thickness2()) / 2 + 0.0201843) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.93))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.93)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0799255 * (1 - exp(-0.374478 * (distance() + 0.142638))) * pow(width() , 0.0281264 * distance() + -0.0976662) * (0.0609568 * thickness() + 0.22532)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0185822 * (1 - exp(-0.492254 * (distance() + -0.00208596)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.109876 * pow(width() , 0.0390633) * (0.066891 * thickness() + 0.132157)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00067484 * distance() + 0.0386574) * (0.833464 - exp(-(0.288696 * distance() + 0.342856) * (enclose() + 0.285871 * distance() + 1.11401) - (-0.00736184 * distance() + -0.0308177) * (enclose() + 0.873128 * distance() + 3.1827) * (enclose() + 0.873128 * distance() + 3.1827)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0799255 * (1 - exp(-0.374478 * (distance() + 0.142638))) * pow(width() , 0.0281264 * distance() + -0.0976662) * (0.0609568 * thickness() + 0.22532)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0210158 * (0.833464 - exp(-1.78634 * (enclose() + 2.54336) - -0.0676269 * (enclose() + 7.54834) * (enclose() + 7.54834)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0911112 * (1 - exp(-0.380046 * (distance() + 0.136477))) * pow(width() , 0.0277926 * distance() + -0.0971683) * (0.056267 * thickness() + 0.229766) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0911112 * (1 - exp(-0.380046 * (distance() + 0.136477))) * pow(width() , 0.0277926 * distance() + -0.0971683) * (0.056267 * thickness() + 0.229766) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0214173 * (1 - exp(-0.496586 * (distance() + -0.00326818))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.11633 * pow(width() , 0.0376174) * (0.068463 * thickness() + 0.146226) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00278058 * distance() + 0.046535) * (0.613289 - exp(-(0.054466 * distance() + 0.186825) * (enclose() + -0.0515273 * distance() + 1.6462) - (0.0316615 * distance() + 0.0017801) * (enclose() + -0.468584 * distance() + 2.75953) * (enclose() + -0.468584 * distance() + 2.75953))) + 0.5 * 0.438114 * exp(-0.795877 * enclose()) * (0.0967452 - 0.105699 * exp(-0.265205 * distance()) * pow(distance() , 0.108477))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00278058 * distance() + 0.046535) * (0.613289 - exp(-(0.054466 * distance() + 0.186825) * (enclose() + -0.0515273 * distance() + 1.6462) - (0.0316615 * distance() + 0.0017801) * (enclose() + -0.468584 * distance() + 2.75953) * (enclose() + -0.468584 * distance() + 2.75953))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0217926 * (1 - exp(-0.583791 * (enclose() + 0.254502))) * pow(width() , 0.00785887 * enclose() + 0.00705031) + 0.5 * (0.0159335 * exp(-0.635066 * enclose()) + 0.0159333 * exp(-0.635442 * enclose())) * pow(width() , -0.00162635 * enclose() + 0.0605238)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0217926 * (1 - exp(-0.583791 * (enclose() + 0.254502))) * pow(width() , 0.00785887 * enclose() + 0.00705031) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.10403 - 0.704827 * distance()) + 0.115589 / pow(distance() , 1.15085)) * 3.62821 * pow((width1() + width2()) / 2 , 0.101718) * (0.0426469 * (thickness1() + thickness2()) / 2 + 0.0172557) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.93))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.93)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0619909 * (1 - exp(-0.298556 * (distance() + 0.23933))) * pow(width() , 0.0310812 * distance() + -0.110081) * (0.0605155 * thickness() + 0.221859)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0137572 * (1 - exp(-0.408803 * (distance() + 0.0339935)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0983247 * pow(width() , 0.0492701) * (0.0581764 * thickness() + 0.108458)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (5.80021e-05 * distance() + 0.0292849) * (0.877872 - exp(-(0.178101 * distance() + 0.203124) * (enclose() + 0.403044 * distance() + 0.878608) - (-0.00246948 * distance() + -0.0221524) * (enclose() + 1.25827 * distance() + 2.41166) * (enclose() + 1.25827 * distance() + 2.41166)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0619909 * (1 - exp(-0.298556 * (distance() + 0.23933))) * pow(width() , 0.0310812 * distance() + -0.110081) * (0.0605155 * thickness() + 0.221859)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0147875 * (0.877872 - exp(-1.09363 * (enclose() + 2.89383) - -0.0344998 * (enclose() + 8.70301) * (enclose() + 8.70301)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.105934 * (1 - exp(-0.314013 * (distance() + 0.217617))) * pow(width() , 0.0304442 * distance() + -0.109714) * (0.0422236 * thickness() + 0.239048) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.105934 * (1 - exp(-0.314013 * (distance() + 0.217617))) * pow(width() , 0.0304442 * distance() + -0.109714) * (0.0422236 * thickness() + 0.239048) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0244865 * (1 - exp(-0.422141 * (distance() + 0.0308833))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123145 * pow(width() , 0.0450281) * (0.0648688 * thickness() + 0.164196) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00020336 * distance() + 0.073449) * (0.575661 - exp(-(0.0481069 * distance() + 0.44316) * (enclose() + -0.0858694 * distance() + 1.46912) - (0.0223463 * distance() + -0.0934635) * (enclose() + -0.45792 * distance() + 1.51603) * (enclose() + -0.45792 * distance() + 1.51603))) + 0.5 * 0.544183 * exp(-0.724119 * enclose()) * (0.0996015 - 0.10302 * exp(-0.195115 * distance()) * pow(distance() , 0.0752477))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00020336 * distance() + 0.073449) * (0.575661 - exp(-(0.0481069 * distance() + 0.44316) * (enclose() + -0.0858694 * distance() + 1.46912) - (0.0223463 * distance() + -0.0934635) * (enclose() + -0.45792 * distance() + 1.51603) * (enclose() + -0.45792 * distance() + 1.51603))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0248004 * (1 - exp(-0.523212 * (enclose() + 0.274848))) * pow(width() , 0.00691461 * enclose() + 0.0155949) + 0.5 * (0.0186601 * exp(-0.542902 * enclose()) + 0.01866 * exp(-0.541767 * enclose())) * pow(width() , 0.0021052 * enclose() + 0.0630244)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0248004 * (1 - exp(-0.523212 * (enclose() + 0.274848))) * pow(width() , 0.00691461 * enclose() + 0.0155949) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,ctm
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.12252 - 0.682268 * distance()) + 0.131482 / pow(distance() , 1.11382)) * 3.62303 * pow((width1() + width2()) / 2 , 0.103977) * (0.0406598 * (thickness1() + thickness2()) / 2 + 0.0165617) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.93))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.93)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,ctm
//
CAPACITANCE CROSSOVER FRINGE metal2 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0563587 * (1 - exp(-0.288134 * (distance() + 0.266003))) * pow(width() , 0.0308647 * distance() + -0.109224) * (0.0604967 * thickness() + 0.220851)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0124385 * (1 - exp(-0.392771 * (distance() + 0.045663)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0950526 * pow(width() , 0.0522781) * (0.0555275 * thickness() + 0.101552)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00046249 * distance() + 0.02921) * (0.8914 - exp(-(0.157424 * distance() + 0.213695) * (enclose() + 0.391363 * distance() + 1.03123) - (-0.00143852 * distance() + -0.0240156) * (enclose() + 1.26818 * distance() + 2.66874) * (enclose() + 1.26818 * distance() + 2.66874)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0563587 * (1 - exp(-0.288134 * (distance() + 0.266003))) * pow(width() , 0.0308647 * distance() + -0.109224) * (0.0604967 * thickness() + 0.220851)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0134488 * (0.8914 - exp(-1.00082 * (enclose() + 2.98805) - -0.0312082 * (enclose() + 9.00964) * (enclose() + 9.00964)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,ctm
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.109399 * (1 - exp(-0.304627 * (distance() + 0.239912))) * pow(width() , 0.0303709 * distance() + -0.10974) * (0.0389384 * thickness() + 0.241124) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.109399 * (1 - exp(-0.304627 * (distance() + 0.239912))) * pow(width() , 0.0303709 * distance() + -0.10974) * (0.0389384 * thickness() + 0.241124) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0252967 * (1 - exp(-0.40756 * (distance() + 0.0414985))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.125069 * pow(width() , 0.047451) * (0.0644492 * thickness() + 0.169066) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00028217 * distance() + 0.0748167) * (0.582717 - exp(-(0.0476454 * distance() + 0.428151) * (enclose() + -0.0847088 * distance() + 1.47236) - (0.0203622 * distance() + -0.0910064) * (enclose() + -0.471144 * distance() + 1.49256) * (enclose() + -0.471144 * distance() + 1.49256))) + 0.5 * 0.56875 * exp(-0.700898 * enclose()) * (0.100294 - 0.102475 * exp(-0.183158 * distance()) * pow(distance() , 0.0686019))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00028217 * distance() + 0.0748167) * (0.582717 - exp(-(0.0476454 * distance() + 0.428151) * (enclose() + -0.0847088 * distance() + 1.47236) - (0.0203622 * distance() + -0.0910064) * (enclose() + -0.471144 * distance() + 1.49256) * (enclose() + -0.471144 * distance() + 1.49256))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.025636 * (1 - exp(-0.507643 * (enclose() + 0.28006))) * pow(width() , 0.00676605 * enclose() + 0.0176659) + 0.5 * (0.0194967 * exp(-0.517293 * enclose()) + 0.0194975 * exp(-0.515128 * enclose())) * pow(width() , 0.0031934 * enclose() + 0.0643332)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.025636 * (1 - exp(-0.507643 * (enclose() + 0.28006))) * pow(width() , 0.00676605 * enclose() + 0.0176659) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.12603 - 0.647813 * distance()) + 0.155656 / pow(distance() , 1.07595)) * 3.65358 * pow((width1() + width2()) / 2 , 0.106492) * (0.0370731 * (thickness1() + thickness2()) / 2 + 0.0152785) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.93))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.93)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0483242 * (1 - exp(-0.280899 * (distance() + 0.300053))) * pow(width() , 0.0297745 * distance() + -0.104524) * (0.060659 * thickness() + 0.219737)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0106692 * (1 - exp(-0.37572 * (distance() + 0.0622892)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0906438 * pow(width() , 0.0565149) * (0.0515871 * thickness() + 0.0917795)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.000206 * distance() + 0.0230968) * (0.945986 - exp(-(0.105758 * distance() + 0.15448) * (enclose() + 0.550694 * distance() + 1.15419) - (-0.00115555 * distance() + -0.00993369) * (enclose() + 1.50162 * distance() + 4.38943) * (enclose() + 1.50162 * distance() + 4.38943)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0483242 * (1 - exp(-0.280899 * (distance() + 0.300053))) * pow(width() , 0.0297745 * distance() + -0.104524) * (0.060659 * thickness() + 0.219737)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0110334 * (0.945986 - exp(-0.68327 * (enclose() + 3.90766) - -0.0157114 * (enclose() + 11.8975) * (enclose() + 11.8975)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.113207 * (1 - exp(-0.297832 * (distance() + 0.268433))) * pow(width() , 0.0296178 * distance() + -0.106842) * (0.0348894 * thickness() + 0.243943) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.113207 * (1 - exp(-0.297832 * (distance() + 0.268433))) * pow(width() , 0.0296178 * distance() + -0.106842) * (0.0348894 * thickness() + 0.243943) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0263378 * (1 - exp(-0.391931 * (distance() + 0.0564117))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.127671 * pow(width() , 0.0509737) * (0.0641892 * thickness() + 0.175666) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-4.40722e-05 * distance() + 0.0744407) * (0.590284 - exp(-(0.0448903 * distance() + 0.428579) * (enclose() + -0.0807832 * distance() + 1.45317) - (0.0199614 * distance() + -0.0894464) * (enclose() + -0.472498 * distance() + 1.52064) * (enclose() + -0.472498 * distance() + 1.52064))) + 0.5 * 0.592794 * exp(-0.666834 * enclose()) * (0.101196 - 0.101855 * exp(-0.172551 * distance()) * pow(distance() , 0.0615258))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-4.40722e-05 * distance() + 0.0744407) * (0.590284 - exp(-(0.0448903 * distance() + 0.428579) * (enclose() + -0.0807832 * distance() + 1.45317) - (0.0199614 * distance() + -0.0894464) * (enclose() + -0.472498 * distance() + 1.52064) * (enclose() + -0.472498 * distance() + 1.52064))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0267444 * (1 - exp(-0.48782 * (enclose() + 0.286915))) * pow(width() , 0.00662738 * enclose() + 0.0202026) + 0.5 * (0.0206666 * exp(-0.402615 * enclose()) + 0.0208233 * exp(-0.580596 * enclose())) * pow(width() , 0.00470102 * enclose() + 0.0667114)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0267444 * (1 - exp(-0.48782 * (enclose() + 0.286915))) * pow(width() , 0.00662738 * enclose() + 0.0202026) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 1.93))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 1.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.22861 - 0.995104 * distance()) + 0.063622 / pow(distance() , 1.28235)) * 3.6713 * pow((width1() + width2()) / 2 , 0.0786406) * (0.0600405 * (thickness1() + thickness2()) / 2 + 0.0200935) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.31))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.31)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.125062 * (1 - exp(-0.599396 * (distance() + 0.0734736))) * pow(width() , 0.0178954 * distance() + -0.0545891) * (0.0502902 * thickness() + 0.254405)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0335657 * (1 - exp(-0.690965 * (distance() + -0.00289421)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.138178 * pow(width() , 0.0328536) * (0.0824623 * thickness() + 0.20436)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0304718 * distance() + 1.05965) * (0.0512691 - exp(-(-0.165352 * distance() + 0.347002) * (enclose() + 1.60075 * distance() + 7.03839) - (0.0165467 * distance() + 0.00521183) * (enclose() + 0.571692 * distance() + 8.22779) * (enclose() + 0.571692 * distance() + 8.22779)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.125062 * (1 - exp(-0.599396 * (distance() + 0.0734736))) * pow(width() , 0.0178954 * distance() + -0.0545891) * (0.0502902 * thickness() + 0.254405)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.606004 * (0.0512691 - exp(0.479758 * (enclose() + 15.0421) - 0.0879453 * (enclose() + 11.0862) * (enclose() + 11.0862)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0450468 * (1 - exp(-0.519732 * (distance() + 0.0926842))) * pow(width() , 0.0230996 * distance() + -0.0680192) * (0.101372 * thickness() + 0.199475) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0450468 * (1 - exp(-0.519732 * (distance() + 0.0926842))) * pow(width() , 0.0230996 * distance() + -0.0680192) * (0.101372 * thickness() + 0.199475) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0106411 * (1 - exp(-0.632197 * (distance() + -0.015173))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0894039 * pow(width() , 0.0451901) * (0.073515 * thickness() + 0.0826831) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00112007 * distance() + 0.0316689) * (0.735395 - exp(-(0.0941443 * distance() + 0.15582) * (enclose() + -0.163163 * distance() + 1.40227) - (0.0121325 * distance() + -0.0253562) * (enclose() + -0.460349 * distance() + 1.12188) * (enclose() + -0.460349 * distance() + 1.12188))) + 0.5 * 0.212386 * exp(-0.575458 * enclose()) * (0.1007 - 0.0978741 * exp(-0.264965 * distance()) * pow(distance() , 0.0703543))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00112007 * distance() + 0.0316689) * (0.735395 - exp(-(0.0941443 * distance() + 0.15582) * (enclose() + -0.163163 * distance() + 1.40227) - (0.0121325 * distance() + -0.0253562) * (enclose() + -0.460349 * distance() + 1.12188) * (enclose() + -0.460349 * distance() + 1.12188))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0120806 * (1 - exp(-0.403999 * (enclose() + 0.147266))) * pow(width() , 0.0250847 * enclose() + -0.061848) + 0.5 * (0.00959142 * exp(-0.548051 * enclose()) + 0.00959165 * exp(-0.547808 * enclose())) * pow(width() , -0.00741063 * enclose() + 0.117376)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0120806 * (1 - exp(-0.403999 * (enclose() + 0.147266))) * pow(width() , 0.0250847 * enclose() + -0.061848) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.09043 - 0.678882 * distance()) + 0.120595 / pow(distance() , 1.14323)) * 3.59147 * pow((width1() + width2()) / 2 , 0.103997) * (0.0418578 * (thickness1() + thickness2()) / 2 + 0.017182) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.31))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.31)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0989828 * (1 - exp(-0.284149 * (distance() + 0.24607))) * pow(width() , 0.0323417 * distance() + -0.118446) * (0.0426144 * thickness() + 0.235758)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0221381 * (1 - exp(-0.396603 * (distance() + 0.0370918)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.118349 * pow(width() , 0.0460335) * (0.0611917 * thickness() + 0.152782)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00193034 * distance() + 0.0432112) * (0.687811 - exp(-(0.130254 * distance() + 0.183004) * (enclose() + 0.0411632 * distance() + 1.44403) - (-3.32529e-05 * distance() + -2.40707e-05) * (enclose() + -13.8236 * distance() + 20.1389) * (enclose() + -13.8236 * distance() + 20.1389)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0989828 * (1 - exp(-0.284149 * (distance() + 0.24607))) * pow(width() , 0.0323417 * distance() + -0.118446) * (0.0426144 * thickness() + 0.235758)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0264314 * (0.687811 - exp(-0.834274 * (enclose() + 1.64985) - -0.000190335 * (enclose() + -48.9791) * (enclose() + -48.9791)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0718756 * (1 - exp(-0.275189 * (distance() + 0.256002))) * pow(width() , 0.032794 * distance() + -0.119498) * (0.0542183 * thickness() + 0.225097) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0718756 * (1 - exp(-0.275189 * (distance() + 0.256002))) * pow(width() , 0.032794 * distance() + -0.119498) * (0.0542183 * thickness() + 0.225097) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0156713 * (1 - exp(-0.38888 * (distance() + 0.0367892))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.103069 * pow(width() , 0.0480222) * (0.0587079 * thickness() + 0.118962) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00237523 * distance() + 0.045136) * (0.84904 - exp(-(0.0603369 * distance() + 0.119291) * (enclose() + -0.0565075 * distance() + 0.999293) - (0.00206332 * distance() + -0.0227323) * (enclose() + -0.55926 * distance() + 1.09431) * (enclose() + -0.55926 * distance() + 1.09431))) + 0.5 * 0.514468 * exp(-0.52294 * enclose()) * (0.102786 - 0.0992132 * exp(-0.09605 * distance()) * pow(distance() , 0.0268947))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00237523 * distance() + 0.045136) * (0.84904 - exp(-(0.0603369 * distance() + 0.119291) * (enclose() + -0.0565075 * distance() + 0.999293) - (0.00206332 * distance() + -0.0227323) * (enclose() + -0.55926 * distance() + 1.09431) * (enclose() + -0.55926 * distance() + 1.09431))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0170651 * (1 - exp(-0.349175 * (enclose() + 0.22069))) * pow(width() , 0.0167426 * enclose() + -0.0245802) + 0.5 * (0.0132575 * exp(-0.451217 * enclose()) + 0.013258 * exp(-0.443711 * enclose())) * pow(width() , -0.0041127 * enclose() + 0.0893244)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0170651 * (1 - exp(-0.349175 * (enclose() + 0.22069))) * pow(width() , 0.0167426 * enclose() + -0.0245802) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,ctm
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.04696 - 0.620408 * distance()) + 0.148575 / pow(distance() , 1.10397)) * 3.60749 * pow((width1() + width2()) / 2 , 0.108952) * (0.0367151 * (thickness1() + thickness2()) / 2 + 0.0154944) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.31))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.31)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE metal3 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0924304 * (1 - exp(-0.241695 * (distance() + 0.310003))) * pow(width() , 0.0345198 * distance() + -0.129466) * (0.0407323 * thickness() + 0.233371)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0197623 * (1 - exp(-0.353376 * (distance() + 0.0560865)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.112974 * pow(width() , 0.0495411) * (0.0570865 * thickness() + 0.141904)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00074584 * distance() + 0.0369823) * (0.861102 - exp(-(0.217763 * distance() + 0.338671) * (enclose() + 0.30066 * distance() + 1.19856) - (-0.00355525 * distance() + -0.0332152) * (enclose() + 0.972425 * distance() + 3.19141) * (enclose() + 0.972425 * distance() + 3.19141)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0924304 * (1 - exp(-0.241695 * (distance() + 0.310003))) * pow(width() , 0.0345198 * distance() + -0.129466) * (0.0407323 * thickness() + 0.233371)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0203558 * (0.861102 - exp(-1.42749 * (enclose() + 2.70186) - -0.0509915 * (enclose() + 8.05354) * (enclose() + 8.05354)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0806152 * (1 - exp(-0.238834 * (distance() + 0.312852))) * pow(width() , 0.0346391 * distance() + -0.129825) * (0.0455466 * thickness() + 0.229182) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0806152 * (1 - exp(-0.238834 * (distance() + 0.312852))) * pow(width() , 0.0346391 * distance() + -0.129825) * (0.0455466 * thickness() + 0.229182) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0170746 * (1 - exp(-0.350691 * (distance() + 0.055345))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.106836 * pow(width() , 0.0501127) * (0.0559089 * thickness() + 0.127407) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00246283 * distance() + 0.0478067) * (0.855406 - exp(-(0.0546132 * distance() + 0.111313) * (enclose() + -0.0370188 * distance() + 0.999888) - (0.00159741 * distance() + -0.0222099) * (enclose() + -0.596303 * distance() + 0.994235) * (enclose() + -0.596303 * distance() + 0.994235))) + 0.5 * 0.760122 * exp(-0.506982 * enclose()) * (0.103148 - 0.0998902 * exp(-0.0607568 * distance()) * pow(distance() , 0.015981))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00246283 * distance() + 0.0478067) * (0.855406 - exp(-(0.0546132 * distance() + 0.111313) * (enclose() + -0.0370188 * distance() + 0.999888) - (0.00159741 * distance() + -0.0222099) * (enclose() + -0.596303 * distance() + 0.994235) * (enclose() + -0.596303 * distance() + 0.994235))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0184501 * (1 - exp(-0.333906 * (enclose() + 0.235229))) * pow(width() , 0.0153815 * enclose() + -0.0173294) + 0.5 * (0.0143579 * exp(-0.420969 * enclose()) + 0.014358 * exp(-0.420541 * enclose())) * pow(width() , -0.00331864 * enclose() + 0.086273)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0184501 * (1 - exp(-0.333906 * (enclose() + 0.235229))) * pow(width() , 0.0153815 * enclose() + -0.0173294) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.01682 - 0.555452 * distance()) + 0.196284 / pow(distance() , 1.04822)) * 3.63534 * pow((width1() + width2()) / 2 , 0.114126) * (0.0310821 * (thickness1() + thickness2()) / 2 + 0.0134589) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.31))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.31)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0808392 * (1 - exp(-0.207487 * (distance() + 0.39525))) * pow(width() , 0.0353163 * distance() + -0.135222) * (0.0387269 * thickness() + 0.231785)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0166975 * (1 - exp(-0.311345 * (distance() + 0.0844142)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.106275 * pow(width() , 0.0542798) * (0.0517809 * thickness() + 0.12719)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00095844 * distance() + 0.0279467) * (0.88 - exp(-(0.146271 * distance() + 0.170423) * (enclose() + 0.472839 * distance() + 0.777736) - (-0.00222893 * distance() + -0.0147133) * (enclose() + 1.36788 * distance() + 2.42858) * (enclose() + 1.36788 * distance() + 2.42858)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0808392 * (1 - exp(-0.207487 * (distance() + 0.39525))) * pow(width() , 0.0353163 * distance() + -0.135222) * (0.0387269 * thickness() + 0.231785)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0163695 * (0.88 - exp(-0.901778 * (enclose() + 3.14193) - -0.025858 * (enclose() + 9.26798) * (enclose() + 9.26798)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0909703 * (1 - exp(-0.208901 * (distance() + 0.388144))) * pow(width() , 0.0354799 * distance() + -0.136471) * (0.0359146 * thickness() + 0.234051) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0909703 * (1 - exp(-0.208901 * (distance() + 0.388144))) * pow(width() , 0.0354799 * distance() + -0.136471) * (0.0359146 * thickness() + 0.234051) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0188718 * (1 - exp(-0.313029 * (distance() + 0.0821017))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.111142 * pow(width() , 0.0534226) * (0.0537466 * thickness() + 0.138746) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00111892 * distance() + 0.0428292) * (0.80785 - exp(-(0.0630275 * distance() + 0.141296) * (enclose() + -0.101177 * distance() + 1.21447) - (0.00484974 * distance() + -0.0250843) * (enclose() + -0.42193 * distance() + 1.20828) * (enclose() + -0.42193 * distance() + 1.20828))) + 0.5 * 1.45971 * exp(-0.48271 * enclose()) * (0.10322 - 0.100889 * exp(-0.0292951 * distance()) * pow(distance() , 0.00669894))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00111892 * distance() + 0.0428292) * (0.80785 - exp(-(0.0630275 * distance() + 0.141296) * (enclose() + -0.101177 * distance() + 1.21447) - (0.00484974 * distance() + -0.0250843) * (enclose() + -0.42193 * distance() + 1.20828) * (enclose() + -0.42193 * distance() + 1.20828))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0203031 * (1 - exp(-0.314271 * (enclose() + 0.252411))) * pow(width() , 0.0139583 * enclose() + -0.00912661) + 0.5 * (0.0159128 * exp(-0.384865 * enclose()) + 0.0159129 * exp(-0.385783 * enclose())) * pow(width() , -0.00202357 * enclose() + 0.0839409)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0203031 * (1 - exp(-0.314271 * (enclose() + 0.252411))) * pow(width() , 0.0139583 * enclose() + -0.00912661) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.31))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.31)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.26524 - 0.933848 * distance()) + 0.084727 / pow(distance() , 1.19956)) * 3.71606 * pow((width1() + width2()) / 2 , 0.0846155) * (0.0535002 * (thickness1() + thickness2()) / 2 + 0.0182901) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.69))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.69)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.131605 * (1 - exp(-0.573112 * (distance() + 0.101662))) * pow(width() , 0.0178359 * distance() + -0.0527127) * (0.0448414 * thickness() + 0.257695)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0353198 * (1 - exp(-0.652037 * (distance() + 0.0152149)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142212 * pow(width() , 0.0401794) * (0.0831162 * thickness() + 0.213733)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0109285 * distance() + 0.267951) * (0.199843 - exp(-(-0.101083 * distance() + 0.185504) * (enclose() + 2.20598 * distance() + 4.16458) - (0.0161193 * distance() + 0.0248503) * (enclose() + 0.609028 * distance() + 5.15268) * (enclose() + 0.609028 * distance() + 5.15268)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.131605 * (1 - exp(-0.573112 * (distance() + 0.101662))) * pow(width() , 0.0178359 * distance() + -0.0527127) * (0.0448414 * thickness() + 0.257695)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.161297 * (0.199843 - exp(0.319911 * (enclose() + 15.1945) - 0.105447 * (enclose() + 8.19782) * (enclose() + 8.19782)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0349015 * (1 - exp(-0.492975 * (distance() + 0.130243))) * pow(width() , 0.0226665 * distance() + -0.0636233) * (0.103187 * thickness() + 0.193862) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0349015 * (1 - exp(-0.492975 * (distance() + 0.130243))) * pow(width() , 0.0226665 * distance() + -0.0636233) * (0.103187 * thickness() + 0.193862) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00807457 * (1 - exp(-0.591234 * (distance() + 0.00623621))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0821675 * pow(width() , 0.0542827) * (0.0649297 * thickness() + 0.0677007) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00139447 * distance() + 0.0246678) * (0.834601 - exp(-(0.0680979 * distance() + 0.0862541) * (enclose() + -0.12673 * distance() + 1.15074) - (0.00439351 * distance() + -0.0114741) * (enclose() + -0.505386 * distance() + 1.0624) * (enclose() + -0.505386 * distance() + 1.0624))) + 0.5 * 0.166492 * exp(-0.41692 * enclose()) * (0.104857 - 0.0905711 * exp(-0.195749 * distance()) * pow(distance() , 0.0107612))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00139447 * distance() + 0.0246678) * (0.834601 - exp(-(0.0680979 * distance() + 0.0862541) * (enclose() + -0.12673 * distance() + 1.15074) - (0.00439351 * distance() + -0.0114741) * (enclose() + -0.505386 * distance() + 1.0624) * (enclose() + -0.505386 * distance() + 1.0624))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0108219 * (1 - exp(-0.238374 * (enclose() + 0.0702535))) * pow(width() , 0.0400115 * enclose() + -0.130165) + 0.5 * (0.00813096 * exp(-0.408794 * enclose()) + 0.00813119 * exp(-0.40901 * enclose())) * pow(width() , -0.00609849 * enclose() + 0.145722)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0108219 * (1 - exp(-0.238374 * (enclose() + 0.0702535))) * pow(width() , 0.0400115 * enclose() + -0.130165) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,ctm
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.19991 - 0.758288 * distance()) + 0.1141 / pow(distance() , 1.13197)) * 3.67401 * pow((width1() + width2()) / 2 , 0.0972311) * (0.0450275 * (thickness1() + thickness2()) / 2 + 0.0174287) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.69))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.69)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.117669 * (1 - exp(-0.388687 * (distance() + 0.179561))) * pow(width() , 0.0253739 * distance() + -0.0861895) * (0.0395432 * thickness() + 0.248389)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0291387 * (1 - exp(-0.480472 * (distance() + 0.0306309)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.132623 * pow(width() , 0.045644) * (0.0700971 * thickness() + 0.18784)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00294093 * distance() + 0.058631) * (0.686842 - exp(-(0.125511 * distance() + 0.456789) * (enclose() + -0.158935 * distance() + 1.60206) - (0.0037358 * distance() + -0.017408) * (enclose() + -0.651396 * distance() + 5.39005) * (enclose() + -0.651396 * distance() + 5.39005)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.117669 * (1 - exp(-0.388687 * (distance() + 0.179561))) * pow(width() , 0.0253739 * distance() + -0.0861895) * (0.0395432 * thickness() + 0.248389)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0366678 * (0.686842 - exp(-1.08434 * (enclose() + 0.807385) - 0.001271 * (enclose() + 2.13307) * (enclose() + 2.13307)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0440866 * (1 - exp(-0.355132 * (distance() + 0.209371))) * pow(width() , 0.0270642 * distance() + -0.0890595) * (0.0757514 * thickness() + 0.211332) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0440866 * (1 - exp(-0.355132 * (distance() + 0.209371))) * pow(width() , 0.0270642 * distance() + -0.0890595) * (0.0757514 * thickness() + 0.211332) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0100188 * (1 - exp(-0.452635 * (distance() + 0.0310777))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0884895 * pow(width() , 0.053346) * (0.0577499 * thickness() + 0.0848507) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00187304 * distance() + 0.0306677) * (0.876449 - exp(-(0.0566264 * distance() + 0.0699178) * (enclose() + -0.0837929 * distance() + 0.997278) - (0.00155606 * distance() + -0.0100238) * (enclose() + -0.505356 * distance() + 0.956142) * (enclose() + -0.505356 * distance() + 0.956142))) + 0.5 * 0.253484 * exp(-0.401787 * enclose()) * (0.104527 - 0.094201 * exp(-0.127326 * distance()) * pow(distance() , 0.011721))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00187304 * distance() + 0.0306677) * (0.876449 - exp(-(0.0566264 * distance() + 0.0699178) * (enclose() + -0.0837929 * distance() + 0.997278) - (0.00155606 * distance() + -0.0100238) * (enclose() + -0.505356 * distance() + 0.956142) * (enclose() + -0.505356 * distance() + 0.956142))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0129521 * (1 - exp(-0.232002 * (enclose() + 0.130487))) * pow(width() , 0.0325408 * enclose() + -0.0965557) + 0.5 * (0.00958801 * exp(-0.378436 * enclose()) + 0.00958803 * exp(-0.37844 * enclose())) * pow(width() , -0.00517284 * enclose() + 0.123234)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0129521 * (1 - exp(-0.232002 * (enclose() + 0.130487))) * pow(width() , 0.0325408 * enclose() + -0.0965557) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.06943 - 0.597802 * distance()) + 0.175512 / pow(distance() , 1.06141)) * 3.64787 * pow((width1() + width2()) / 2 , 0.110527) * (0.0338042 * (thickness1() + thickness2()) / 2 + 0.0143148) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.69))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.69)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.106728 * (1 - exp(-0.249674 * (distance() + 0.327159))) * pow(width() , 0.0327057 * distance() + -0.122249) * (0.0332477 * thickness() + 0.23984)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0235116 * (1 - exp(-0.3495 * (distance() + 0.0700664)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.12207 * pow(width() , 0.0521521) * (0.0585445 * thickness() + 0.161931)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0031882 * distance() + 0.0349383) * (0.774123 - exp(-(0.242273 * distance() + 0.231772) * (enclose() + 0.377728 * distance() + 1.15065) - (-0.00722052 * distance() + -0.00790797) * (enclose() + 0.968087 * distance() + 4.11375) * (enclose() + 0.968087 * distance() + 4.11375)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.106728 * (1 - exp(-0.249674 * (distance() + 0.327159))) * pow(width() , 0.0327057 * distance() + -0.122249) * (0.0332477 * thickness() + 0.23984)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0254397 * (0.774123 - exp(-1.44314 * (enclose() + 3.03929) - -0.0440106 * (enclose() + 8.95418) * (enclose() + 8.95418)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0593494 * (1 - exp(-0.240117 * (distance() + 0.345888))) * pow(width() , 0.032676 * distance() + -0.120734) * (0.0508877 * thickness() + 0.224298) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0593494 * (1 - exp(-0.240117 * (distance() + 0.345888))) * pow(width() , 0.032676 * distance() + -0.120734) * (0.0508877 * thickness() + 0.224298) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0126593 * (1 - exp(-0.339674 * (distance() + 0.0722637))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0957983 * pow(width() , 0.0551604) * (0.0515136 * thickness() + 0.104449) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00213146 * distance() + 0.036706) * (0.896367 - exp(-(0.0446417 * distance() + 0.0610253) * (enclose() + -0.0403693 * distance() + 0.952181) - (0.00057395 * distance() + -0.00995226) * (enclose() + -0.602828 * distance() + 0.780437) * (enclose() + -0.602828 * distance() + 0.780437))) + 0.5 * 0.672698 * exp(-0.382205 * enclose()) * (0.103734 - 0.0985796 * exp(-0.0414741 * distance()) * pow(distance() , 0.00278322))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00213146 * distance() + 0.036706) * (0.896367 - exp(-(0.0446417 * distance() + 0.0610253) * (enclose() + -0.0403693 * distance() + 0.952181) - (0.00057395 * distance() + -0.00995226) * (enclose() + -0.602828 * distance() + 0.780437) * (enclose() + -0.602828 * distance() + 0.780437))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.015764 * (1 - exp(-0.218933 * (enclose() + 0.185334))) * pow(width() , 0.0258882 * enclose() + -0.0648277) + 0.5 * (0.0115802 * exp(-0.339686 * enclose()) + 0.0115803 * exp(-0.339293 * enclose())) * pow(width() , -0.00398415 * enclose() + 0.106343)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.015764 * (1 - exp(-0.218933 * (enclose() + 0.185334))) * pow(width() , 0.0258882 * enclose() + -0.0648277) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.69))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.69)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal5,ctm
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.51241 - 1.45894 * distance()) + 0.0670348 / pow(distance() , 1.31662)) * 3.64984 * pow((width1() + width2()) / 2 , 0.105879) * (0.0701246 * (thickness1() + thickness2()) / 2 + 0.00510016) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.07))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.07)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0435014 * (1 - exp(-1.8416 * (distance() + 0.349721))) * pow(width() , -0.00340999 * distance() + 0.0102759) * (0.274837 * thickness() + 2.38592)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.108405 * (1 - exp(-1.60002 * (distance() + 0.444251)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.242687 * pow(width() , 0.0175528) * (0.311306 * thickness() + 0.316708)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00852235 * distance() + 0.116093) * (1.31493 - exp(-(-60.4606 * distance() + -29.7938) * (enclose() + 100 * distance() + -32.6421) - (79.518 * distance() + 41.8943) * (enclose() + -44.6178 * distance() + 59.6716) * (enclose() + -44.6178 * distance() + 59.6716)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0435014 * (1 - exp(-1.8416 * (distance() + 0.349721))) * pow(width() , -0.00340999 * distance() + 0.0102759) * (0.274837 * thickness() + 2.38592)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0793524 * (1.31493 - exp(332.097 * (enclose() + 467.358) - 439.484 * (enclose() + -163.417) * (enclose() + -163.417)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0151939 * (1 - exp(-1.00439 * (distance() + 0.0820809))) * pow(width() , 0.0145634 * distance() + -0.00864751) * (0.245568 * thickness() + 0.107421) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0151939 * (1 - exp(-1.00439 * (distance() + 0.0820809))) * pow(width() , 0.0145634 * distance() + -0.00864751) * (0.245568 * thickness() + 0.107421) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00338906 * (1 - exp(-1.15886 * (distance() + 0.00823593))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0644004 * pow(width() , 0.07859) * (0.0815943 * thickness() + 0.0138153) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00036231 * distance() + 0.010982) * (0.650142 - exp(-(0.0589104 * distance() + 0.129316) * (enclose() + -0.248212 * distance() + 1.96869) - (0.00836579 * distance() + -0.00462419) * (enclose() + -0.783626 * distance() + 2.38878) * (enclose() + -0.783626 * distance() + 2.38878))) + 0.5 * 0.260991 * exp(-0.348309 * enclose()) * (0.106487 - 0.0933224 * exp(-0.00874132 * distance()) * pow(distance() , -0.0317354))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00036231 * distance() + 0.010982) * (0.650142 - exp(-(0.0589104 * distance() + 0.129316) * (enclose() + -0.248212 * distance() + 1.96869) - (0.00836579 * distance() + -0.00462419) * (enclose() + -0.783626 * distance() + 2.38878) * (enclose() + -0.783626 * distance() + 2.38878))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00521229 * (1 - exp(-0.184154 * (enclose() + -0.363828))) * pow(width() , 0.0448323 * enclose() + -0.18179) + 0.5 * (0.00455494 * exp(-0.363804 * enclose()) + 0.00455535 * exp(-0.362363 * enclose())) * pow(width() , -0.00615186 * enclose() + 0.276331)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00521229 * (1 - exp(-0.184154 * (enclose() + -0.363828))) * pow(width() , 0.0448323 * enclose() + -0.18179) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.34017 - 0.854671 * distance()) + 0.102221 / pow(distance() , 1.13249)) * 3.9511 * pow((width1() + width2()) / 2 , 0.0907803) * (0.0478284 * (thickness1() + thickness2()) / 2 + 0.0171767) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.07))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.07)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.130547 * (1 - exp(-0.510478 * (distance() + 0.135653))) * pow(width() , 0.0189739 * distance() + -0.0565328) * (0.0407413 * thickness() + 0.257366)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0344976 * (1 - exp(-0.582476 * (distance() + 0.0296005)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142219 * pow(width() , 0.0467133) * (0.0799098 * thickness() + 0.212282)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00034683 * distance() + 0.0658137) * (0.998855 - exp(-(-0.107693 * distance() + 2.09662) * (enclose() + -0.161014 * distance() + 1.85731) - (0.0226399 * distance() + -0.194087) * (enclose() + -0.241964 * distance() + 4.55415) * (enclose() + -0.241964 * distance() + 4.55415)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.130547 * (1 - exp(-0.510478 * (distance() + 0.135653))) * pow(width() , 0.0189739 * distance() + -0.0565328) * (0.0407413 * thickness() + 0.257366)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0320398 * (0.998855 - exp(-1.55816 * (enclose() + 1.05224) - -0.0808875 * (enclose() + 3.34433) * (enclose() + 3.34433)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0296149 * (1 - exp(-0.448067 * (distance() + 0.171133))) * pow(width() , 0.0222044 * distance() + -0.0618935) * (0.0981665 * thickness() + 0.199626) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0296149 * (1 - exp(-0.448067 * (distance() + 0.171133))) * pow(width() , 0.0222044 * distance() + -0.0618935) * (0.0981665 * thickness() + 0.199626) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00691556 * (1 - exp(-0.53249 * (distance() + 0.0269172))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0787649 * pow(width() , 0.0597982) * (0.0564174 * thickness() + 0.0622752) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00129217 * distance() + 0.0209977) * (0.872108 - exp(-(0.0516002 * distance() + 0.0595597) * (enclose() + -0.109684 * distance() + 1.0547) - (0.00271779 * distance() + -0.00746633) * (enclose() + -0.491307 * distance() + 0.965421) * (enclose() + -0.491307 * distance() + 0.965421))) + 0.5 * 0.134173 * exp(-0.321579 * enclose()) * (0.106728 - 0.086617 * exp(-0.191966 * distance()) * pow(distance() , -0.0150568))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00129217 * distance() + 0.0209977) * (0.872108 - exp(-(0.0516002 * distance() + 0.0595597) * (enclose() + -0.109684 * distance() + 1.0547) - (0.00271779 * distance() + -0.00746633) * (enclose() + -0.491307 * distance() + 0.965421) * (enclose() + -0.491307 * distance() + 0.965421))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0122103 * (1 - exp(-0.132924 * (enclose() + 0.0105847))) * pow(width() , 0.0523621 * enclose() + -0.19491) + 0.5 * (0.0073595 * exp(-0.317386 * enclose()) + 0.00735951 * exp(-0.317565 * enclose())) * pow(width() , -0.00474531 * enclose() + 0.156293)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0122103 * (1 - exp(-0.132924 * (enclose() + 0.0105847))) * pow(width() , 0.0523621 * enclose() + -0.19491) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.07))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.07)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,ctm,metal6
//
CAPACITANCE NEARBODY ctm INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.87681 - 0.605179 * distance()) + 0.0645454 / pow(distance() , 1.21811)) * 2.77922 * pow((width1() + width2()) / 2 , 0.0512451) * (0.100274 * (thickness1() + thickness2()) / 2 + 0.047113) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.619))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.619)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0609257 * (1 - exp(-0.695006 * (distance()))) * pow(width() , 0.0133772 * distance() + -0.119825) * (-4.08701 * thickness() + 1.44723)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0687988 * (1 - exp(-0.117976 * (distance() + 1.75348)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.164485 * pow(width() , 0.0118096) * (0.116784 * thickness() + 0.20438)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00267255 * distance() + 0.0684724) * (0.850997 - exp(-(-0.0939766 * distance() + 1.47414) * (enclose() + 0.14541 * distance() + 0.260407) - (0.111966 * distance() + -0.485947) * (enclose() + -0.310439 * distance() + 0.940645) * (enclose() + -0.310439 * distance() + 0.940645)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0609257 * (1 - exp(-0.695006 * (distance()))) * pow(width() , 0.0133772 * distance() + -0.119825) * (-4.08701 * thickness() + 1.44723)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0398486 * (0.850997 - exp(-1.07944 * (enclose() + 0.871129) - -0.0156898 * (enclose() + -0.363199) * (enclose() + -0.363199)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0244021 * (1 - exp(-0.675733 * (distance()))) * pow(width() , 0.0110703 * distance() + -0.0441889) * (0.193663 * thickness() + 0.158756) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0244021 * (1 - exp(-0.675733 * (distance()))) * pow(width() , 0.0110703 * distance() + -0.0441889) * (0.193663 * thickness() + 0.158756) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00490194 * (1 - exp(-0.588355 * (distance() + 0.00562196))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0637118 * pow(width() , 0.0136715) * (0.0751323 * thickness() + 0.06311) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00546637 * distance() + 0.0595353) * (0.615417 - exp(-(0.0285711 * distance() + 0.100794) * (enclose() + -0.187077 * distance() + 2.29392) - (0.00014925 * distance() + -0.00898014) * (enclose() + 0.617995 * distance() + 0.807629) * (enclose() + 0.617995 * distance() + 0.807629))) + 0.5 * 0.173146 * exp(-0.317442 * enclose()) * (0.0931031 - 0.109471 * exp(-0.330734 * distance()) * pow(distance() , 0.226682))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00546637 * distance() + 0.0595353) * (0.615417 - exp(-(0.0285711 * distance() + 0.100794) * (enclose() + -0.187077 * distance() + 2.29392) - (0.00014925 * distance() + -0.00898014) * (enclose() + 0.617995 * distance() + 0.807629) * (enclose() + 0.617995 * distance() + 0.807629))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.619)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0315966 * (1 - exp(-0.0420537 * (enclose() + -1.6328))) * pow(width() , -0.0665012 * enclose() + 0.244603) + 0.5 * (0.00701567 * exp(-0.323031 * enclose()) + 0.00701956 * exp(-0.321999 * enclose())) * pow(width() , -0.00541231 * enclose() + 0.0738448)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.619)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0315966 * (1 - exp(-0.0420537 * (enclose() + -1.6328))) * pow(width() , -0.0665012 * enclose() + 0.244603) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.619))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.619)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.20737 - 1.54799 * distance()) + 0.0181655 / pow(distance() , 1.6274)) * 3.64957 * pow((width1() + width2()) / 2 , 0.035268) * (0.0967477 * (thickness1() + thickness2()) / 2 + 0.025311) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.106383 * (1 - exp(-0.856491 * (distance()))) * pow(width() , 0.0178144 * distance() + -0.0516273) * (-0.0115347 * thickness() + 0.253232)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0248943 * (1 - exp(-1.06143 * (distance() + -0.0614721)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.120588 * pow(width() , 0.0120567) * (0.10241 * thickness() + 0.151377)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0003414 * distance() + 0.171678) * (0.282316 - exp(-(-0.025159 * distance() + 0.0140835) * (enclose() + 12.2321 * distance() + 6.02183) - (0.0102969 * distance() + 0.0520347) * (enclose() + 1.04995 * distance() + 4.30859) * (enclose() + 1.04995 * distance() + 4.30859)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.106383 * (1 - exp(-0.856491 * (distance()))) * pow(width() , 0.0178144 * distance() + -0.0516273) * (-0.0115347 * thickness() + 0.253232)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0849855 * (0.282316 - exp(0.111711 * (enclose() + 67.1823) - 0.103519 * (enclose() + 9.55834) * (enclose() + 9.55834)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107737 * (1 - exp(-0.850954 * (distance()))) * pow(width() , 0.0181238 * distance() + -0.0524985) * (-0.0102023 * thickness() + 0.252275) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107737 * (1 - exp(-0.850954 * (distance()))) * pow(width() , 0.0181238 * distance() + -0.0524985) * (-0.0102023 * thickness() + 0.252275) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0251704 * (1 - exp(-1.05661 * (distance() + -0.06235))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.121507 * pow(width() , 0.0126474) * (0.103969 * thickness() + 0.151281) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00187553 * distance() + 0.288042) * (0.173695 - exp(-(-0.11968 * distance() + 0.0569488) * (enclose() + 7.89368 * distance() + 11.6493) - (0.0007998 * distance() + 0.0392521) * (enclose() + 4.31619 * distance() + 4.72881) * (enclose() + 4.31619 * distance() + 4.72881))) + 0.5 * 0.28835 * exp(-1.62622 * enclose()) * (0.0921928 - 0.108659 * exp(-0.221013 * distance()) * pow(distance() , 0.106325))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00187553 * distance() + 0.288042) * (0.173695 - exp(-(-0.11968 * distance() + 0.0569488) * (enclose() + 7.89368 * distance() + 11.6493) - (0.0007998 * distance() + 0.0392521) * (enclose() + 4.31619 * distance() + 4.72881) * (enclose() + 4.31619 * distance() + 4.72881))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0251863 * (1 - exp(-1.35951 * (enclose() + 0.398081))) * pow(width() , 0.00079147 * enclose() + 0.0112458) + 0.5 * (0.00680807 * exp(-1.31572 * enclose()) + 0.00681378 * exp(-1.31625 * enclose())) * pow(width() , 0.00969587 * enclose() + 0.0124593)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0251863 * (1 - exp(-1.35951 * (enclose() + 0.398081))) * pow(width() , 0.00079147 * enclose() + 0.0112458) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.20893 - 1.11149 * distance()) + 0.044305 / pow(distance() , 1.38171)) * 3.63813 * pow((width1() + width2()) / 2 , 0.0690482) * (0.0693941 * (thickness1() + thickness2()) / 2 + 0.022388) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0573036 * (1 - exp(-0.593657 * (distance() + 0.0452633))) * pow(width() , 0.020966 * distance() + -0.0614153) * (0.0983743 * thickness() + 0.205962)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0139543 * (1 - exp(-0.713025 * (distance() + -0.035677)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0977661 * pow(width() , 0.0352393) * (0.0835131 * thickness() + 0.0998282)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-8.58362e-05 * distance() + 0.0348022) * (0.789737 - exp(-(0.33964 * distance() + 0.248946) * (enclose() + 0.224915 * distance() + 1.05526) - (-0.0124515 * distance() + -0.0149256) * (enclose() + 0.679635 * distance() + 3.31162) * (enclose() + 0.679635 * distance() + 3.31162)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0573036 * (1 - exp(-0.593657 * (distance() + 0.0452633))) * pow(width() , 0.020966 * distance() + -0.0614153) * (0.0983743 * thickness() + 0.205962)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0171865 * (0.789737 - exp(-1.94715 * (enclose() + 2.17984) - -0.0771831 * (enclose() + 6.70979) * (enclose() + 6.70979)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.116896 * (1 - exp(-0.663717 * (distance() + 0.0343792))) * pow(width() , 0.0165094 * distance() + -0.0496075) * (0.0599038 * thickness() + 0.248612) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.116896 * (1 - exp(-0.663717 * (distance() + 0.0343792))) * pow(width() , 0.0165094 * distance() + -0.0496075) * (0.0599038 * thickness() + 0.248612) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0314525 * (1 - exp(-0.764014 * (distance() + -0.0255461))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135361 * pow(width() , 0.0256826) * (0.085309 * thickness() + 0.189111) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00522154 * distance() + 0.353506) * (0.156284 - exp(-(-0.0764791 * distance() + 0.0338206) * (enclose() + 9.26958 * distance() + 16.9029) - (0.00054755 * distance() + 0.0284155) * (enclose() + 4.34676 * distance() + 6.23198) * (enclose() + 4.34676 * distance() + 6.23198))) + 0.5 * 0.853589 * exp(-1.2226 * enclose()) * (0.0982092 - 0.102609 * exp(-0.0721576 * distance()) * pow(distance() , 0.0368726))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00522154 * distance() + 0.353506) * (0.156284 - exp(-(-0.0764791 * distance() + 0.0338206) * (enclose() + 9.26958 * distance() + 16.9029) - (0.00054755 * distance() + 0.0284155) * (enclose() + 4.34676 * distance() + 6.23198) * (enclose() + 4.34676 * distance() + 6.23198))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0315134 * (1 - exp(-1.08592 * (enclose() + 0.454764))) * pow(width() , -0.00054287 * enclose() + 0.0259431) + 0.5 * (0.0194977 * exp(-0.997449 * enclose()) + 0.00166533 * exp(-0.526493 * enclose())) * pow(width() , 0.0173168 * enclose() + 0.0276993)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0315134 * (1 - exp(-1.08592 * (enclose() + 0.454764))) * pow(width() , -0.00054287 * enclose() + 0.0259431) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.23781 - 0.97965 * distance()) + 0.0684999 / pow(distance() , 1.25962)) * 3.6771 * pow((width1() + width2()) / 2 , 0.0809) * (0.0584523 * (thickness1() + thickness2()) / 2 + 0.0197182) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.041756 * (1 - exp(-0.513455 * (distance() + 0.105367))) * pow(width() , 0.0230605 * distance() + -0.0662966) * (0.101871 * thickness() + 0.198909)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00984124 * (1 - exp(-0.622384 * (distance() + -0.00761863)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0872852 * pow(width() , 0.0486477) * (0.0707823 * thickness() + 0.0783035)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00026608 * distance() + 0.0241149) * (0.84615 - exp(-(0.193034 * distance() + 0.162286) * (enclose() + 0.282403 * distance() + 0.805808) - (-0.00486917 * distance() + -0.0131752) * (enclose() + 0.95572 * distance() + 2.32109) * (enclose() + 0.95572 * distance() + 2.32109)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.041756 * (1 - exp(-0.513455 * (distance() + 0.105367))) * pow(width() , 0.0230605 * distance() + -0.0662966) * (0.101871 * thickness() + 0.198909)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0113923 * (0.84615 - exp(-1.12746 * (enclose() + 2.21782) - -0.0375211 * (enclose() + 7.09969) * (enclose() + 7.09969)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.12807 * (1 - exp(-0.591525 * (distance() + 0.0800892))) * pow(width() , 0.0181474 * distance() + -0.054449) * (0.0496278 * thickness() + 0.254558) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.12807 * (1 - exp(-0.591525 * (distance() + 0.0800892))) * pow(width() , 0.0181474 * distance() + -0.054449) * (0.0496278 * thickness() + 0.254558) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0343024 * (1 - exp(-0.680861 * (distance() + 0.00054605))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.141619 * pow(width() , 0.0355708) * (0.0829106 * thickness() + 0.204494) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0129666 * distance() + 0.481394) * (0.116335 - exp(-(-0.0914895 * distance() + 0.0484071) * (enclose() + 8.43755 * distance() + 16.8009) - (0.00041014 * distance() + 0.0249456) * (enclose() + 4.92809 * distance() + 6.82382) * (enclose() + 4.92809 * distance() + 6.82382))) + 0.5 * 1.53923 * exp(-1.03407 * enclose()) * (0.0998249 - 0.101341 * exp(-0.037653 * distance()) * pow(distance() , 0.0182572))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0129666 * distance() + 0.481394) * (0.116335 - exp(-(-0.0914895 * distance() + 0.0484071) * (enclose() + 8.43755 * distance() + 16.8009) - (0.00041014 * distance() + 0.0249456) * (enclose() + 4.92809 * distance() + 6.82382) * (enclose() + 4.92809 * distance() + 6.82382))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0345214 * (1 - exp(-0.972684 * (enclose() + 0.484399))) * pow(width() , -0.00078142 * enclose() + 0.0338791) + 0.5 * (0.0165042 * exp(-1.01032 * enclose()) + 0.00967661 * exp(-0.520993 * enclose())) * pow(width() , 0.021671 * enclose() + 0.0409833)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0345214 * (1 - exp(-0.972684 * (enclose() + 0.484399))) * pow(width() , -0.00078142 * enclose() + 0.0338791) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,ctm
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.25117 - 0.953727 * distance()) + 0.0773009 / pow(distance() , 1.22555)) * 3.70289 * pow((width1() + width2()) / 2 , 0.0833847) * (0.0555318 * (thickness1() + thickness2()) / 2 + 0.0188691) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,ctm
//
CAPACITANCE CROSSOVER FRINGE metal2 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.037548 * (1 - exp(-0.502256 * (distance() + 0.120942))) * pow(width() , 0.0229029 * distance() + -0.0645833) * (0.102825 * thickness() + 0.196955)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00878974 * (1 - exp(-0.605343 * (distance() + 0.00112566)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0843047 * pow(width() , 0.0524598) * (0.0671748 * thickness() + 0.0722519)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00054661 * distance() + 0.0234118) * (0.850265 - exp(-(0.161319 * distance() + 0.158734) * (enclose() + 0.218764 * distance() + 0.83239) - (-0.0035847 * distance() + -0.0173974) * (enclose() + 0.855874 * distance() + 1.89069) * (enclose() + 0.855874 * distance() + 1.89069)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.037548 * (1 - exp(-0.502256 * (distance() + 0.120942))) * pow(width() , 0.0229029 * distance() + -0.0645833) * (0.102825 * thickness() + 0.196955)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0103394 * (0.850265 - exp(-0.965329 * (enclose() + 1.92621) - -0.0353209 * (enclose() + 6.17006) * (enclose() + 6.17006)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,ctm
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.13071 * (1 - exp(-0.580742 * (distance() + 0.0916633))) * pow(width() , 0.0181286 * distance() + -0.053717) * (0.0473309 * thickness() + 0.25606) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.13071 * (1 - exp(-0.580742 * (distance() + 0.0916633))) * pow(width() , 0.0181286 * distance() + -0.053717) * (0.0473309 * thickness() + 0.25606) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0350226 * (1 - exp(-0.66489 * (distance() + 0.00790341))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143269 * pow(width() , 0.0385874) * (0.0829053 * thickness() + 0.208486) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00560242 * distance() + 0.183485) * (0.303685 - exp(-(-0.0766393 * distance() + 0.0655403) * (enclose() + 4.91088 * distance() + 6.37514) - (0.00199615 * distance() + 0.0450601) * (enclose() + 2.15635 * distance() + 3.64031) * (enclose() + 2.15635 * distance() + 3.64031))) + 0.5 * 2.57586 * exp(-0.981931 * enclose()) * (0.100417 - 0.101083 * exp(-0.0215745 * distance()) * pow(distance() , 0.0100633))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00560242 * distance() + 0.183485) * (0.303685 - exp(-(-0.0766393 * distance() + 0.0655403) * (enclose() + 4.91088 * distance() + 6.37514) - (0.00199615 * distance() + 0.0450601) * (enclose() + 2.15635 * distance() + 3.64031) * (enclose() + 2.15635 * distance() + 3.64031))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0352923 * (1 - exp(-0.944633 * (enclose() + 0.493905))) * pow(width() , -0.00090388 * enclose() + 0.0359868) + 0.5 * (0.0139626 * exp(-1.07657 * enclose()) + 0.0138053 * exp(-0.521814 * enclose())) * pow(width() , 0.0228621 * enclose() + 0.0451486)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0352923 * (1 - exp(-0.944633 * (enclose() + 0.493905))) * pow(width() , -0.00090388 * enclose() + 0.0359868) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.27767 - 0.926836 * distance()) + 0.090079 / pow(distance() , 1.18114)) * 3.73024 * pow((width1() + width2()) / 2 , 0.0862227) * (0.0522576 * (thickness1() + thickness2()) / 2 + 0.0178915) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0320988 * (1 - exp(-0.494104 * (distance() + 0.140194))) * pow(width() , 0.0221032 * distance() + -0.0597002) * (0.103807 * thickness() + 0.193432)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00742748 * (1 - exp(-0.58742 * (distance() + 0.013176)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0802253 * pow(width() , 0.0576772) * (0.0619948 * thickness() + 0.0639803)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00045064 * distance() + 0.0191699) * (0.885412 - exp(-(0.118486 * distance() + 0.115887) * (enclose() + 0.407234 * distance() + 0.798376) - (-0.00209781 * distance() + -0.00743729) * (enclose() + 1.22734 * distance() + 2.91872) * (enclose() + 1.22734 * distance() + 2.91872)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0320988 * (1 - exp(-0.494104 * (distance() + 0.140194))) * pow(width() , 0.0221032 * distance() + -0.0597002) * (0.103807 * thickness() + 0.193432)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00845835 * (0.885412 - exp(-0.708317 * (enclose() + 2.83455) - -0.0179263 * (enclose() + 9.05542) * (enclose() + 9.05542)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = field_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.133921 * (1 - exp(-0.5721 * (distance() + 0.105862))) * pow(width() , 0.0177043 * distance() + -0.0509459) * (0.0448034 * thickness() + 0.257878) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.133921 * (1 - exp(-0.5721 * (distance() + 0.105862))) * pow(width() , 0.0177043 * distance() + -0.0509459) * (0.0448034 * thickness() + 0.257878) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0359418 * (1 - exp(-0.647921 * (distance() + 0.0176653))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.145428 * pow(width() , 0.0427846) * (0.0834945 * thickness() + 0.213609) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.021617 * distance() + 0.638918) * (0.0878808 - exp(-(-0.106804 * distance() + 0.0642129) * (enclose() + 8.15676 * distance() + 16.8385) - (0.00032253 * distance() + 0.0228252) * (enclose() + 5.5655 * distance() + 7.1752) * (enclose() + 5.5655 * distance() + 7.1752))) + 0.5 * 2.30286 * exp(-0.911138 * enclose()) * (0.100802 - 0.101239 * exp(-0.0237375 * distance()) * pow(distance() , 0.010416))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.021617 * distance() + 0.638918) * (0.0878808 - exp(-(-0.106804 * distance() + 0.0642129) * (enclose() + 8.15676 * distance() + 16.8385) - (0.00032253 * distance() + 0.0228252) * (enclose() + 5.5655 * distance() + 7.1752) * (enclose() + 5.5655 * distance() + 7.1752))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0362934 * (1 - exp(-0.909653 * (enclose() + 0.505321))) * pow(width() , -0.00088075 * enclose() + 0.0383957) + 0.5 * (0.0151287 * exp(-1.05685 * enclose()) + 0.0149214 * exp(-0.456253 * enclose())) * pow(width() , 0.0243276 * enclose() + 0.0510478)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0362934 * (1 - exp(-0.909653 * (enclose() + 0.505321))) * pow(width() , -0.00088075 * enclose() + 0.0383957) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.20721 - 1.11026 * distance()) + 0.0443754 / pow(distance() , 1.3808)) * 3.63994 * pow((width1() + width2()) / 2 , 0.0686265) * (0.0691803 * (thickness1() + thickness2()) / 2 + 0.0224031) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.115824 * (1 - exp(-0.663153 * (distance() + 0.0355482))) * pow(width() , 0.0163332 * distance() + -0.0496493) * (0.0589169 * thickness() + 0.249336)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.031211 * (1 - exp(-0.762531 * (distance() + -0.024047)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133448 * pow(width() , 0.0249312) * (0.0849081 * thickness() + 0.190748)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0116642 * distance() + 0.669352) * (0.0804662 - exp(-(-0.185619 * distance() + 0.404045) * (enclose() + 1.36219 * distance() + 5.53438) - (0.0220279 * distance() + 0.00223991) * (enclose() + 0.483258 * distance() + 6.8364) * (enclose() + 0.483258 * distance() + 6.8364)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.115824 * (1 - exp(-0.663153 * (distance() + 0.0355482))) * pow(width() , 0.0163332 * distance() + -0.0496493) * (0.0589169 * thickness() + 0.249336)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.363836 * (0.0804662 - exp(0.52405 * (enclose() + 12.3453) - 0.112379 * (enclose() + 9.25269) * (enclose() + 9.25269)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0579063 * (1 - exp(-0.589728 * (distance() + 0.0437997))) * pow(width() , 0.0210538 * distance() + -0.0623516) * (0.0987013 * thickness() + 0.205047) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0579063 * (1 - exp(-0.589728 * (distance() + 0.0437997))) * pow(width() , 0.0210538 * distance() + -0.0623516) * (0.0987013 * thickness() + 0.205047) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0140597 * (1 - exp(-0.709288 * (distance() + -0.0370363))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0979381 * pow(width() , 0.0345166) * (0.0847801 * thickness() + 0.0998187) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0010353 * distance() + 0.0418784) * (0.755399 - exp(-(0.250821 * distance() + 0.204744) * (enclose() + 0.0718863 * distance() + 1.1132) - (-0.010177 * distance() + -0.0146577) * (enclose() + 0.509044 * distance() + 2.30376) * (enclose() + 0.509044 * distance() + 2.30376))) + 0.5 * 4.01522 * exp(-0.831012 * enclose()) * (0.100523 - 0.100626 * exp(-0.00667923 * distance()) * pow(distance() , 0.00366595))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0010353 * distance() + 0.0418784) * (0.755399 - exp(-(0.250821 * distance() + 0.204744) * (enclose() + 0.0718863 * distance() + 1.1132) - (-0.010177 * distance() + -0.0146577) * (enclose() + 0.509044 * distance() + 2.30376) * (enclose() + 0.509044 * distance() + 2.30376))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0145844 * (1 - exp(-0.658698 * (enclose() + 0.507255))) * pow(width() , 0.009619 * enclose() + -0.00211888) + 0.5 * (0.00664048 * exp(-0.748037 * enclose()) + 0.00664061 * exp(-0.747685 * enclose())) * pow(width() , -0.00066658 * enclose() + 0.072184)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0145844 * (1 - exp(-0.658698 * (enclose() + 0.507255))) * pow(width() , 0.009619 * enclose() + -0.00211888) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.09794 - 0.774726 * distance()) + 0.0855744 / pow(distance() , 1.22976)) * 3.5853 * pow((width1() + width2()) / 2 , 0.0955794) * (0.0491259 * (thickness1() + thickness2()) / 2 + 0.0194158) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0854677 * (1 - exp(-0.34146 * (distance() + 0.170932))) * pow(width() , 0.029787 * distance() + -0.105398) * (0.0550772 * thickness() + 0.228772)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0196154 * (1 - exp(-0.459444 * (distance() + 0.00820902)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.112328 * pow(width() , 0.0407975) * (0.0648184 * thickness() + 0.138295)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00090639 * distance() + 0.0388904) * (0.837415 - exp(-(0.282762 * distance() + 0.364274) * (enclose() + 0.297153 * distance() + 1.18669) - (-0.00667129 * distance() + -0.0314811) * (enclose() + 0.902682 * distance() + 3.4057) * (enclose() + 0.902682 * distance() + 3.4057)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0854677 * (1 - exp(-0.34146 * (distance() + 0.170932))) * pow(width() , 0.029787 * distance() + -0.105398) * (0.0550772 * thickness() + 0.228772)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0217112 * (0.837415 - exp(-1.77808 * (enclose() + 2.67246) - -0.0648375 * (enclose() + 7.91911) * (enclose() + 7.91911)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0865118 * (1 - exp(-0.341145 * (distance() + 0.169374))) * pow(width() , 0.0298402 * distance() + -0.106188) * (0.0554614 * thickness() + 0.226492) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0865118 * (1 - exp(-0.341145 * (distance() + 0.169374))) * pow(width() , 0.0298402 * distance() + -0.106188) * (0.0554614 * thickness() + 0.226492) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0197026 * (1 - exp(-0.459194 * (distance() + 0.00680021))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.112462 * pow(width() , 0.0405643) * (0.0655323 * thickness() + 0.1384) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00034702 * distance() + 0.0445767) * (0.799649 - exp(-(0.231419 * distance() + 0.238682) * (enclose() + 0.17395 * distance() + 1.3053) - (-0.00721271 * distance() + -0.0151956) * (enclose() + 0.673189 * distance() + 3.62226) * (enclose() + 0.673189 * distance() + 3.62226))) + 0.5 * 16.8478 * exp(-0.721642 * enclose()) * (0.100953 - 0.100979 * exp(-0.00202743 * distance()) * pow(distance() , 0.0012634))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00034702 * distance() + 0.0445767) * (0.799649 - exp(-(0.231419 * distance() + 0.238682) * (enclose() + 0.17395 * distance() + 1.3053) - (-0.00721271 * distance() + -0.0151956) * (enclose() + 0.673189 * distance() + 3.62226) * (enclose() + 0.673189 * distance() + 3.62226))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0198982 * (1 - exp(-0.548839 * (enclose() + 0.584577))) * pow(width() , 0.00631214 * enclose() + 0.015536) + 0.5 * (0.00980734 * exp(-0.585101 * enclose()) + 0.00980794 * exp(-0.584307 * enclose())) * pow(width() , 0.00246786 * enclose() + 0.0588918)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0198982 * (1 - exp(-0.548839 * (enclose() + 0.584577))) * pow(width() , 0.00631214 * enclose() + 0.015536) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,ctm
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.08618 - 0.715905 * distance()) + 0.104013 / pow(distance() , 1.18214)) * 3.60707 * pow((width1() + width2()) / 2 , 0.100792) * (0.0444672 * (thickness1() + thickness2()) / 2 + 0.0180062) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE metal3 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0782534 * (1 - exp(-0.298208 * (distance() + 0.221321))) * pow(width() , 0.0319092 * distance() + -0.115077) * (0.0540119 * thickness() + 0.22514)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0172712 * (1 - exp(-0.415256 * (distance() + 0.0247131)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107114 * pow(width() , 0.0451345) * (0.0606984 * thickness() + 0.126908)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00086301 * distance() + 0.0378354) * (0.681713 - exp(-(0.10498 * distance() + 0.210712) * (enclose() + -0.116457 * distance() + 1.71941) - (0.00372677 * distance() + -0.0113493) * (enclose() + -0.00557266 * distance() + 2.86256) * (enclose() + -0.00557266 * distance() + 2.86256)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0782534 * (1 - exp(-0.298208 * (distance() + 0.221321))) * pow(width() , 0.0319092 * distance() + -0.115077) * (0.0540119 * thickness() + 0.22514)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0210752 * (0.681713 - exp(-0.735612 * (enclose() + 1.13713) - 0.00728455 * (enclose() + 2.8347) * (enclose() + 2.8347)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0946183 * (1 - exp(-0.304004 * (distance() + 0.212228))) * pow(width() , 0.0316439 * distance() + -0.114877) * (0.0477864 * thickness() + 0.230733) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0946183 * (1 - exp(-0.304004 * (distance() + 0.212228))) * pow(width() , 0.0316439 * distance() + -0.114877) * (0.0477864 * thickness() + 0.230733) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.021173 * (1 - exp(-0.420098 * (distance() + 0.0229482))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.115951 * pow(width() , 0.0434804) * (0.0631605 * thickness() + 0.146867) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00072102 * distance() + 0.0455818) * (0.791775 - exp(-(0.218576 * distance() + 0.237196) * (enclose() + 0.176249 * distance() + 1.32285) - (-0.00656472 * distance() + -0.014938) * (enclose() + 0.702987 * distance() + 3.56424) * (enclose() + 0.702987 * distance() + 3.56424))) + 0.5 * 17.8993 * exp(-0.688262 * enclose()) * (0.101145 - 0.101154 * exp(-0.00194609 * distance()) * pow(distance() , 0.00122114))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00072102 * distance() + 0.0455818) * (0.791775 - exp(-(0.218576 * distance() + 0.237196) * (enclose() + 0.176249 * distance() + 1.32285) - (-0.00656472 * distance() + -0.014938) * (enclose() + 0.702987 * distance() + 3.56424) * (enclose() + 0.702987 * distance() + 3.56424))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0212904 * (1 - exp(-0.521992 * (enclose() + 0.602704))) * pow(width() , 0.0058646 * enclose() + 0.019485) + 0.5 * (0.0107875 * exp(-0.543774 * enclose()) + 0.0107901 * exp(-0.54325 * enclose())) * pow(width() , 0.00348066 * enclose() + 0.0591113)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0212904 * (1 - exp(-0.521992 * (enclose() + 0.602704))) * pow(width() , 0.0058646 * enclose() + 0.019485) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.08038 - 0.652486 * distance()) + 0.136211 / pow(distance() , 1.11662)) * 3.6054 * pow((width1() + width2()) / 2 , 0.106218) * (0.0390992 * (thickness1() + thickness2()) / 2 + 0.0162223) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.066299 * (1 - exp(-0.263243 * (distance() + 0.286687))) * pow(width() , 0.0327852 * distance() + -0.119419) * (0.0535366 * thickness() + 0.224585)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0143393 * (1 - exp(-0.372566 * (distance() + 0.049384)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0999555 * pow(width() , 0.0509803) * (0.0556264 * thickness() + 0.112634)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00065613 * distance() + 0.0258885) * (0.886603 - exp(-(0.157669 * distance() + 0.165852) * (enclose() + 0.481164 * distance() + 0.759063) - (-0.00262672 * distance() + -0.0128603) * (enclose() + 1.37817 * distance() + 2.71198) * (enclose() + 1.37817 * distance() + 2.71198)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.066299 * (1 - exp(-0.263243 * (distance() + 0.286687))) * pow(width() , 0.0327852 * distance() + -0.119419) * (0.0535366 * thickness() + 0.224585)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0145846 * (0.886603 - exp(-0.954197 * (enclose() + 3.16488) - -0.0259939 * (enclose() + 9.60283) * (enclose() + 9.60283)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.103266 * (1 - exp(-0.273251 * (distance() + 0.267825))) * pow(width() , 0.0325681 * distance() + -0.12016) * (0.0396775 * thickness() + 0.237179) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.103266 * (1 - exp(-0.273251 * (distance() + 0.267825))) * pow(width() , 0.0325681 * distance() + -0.12016) * (0.0396775 * thickness() + 0.237179) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0229994 * (1 - exp(-0.381781 * (distance() + 0.0458858))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.120403 * pow(width() , 0.0478576) * (0.061055 * thickness() + 0.157458) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00558844 * distance() + 0.0821647) * (0.855272 - exp(-(0.0774767 * distance() + 0.265662) * (enclose() + -0.137336 * distance() + 1.53234) - (-0.00444533 * distance() + -0.0264547) * (enclose() + -0.374256 * distance() + 3.42956) * (enclose() + -0.374256 * distance() + 3.42956))) + 0.5 * 17.5254 * exp(-0.63924 * enclose()) * (0.101351 - 0.101328 * exp(-0.00199397 * distance()) * pow(distance() , 0.00122968))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00558844 * distance() + 0.0821647) * (0.855272 - exp(-(0.0774767 * distance() + 0.265662) * (enclose() + -0.137336 * distance() + 1.53234) - (-0.00444533 * distance() + -0.0264547) * (enclose() + -0.374256 * distance() + 3.42956) * (enclose() + -0.374256 * distance() + 3.42956))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0230998 * (1 - exp(-0.488841 * (enclose() + 0.625472))) * pow(width() , 0.00548745 * enclose() + 0.0240565) + 0.5 * (0.0121939 * exp(-0.490123 * enclose()) + 0.012194 * exp(-0.490641 * enclose())) * pow(width() , 0.00490775 * enclose() + 0.0608487)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0230998 * (1 - exp(-0.488841 * (enclose() + 0.625472))) * pow(width() , 0.00548745 * enclose() + 0.0240565) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.23704 - 0.974854 * distance()) + 0.069007 / pow(distance() , 1.2591)) * 3.68258 * pow((width1() + width2()) / 2 , 0.0805082) * (0.0581215 * (thickness1() + thickness2()) / 2 + 0.0195718) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.61))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.61)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.127083 * (1 - exp(-0.589763 * (distance() + 0.0817725))) * pow(width() , 0.0180232 * distance() + -0.0546996) * (0.0485755 * thickness() + 0.255234)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0340748 * (1 - exp(-0.678337 * (distance() + 0.00219611)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.139262 * pow(width() , 0.0348359) * (0.0829014 * thickness() + 0.206908)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0210976 * distance() + 0.654382) * (0.0826673 - exp(-(-0.143852 * distance() + 0.286634) * (enclose() + 1.74963 * distance() + 6.20482) - (0.0165719 * distance() + 0.01027) * (enclose() + 0.580789 * distance() + 7.23942) * (enclose() + 0.580789 * distance() + 7.23942)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.127083 * (1 - exp(-0.589763 * (distance() + 0.0817725))) * pow(width() , 0.0180232 * distance() + -0.0546996) * (0.0485755 * thickness() + 0.255234)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.379935 * (0.0826673 - exp(0.432626 * (enclose() + 14.953) - 0.0931295 * (enclose() + 10.1434) * (enclose() + 10.1434)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0421464 * (1 - exp(-0.509684 * (distance() + 0.103749))) * pow(width() , 0.0231657 * distance() + -0.0675183) * (0.101908 * thickness() + 0.198279) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0421464 * (1 - exp(-0.509684 * (distance() + 0.103749))) * pow(width() , 0.0231657 * distance() + -0.0675183) * (0.101908 * thickness() + 0.198279) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.009908 * (1 - exp(-0.618791 * (distance() + -0.00909794))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0874058 * pow(width() , 0.0476859) * (0.0714477 * thickness() + 0.078415) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00056389 * distance() + 0.0261143) * (0.839874 - exp(-(0.158418 * distance() + 0.0864042) * (enclose() + 0.201336 * distance() + 0.819064) - (-0.0049374 * distance() + -0.00083873) * (enclose() + 0.855949 * distance() + 2.54214) * (enclose() + 0.855949 * distance() + 2.54214))) + 0.5 * 6.64736 * exp(-0.553816 * enclose()) * (0.100889 - 0.100687 * exp(-0.00220365 * distance()) * pow(distance() , 0.0008373))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00056389 * distance() + 0.0261143) * (0.839874 - exp(-(0.158418 * distance() + 0.0864042) * (enclose() + 0.201336 * distance() + 0.819064) - (-0.0049374 * distance() + -0.00083873) * (enclose() + 0.855949 * distance() + 2.54214) * (enclose() + 0.855949 * distance() + 2.54214))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0110371 * (1 - exp(-0.400689 * (enclose() + 0.527333))) * pow(width() , 0.0205137 * enclose() + -0.0403139) + 0.5 * (0.00615183 * exp(-0.52269 * enclose()) + 0.00615195 * exp(-0.524714 * enclose())) * pow(width() , -0.00237329 * enclose() + 0.113954)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0110371 * (1 - exp(-0.400689 * (enclose() + 0.527333))) * pow(width() , 0.0205137 * enclose() + -0.0403139) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,ctm
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.16004 - 0.799119 * distance()) + 0.0917242 / pow(distance() , 1.19676)) * 3.60347 * pow((width1() + width2()) / 2 , 0.0933719) * (0.0494994 * (thickness1() + thickness2()) / 2 + 0.0189677) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.61))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.61)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.113285 * (1 - exp(-0.403718 * (distance() + 0.150182))) * pow(width() , 0.0256333 * distance() + -0.0877589) * (0.044421 * thickness() + 0.243256)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0277944 * (1 - exp(-0.505189 * (distance() + 0.0142273)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.129374 * pow(width() , 0.0408073) * (0.070465 * thickness() + 0.1799)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00157189 * distance() + 0.0693551) * (0.831273 - exp(-(0.0531631 * distance() + 0.746475) * (enclose() + -0.0305274 * distance() + 0.823352) - (0.00896268 * distance() + -0.129394) * (enclose() + -0.356186 * distance() + 2.03855) * (enclose() + -0.356186 * distance() + 2.03855)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.113285 * (1 - exp(-0.403718 * (distance() + 0.150182))) * pow(width() , 0.0256333 * distance() + -0.0877589) * (0.044421 * thickness() + 0.243256)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0307478 * (0.831273 - exp(-1.01229 * (enclose() + 0.670715) - -0.0845806 * (enclose() + 0.25762) * (enclose() + 0.25762)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.053055 * (1 - exp(-0.371109 * (distance() + 0.172689))) * pow(width() , 0.0276596 * distance() + -0.092296) * (0.0751557 * thickness() + 0.213549) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.053055 * (1 - exp(-0.371109 * (distance() + 0.172689))) * pow(width() , 0.0276596 * distance() + -0.092296) * (0.0751557 * thickness() + 0.213549) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0121518 * (1 - exp(-0.479325 * (distance() + 0.0129107))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0937626 * pow(width() , 0.0474843) * (0.0651355 * thickness() + 0.0959806) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00047828 * distance() + 0.0293865) * (0.874415 - exp(-(0.151331 * distance() + 0.127062) * (enclose() + 0.283563 * distance() + 0.884072) - (-0.003119 * distance() + -0.00920133) * (enclose() + 1.08649 * distance() + 2.74984) * (enclose() + 1.08649 * distance() + 2.74984))) + 0.5 * 7.81723 * exp(-0.52864 * enclose()) * (0.100976 - 0.10081 * exp(-0.002296 * distance()) * pow(distance() , 0.00104298))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00047828 * distance() + 0.0293865) * (0.874415 - exp(-(0.151331 * distance() + 0.127062) * (enclose() + 0.283563 * distance() + 0.884072) - (-0.003119 * distance() + -0.00920133) * (enclose() + 1.08649 * distance() + 2.74984) * (enclose() + 1.08649 * distance() + 2.74984))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0132038 * (1 - exp(-0.380271 * (enclose() + 0.582402))) * pow(width() , 0.0158293 * enclose() + -0.0210024) + 0.5 * (0.00740092 * exp(-0.478417 * enclose()) + 0.00740179 * exp(-0.478295 * enclose())) * pow(width() , -0.00149926 * enclose() + 0.0955821)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0132038 * (1 - exp(-0.380271 * (enclose() + 0.582402))) * pow(width() , 0.0158293 * enclose() + -0.0210024) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.07076 - 0.643288 * distance()) + 0.138955 / pow(distance() , 1.11413)) * 3.60507 * pow((width1() + width2()) / 2 , 0.106943) * (0.0385009 * (thickness1() + thickness2()) / 2 + 0.0160541) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.61))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.61)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.10034 * (1 - exp(-0.263259 * (distance() + 0.280643))) * pow(width() , 0.0331966 * distance() + -0.123097) * (0.0395357 * thickness() + 0.23634)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.02208 * (1 - exp(-0.373166 * (distance() + 0.0492738)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.118347 * pow(width() , 0.0482641) * (0.0595708 * thickness() + 0.153232)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00244494 * distance() + 0.0343663) * (0.815525 - exp(-(0.251532 * distance() + 0.266174) * (enclose() + 0.390298 * distance() + 1.10507) - (-0.00666771 * distance() + -0.0140254) * (enclose() + 1.02848 * distance() + 3.86963) * (enclose() + 1.02848 * distance() + 3.86963)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.10034 * (1 - exp(-0.263259 * (distance() + 0.280643))) * pow(width() , 0.0331966 * distance() + -0.123097) * (0.0395357 * thickness() + 0.23634)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0232955 * (0.815525 - exp(-1.52383 * (enclose() + 3.05656) - -0.047364 * (enclose() + 9.01203) * (enclose() + 9.01203)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0703828 * (1 - exp(-0.255197 * (distance() + 0.291978))) * pow(width() , 0.0334478 * distance() + -0.123208) * (0.0515419 * thickness() + 0.22547) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0703828 * (1 - exp(-0.255197 * (distance() + 0.291978))) * pow(width() , 0.0334478 * distance() + -0.123208) * (0.0515419 * thickness() + 0.22547) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0151029 * (1 - exp(-0.365718 * (distance() + 0.0496611))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.101924 * pow(width() , 0.0503457) * (0.0560209 * thickness() + 0.116598) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00020405 * distance() + 0.0326291) * (0.888588 - exp(-(0.141986 * distance() + 0.151933) * (enclose() + 0.330761 * distance() + 1.015) - (-0.0022403 * distance() + -0.012673) * (enclose() + 1.2015 * distance() + 3.02619) * (enclose() + 1.2015 * distance() + 3.02619))) + 0.5 * 7.33452 * exp(-0.495219 * enclose()) * (0.101265 - 0.101053 * exp(-0.00280124 * distance()) * pow(distance() , 0.00143296))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00020405 * distance() + 0.0326291) * (0.888588 - exp(-(0.141986 * distance() + 0.151933) * (enclose() + 0.330761 * distance() + 1.015) - (-0.0022403 * distance() + -0.012673) * (enclose() + 1.2015 * distance() + 3.02619) * (enclose() + 1.2015 * distance() + 3.02619))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0159772 * (1 - exp(-0.349774 * (enclose() + 0.637882))) * pow(width() , 0.0122348 * enclose() + -0.00338007) + 0.5 * (0.00915728 * exp(-0.422723 * enclose()) + 0.00915716 * exp(-0.421316 * enclose())) * pow(width() , -0.00034073 * enclose() + 0.083451)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0159772 * (1 - exp(-0.349774 * (enclose() + 0.637882))) * pow(width() , 0.0122348 * enclose() + -0.00338007) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal5,ctm
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.48911 - 1.46823 * distance()) + 0.0599042 / pow(distance() , 1.34834)) * 3.61616 * pow((width1() + width2()) / 2 , 0.103316) * (0.0738109 * (thickness1() + thickness2()) / 2 + 0.00531346) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.99))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.99)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0434749 * (1 - exp(-1.84832 * (distance() + 0.348159))) * pow(width() , -0.00325963 * distance() + 0.00899667) * (0.272318 * thickness() + 2.37366)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.107263 * (1 - exp(-1.62008 * (distance() + 0.434684)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.252242 * pow(width() , 0.0211304) * (0.103956 * thickness() + 0.411463)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00855481 * distance() + 0.117872) * (1.29523 - exp(-(-62.9773 * distance() + -33.8173) * (enclose() + 100 * distance() + 91.8704) - (88.3236 * distance() + 43.9126) * (enclose() + -43.6282 * distance() + -39.6294) * (enclose() + -43.6282 * distance() + -39.6294)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0434749 * (1 - exp(-1.84832 * (distance() + 0.348159))) * pow(width() , -0.00325963 * distance() + 0.00899667) * (0.272318 * thickness() + 2.37366)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.080323 * (1.29523 - exp(348.704 * (enclose() + 591.87) - 485.531 * (enclose() + -257.77) * (enclose() + -257.77)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0181359 * (1 - exp(-1.00969 * (distance() + 0.0744694))) * pow(width() , 0.015008 * distance() + -0.0130485) * (0.240462 * thickness() + 0.109053) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0181359 * (1 - exp(-1.00969 * (distance() + 0.0744694))) * pow(width() , 0.015008 * distance() + -0.0130485) * (0.240462 * thickness() + 0.109053) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00403498 * (1 - exp(-1.1733 * (distance() + 0.00194767))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0667456 * pow(width() , 0.0736247) * (0.0913599 * thickness() + 0.0166538) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0011329 * distance() + 0.0192061) * (0.671755 - exp(-(0.0586724 * distance() + 0.105825) * (enclose() + -0.233969 * distance() + 2.43396) - (-0.00692569 * distance() + 0.0117738) * (enclose() + 0.127306 * distance() + -0.488636) * (enclose() + 0.127306 * distance() + -0.488636))) + 0.5 * 1.05608 * exp(-0.449453 * enclose()) * (0.10176 - 0.099951 * exp(-0.0033018 * distance()) * pow(distance() , -0.00024938))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0011329 * distance() + 0.0192061) * (0.671755 - exp(-(0.0586724 * distance() + 0.105825) * (enclose() + -0.233969 * distance() + 2.43396) - (-0.00692569 * distance() + 0.0117738) * (enclose() + 0.127306 * distance() + -0.488636) * (enclose() + 0.127306 * distance() + -0.488636))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00613367 * (1 - exp(-0.210961 * (enclose() + 0.230813))) * pow(width() , 0.0696985 * enclose() + -0.245403) + 0.5 * (0.00353814 * exp(-0.45693 * enclose()) + 0.00353803 * exp(-0.45739 * enclose())) * pow(width() , -0.00375263 * enclose() + 0.247325)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00613367 * (1 - exp(-0.210961 * (enclose() + 0.230813))) * pow(width() , 0.0696985 * enclose() + -0.245403) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.24352 - 0.872631 * distance()) + 0.0965868 / pow(distance() , 1.16893)) * 3.69083 * pow((width1() + width2()) / 2 , 0.0887425) * (0.0499456 * (thickness1() + thickness2()) / 2 + 0.0178429) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.99))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.99)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.128344 * (1 - exp(-0.513667 * (distance() + 0.122957))) * pow(width() , 0.019707 * distance() + -0.0604745) * (0.0426188 * thickness() + 0.255015)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0337214 * (1 - exp(-0.593646 * (distance() + 0.0205717)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.140168 * pow(width() , 0.0429984) * (0.0791792 * thickness() + 0.207952)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00084133 * distance() + 0.0729863) * (0.92758 - exp(-(-0.0629061 * distance() + 1.94427) * (enclose() + -0.154755 * distance() + 1.90641) - (0.016407 * distance() + -0.176044) * (enclose() + -0.257273 * distance() + 4.6214) * (enclose() + -0.257273 * distance() + 4.6214)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.128344 * (1 - exp(-0.513667 * (distance() + 0.122957))) * pow(width() , 0.019707 * distance() + -0.0604745) * (0.0426188 * thickness() + 0.255015)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0343898 * (0.92758 - exp(-1.62974 * (enclose() + 1.13264) - -0.094009 * (enclose() + 3.33504) * (enclose() + 3.33504)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0352495 * (1 - exp(-0.450413 * (distance() + 0.154589))) * pow(width() , 0.0233371 * distance() + -0.0678824) * (0.0952592 * thickness() + 0.198244) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0352495 * (1 - exp(-0.450413 * (distance() + 0.154589))) * pow(width() , 0.0233371 * distance() + -0.0678824) * (0.0952592 * thickness() + 0.198244) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00811921 * (1 - exp(-0.544014 * (distance() + 0.0161217))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0825494 * pow(width() , 0.0554399) * (0.0611026 * thickness() + 0.0697357) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00070615 * distance() + 0.0220677) * (0.889451 - exp(-(0.107098 * distance() + 0.0787626) * (enclose() + 0.303778 * distance() + 0.920127) - (-0.00210142 * distance() + -0.00287057) * (enclose() + 1.12658 * distance() + 3.83932) * (enclose() + 1.12658 * distance() + 3.83932))) + 0.5 * 1.42504 * exp(-0.408448 * enclose()) * (0.101486 - 0.0999607 * exp(-0.00638837 * distance()) * pow(distance() , 0.00014076))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00070615 * distance() + 0.0220677) * (0.889451 - exp(-(0.107098 * distance() + 0.0787626) * (enclose() + 0.303778 * distance() + 0.920127) - (-0.00210142 * distance() + -0.00287057) * (enclose() + 1.12658 * distance() + 3.83932) * (enclose() + 1.12658 * distance() + 3.83932))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0100843 * (1 - exp(-0.255327 * (enclose() + 0.533579))) * pow(width() , 0.0307623 * enclose() + -0.0858355) + 0.5 * (0.00589654 * exp(-0.391045 * enclose()) + 0.00589646 * exp(-0.392475 * enclose())) * pow(width() , -0.00224149 * enclose() + 0.133407)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0100843 * (1 - exp(-0.255327 * (enclose() + 0.533579))) * pow(width() , 0.0307623 * enclose() + -0.0858355) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.99))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.99)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,ctm,metal6
//
CAPACITANCE NEARBODY ctm INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.83943 - 0.64491 * distance()) + 0.0539574 / pow(distance() , 1.34084)) * 2.81573 * pow((width1() + width2()) / 2 , 0.0338554) * (0.110735 * (thickness1() + thickness2()) / 2 + 0.0483061) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 5.539))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 5.539)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0598177 * (1 - exp(-0.687879 * (distance()))) * pow(width() , 0.0102851 * distance() + -0.108152) * (-4.13764 * thickness() + 1.45666)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0358376 * (1 - exp(-0.633459 * (distance() + 0.0263022)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.169949 * pow(width() , 0.00679997) * (0.0838051 * thickness() + 0.19659)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00036075 * distance() + 0.0502195) * (1.31607 - exp(-(-0.641546 * distance() + 4.59171) * (enclose() + -0.299287 * distance() + 2.38764) - (0.0654714 * distance() + -0.372238) * (enclose() + -0.331261 * distance() + 5.53045) * (enclose() + -0.331261 * distance() + 5.53045)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0598177 * (1 - exp(-0.687879 * (distance()))) * pow(width() , 0.0102851 * distance() + -0.108152) * (-4.13764 * thickness() + 1.45666)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0258673 * (1.31607 - exp(-1.89722 * (enclose() + 1.13063) - -0.0972581 * (enclose() + 4.13915) * (enclose() + 4.13915)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0312639 * (1 - exp(-0.373612 * (distance() + 0.253929))) * pow(width() , -0.0174346 * distance() + 0.0320598) * (0.188531 * thickness() + 0.185415) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0312639 * (1 - exp(-0.373612 * (distance() + 0.253929))) * pow(width() , -0.0174346 * distance() + 0.0320598) * (0.188531 * thickness() + 0.185415) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0149856 * (1 - exp(-0.0696361 * (distance() + 2.08601))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0693957 * pow(width() , 0.00972546) * (0.0792184 * thickness() + 0.0663334) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00643729 * distance() + 0.0839089) * (0.625581 - exp(-(0.0329915 * distance() + 0.108872) * (enclose() + 0.102329 * distance() + 2.39854) - (0.00020409 * distance() + -0.0115789) * (enclose() + 1.10148 * distance() + 0.837665) * (enclose() + 1.10148 * distance() + 0.837665))) + 0.5 * 0.233617 * exp(-0.391209 * enclose()) * (0.0953381 - 0.105179 * exp(-0.141685 * distance()) * pow(distance() , 0.14513))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00643729 * distance() + 0.0839089) * (0.625581 - exp(-(0.0329915 * distance() + 0.108872) * (enclose() + 0.102329 * distance() + 2.39854) - (0.00020409 * distance() + -0.0115789) * (enclose() + 1.10148 * distance() + 0.837665) * (enclose() + 1.10148 * distance() + 0.837665))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 5.539)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0229875 * (1 - exp(-0.0696618 * (enclose() + -0.663688))) * pow(width() , -0.0692488 * enclose() + 0.222668) + 0.5 * (0.00542995 * exp(-0.398404 * enclose()) + 0.00543519 * exp(-0.393614 * enclose())) * pow(width() , -0.00275454 * enclose() + 0.0432964)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 5.539)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0229875 * (1 - exp(-0.0696618 * (enclose() + -0.663688))) * pow(width() , -0.0692488 * enclose() + 0.222668) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 5.539))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 5.539)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.20736 - 1.54799 * distance()) + 0.0181656 / pow(distance() , 1.62739)) * 3.64956 * pow((width1() + width2()) / 2 , 0.035268) * (0.0967476 * (thickness1() + thickness2()) / 2 + 0.0253109) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.106383 * (1 - exp(-0.856491 * (distance()))) * pow(width() , 0.0178143 * distance() + -0.0516268) * (-0.0115373 * thickness() + 0.253233)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0248943 * (1 - exp(-1.06143 * (distance() + -0.0614737)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.120588 * pow(width() , 0.0120559) * (0.102413 * thickness() + 0.151376)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00031684 * distance() + 0.148692) * (0.325509 - exp(-(-0.0451973 * distance() + 0.0415867) * (enclose() + 6.41582 * distance() + 3.23982) - (0.0144186 * distance() + 0.0576222) * (enclose() + 0.868985 * distance() + 3.66439) * (enclose() + 0.868985 * distance() + 3.66439)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.106383 * (1 - exp(-0.856491 * (distance()))) * pow(width() , 0.0178143 * distance() + -0.0516268) * (-0.0115373 * thickness() + 0.253233)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0735539 * (0.325509 - exp(0.1844 * (enclose() + 35.3189) - 0.129715 * (enclose() + 8.00932) * (enclose() + 8.00932)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107737 * (1 - exp(-0.850961 * (distance()))) * pow(width() , 0.0181225 * distance() + -0.0524913) * (-0.0101955 * thickness() + 0.252272) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107737 * (1 - exp(-0.850961 * (distance()))) * pow(width() , 0.0181225 * distance() + -0.0524913) * (-0.0101955 * thickness() + 0.252272) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0251704 * (1 - exp(-1.05661 * (distance() + -0.062351))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.121507 * pow(width() , 0.0126482) * (0.103971 * thickness() + 0.15128) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00063879 * distance() + 0.197682) * (0.248988 - exp(-(-0.0545863 * distance() + 0.0185585) * (enclose() + 11.2277 * distance() + 14.6124) - (0.00132494 * distance() + 0.0461744) * (enclose() + 3.00196 * distance() + 4.4415) * (enclose() + 3.00196 * distance() + 4.4415))) + 0.5 * 0.25963 * exp(-1.65246 * enclose()) * (0.0957846 - 0.104483 * exp(-0.11835 * distance()) * pow(distance() , 0.0593247))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00063879 * distance() + 0.197682) * (0.248988 - exp(-(-0.0545863 * distance() + 0.0185585) * (enclose() + 11.2277 * distance() + 14.6124) - (0.00132494 * distance() + 0.0461744) * (enclose() + 3.00196 * distance() + 4.4415) * (enclose() + 3.00196 * distance() + 4.4415))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0251625 * (1 - exp(-1.39698 * (enclose() + 0.451374))) * pow(width() , 0.0001338 * enclose() + 0.0134585) + 0.5 * (0.00427537 * exp(-1.32648 * enclose()) + 0.00428467 * exp(-1.32717 * enclose())) * pow(width() , 0.0237119 * enclose() + -3.99172e-05)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0251625 * (1 - exp(-1.39698 * (enclose() + 0.451374))) * pow(width() , 0.0001338 * enclose() + 0.0134585) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.20894 - 1.11149 * distance()) + 0.0443051 / pow(distance() , 1.38171)) * 3.63815 * pow((width1() + width2()) / 2 , 0.0690482) * (0.0693937 * (thickness1() + thickness2()) / 2 + 0.0223879) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0573036 * (1 - exp(-0.593657 * (distance() + 0.0452633))) * pow(width() , 0.020966 * distance() + -0.0614153) * (0.0983743 * thickness() + 0.205962)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0139543 * (1 - exp(-0.713025 * (distance() + -0.035677)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0977661 * pow(width() , 0.0352393) * (0.0835131 * thickness() + 0.0998282)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-2.06146e-05 * distance() + 0.0349037) * (0.771986 - exp(-(0.322307 * distance() + 0.209894) * (enclose() + 0.226793 * distance() + 1.01233) - (-0.0112546 * distance() + -0.00887604) * (enclose() + 0.734089 * distance() + 3.2353) * (enclose() + 0.734089 * distance() + 3.2353)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0573036 * (1 - exp(-0.593657 * (distance() + 0.0452633))) * pow(width() , 0.020966 * distance() + -0.0614153) * (0.0983743 * thickness() + 0.205962)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0174003 * (0.771986 - exp(-1.82143 * (enclose() + 2.14629) - -0.065149 * (enclose() + 6.90574) * (enclose() + 6.90574)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.116896 * (1 - exp(-0.663717 * (distance() + 0.0343792))) * pow(width() , 0.0165093 * distance() + -0.0496066) * (0.0599032 * thickness() + 0.24861) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.116896 * (1 - exp(-0.663717 * (distance() + 0.0343792))) * pow(width() , 0.0165093 * distance() + -0.0496066) * (0.0599032 * thickness() + 0.24861) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0314524 * (1 - exp(-0.764014 * (distance() + -0.0255507))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135363 * pow(width() , 0.0256885) * (0.0852907 * thickness() + 0.18912) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00780139 * distance() + 0.544321) * (0.101329 - exp(-(-0.0484492 * distance() + 0.0585309) * (enclose() + 5.45389 * distance() + 12.5583) - (0.00890788 * distance() + 0.0225765) * (enclose() + 0.899542 * distance() + 7.80374) * (enclose() + 0.899542 * distance() + 7.80374))) + 0.5 * 6.1974 * exp(-1.22124 * enclose()) * (0.1001 - 0.10038 * exp(-0.00501351 * distance()) * pow(distance() , 0.00273832))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00780139 * distance() + 0.544321) * (0.101329 - exp(-(-0.0484492 * distance() + 0.0585309) * (enclose() + 5.45389 * distance() + 12.5583) - (0.00890788 * distance() + 0.0225765) * (enclose() + 0.899542 * distance() + 7.80374) * (enclose() + 0.899542 * distance() + 7.80374))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0314895 * (1 - exp(-1.12998 * (enclose() + 0.521625))) * pow(width() , -0.00121882 * enclose() + 0.0283424) + 0.5 * (0.00705036 * exp(-0.77787 * enclose()) + 0.00708386 * exp(-1.16894 * enclose())) * pow(width() , 0.0267115 * enclose() + 0.0177581)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0314895 * (1 - exp(-1.12998 * (enclose() + 0.521625))) * pow(width() , -0.00121882 * enclose() + 0.0283424) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,ctm
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.21796 - 1.04074 * distance()) + 0.0546393 / pow(distance() , 1.32433)) * 3.657 * pow((width1() + width2()) / 2 , 0.0752674) * (0.0638299 * (thickness1() + thickness2()) / 2 + 0.0210686) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE metal3 ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0498488 * (1 - exp(-0.547487 * (distance() + 0.0739176))) * pow(width() , 0.0224471 * distance() + -0.0658163) * (0.100059 * thickness() + 0.202546)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0119331 * (1 - exp(-0.664141 * (distance() + -0.0234569)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0927782 * pow(width() , 0.0414999) * (0.0773812 * thickness() + 0.0897309)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00044337 * distance() + 0.0307309) * (0.816628 - exp(-(0.256174 * distance() + 0.219349) * (enclose() + 0.179393 * distance() + 0.831589) - (-0.00797819 * distance() + -0.0243672) * (enclose() + 0.715119 * distance() + 1.96026) * (enclose() + 0.715119 * distance() + 1.96026)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0498488 * (1 - exp(-0.547487 * (distance() + 0.0739176))) * pow(width() , 0.0224471 * distance() + -0.0658163) * (0.100059 * thickness() + 0.202546)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.014257 * (0.816628 - exp(-1.50022 * (enclose() + 1.72855) - -0.0642581 * (enclose() + 5.53585) * (enclose() + 5.53585)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,ctm
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.122444 * (1 - exp(-0.622902 * (distance() + 0.0564419))) * pow(width() , 0.0175619 * distance() + -0.0531437) * (0.0545869 * thickness() + 0.251633) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.122444 * (1 - exp(-0.622902 * (distance() + 0.0564419))) * pow(width() , 0.0175619 * distance() + -0.0531437) * (0.0545869 * thickness() + 0.251633) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0328492 * (1 - exp(-0.719454 * (distance() + -0.0135101))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.138396 * pow(width() , 0.03017) * (0.0837074 * thickness() + 0.196605) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0121666 * distance() + 0.600993) * (0.0925022 - exp(-(-0.0527536 * distance() + 0.0693351) * (enclose() + 4.91826 * distance() + 12.0673) - (0.0085684 * distance() + 0.0212806) * (enclose() + 0.900796 * distance() + 8.01483) * (enclose() + 0.900796 * distance() + 8.01483))) + 0.5 * 10.6478 * exp(-1.1235 * enclose()) * (0.100254 - 0.100387 * exp(-0.00291534 * distance()) * pow(distance() , 0.0015824))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0121666 * distance() + 0.600993) * (0.0925022 - exp(-(-0.0527536 * distance() + 0.0693351) * (enclose() + 4.91826 * distance() + 12.0673) - (0.0085684 * distance() + 0.0212806) * (enclose() + 0.900796 * distance() + 8.01483) * (enclose() + 0.900796 * distance() + 8.01483))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0329492 * (1 - exp(-1.07432 * (enclose() + 0.539678))) * pow(width() , -0.00139909 * enclose() + 0.0322513) + 0.5 * (0.00798267 * exp(-1.1429 * enclose()) + 0.00793276 * exp(-0.669101 * enclose())) * pow(width() , 0.0273776 * enclose() + 0.02452)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0329492 * (1 - exp(-1.07432 * (enclose() + 0.539678))) * pow(width() , -0.00139909 * enclose() + 0.0322513) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.24188 - 0.971078 * distance()) + 0.0706862 / pow(distance() , 1.2514)) * 3.68743 * pow((width1() + width2()) / 2 , 0.0816017) * (0.0576573 * (thickness1() + thickness2()) / 2 + 0.0194361) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0406703 * (1 - exp(-0.510069 * (distance() + 0.109466))) * pow(width() , 0.0230667 * distance() + -0.0660871) * (0.102114 * thickness() + 0.198364)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00956559 * (1 - exp(-0.617724 * (distance() + -0.00540783)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0865227 * pow(width() , 0.0496018) * (0.0699231 * thickness() + 0.0767016)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-1.8506e-05 * distance() + 0.0219566) * (0.847705 - exp(-(0.171589 * distance() + 0.108862) * (enclose() + 0.347728 * distance() + 0.578629) - (-0.00491023 * distance() + -0.00350953) * (enclose() + 1.09891 * distance() + 1.91557) * (enclose() + 1.09891 * distance() + 1.91557)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0406703 * (1 - exp(-0.510069 * (distance() + 0.109466))) * pow(width() , 0.0230667 * distance() + -0.0660871) * (0.102114 * thickness() + 0.198364)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.010932 * (0.847705 - exp(-0.966807 * (enclose() + 2.31727) - -0.0280607 * (enclose() + 7.41012) * (enclose() + 7.41012)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = field_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.128715 * (1 - exp(-0.588331 * (distance() + 0.0831527))) * pow(width() , 0.0181696 * distance() + -0.0543785) * (0.0490243 * thickness() + 0.255078) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.128715 * (1 - exp(-0.588331 * (distance() + 0.0831527))) * pow(width() , 0.0181696 * distance() + -0.0543785) * (0.0490243 * thickness() + 0.255078) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0344919 * (1 - exp(-0.67638 * (distance() + 0.00246163))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142046 * pow(width() , 0.0363428) * (0.0829089 * thickness() + 0.205525) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0116503 * distance() + 0.388733) * (0.142449 - exp(-(-0.0313502 * distance() + 0.0355598) * (enclose() + 7.88993 * distance() + 13.4588) - (0.00647026 * distance() + 0.0276433) * (enclose() + 1.0677 * distance() + 6.83878) * (enclose() + 1.0677 * distance() + 6.83878))) + 0.5 * 7.59108 * exp(-1.00107 * enclose()) * (0.100476 - 0.100583 * exp(-0.0040158 * distance()) * pow(distance() , 0.00209201))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0116503 * distance() + 0.388733) * (0.142449 - exp(-(-0.0313502 * distance() + 0.0355598) * (enclose() + 7.88993 * distance() + 13.4588) - (0.00647026 * distance() + 0.0276433) * (enclose() + 1.0677 * distance() + 6.83878) * (enclose() + 1.0677 * distance() + 6.83878))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0347241 * (1 - exp(-1.01042 * (enclose() + 0.562224))) * pow(width() , -0.00155369 * enclose() + 0.0370978) + 0.5 * (0.00923168 * exp(-0.549771 * enclose()) + 0.00926446 * exp(-1.1036 * enclose())) * pow(width() , 0.0287129 * enclose() + 0.0339079)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0347241 * (1 - exp(-1.01042 * (enclose() + 0.562224))) * pow(width() , -0.00155369 * enclose() + 0.0370978) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.20721 - 1.11026 * distance()) + 0.0443753 / pow(distance() , 1.3808)) * 3.63996 * pow((width1() + width2()) / 2 , 0.0686261) * (0.0691795 * (thickness1() + thickness2()) / 2 + 0.0224031) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.115824 * (1 - exp(-0.663153 * (distance() + 0.0355479))) * pow(width() , 0.0163334 * distance() + -0.0496497) * (0.0589202 * thickness() + 0.249334)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.031211 * (1 - exp(-0.762531 * (distance() + -0.024047)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133448 * pow(width() , 0.0249312) * (0.0849081 * thickness() + 0.190748)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0117526 * distance() + 0.671494) * (0.0801761 - exp(-(-0.187871 * distance() + 0.410647) * (enclose() + 1.35383 * distance() + 5.50521) - (0.0222971 * distance() + 0.00180463) * (enclose() + 0.483275 * distance() + 6.8108) * (enclose() + 0.483275 * distance() + 6.8108)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.115824 * (1 - exp(-0.663153 * (distance() + 0.0355479))) * pow(width() , 0.0163334 * distance() + -0.0496497) * (0.0589202 * thickness() + 0.249334)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.365129 * (0.0801761 - exp(0.528708 * (enclose() + 12.2744) - 0.11329 * (enclose() + 9.22718) * (enclose() + 9.22718)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.057906 * (1 - exp(-0.589727 * (distance() + 0.0437998))) * pow(width() , 0.0210541 * distance() + -0.0623527) * (0.0986983 * thickness() + 0.20505) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.057906 * (1 - exp(-0.589727 * (distance() + 0.0437998))) * pow(width() , 0.0210541 * distance() + -0.0623527) * (0.0986983 * thickness() + 0.20505) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0140597 * (1 - exp(-0.709287 * (distance() + -0.0370355))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0979381 * pow(width() , 0.0345166) * (0.0847801 * thickness() + 0.0998187) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00037391 * distance() + 0.0370799) * (0.773745 - exp(-(0.300256 * distance() + 0.205538) * (enclose() + 0.199827 * distance() + 1.08408) - (-0.010525 * distance() + -0.00934768) * (enclose() + 0.69711 * distance() + 3.31547) * (enclose() + 0.69711 * distance() + 3.31547))) + 0.5 * 1.43439 * exp(-0.849729 * enclose()) * (0.100269 - 0.100388 * exp(-0.0110405 * distance()) * pow(distance() , 0.00683524))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00037391 * distance() + 0.0370799) * (0.773745 - exp(-(0.300256 * distance() + 0.205538) * (enclose() + 0.199827 * distance() + 1.08408) - (-0.010525 * distance() + -0.00934768) * (enclose() + 0.69711 * distance() + 3.31547) * (enclose() + 0.69711 * distance() + 3.31547))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.014478 * (1 - exp(-0.68573 * (enclose() + 0.649317))) * pow(width() , 0.00719532 * enclose() + 0.00728926) + 0.5 * (0.00451083 * exp(-0.757778 * enclose()) + 0.00451101 * exp(-0.755817 * enclose())) * pow(width() , 0.00712138 * enclose() + 0.0614831)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.014478 * (1 - exp(-0.68573 * (enclose() + 0.649317))) * pow(width() , 0.00719532 * enclose() + 0.00728926) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,ctm
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.14254 - 0.92164 * distance()) + 0.0597512 / pow(distance() , 1.31426)) * 3.53941 * pow((width1() + width2()) / 2 , 0.0825226) * (0.059782 * (thickness1() + thickness2()) / 2 + 0.0219341) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.100977 * (1 - exp(-0.472266 * (distance() + 0.0849467))) * pow(width() , 0.0233188 * distance() + -0.0777912) * (0.0563965 * thickness() + 0.233427)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0247812 * (1 - exp(-0.583109 * (distance() + -0.016418)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.122673 * pow(width() , 0.0322652) * (0.0742737 * thickness() + 0.162397)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00186739 * distance() + 0.0632576) * (0.861507 - exp(-(0.0695084 * distance() + 0.822106) * (enclose() + -0.0557129 * distance() + 0.857584) - (0.00829389 * distance() + -0.132935) * (enclose() + -0.341575 * distance() + 2.24815) * (enclose() + -0.341575 * distance() + 2.24815)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.100977 * (1 - exp(-0.472266 * (distance() + 0.0849467))) * pow(width() , 0.0233188 * distance() + -0.0777912) * (0.0563965 * thickness() + 0.233427)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0269603 * (0.861507 - exp(-1.16965 * (enclose() + 0.57902) - -0.0914656 * (enclose() + 0.540275) * (enclose() + 0.540275)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0711975 * (1 - exp(-0.448495 * (distance() + 0.0933525))) * pow(width() , 0.0250005 * distance() + -0.0824867) * (0.0735975 * thickness() + 0.216175) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0711975 * (1 - exp(-0.448495 * (distance() + 0.0933525))) * pow(width() , 0.0250005 * distance() + -0.0824867) * (0.0735975 * thickness() + 0.216175) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0167666 * (1 - exp(-0.565353 * (distance() + -0.0201282))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.105397 * pow(width() , 0.0361555) * (0.0727209 * thickness() + 0.120104) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (3.69002e-05 * distance() + 0.0392869) * (0.815388 - exp(-(0.276143 * distance() + 0.265013) * (enclose() + 0.24411 * distance() + 1.09296) - (-0.00796556 * distance() + -0.020191) * (enclose() + 0.819487 * distance() + 3.18857) * (enclose() + 0.819487 * distance() + 3.18857))) + 0.5 * 2.49214 * exp(-0.795495 * enclose()) * (0.100368 - 0.100466 * exp(-0.00741837 * distance()) * pow(distance() , 0.00493703))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (3.69002e-05 * distance() + 0.0392869) * (0.815388 - exp(-(0.276143 * distance() + 0.265013) * (enclose() + 0.24411 * distance() + 1.09296) - (-0.00796556 * distance() + -0.020191) * (enclose() + 0.819487 * distance() + 3.18857) * (enclose() + 0.819487 * distance() + 3.18857))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.017025 * (1 - exp(-0.631524 * (enclose() + 0.700149))) * pow(width() , 0.00546016 * enclose() + 0.0153134) + 0.5 * (0.00559126 * exp(-0.674323 * enclose()) + 0.00559386 * exp(-0.674101 * enclose())) * pow(width() , 0.00749065 * enclose() + 0.0522928)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.017025 * (1 - exp(-0.631524 * (enclose() + 0.700149))) * pow(width() , 0.00546016 * enclose() + 0.0153134) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.09678 - 0.758738 * distance()) + 0.0897785 / pow(distance() , 1.21737)) * 3.59539 * pow((width1() + width2()) / 2 , 0.0969968) * (0.0479398 * (thickness1() + thickness2()) / 2 + 0.0190753) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0836062 * (1 - exp(-0.328789 * (distance() + 0.183568))) * pow(width() , 0.030447 * distance() + -0.108368) * (0.0548258 * thickness() + 0.227861)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.01899 * (1 - exp(-0.446898 * (distance() + 0.0121943)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.110881 * pow(width() , 0.041876) * (0.0638154 * thickness() + 0.135344)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00133956 * distance() + 0.0353237) * (0.823694 - exp(-(0.261609 * distance() + 0.255601) * (enclose() + 0.375415 * distance() + 0.862577) - (-0.00640233 * distance() + -0.0214589) * (enclose() + 1.09372 * distance() + 2.58707) * (enclose() + 1.09372 * distance() + 2.58707)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0836062 * (1 - exp(-0.328789 * (distance() + 0.183568))) * pow(width() , 0.030447 * distance() + -0.108368) * (0.0548258 * thickness() + 0.227861)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0210108 * (0.823694 - exp(-1.56365 * (enclose() + 2.73965) - -0.0534706 * (enclose() + 8.05567) * (enclose() + 8.05567)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0881628 * (1 - exp(-0.330369 * (distance() + 0.179858))) * pow(width() , 0.0303823 * distance() + -0.108484) * (0.0534965 * thickness() + 0.229064) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0881628 * (1 - exp(-0.330369 * (distance() + 0.179858))) * pow(width() , 0.0303823 * distance() + -0.108484) * (0.0534965 * thickness() + 0.229064) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0200885 * (1 - exp(-0.448149 * (distance() + 0.0108277))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.113374 * pow(width() , 0.0412827) * (0.0648823 * thickness() + 0.140619) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00081203 * distance() + 0.040861) * (0.824251 - exp(-(0.253456 * distance() + 0.290443) * (enclose() + 0.276053 * distance() + 1.21681) - (-0.0063524 * distance() + -0.0211898) * (enclose() + 0.897376 * distance() + 3.58367) * (enclose() + 0.897376 * distance() + 3.58367))) + 0.5 * 2.58027 * exp(-0.72216 * enclose()) * (0.100598 - 0.100608 * exp(-0.0079222 * distance()) * pow(distance() , 0.00561242))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00081203 * distance() + 0.040861) * (0.824251 - exp(-(0.253456 * distance() + 0.290443) * (enclose() + 0.276053 * distance() + 1.21681) - (-0.0063524 * distance() + -0.0211898) * (enclose() + 0.897376 * distance() + 3.58367) * (enclose() + 0.897376 * distance() + 3.58367))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0200807 * (1 - exp(-0.569015 * (enclose() + 0.756037))) * pow(width() , 0.00420513 * enclose() + 0.024328) + 0.5 * (0.0071474 * exp(-0.578239 * enclose()) + 0.00714734 * exp(-0.578294 * enclose())) * pow(width() , 0.00813881 * enclose() + 0.0499211)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0200807 * (1 - exp(-0.569015 * (enclose() + 0.756037))) * pow(width() , 0.00420513 * enclose() + 0.024328) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal5,ctm
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.44253 - 1.52711 * distance()) + 0.0470012 / pow(distance() , 1.41296)) * 3.54839 * pow((width1() + width2()) / 2 , 0.0968651) * (0.0829951 * (thickness1() + thickness2()) / 2 + 0.00544802) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.61))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.61)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0434456 * (1 - exp(-1.8694 * (distance() + 0.342698))) * pow(width() , -0.00287448 * distance() + 0.00673216) * (0.269922 * thickness() + 2.34784)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.106959 * (1 - exp(-1.30867 * (distance() + 0.581873)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.250065 * pow(width() , 0.0174535) * (0.100702 * thickness() + 0.409591)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00812453 * distance() + 0.115173) * (1.32625 - exp(-(-53.6698 * distance() + -28.1532) * (enclose() + 100 * distance() + 4.86682) - (70.5011 * distance() + 44.6908) * (enclose() + -34.5696 * distance() + -1.80073) * (enclose() + -34.5696 * distance() + -1.80073)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0434456 * (1 - exp(-1.8694 * (distance() + 0.342698))) * pow(width() , -0.00287448 * distance() + 0.00673216) * (0.269922 * thickness() + 2.34784)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0778978 * (1.32625 - exp(296.502 * (enclose() + 504.867) - 397.196 * (enclose() + -174.649) * (enclose() + -174.649)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0242425 * (1 - exp(-1.03202 * (distance() + 0.0588818))) * pow(width() , 0.0153677 * distance() + -0.0196512) * (0.229569 * thickness() + 0.112754) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0242425 * (1 - exp(-1.03202 * (distance() + 0.0588818))) * pow(width() , 0.0153677 * distance() + -0.0196512) * (0.229569 * thickness() + 0.112754) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00537146 * (1 - exp(-1.21299 * (distance() + -0.0107956))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0696316 * pow(width() , 0.0637401) * (0.110911 * thickness() + 0.0231029) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00050139 * distance() + 0.01904) * (0.593384 - exp(-(0.0681748 * distance() + 0.154845) * (enclose() + -0.0279021 * distance() + 1.75597) - (0.0147846 * distance() + 0.00346917) * (enclose() + -0.428005 * distance() + 3.18344) * (enclose() + -0.428005 * distance() + 3.18344))) + 0.5 * 0.244549 * exp(-0.631807 * enclose()) * (0.101997 - 0.098163 * exp(-0.0192938 * distance()) * pow(distance() , 0.0120886))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00050139 * distance() + 0.01904) * (0.593384 - exp(-(0.0681748 * distance() + 0.154845) * (enclose() + -0.0279021 * distance() + 1.75597) - (0.0147846 * distance() + 0.00346917) * (enclose() + -0.428005 * distance() + 3.18344) * (enclose() + -0.428005 * distance() + 3.18344))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0062273 * (1 - exp(-0.441326 * (enclose() + 0.520992))) * pow(width() , 0.035018 * enclose() + -0.0851565) + 0.5 * (0.00267387 * exp(-0.62868 * enclose()) + 0.00267384 * exp(-0.630207 * enclose())) * pow(width() , 0.00169543 * enclose() + 0.198977)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0062273 * (1 - exp(-0.441326 * (enclose() + 0.520992))) * pow(width() , 0.035018 * enclose() + -0.0851565) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.22131 - 0.923088 * distance()) + 0.0734533 / pow(distance() , 1.24312)) * 3.65507 * pow((width1() + width2()) / 2 , 0.0838309) * (0.0562364 * (thickness1() + thickness2()) / 2 + 0.0197745) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.61))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.61)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.122833 * (1 - exp(-0.532206 * (distance() + 0.0954738))) * pow(width() , 0.0200868 * distance() + -0.0632298) * (0.0474414 * thickness() + 0.251346)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0321525 * (1 - exp(-0.624447 * (distance() + 0.00330729)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13637 * pow(width() , 0.0364124) * (0.0789133 * thickness() + 0.199185)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00565638 * distance() + 0.0735937) * (0.584491 - exp(-(-0.0348715 * distance() + 0.357163) * (enclose() + 0.550286 * distance() + 0.793497) - (0.0458935 * distance() + 0.00583071) * (enclose() + -0.256602 * distance() + 2.98318) * (enclose() + -0.256602 * distance() + 2.98318)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.122833 * (1 - exp(-0.532206 * (distance() + 0.0954738))) * pow(width() , 0.0200868 * distance() + -0.0632298) * (0.0474414 * thickness() + 0.251346)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0509378 * (0.584491 - exp(-0.182806 * (enclose() + 3.54493) - 0.235298 * (enclose() + 1.70017) * (enclose() + 1.70017)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0447658 * (1 - exp(-0.468909 * (distance() + 0.119024))) * pow(width() , 0.0242003 * distance() + -0.0735084) * (0.0940358 * thickness() + 0.203093) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0447658 * (1 - exp(-0.468909 * (distance() + 0.119024))) * pow(width() , 0.0242003 * distance() + -0.0735084) * (0.0940358 * thickness() + 0.203093) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0104884 * (1 - exp(-0.576666 * (distance() + -0.00426723))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0892067 * pow(width() , 0.0473487) * (0.069001 * thickness() + 0.0834875) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00030177 * distance() + 0.0252521) * (0.857345 - exp(-(0.170693 * distance() + 0.11802) * (enclose() + 0.299609 * distance() + 0.730497) - (-0.00417838 * distance() + -0.00659596) * (enclose() + 1.08692 * distance() + 2.37254) * (enclose() + 1.08692 * distance() + 2.37254))) + 0.5 * 3.23807 * exp(-0.562307 * enclose()) * (0.100659 - 0.100329 * exp(-0.00276684 * distance()) * pow(distance() , 0.00128369))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00030177 * distance() + 0.0252521) * (0.857345 - exp(-(0.170693 * distance() + 0.11802) * (enclose() + 0.299609 * distance() + 0.730497) - (-0.00417838 * distance() + -0.00659596) * (enclose() + 1.08692 * distance() + 2.37254) * (enclose() + 1.08692 * distance() + 2.37254))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0113294 * (1 - exp(-0.424613 * (enclose() + 0.762355))) * pow(width() , 0.0143829 * enclose() + -0.0145295) + 0.5 * (0.00466056 * exp(-0.519313 * enclose()) + 0.00466057 * exp(-0.519296 * enclose())) * pow(width() , 0.00266444 * enclose() + 0.0987661)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0113294 * (1 - exp(-0.424613 * (enclose() + 0.762355))) * pow(width() , 0.0143829 * enclose() + -0.0145295) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.61))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.61)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,ctm,metal6
//
CAPACITANCE NEARBODY ctm INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.76234 - 0.735598 * distance()) + 0.038583 / pow(distance() , 1.56442)) * 2.81565 * pow((width1() + width2()) / 2 , 0.0144581) * (0.130971 * (thickness1() + thickness2()) / 2 + 0.0494378) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.159))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.159)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.057505 * (1 - exp(-0.693157 * (distance()))) * pow(width() , 0.00494795 * distance() + -0.0920474) * (-4.25334 * thickness() + 1.48105)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0336727 * (1 - exp(-0.703531 * (distance() + -0.00880862)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.164561 * pow(width() , 0.00148871) * (0.0874683 * thickness() + 0.188931)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00134737 * distance() + 0.0710928) * (0.833472 - exp(-(-0.0766659 * distance() + 1.4591) * (enclose() + 0.135633 * distance() + 0.291414) - (0.101934 * distance() + -0.4722) * (enclose() + -0.308167 * distance() + 0.960285) * (enclose() + -0.308167 * distance() + 0.960285)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.057505 * (1 - exp(-0.693157 * (distance()))) * pow(width() , 0.00494795 * distance() + -0.0920474) * (-4.25334 * thickness() + 1.48105)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0383759 * (0.833472 - exp(-1.1371 * (enclose() + 0.861073) - -0.0440772 * (enclose() + -0.334016) * (enclose() + -0.334016)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0354045 * (1 - exp(-0.642539 * (distance()))) * pow(width() , -0.00173345 * distance() + -0.00134759) * (0.19762 * thickness() + 0.165951) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0354045 * (1 - exp(-0.642539 * (distance()))) * pow(width() , -0.00173345 * distance() + -0.00134759) * (0.19762 * thickness() + 0.165951) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00700077 * (1 - exp(-0.682614 * (distance() + -0.034629))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.078116 * pow(width() , 0.00393383) * (0.0840389 * thickness() + 0.0734751) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00409735 * distance() + 0.0647282) * (0.56249 - exp(-(0.0574073 * distance() + 0.140671) * (enclose() + 0.0275388 * distance() + 1.88972) - (0.00032213 * distance() + -0.0250918) * (enclose() + 0.849659 * distance() + -0.691651) * (enclose() + 0.849659 * distance() + -0.691651))) + 0.5 * 0.395468 * exp(-0.513866 * enclose()) * (0.0979414 - 0.10221 * exp(-0.0513256 * distance()) * pow(distance() , 0.0505939))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00409735 * distance() + 0.0647282) * (0.56249 - exp(-(0.0574073 * distance() + 0.140671) * (enclose() + 0.0275388 * distance() + 1.88972) - (0.00032213 * distance() + -0.0250918) * (enclose() + 0.849659 * distance() + -0.691651) * (enclose() + 0.849659 * distance() + -0.691651))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.159)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00741059 * (1 - exp(-0.474616 * (enclose() + 0.0710246))) * pow(width() , 0.00154824 * enclose() + -0.0106612) + 0.5 * (0.00400299 * exp(-0.517358 * enclose()) + 0.00400152 * exp(-0.519496 * enclose())) * pow(width() , -0.00144087 * enclose() + 0.017732)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 4.159)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00741059 * (1 - exp(-0.474616 * (enclose() + 0.0710246))) * pow(width() , 0.00154824 * enclose() + -0.0106612) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 4.159))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 4.159)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.20737 - 1.54799 * distance()) + 0.0181657 / pow(distance() , 1.62739)) * 3.64957 * pow((width1() + width2()) / 2 , 0.0352679) * (0.0967477 * (thickness1() + thickness2()) / 2 + 0.0253111) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.106383 * (1 - exp(-0.856498 * (distance()))) * pow(width() , 0.0178132 * distance() + -0.0516204) * (-0.0115248 * thickness() + 0.253227)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0248943 * (1 - exp(-1.06143 * (distance() + -0.0614721)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.120588 * pow(width() , 0.0120567) * (0.10241 * thickness() + 0.151377)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00123323 * distance() + 0.322857) * (0.151187 - exp(-(-0.00779321 * distance() + -0.0042368) * (enclose() + 43.7177 * distance() + 23.618) - (0.00890914 * distance() + 0.0297548) * (enclose() + 1.10189 * distance() + 7.73313) * (enclose() + 1.10189 * distance() + 7.73313)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.106383 * (1 - exp(-0.856498 * (distance()))) * pow(width() , 0.0178132 * distance() + -0.0516204) * (-0.0115248 * thickness() + 0.253227)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.158345 * (0.151187 - exp(0.0432029 * (enclose() + 242.207) - 0.0743005 * (enclose() + 13.2426) * (enclose() + 13.2426)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107737 * (1 - exp(-0.850961 * (distance()))) * pow(width() , 0.0181225 * distance() + -0.0524913) * (-0.0101955 * thickness() + 0.252272) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107737 * (1 - exp(-0.850961 * (distance()))) * pow(width() , 0.0181225 * distance() + -0.0524913) * (-0.0101955 * thickness() + 0.252272) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0251704 * (1 - exp(-1.05661 * (distance() + -0.0623521))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.121507 * pow(width() , 0.0126476) * (0.103968 * thickness() + 0.151282) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00065218 * distance() + 0.274826) * (0.178589 - exp(-(-0.0481942 * distance() + 0.00613991) * (enclose() + 17.4101 * distance() + 27.6768) - (0.00058777 * distance() + 0.0331358) * (enclose() + 4.42453 * distance() + 6.37212) * (enclose() + 4.42453 * distance() + 6.37212))) + 0.5 * 0.200783 * exp(-1.66861 * enclose()) * (0.0962646 - 0.103957 * exp(-0.105321 * distance()) * pow(distance() , 0.0533585))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00065218 * distance() + 0.274826) * (0.178589 - exp(-(-0.0481942 * distance() + 0.00613991) * (enclose() + 17.4101 * distance() + 27.6768) - (0.00058777 * distance() + 0.0331358) * (enclose() + 4.42453 * distance() + 6.37212) * (enclose() + 4.42453 * distance() + 6.37212))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0251534 * (1 - exp(-1.41912 * (enclose() + 0.469311))) * pow(width() , -0.00020681 * enclose() + 0.0146787) + 0.5 * (0.00315978 * exp(-1.34061 * enclose()) + 0.0031695 * exp(-1.33692 * enclose())) * pow(width() , 0.0306147 * enclose() + -0.00823132)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0251534 * (1 - exp(-1.41912 * (enclose() + 0.469311))) * pow(width() , -0.00020681 * enclose() + 0.0146787) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,ctm
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.20023 - 1.30108 * distance()) + 0.0287316 / pow(distance() , 1.49792)) * 3.61904 * pow((width1() + width2()) / 2 , 0.0530892) * (0.0820064 * (thickness1() + thickness2()) / 2 + 0.0246194) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 ctm INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.07402 * (1 - exp(-0.727014 * (distance()))) * pow(width() , 0.0166792 * distance() + -0.047489) * (0.0900519 * thickness() + 0.21618)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0187033 * (1 - exp(-0.85309 * (distance() + -0.0557133)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.108964 * pow(width() , 0.0227727) * (0.091882 * thickness() + 0.123153)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00084251 * distance() + 0.0696593) * (0.539875 - exp(-(0.0588106 * distance() + 0.960997) * (enclose() + -0.0256081 * distance() + 1.06646) - (0.0494906 * distance() + -0.196491) * (enclose() + -0.321438 * distance() + 1.72431) * (enclose() + -0.321438 * distance() + 1.72431)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.07402 * (1 - exp(-0.727014 * (distance()))) * pow(width() , 0.0166792 * distance() + -0.047489) * (0.0900519 * thickness() + 0.21618)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0327234 * (0.539875 - exp(-1.25505 * (enclose() + 0.938419) - 0.050962 * (enclose() + 0.11712) * (enclose() + 0.11712)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,ctm
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107679 * (1 - exp(-0.772447 * (distance()))) * pow(width() , 0.0140037 * distance() + -0.040094) * (0.061886 * thickness() + 0.240568) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107679 * (1 - exp(-0.772447 * (distance()))) * pow(width() , 0.0140037 * distance() + -0.040094) * (0.061886 * thickness() + 0.240568) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0283985 * (1 - exp(-0.886414 * (distance() + -0.0471088))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.128774 * pow(width() , 0.0178712) * (0.0915616 * thickness() + 0.172006) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00084243 * distance() + 0.227398) * (0.232825 - exp(-(-0.0967526 * distance() + 0.150539) * (enclose() + 2.55733 * distance() + 4.01646) - (0.0196217 * distance() + 0.0301023) * (enclose() + 0.606639 * distance() + 4.69388) * (enclose() + 0.606639 * distance() + 4.69388))) + 0.5 * 0.858375 * exp(-1.4445 * enclose()) * (0.0991831 - 0.100868 * exp(-0.0248952 * distance()) * pow(distance() , 0.0134273))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00084243 * distance() + 0.227398) * (0.232825 - exp(-(-0.0967526 * distance() + 0.150539) * (enclose() + 2.55733 * distance() + 4.01646) - (0.0196217 * distance() + 0.0301023) * (enclose() + 0.606639 * distance() + 4.69388) * (enclose() + 0.606639 * distance() + 4.69388))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0283733 * (1 - exp(-1.28086 * (enclose() + 0.50662))) * pow(width() , -0.00100556 * enclose() + 0.0216491) + 0.5 * (0.00305836 * exp(-1.0408 * enclose()) + 0.00522708 * exp(-1.20135 * enclose())) * pow(width() , 0.0312457 * enclose() + -0.00132015)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0283733 * (1 - exp(-1.28086 * (enclose() + 0.50662))) * pow(width() , -0.00100556 * enclose() + 0.0216491) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.21051 - 1.09164 * distance()) + 0.0467475 / pow(distance() , 1.36728)) * 3.6341 * pow((width1() + width2()) / 2 , 0.0707488) * (0.0680773 * (thickness1() + thickness2()) / 2 + 0.0221005) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0553211 * (1 - exp(-0.58009 * (distance() + 0.052586))) * pow(width() , 0.0214296 * distance() + -0.0629298) * (0.0988573 * thickness() + 0.204955)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0134048 * (1 - exp(-0.699302 * (distance() + -0.0329739)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0964328 * pow(width() , 0.0367976) * (0.0817983 * thickness() + 0.0971973)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00093125 * distance() + 0.0409274) * (0.675001 - exp(-(0.137861 * distance() + 0.159494) * (enclose() + -0.0525105 * distance() + 1.61937) - (0.00349595 * distance() + -0.00225007) * (enclose() + 0.0243884 * distance() + 1.91524) * (enclose() + 0.0243884 * distance() + 1.91524)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0553211 * (1 - exp(-0.58009 * (distance() + 0.052586))) * pow(width() , 0.0214296 * distance() + -0.0629298) * (0.0988573 * thickness() + 0.204955)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0181356 * (0.675001 - exp(-0.848799 * (enclose() + 1.35682) - 0.0152297 * (enclose() + 2.03718) * (enclose() + 2.03718)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = field_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.11839 * (1 - exp(-0.651894 * (distance() + 0.0400987))) * pow(width() , 0.016816 * distance() + -0.0506917) * (0.0584776 * thickness() + 0.2494) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.11839 * (1 - exp(-0.651894 * (distance() + 0.0400987))) * pow(width() , 0.016816 * distance() + -0.0506917) * (0.0584776 * thickness() + 0.2494) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0318263 * (1 - exp(-0.751413 * (distance() + -0.0224969))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.136163 * pow(width() , 0.0267951) * (0.0848403 * thickness() + 0.191102) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0104445 * distance() + 0.669438) * (0.0824901 - exp(-(-0.134709 * distance() + 0.250719) * (enclose() + 1.91731 * distance() + 6.7339) - (0.0159773 * distance() + 0.0113877) * (enclose() + 0.602979 * distance() + 7.43808) * (enclose() + 0.602979 * distance() + 7.43808))) + 0.5 * 4.60077 * exp(-1.20056 * enclose()) * (0.100066 - 0.10031 * exp(-0.00475817 * distance()) * pow(distance() , 0.00265983))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0104445 * distance() + 0.669438) * (0.0824901 - exp(-(-0.134709 * distance() + 0.250719) * (enclose() + 1.91731 * distance() + 6.7339) - (0.0159773 * distance() + 0.0113877) * (enclose() + 0.602979 * distance() + 7.43808) * (enclose() + 0.602979 * distance() + 7.43808))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0318651 * (1 - exp(-1.14482 * (enclose() + 0.549856))) * pow(width() , -0.00170944 * enclose() + 0.0309833) + 0.5 * (0.00556864 * exp(-0.739238 * enclose()) + 0.00558856 * exp(-1.20015 * enclose())) * pow(width() , 0.032068 * enclose() + 0.0120912)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0318651 * (1 - exp(-1.14482 * (enclose() + 0.549856))) * pow(width() , -0.00170944 * enclose() + 0.0309833) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal5,ctm
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.34939 - 1.71392 * distance()) + 0.0295154 / pow(distance() , 1.54519)) * 3.38302 * pow((width1() + width2()) / 2 , 0.0804476) * (0.100983 * (thickness1() + thickness2()) / 2 + 0.00609343) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 ctm INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.255555 * (1 - exp(-1.94321 * (distance() + 0.324027))) * pow(width() , -0.0019252 * distance() + 0.00335603) * (0.046699 * thickness() + 0.390001)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.103719 * (1 - exp(-1.82924 * (distance() + 0.372996)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.245978 * pow(width() , 0.0115577) * (0.0981489 * thickness() + 0.404694)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00761041 * distance() + 0.115725) * (1.32184 - exp(-(-47.6918 * distance() + -32.3356) * (enclose() + 98.9498 * distance() + 93.3418) - (64.8028 * distance() + 52.9445) * (enclose() + -23.4471 * distance() + -100) * (enclose() + -23.4471 * distance() + -100)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.255555 * (1 - exp(-1.94321 * (distance() + 0.324027))) * pow(width() , -0.0019252 * distance() + 0.00335603) * (0.046699 * thickness() + 0.390001)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0768885 * (1.32184 - exp(270.795 * (enclose() + 588.091) - 376.959 * (enclose() + -217.236) * (enclose() + -217.236)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0358928 * (1 - exp(-1.13701 * (distance() + 0.0212529))) * pow(width() , 0.0138172 * distance() + -0.0221767) * (0.215208 * thickness() + 0.12022) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0358928 * (1 - exp(-1.13701 * (distance() + 0.0212529))) * pow(width() , 0.0138172 * distance() + -0.0221767) * (0.215208 * thickness() + 0.12022) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00803097 * (1 - exp(-1.33011 * (distance() + -0.0337701))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.07895 * pow(width() , 0.046597) * (0.133878 * thickness() + 0.0347587) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00090397 * distance() + 0.0322829) * (0.55709 - exp(-(0.371173 * distance() + 0.0546185) * (enclose() + -0.0298209 * distance() + 1.40384) - (-0.0254995 * distance() + 0.034512) * (enclose() + 0.189494 * distance() + 2.89674) * (enclose() + 0.189494 * distance() + 2.89674))) + 0.5 * 0.336732 * exp(-0.994888 * enclose()) * (0.100271 - 0.0998923 * exp(-0.0191869 * distance()) * pow(distance() , 0.0132922))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00090397 * distance() + 0.0322829) * (0.55709 - exp(-(0.371173 * distance() + 0.0546185) * (enclose() + -0.0298209 * distance() + 1.40384) - (-0.0254995 * distance() + 0.034512) * (enclose() + 0.189494 * distance() + 2.89674) * (enclose() + 0.189494 * distance() + 2.89674))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.008521 * (1 - exp(-0.817568 * (enclose() + 0.541478))) * pow(width() , 0.0135717 * enclose() + -0.00557136) + 0.5 * (0.001565 * exp(-0.962809 * enclose()) + 0.00233367 * exp(-0.963772 * enclose())) * pow(width() , 0.0119665 * enclose() + 0.122474)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.008521 * (1 - exp(-0.817568 * (enclose() + 0.541478))) * pow(width() , 0.0135717 * enclose() + -0.00557136) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.19023 - 1.05359 * distance()) + 0.0477122 / pow(distance() , 1.36434)) * 3.62906 * pow((width1() + width2()) / 2 , 0.0722722) * (0.0663512 * (thickness1() + thickness2()) / 2 + 0.022392) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.23))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.23)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.111192 * (1 - exp(-0.603944 * (distance() + 0.0449323))) * pow(width() , 0.0182105 * distance() + -0.0569207) * (0.0584008 * thickness() + 0.244735)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0292466 * (1 - exp(-0.706589 * (distance() + -0.0240204)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.130137 * pow(width() , 0.026805) * (0.0815845 * thickness() + 0.18281)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00079004 * distance() + 0.0544874) * (1.09927 - exp(-(-0.128086 * distance() + 2.36156) * (enclose() + -0.177211 * distance() + 1.75505) - (0.0305989 * distance() + -0.230163) * (enclose() + -0.228987 * distance() + 4.36629) * (enclose() + -0.228987 * distance() + 4.36629)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.111192 * (1 - exp(-0.603944 * (distance() + 0.0449323))) * pow(width() , 0.0182105 * distance() + -0.0569207) * (0.0584008 * thickness() + 0.244735)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0252686 * (1.09927 - exp(-1.72113 * (enclose() + 0.868995) - -0.0771685 * (enclose() + 3.22135) * (enclose() + 3.22135)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.061061 * (1 - exp(-0.548155 * (distance() + 0.0545509))) * pow(width() , 0.0219346 * distance() + -0.0671165) * (0.0913024 * thickness() + 0.20925) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.061061 * (1 - exp(-0.548155 * (distance() + 0.0545509))) * pow(width() , 0.0219346 * distance() + -0.0671165) * (0.0913024 * thickness() + 0.20925) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0147761 * (1 - exp(-0.665888 * (distance() + -0.0337468))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.10002 * pow(width() , 0.0346523) * (0.0803001 * thickness() + 0.105992) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00016409 * distance() + 0.0373986) * (0.78242 - exp(-(0.286189 * distance() + 0.212333) * (enclose() + 0.19301 * distance() + 0.908205) - (-0.0101533 * distance() + -0.0155198) * (enclose() + 0.745423 * distance() + 2.24735) * (enclose() + 0.745423 * distance() + 2.24735))) + 0.5 * 3.98734 * exp(-0.847038 * enclose()) * (0.100088 - 0.100106 * exp(-0.00291601 * distance()) * pow(distance() , 0.00190349))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00016409 * distance() + 0.0373986) * (0.78242 - exp(-(0.286189 * distance() + 0.212333) * (enclose() + 0.19301 * distance() + 0.908205) - (-0.0101533 * distance() + -0.0155198) * (enclose() + 0.745423 * distance() + 2.24735) * (enclose() + 0.745423 * distance() + 2.24735))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0151018 * (1 - exp(-0.689661 * (enclose() + 0.725883))) * pow(width() , 0.00538216 * enclose() + 0.0142293) + 0.5 * (0.00367741 * exp(-0.745057 * enclose()) + 0.00367744 * exp(-0.739699 * enclose())) * pow(width() , 0.0119943 * enclose() + 0.0506381)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0151018 * (1 - exp(-0.689661 * (enclose() + 0.725883))) * pow(width() , 0.00538216 * enclose() + 0.0142293) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.23))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.23)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,ctm,metal6
//
CAPACITANCE NEARBODY ctm INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.71948 - 0.90075 * distance()) + 0.0211939 / pow(distance() , 2.01756)) * 2.80371 * pow((width1() + width2()) / 2 , 0.00276749) * (0.174557 * (thickness1() + thickness2()) / 2 + 0.0502834) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.779))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.779)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0538706 * (1 - exp(-0.703866 * (distance()))) * pow(width() , -0.00597881 * distance() + -0.0691671) * (-4.51523 * thickness() + 1.54239)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0468193 * (1 - exp(-0.158775 * (distance() + 2.14322)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.153543 * pow(width() , 0) * (0.122441 * thickness() + 0.173105)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00513606 * distance() + 0.0536857) * (0.781645 - exp(-(0.0038628 * distance() + 1.13099) * (enclose() + 0.101497 * distance() + 0.508031) - (0.0824103 * distance() + -0.103129) * (enclose() + -0.694481 * distance() + 2.99899) * (enclose() + -0.694481 * distance() + 2.99899)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0538706 * (1 - exp(-0.703866 * (distance()))) * pow(width() , -0.00597881 * distance() + -0.0691671) * (-4.51523 * thickness() + 1.54239)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0376286 * (0.781645 - exp(-1.14721 * (enclose() + 0.934318) - 0.242994 * (enclose() + 0.0821698) * (enclose() + 0.0821698)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0668 * (1 - exp(-0.126102 * (distance() + 0.748763))) * pow(width() , -0.0572468 * distance() + 0.128012) * (0.301607 * thickness() + 0.295085) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0668 * (1 - exp(-0.126102 * (distance() + 0.748763))) * pow(width() , -0.0572468 * distance() + 0.128012) * (0.301607 * thickness() + 0.295085) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0238679 * (1 - exp(-0.0675395 * (distance() + 2.96268))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0898077 * pow(width() , 0.00026673) * (0.0842158 * thickness() + 0.0867392) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00125673 * distance() + 0.0396196) * (0.39558 - exp(-(-0.0238189 * distance() + 0.684321) * (enclose() + 0.242726 * distance() + 0.983738) - (0.0261155 * distance() + -0.0470268) * (enclose() + -0.616137 * distance() + 3.29796) * (enclose() + -0.616137 * distance() + 3.29796))) + 0.5 * 0.102067 * exp(-0.735913 * enclose()) * (0.0916989 - 0.109313 * exp(-0.190805 * distance()) * pow(distance() , 0.174836))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00125673 * distance() + 0.0396196) * (0.39558 - exp(-(-0.0238189 * distance() + 0.684321) * (enclose() + 0.242726 * distance() + 0.983738) - (0.0261155 * distance() + -0.0470268) * (enclose() + -0.616137 * distance() + 3.29796) * (enclose() + -0.616137 * distance() + 3.29796))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.779)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00965181 * (1 - exp(-0.64136 * (enclose() + 0.448848))) * pow(width() , -0.00407792 * enclose() + 0.00631662) + 0.5 * (0.00282963 * exp(-0.721808 * enclose()) + 0.00282709 * exp(-0.725144 * enclose())) * pow(width() , -0.00089575 * enclose() + 0.00436158)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 2.779)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00965181 * (1 - exp(-0.64136 * (enclose() + 0.448848))) * pow(width() , -0.00407792 * enclose() + 0.00631662) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 2.779))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 2.779)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal5,ctm
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.29276 - 2.4302 * distance()) + 0.00695829 / pow(distance() , 1.97071)) * 3.44207 * pow((width1() + width2()) / 2 , 0.0292144) * (0.160292 * (thickness1() + thickness2()) / 2 + 0.00196829) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 ctm INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.25003 * (1 - exp(-2.47193 * (distance() + 0.223068))) * pow(width() , -0.0004096 * distance() + 0.00054613) * (0.054892 * thickness() + 0.371172)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0973373 * (1 - exp(-2.53945 * (distance() + 0.236403)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.242531 * pow(width() , 0.00257284) * (0.100025 * thickness() + 0.37781)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00582243 * distance() + 0.114764) * (1.33545 - exp(-(-43.7987 * distance() + -44.7578) * (enclose() + 48.6351 * distance() + -57.956) - (8.49993 * distance() + 100) * (enclose() + 100 * distance() + -46.2103) * (enclose() + 100 * distance() + -46.2103)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.25003 * (1 - exp(-2.47193 * (distance() + 0.223068))) * pow(width() , -0.0004096 * distance() + 0.00054613) * (0.054892 * thickness() + 0.371172)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0719381 * (1.33545 - exp(263.751 * (enclose() + 185.219) - 142.5 * (enclose() + 453.79) * (enclose() + 453.79)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal5,ctm
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0729384 * (1 - exp(-1.4485 * (distance()))) * pow(width() , 0.0101771 * distance() + -0.0176317) * (0.165929 * thickness() + 0.143828) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0729384 * (1 - exp(-1.4485 * (distance()))) * pow(width() , 0.0101771 * distance() + -0.0176317) * (0.165929 * thickness() + 0.143828) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0162388 * (1 - exp(-1.84797 * (distance() + -0.0688968))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0992461 * pow(width() , 0.0140227) * (0.176442 * thickness() + 0.0710377) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00282705 * distance() + 0.188969) * (0.179092 - exp(-(-0.446491 * distance() + 0.309852) * (enclose() + 3.6397 * distance() + 3.34578) - (0.00257601 * distance() + 0.104716) * (enclose() + 3.48591 * distance() + 1.87726) * (enclose() + 3.48591 * distance() + 1.87726))) + 0.5 * 0.0336715 * exp(-2.15081 * enclose()) * (0.0862065 - 0.116586 * exp(-0.416556 * distance()) * pow(distance() , 0.197562))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00282705 * distance() + 0.188969) * (0.179092 - exp(-(-0.446491 * distance() + 0.309852) * (enclose() + 3.6397 * distance() + 3.34578) - (0.00257601 * distance() + 0.104716) * (enclose() + 3.48591 * distance() + 1.87726) * (enclose() + 3.48591 * distance() + 1.87726))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0164246 * (1 - exp(-1.87871 * (enclose() + 0.372756))) * pow(width() , 0.00148683 * enclose() + 0.011659) + 0.5 * (0.00124352 * exp(-1.9527 * enclose()) + 0.00109897 * exp(-1.95494 * enclose())) * pow(width() , 0.0369812 * enclose() + -0.00139616)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0164246 * (1 - exp(-1.87871 * (enclose() + 0.372756))) * pow(width() , 0.00148683 * enclose() + 0.011659) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.20583 - 1.47397 * distance()) + 0.0206169 / pow(distance() , 1.5901)) * 3.64343 * pow((width1() + width2()) / 2 , 0.0400741) * (0.0924507 * (thickness1() + thickness2()) / 2 + 0.0253222) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.85))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.85)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0989607 * (1 - exp(-0.806247 * (distance()))) * pow(width() , 0.0186386 * distance() + -0.0542119) * (-0.00577557 * thickness() + 0.248907)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0229923 * (1 - exp(-0.994648 * (distance() + -0.0612863)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.117042 * pow(width() , 0.0147603) * (0.0994802 * thickness() + 0.143125)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00090101 * distance() + 0.061698) * (0.77386 - exp(-(-0.00297643 * distance() + 1.67388) * (enclose() + -0.0732988 * distance() + 1.06351) - (0.0384096 * distance() + -0.232713) * (enclose() + -0.28915 * distance() + 2.71447) * (enclose() + -0.28915 * distance() + 2.71447)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0989607 * (1 - exp(-0.806247 * (distance()))) * pow(width() , 0.0186386 * distance() + -0.0542119) * (-0.00577557 * thickness() + 0.248907)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0285965 * (0.77386 - exp(-1.659 * (enclose() + 0.697016) - -0.040665 * (enclose() + 1.26872) * (enclose() + 1.26872)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = field_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.111181 * (1 - exp(-0.821603 * (distance()))) * pow(width() , 0.0178843 * distance() + -0.0524571) * (-0.016406 * thickness() + 0.256178) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.111181 * (1 - exp(-0.821603 * (distance()))) * pow(width() , 0.0178843 * distance() + -0.0524571) * (-0.016406 * thickness() + 0.256178) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0260697 * (1 - exp(-1.00476 * (distance() + -0.0588946))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123505 * pow(width() , 0.0137601) * (0.10058 * thickness() + 0.157094) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0003349 * distance() + 0.198902) * (0.253512 - exp(-(-0.0326152 * distance() + 0.0255775) * (enclose() + 8.7893 * distance() + 6.91731) - (0.01175 * distance() + 0.0444779) * (enclose() + 0.924103 * distance() + 4.72094) * (enclose() + 0.924103 * distance() + 4.72094))) + 0.5 * 0.203117 * exp(-1.61801 * enclose()) * (0.0971373 - 0.103011 * exp(-0.0815251 * distance()) * pow(distance() , 0.0422812))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0003349 * distance() + 0.198902) * (0.253512 - exp(-(-0.0326152 * distance() + 0.0255775) * (enclose() + 8.7893 * distance() + 6.91731) - (0.01175 * distance() + 0.0444779) * (enclose() + 0.924103 * distance() + 4.72094) * (enclose() + 0.924103 * distance() + 4.72094))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0260414 * (1 - exp(-1.39301 * (enclose() + 0.488088))) * pow(width() , -0.00061667 * enclose() + 0.0170595) + 0.5 * (0.00272939 * exp(-1.29059 * enclose()) + 0.00272937 * exp(-1.2906 * enclose())) * pow(width() , 0.0338358 * enclose() + -0.0117007)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0260414 * (1 - exp(-1.39301 * (enclose() + 0.488088))) * pow(width() , -0.00061667 * enclose() + 0.0170595) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.85))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.85)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,ctm,metal6
//
CAPACITANCE NEARBODY ctm INSIDE OF metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.5954 - 1.31941 * distance()) + 0.00671653 / pow(distance() , 2.75765)) * 2.70697 * pow((width1() + width2()) / 2 , 1.8772e-05) * (0.274817 * (thickness1() + thickness2()) / 2 + 0.0419671) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 1.399))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 1.399)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.051423 * (1 - exp(-0.428133 * (distance() + 0.491543))) * pow(width() , -0.037541 * distance() + -0.012631) * (-5.31718 * thickness() + 1.74109)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0473799 * (1 - exp(-0.0818273 * (distance() + 4.86729)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.137117 * pow(width() , 0) * (0.130505 * thickness() + 0.152905)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-7.26566e-05 * distance() + 0.060281) * (0.805671 - exp(-(-0.10347 * distance() + 1.90785) * (enclose() + 0.103099 * distance() + 0.299368) - (0.142257 * distance() + -0.597511) * (enclose() + -0.323304 * distance() + 1.00251) * (enclose() + -0.323304 * distance() + 1.00251)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.051423 * (1 - exp(-0.428133 * (distance() + 0.491543))) * pow(width() , -0.037541 * distance() + -0.012631) * (-5.31718 * thickness() + 1.74109)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0299879 * (0.805671 - exp(-1.47328 * (enclose() + 0.732384) - -3.16e-05 * (enclose() + -0.355367) * (enclose() + -0.355367)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0659198 * (1 - exp(-0.941452 * (distance()))) * pow(width() , -0.00909354 * distance() + 0.0151556) * (0.224135 * thickness() + 0.184238) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0659198 * (1 - exp(-0.941452 * (distance()))) * pow(width() , -0.00909354 * distance() + 0.0151556) * (0.224135 * thickness() + 0.184238) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0313032 * (1 - exp(-0.0667021 * (distance() + 5.05843))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.106037 * pow(width() , -0.00110717) * (0.134983 * thickness() + 0.107236) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00060792 * distance() + 0.0618469) * (0.484813 - exp(-(0.367537 * distance() + 0.568006) * (enclose() + 0.00803352 * distance() + 1.00787) - (-0.0218267 * distance() + -0.0617802) * (enclose() + 0.442499 * distance() + 0.639299) * (enclose() + 0.442499 * distance() + 0.639299))) + 0.5 * 0.0468371 * exp(-1.29996 * enclose()) * (0.079694 - 0.127609 * exp(-0.509183 * distance()) * pow(distance() , 0.423259))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00060792 * distance() + 0.0618469) * (0.484813 - exp(-(0.367537 * distance() + 0.568006) * (enclose() + 0.00803352 * distance() + 1.00787) - (-0.0218267 * distance() + -0.0617802) * (enclose() + 0.442499 * distance() + 0.639299) * (enclose() + 0.442499 * distance() + 0.639299))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 1.399)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0143865 * (1 - exp(-1.11799 * (enclose() + 0.513006))) * pow(width() , -0.00264162 * enclose() + 0.00113225) + 0.5 * (0.00333826 * exp(-1.17506 * enclose()) + 0.00030054 * exp(-1.21019 * enclose())) * pow(width() , -0.00214587 * enclose() + 0.00068745)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 1.399)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0143865 * (1 - exp(-1.11799 * (enclose() + 0.513006))) * pow(width() , -0.00264162 * enclose() + 0.00113225) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 1.399))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 1.399)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal5,ctm,metal6
//
CAPACITANCE NEARBODY ctm INSIDE OF metal5 metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.69208 - 3.06123 * distance()) + 0.00119087 / pow(distance() , 4.21379)) * 2.14681 * pow((width1() + width2()) / 2 , -1.31039e-05) * (0.910744 * (thickness1() + thickness2()) / 2 + -0.0911397) * m33NB_COEF * (1 - m33RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.019))) * (1 - m33RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.019)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal5,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0242959 * (1 - exp(-0.0526568 * (distance() + 9.85468))) * pow(width() , -0.0296006 * distance() + -0.1559) * (-14.6574 * thickness() + 3.89297)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0135758 * (1 - exp(-0.0299776 * (distance() + 32.1912)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0888118 * pow(width() , 0) * (0.090204 * thickness() + 0.0849305)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00044674 * distance() + 0.0352553) * (0.540772 - exp(-(-0.0287954 * distance() + 0.49207) * (enclose() + 1.77488 * distance() + 3.79099) - (0.0451113 * distance() + -0.65371) * (enclose() + -0.271298 * distance() + -1.35729) * (enclose() + -0.271298 * distance() + -1.35729)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0242959 * (1 - exp(-0.0526568 * (distance() + 9.85468))) * pow(width() , -0.0296006 * distance() + -0.1559) * (-14.6574 * thickness() + 3.89297)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0166895 * (0.540772 - exp(-0.371129 * (enclose() + 11.2455) - -0.464243 * (enclose() + -2.49674) * (enclose() + -2.49674)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal5,ctm,metal6
//
CAPACITANCE CROSSOVER FRINGE ctm metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_calenclose = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = field_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.1 * (1 - exp(-0.1 * (distance() + 0.1))) * pow(width() , -0.211911 * distance() + 0.396131) * (10.947 * thickness() + 2.94921) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.1 * (1 - exp(-0.1 * (distance() + 0.1))) * pow(width() , -0.211911 * distance() + 0.396131) * (10.947 * thickness() + 2.94921) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0984817 * (1 - exp(-0.163338 * (distance() + 9.89731))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.186058 * pow(width() , 0.0670003) * (0.835533 * thickness() + 0.222581) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00152803 * distance() + 0.102498) * (1.46529 - exp(-(-12.1712 * distance() + -5.29235) * (enclose() + -100 * distance() + -95.8012) - (18.7471 * distance() + 74.6686) * (enclose() + -30.6733 * distance() + 100) * (enclose() + -30.6733 * distance() + 100))) + 0.5 * 0.130383 * exp(-5.01666 * enclose()) * (0.101151 - 0.0987817 * exp(0.00577039 * distance()) * pow(distance() , -0.0884081))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00152803 * distance() + 0.102498) * (1.46529 - exp(-(-12.1712 * distance() + -5.29235) * (enclose() + -100 * distance() + -95.8012) - (18.7471 * distance() + 74.6686) * (enclose() + -30.6733 * distance() + 100) * (enclose() + -30.6733 * distance() + 100))) * m33IN_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.019)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.131993 * (1 - exp(-0.146409 * (enclose() + 3.53583))) * pow(width() , -0.0493925 * enclose() + 0.18071) + 0.5 * (0.0007435 * exp(-8.14542 * enclose()) + 0.00103305 * exp(-3.16548 * enclose())) * pow(width() , 0.00415981 * enclose() + -0.00314874)) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 0.019)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.131993 * (1 - exp(-0.146409 * (enclose() + 3.53583))) * pow(width() , -0.0493925 * enclose() + 0.18071) * m33IN0_COEF * (1 - m33RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 0.019))) * (1 - m33RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 0.019)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0521924 * exp(-0.815561 * distance()) + 0.00196012) * pow((width1() + width2()) / 2 , 0.293309) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0521924 * exp(-0.815561 * distance()) + 0.00196012) * pow((width1() + width2()) / 2 , 0.293309)
    }
]
CAPACITANCE NEARBODY metal1 poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0521924 * exp(-0.815561 * distance()) + 0.00196012) * pow((width1() + width2()) / 2 , 0.293309) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0521924 * exp(-0.815561 * distance()) + 0.00196012) * pow((width1() + width2()) / 2 , 0.293309)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0102124 * exp(-0.273011 * distance()) + 0.0102123 * exp(-0.273066 * distance())) * pow((width1() + width2()) / 2 , 0.365902) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0102124 * exp(-0.273011 * distance()) + 0.0102123 * exp(-0.273066 * distance())) * pow((width1() + width2()) / 2 , 0.365902)
    }
]
CAPACITANCE NEARBODY metal2 poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0102124 * exp(-0.273011 * distance()) + 0.0102123 * exp(-0.273066 * distance())) * pow((width1() + width2()) / 2 , 0.365902) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0102124 * exp(-0.273011 * distance()) + 0.0102123 * exp(-0.273066 * distance())) * pow((width1() + width2()) / 2 , 0.365902)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00602718 * exp(-0.153388 * distance()) + 0.00603058 * exp(-0.153445 * distance())) * pow((width1() + width2()) / 2 , 0.376733) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00602718 * exp(-0.153388 * distance()) + 0.00603058 * exp(-0.153445 * distance())) * pow((width1() + width2()) / 2 , 0.376733)
    }
]
CAPACITANCE NEARBODY metal3 poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00602718 * exp(-0.153388 * distance()) + 0.00603058 * exp(-0.153445 * distance())) * pow((width1() + width2()) / 2 , 0.376733) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00602718 * exp(-0.153388 * distance()) + 0.00603058 * exp(-0.153445 * distance())) * pow((width1() + width2()) / 2 , 0.376733)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00419367 * exp(-0.0926397 * distance()) + 0.00419369 * exp(-0.0936006 * distance())) * pow((width1() + width2()) / 2 , 0.377606) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00419367 * exp(-0.0926397 * distance()) + 0.00419369 * exp(-0.0936006 * distance())) * pow((width1() + width2()) / 2 , 0.377606)
    }
]
CAPACITANCE NEARBODY metal4 poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00419367 * exp(-0.0926397 * distance()) + 0.00419369 * exp(-0.0936006 * distance())) * pow((width1() + width2()) / 2 , 0.377606) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00419367 * exp(-0.0926397 * distance()) + 0.00419369 * exp(-0.0936006 * distance())) * pow((width1() + width2()) / 2 , 0.377606)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00320062 * exp(-0.0571764 * distance()) + 0.00320057 * exp(-0.0571155 * distance())) * pow((width1() + width2()) / 2 , 0.375996) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00320062 * exp(-0.0571764 * distance()) + 0.00320057 * exp(-0.0571155 * distance())) * pow((width1() + width2()) / 2 , 0.375996)
    }
]
CAPACITANCE NEARBODY metal5 poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00320062 * exp(-0.0571764 * distance()) + 0.00320057 * exp(-0.0571155 * distance())) * pow((width1() + width2()) / 2 , 0.375996) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00320062 * exp(-0.0571764 * distance()) + 0.00320057 * exp(-0.0571155 * distance())) * pow((width1() + width2()) / 2 , 0.375996)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,ctm
//
CAPACITANCE NEARBODY poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00276997 * exp(-0.0476457 * distance()) + 0.00276996 * exp(-0.0486701 * distance())) * pow((width1() + width2()) / 2 , 0.406291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00276997 * exp(-0.0476457 * distance()) + 0.00276996 * exp(-0.0486701 * distance())) * pow((width1() + width2()) / 2 , 0.406291)
    }
]
CAPACITANCE NEARBODY ctm poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00276997 * exp(-0.0476457 * distance()) + 0.00276996 * exp(-0.0486701 * distance())) * pow((width1() + width2()) / 2 , 0.406291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00276997 * exp(-0.0476457 * distance()) + 0.00276996 * exp(-0.0486701 * distance())) * pow((width1() + width2()) / 2 , 0.406291)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00286984 * exp(-0.0252707 * distance()) + 0.00288697 * exp(-0.026149 * distance())) * pow((width1() + width2()) / 2 , 0.331612) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00286984 * exp(-0.0252707 * distance()) + 0.00288697 * exp(-0.026149 * distance())) * pow((width1() + width2()) / 2 , 0.331612)
    }
]
CAPACITANCE NEARBODY metal6 poly MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00286984 * exp(-0.0252707 * distance()) + 0.00288697 * exp(-0.026149 * distance())) * pow((width1() + width2()) / 2 , 0.331612) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00286984 * exp(-0.0252707 * distance()) + 0.00288697 * exp(-0.026149 * distance())) * pow((width1() + width2()) / 2 , 0.331612)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0259753 * exp(-0.239732 * distance()) + 0.0261114 * exp(-0.651538 * distance())) * pow((width1() + width2()) / 2 , 0.258723) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0259753 * exp(-0.239732 * distance()) + 0.0261114 * exp(-0.651538 * distance())) * pow((width1() + width2()) / 2 , 0.258723)
    }
]
CAPACITANCE NEARBODY metal2 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0259753 * exp(-0.239732 * distance()) + 0.0261114 * exp(-0.651538 * distance())) * pow((width1() + width2()) / 2 , 0.258723) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0259753 * exp(-0.239732 * distance()) + 0.0261114 * exp(-0.651538 * distance())) * pow((width1() + width2()) / 2 , 0.258723)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133175 * exp(-0.182918 * distance()) + 0.0133175 * exp(-0.182917 * distance())) * pow((width1() + width2()) / 2 , 0.294828) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133175 * exp(-0.182918 * distance()) + 0.0133175 * exp(-0.182917 * distance())) * pow((width1() + width2()) / 2 , 0.294828)
    }
]
CAPACITANCE NEARBODY metal3 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133175 * exp(-0.182918 * distance()) + 0.0133175 * exp(-0.182917 * distance())) * pow((width1() + width2()) / 2 , 0.294828) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133175 * exp(-0.182918 * distance()) + 0.0133175 * exp(-0.182917 * distance())) * pow((width1() + width2()) / 2 , 0.294828)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00891406 * exp(-0.106303 * distance()) + 0.00891406 * exp(-0.108285 * distance())) * pow((width1() + width2()) / 2 , 0.300207) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00891406 * exp(-0.106303 * distance()) + 0.00891406 * exp(-0.108285 * distance())) * pow((width1() + width2()) / 2 , 0.300207)
    }
]
CAPACITANCE NEARBODY metal4 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00891406 * exp(-0.106303 * distance()) + 0.00891406 * exp(-0.108285 * distance())) * pow((width1() + width2()) / 2 , 0.300207) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00891406 * exp(-0.106303 * distance()) + 0.00891406 * exp(-0.108285 * distance())) * pow((width1() + width2()) / 2 , 0.300207)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00668776 * exp(-0.0654992 * distance()) + 0.00668776 * exp(-0.065513 * distance())) * pow((width1() + width2()) / 2 , 0.29929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00668776 * exp(-0.0654992 * distance()) + 0.00668776 * exp(-0.065513 * distance())) * pow((width1() + width2()) / 2 , 0.29929)
    }
]
CAPACITANCE NEARBODY metal5 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00668776 * exp(-0.0654992 * distance()) + 0.00668776 * exp(-0.065513 * distance())) * pow((width1() + width2()) / 2 , 0.29929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00668776 * exp(-0.0654992 * distance()) + 0.00668776 * exp(-0.065513 * distance())) * pow((width1() + width2()) / 2 , 0.29929)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,ctm
//
CAPACITANCE NEARBODY metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00576246 * exp(-0.0559535 * distance()) + 0.00576249 * exp(-0.05592 * distance())) * pow((width1() + width2()) / 2 , 0.331508) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00576246 * exp(-0.0559535 * distance()) + 0.00576249 * exp(-0.05592 * distance())) * pow((width1() + width2()) / 2 , 0.331508)
    }
]
CAPACITANCE NEARBODY ctm metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00576246 * exp(-0.0559535 * distance()) + 0.00576249 * exp(-0.05592 * distance())) * pow((width1() + width2()) / 2 , 0.331508) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00576246 * exp(-0.0559535 * distance()) + 0.00576249 * exp(-0.05592 * distance())) * pow((width1() + width2()) / 2 , 0.331508)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00592503 * exp(-0.0301719 * distance()) + 0.00592514 * exp(-0.0311877 * distance())) * pow((width1() + width2()) / 2 , 0.251948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00592503 * exp(-0.0301719 * distance()) + 0.00592514 * exp(-0.0311877 * distance())) * pow((width1() + width2()) / 2 , 0.251948)
    }
]
CAPACITANCE NEARBODY metal6 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00592503 * exp(-0.0301719 * distance()) + 0.00592514 * exp(-0.0311877 * distance())) * pow((width1() + width2()) / 2 , 0.251948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00592503 * exp(-0.0301719 * distance()) + 0.00592514 * exp(-0.0311877 * distance())) * pow((width1() + width2()) / 2 , 0.251948)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00980737 + 0.0482682 * exp(-0.465328 * distance())) * pow((width1() + width2()) / 2 , 0.249895) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00980737 + 0.0482682 * exp(-0.465328 * distance())) * pow((width1() + width2()) / 2 , 0.249895)
    }
]
CAPACITANCE NEARBODY metal3 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00980737 + 0.0482682 * exp(-0.465328 * distance())) * pow((width1() + width2()) / 2 , 0.249895) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00980737 + 0.0482682 * exp(-0.465328 * distance())) * pow((width1() + width2()) / 2 , 0.249895)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159297 * exp(-0.147951 * distance()) + 0.0159297 * exp(-0.147732 * distance())) * pow((width1() + width2()) / 2 , 0.281279) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159297 * exp(-0.147951 * distance()) + 0.0159297 * exp(-0.147732 * distance())) * pow((width1() + width2()) / 2 , 0.281279)
    }
]
CAPACITANCE NEARBODY metal4 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159297 * exp(-0.147951 * distance()) + 0.0159297 * exp(-0.147732 * distance())) * pow((width1() + width2()) / 2 , 0.281279) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159297 * exp(-0.147951 * distance()) + 0.0159297 * exp(-0.147732 * distance())) * pow((width1() + width2()) / 2 , 0.281279)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0112262 * exp(-0.0856366 * distance()) + 0.0112262 * exp(-0.0857788 * distance())) * pow((width1() + width2()) / 2 , 0.284187) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0112262 * exp(-0.0856366 * distance()) + 0.0112262 * exp(-0.0857788 * distance())) * pow((width1() + width2()) / 2 , 0.284187)
    }
]
CAPACITANCE NEARBODY metal5 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0112262 * exp(-0.0856366 * distance()) + 0.0112262 * exp(-0.0857788 * distance())) * pow((width1() + width2()) / 2 , 0.284187) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0112262 * exp(-0.0856366 * distance()) + 0.0112262 * exp(-0.0857788 * distance())) * pow((width1() + width2()) / 2 , 0.284187)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,ctm
//
CAPACITANCE NEARBODY metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00951596 * exp(-0.073981 * distance()) + 0.00951598 * exp(-0.0719102 * distance())) * pow((width1() + width2()) / 2 , 0.316412) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00951596 * exp(-0.073981 * distance()) + 0.00951598 * exp(-0.0719102 * distance())) * pow((width1() + width2()) / 2 , 0.316412)
    }
]
CAPACITANCE NEARBODY ctm metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00951596 * exp(-0.073981 * distance()) + 0.00951598 * exp(-0.0719102 * distance())) * pow((width1() + width2()) / 2 , 0.316412) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00951596 * exp(-0.073981 * distance()) + 0.00951598 * exp(-0.0719102 * distance())) * pow((width1() + width2()) / 2 , 0.316412)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0096143 * exp(-0.0404247 * distance()) + 0.0096143 * exp(-0.0404247 * distance())) * pow((width1() + width2()) / 2 , 0.234867) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0096143 * exp(-0.0404247 * distance()) + 0.0096143 * exp(-0.0404247 * distance())) * pow((width1() + width2()) / 2 , 0.234867)
    }
]
CAPACITANCE NEARBODY metal6 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0096143 * exp(-0.0404247 * distance()) + 0.0096143 * exp(-0.0404247 * distance())) * pow((width1() + width2()) / 2 , 0.234867) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0096143 * exp(-0.0404247 * distance()) + 0.0096143 * exp(-0.0404247 * distance())) * pow((width1() + width2()) / 2 , 0.234867)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0475655 * exp(-0.461931 * distance()) + 0.0136495) * pow((width1() + width2()) / 2 , 0.245183) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0475655 * exp(-0.461931 * distance()) + 0.0136495) * pow((width1() + width2()) / 2 , 0.245183)
    }
]
CAPACITANCE NEARBODY metal4 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0475655 * exp(-0.461931 * distance()) + 0.0136495) * pow((width1() + width2()) / 2 , 0.245183) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0475655 * exp(-0.461931 * distance()) + 0.0136495) * pow((width1() + width2()) / 2 , 0.245183)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174111 * exp(-0.125618 * distance()) + 0.017411 * exp(-0.125476 * distance())) * pow((width1() + width2()) / 2 , 0.267742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174111 * exp(-0.125618 * distance()) + 0.017411 * exp(-0.125476 * distance())) * pow((width1() + width2()) / 2 , 0.267742)
    }
]
CAPACITANCE NEARBODY metal5 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174111 * exp(-0.125618 * distance()) + 0.017411 * exp(-0.125476 * distance())) * pow((width1() + width2()) / 2 , 0.267742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174111 * exp(-0.125618 * distance()) + 0.017411 * exp(-0.125476 * distance())) * pow((width1() + width2()) / 2 , 0.267742)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,ctm
//
CAPACITANCE NEARBODY metal3 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014242 * exp(-0.104642 * distance()) + 0.0142419 * exp(-0.104642 * distance())) * pow((width1() + width2()) / 2 , 0.301593) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014242 * exp(-0.104642 * distance()) + 0.0142419 * exp(-0.104642 * distance())) * pow((width1() + width2()) / 2 , 0.301593)
    }
]
CAPACITANCE NEARBODY ctm metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014242 * exp(-0.104642 * distance()) + 0.0142419 * exp(-0.104642 * distance())) * pow((width1() + width2()) / 2 , 0.301593) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014242 * exp(-0.104642 * distance()) + 0.0142419 * exp(-0.104642 * distance())) * pow((width1() + width2()) / 2 , 0.301593)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138622 * exp(-0.0563934 * distance()) + 0.0138622 * exp(-0.0572719 * distance())) * pow((width1() + width2()) / 2 , 0.219915) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138622 * exp(-0.0563934 * distance()) + 0.0138622 * exp(-0.0572719 * distance())) * pow((width1() + width2()) / 2 , 0.219915)
    }
]
CAPACITANCE NEARBODY metal6 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138622 * exp(-0.0563934 * distance()) + 0.0138622 * exp(-0.0572719 * distance())) * pow((width1() + width2()) / 2 , 0.219915) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138622 * exp(-0.0563934 * distance()) + 0.0138622 * exp(-0.0572719 * distance())) * pow((width1() + width2()) / 2 , 0.219915)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167945 + 0.0466486 * exp(-0.464693 * distance())) * pow((width1() + width2()) / 2 , 0.242866) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167945 + 0.0466486 * exp(-0.464693 * distance())) * pow((width1() + width2()) / 2 , 0.242866)
    }
]
CAPACITANCE NEARBODY metal5 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167945 + 0.0466486 * exp(-0.464693 * distance())) * pow((width1() + width2()) / 2 , 0.242866) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167945 + 0.0466486 * exp(-0.464693 * distance())) * pow((width1() + width2()) / 2 , 0.242866)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal4,ctm
//
CAPACITANCE NEARBODY metal4 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.022687 * exp(-0.181532 * distance()) + 0.022687 * exp(-0.181532 * distance())) * pow((width1() + width2()) / 2 , 0.289123) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.022687 * exp(-0.181532 * distance()) + 0.022687 * exp(-0.181532 * distance())) * pow((width1() + width2()) / 2 , 0.289123)
    }
]
CAPACITANCE NEARBODY ctm metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.022687 * exp(-0.181532 * distance()) + 0.022687 * exp(-0.181532 * distance())) * pow((width1() + width2()) / 2 , 0.289123) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.022687 * exp(-0.181532 * distance()) + 0.022687 * exp(-0.181532 * distance())) * pow((width1() + width2()) / 2 , 0.289123)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.020113 * exp(-0.0903195 * distance()) + 0.020113 * exp(-0.0896249 * distance())) * pow((width1() + width2()) / 2 , 0.21589) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.020113 * exp(-0.0903195 * distance()) + 0.020113 * exp(-0.0896249 * distance())) * pow((width1() + width2()) / 2 , 0.21589)
    }
]
CAPACITANCE NEARBODY metal6 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.020113 * exp(-0.0903195 * distance()) + 0.020113 * exp(-0.0896249 * distance())) * pow((width1() + width2()) / 2 , 0.21589) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.020113 * exp(-0.0903195 * distance()) + 0.020113 * exp(-0.0896249 * distance())) * pow((width1() + width2()) / 2 , 0.21589)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal5,ctm
//
CAPACITANCE NEARBODY metal5 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.13928 * exp(-9.39085 * distance()) + 0.0776806 * exp(-0.383192 * distance())) * pow((width1() + width2()) / 2 , 0.113905) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.13928 * exp(-9.39085 * distance()) + 0.0776806 * exp(-0.383192 * distance())) * pow((width1() + width2()) / 2 , 0.113905)
    }
]
CAPACITANCE NEARBODY ctm metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.13928 * exp(-9.39085 * distance()) + 0.0776806 * exp(-0.383192 * distance())) * pow((width1() + width2()) / 2 , 0.113905) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.13928 * exp(-9.39085 * distance()) + 0.0776806 * exp(-0.383192 * distance())) * pow((width1() + width2()) / 2 , 0.113905)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334828 * exp(-0.177559 * distance()) + 0.0334827 * exp(-0.177363 * distance())) * pow((width1() + width2()) / 2 , 0.212732) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334828 * exp(-0.177559 * distance()) + 0.0334827 * exp(-0.177363 * distance())) * pow((width1() + width2()) / 2 , 0.212732)
    }
]
CAPACITANCE NEARBODY metal6 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334828 * exp(-0.177559 * distance()) + 0.0334827 * exp(-0.177363 * distance())) * pow((width1() + width2()) / 2 , 0.212732) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334828 * exp(-0.177559 * distance()) + 0.0334827 * exp(-0.177363 * distance())) * pow((width1() + width2()) / 2 , 0.212732)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0456329 * exp(-0.702277 * distance()) + 0.0445448 * exp(-0.0684949 * distance())) * pow((width1() + width2()) / 2 , 0.0761769) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0456329 * exp(-0.702277 * distance()) + 0.0445448 * exp(-0.0684949 * distance())) * pow((width1() + width2()) / 2 , 0.0761769)
    }
]
CAPACITANCE NEARBODY metal6 ctm MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0456329 * exp(-0.702277 * distance()) + 0.0445448 * exp(-0.0684949 * distance())) * pow((width1() + width2()) / 2 , 0.0761769) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0456329 * exp(-0.702277 * distance()) + 0.0445448 * exp(-0.0684949 * distance())) * pow((width1() + width2()) / 2 , 0.0761769)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249274 * exp(-1.00757 * distance()) + 0.0249274 * exp(-1.00935 * distance())) * pow((width1() + width2()) / 2 , 0.278469) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249274 * exp(-1.00757 * distance()) + 0.0249274 * exp(-1.00935 * distance())) * pow((width1() + width2()) / 2 , 0.278469)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249274 * exp(-1.00757 * distance()) + 0.0249274 * exp(-1.00935 * distance())) * pow((width1() + width2()) / 2 , 0.278469) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249274 * exp(-1.00757 * distance()) + 0.0249274 * exp(-1.00935 * distance())) * pow((width1() + width2()) / 2 , 0.278469)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0261619 * exp(-0.869959 * distance()) + 0.0261617 * exp(-0.868918 * distance())) * pow((width1() + width2()) / 2 , 0.28516) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0261619 * exp(-0.869959 * distance()) + 0.0261617 * exp(-0.868918 * distance())) * pow((width1() + width2()) / 2 , 0.28516)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0261619 * exp(-0.869959 * distance()) + 0.0261617 * exp(-0.868918 * distance())) * pow((width1() + width2()) / 2 , 0.28516) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0261619 * exp(-0.869959 * distance()) + 0.0261617 * exp(-0.868918 * distance())) * pow((width1() + width2()) / 2 , 0.28516)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0265197 * exp(-0.820971 * distance()) + 0.0265194 * exp(-0.821129 * distance())) * pow((width1() + width2()) / 2 , 0.288203) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0265197 * exp(-0.820971 * distance()) + 0.0265194 * exp(-0.821129 * distance())) * pow((width1() + width2()) / 2 , 0.288203)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0265197 * exp(-0.820971 * distance()) + 0.0265194 * exp(-0.821129 * distance())) * pow((width1() + width2()) / 2 , 0.288203) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0265197 * exp(-0.820971 * distance()) + 0.0265194 * exp(-0.821129 * distance())) * pow((width1() + width2()) / 2 , 0.288203)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0267275 * exp(-0.799714 * distance()) + 0.0266106 * exp(-0.796806 * distance())) * pow((width1() + width2()) / 2 , 0.289705) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0267275 * exp(-0.799714 * distance()) + 0.0266106 * exp(-0.796806 * distance())) * pow((width1() + width2()) / 2 , 0.289705)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0267275 * exp(-0.799714 * distance()) + 0.0266106 * exp(-0.796806 * distance())) * pow((width1() + width2()) / 2 , 0.289705) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0267275 * exp(-0.799714 * distance()) + 0.0266106 * exp(-0.796806 * distance())) * pow((width1() + width2()) / 2 , 0.289705)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,ctm
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0267313 * exp(-0.823634 * distance()) + 0.0266792 * exp(-0.761904 * distance())) * pow((width1() + width2()) / 2 , 0.290055) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0267313 * exp(-0.823634 * distance()) + 0.0266792 * exp(-0.761904 * distance())) * pow((width1() + width2()) / 2 , 0.290055)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0267313 * exp(-0.823634 * distance()) + 0.0266792 * exp(-0.761904 * distance())) * pow((width1() + width2()) / 2 , 0.290055) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0267313 * exp(-0.823634 * distance()) + 0.0266792 * exp(-0.761904 * distance())) * pow((width1() + width2()) / 2 , 0.290055)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000987372 + 0.0526078 * exp(-0.818038 * distance())) * pow((width1() + width2()) / 2 , 0.290236) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000987372 + 0.0526078 * exp(-0.818038 * distance())) * pow((width1() + width2()) / 2 , 0.290236)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000987372 + 0.0526078 * exp(-0.818038 * distance())) * pow((width1() + width2()) / 2 , 0.290236) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000987372 + 0.0526078 * exp(-0.818038 * distance())) * pow((width1() + width2()) / 2 , 0.290236)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00849225 * exp(-0.448499 * distance()) + 0.00849136 * exp(-0.447257 * distance())) * pow((width1() + width2()) / 2 , 0.387088) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00849225 * exp(-0.448499 * distance()) + 0.00849136 * exp(-0.447257 * distance())) * pow((width1() + width2()) / 2 , 0.387088)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00849225 * exp(-0.448499 * distance()) + 0.00849136 * exp(-0.447257 * distance())) * pow((width1() + width2()) / 2 , 0.387088) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00849225 * exp(-0.448499 * distance()) + 0.00849136 * exp(-0.447257 * distance())) * pow((width1() + width2()) / 2 , 0.387088)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00938856 * exp(-0.377238 * distance()) + 0.00938772 * exp(-0.37746 * distance())) * pow((width1() + width2()) / 2 , 0.37228) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00938856 * exp(-0.377238 * distance()) + 0.00938772 * exp(-0.37746 * distance())) * pow((width1() + width2()) / 2 , 0.37228)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00938856 * exp(-0.377238 * distance()) + 0.00938772 * exp(-0.37746 * distance())) * pow((width1() + width2()) / 2 , 0.37228) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00938856 * exp(-0.377238 * distance()) + 0.00938772 * exp(-0.37746 * distance())) * pow((width1() + width2()) / 2 , 0.37228)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0097182 * exp(-0.344159 * distance()) + 0.00971801 * exp(-0.344204 * distance())) * pow((width1() + width2()) / 2 , 0.369041) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0097182 * exp(-0.344159 * distance()) + 0.00971801 * exp(-0.344204 * distance())) * pow((width1() + width2()) / 2 , 0.369041)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0097182 * exp(-0.344159 * distance()) + 0.00971801 * exp(-0.344204 * distance())) * pow((width1() + width2()) / 2 , 0.369041) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0097182 * exp(-0.344159 * distance()) + 0.00971801 * exp(-0.344204 * distance())) * pow((width1() + width2()) / 2 , 0.369041)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,ctm
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00979497 * exp(-0.335424 * distance()) + 0.00979441 * exp(-0.335399 * distance())) * pow((width1() + width2()) / 2 , 0.368451) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00979497 * exp(-0.335424 * distance()) + 0.00979441 * exp(-0.335399 * distance())) * pow((width1() + width2()) / 2 , 0.368451)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00979497 * exp(-0.335424 * distance()) + 0.00979441 * exp(-0.335399 * distance())) * pow((width1() + width2()) / 2 , 0.368451) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00979497 * exp(-0.335424 * distance()) + 0.00979441 * exp(-0.335399 * distance())) * pow((width1() + width2()) / 2 , 0.368451)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00988745 * exp(-0.323996 * distance()) + 0.00988714 * exp(-0.323997 * distance())) * pow((width1() + width2()) / 2 , 0.367772) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00988745 * exp(-0.323996 * distance()) + 0.00988714 * exp(-0.323997 * distance())) * pow((width1() + width2()) / 2 , 0.367772)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00988745 * exp(-0.323996 * distance()) + 0.00988714 * exp(-0.323997 * distance())) * pow((width1() + width2()) / 2 , 0.367772) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00988745 * exp(-0.323996 * distance()) + 0.00988714 * exp(-0.323997 * distance())) * pow((width1() + width2()) / 2 , 0.367772)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00443715 * exp(-0.290903 * distance()) + 0.00443718 * exp(-0.290961 * distance())) * pow((width1() + width2()) / 2 , 0.419597) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00443715 * exp(-0.290903 * distance()) + 0.00443718 * exp(-0.290961 * distance())) * pow((width1() + width2()) / 2 , 0.419597)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00443715 * exp(-0.290903 * distance()) + 0.00443718 * exp(-0.290961 * distance())) * pow((width1() + width2()) / 2 , 0.419597) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00443715 * exp(-0.290903 * distance()) + 0.00443718 * exp(-0.290961 * distance())) * pow((width1() + width2()) / 2 , 0.419597)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00513961 * exp(-0.2441 * distance()) + 0.00513963 * exp(-0.244594 * distance())) * pow((width1() + width2()) / 2 , 0.394657) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00513961 * exp(-0.2441 * distance()) + 0.00513963 * exp(-0.244594 * distance())) * pow((width1() + width2()) / 2 , 0.394657)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00513961 * exp(-0.2441 * distance()) + 0.00513963 * exp(-0.244594 * distance())) * pow((width1() + width2()) / 2 , 0.394657) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00513961 * exp(-0.2441 * distance()) + 0.00513963 * exp(-0.244594 * distance())) * pow((width1() + width2()) / 2 , 0.394657)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,ctm
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0052874 * exp(-0.232398 * distance()) + 0.00528685 * exp(-0.232314 * distance())) * pow((width1() + width2()) / 2 , 0.390804) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0052874 * exp(-0.232398 * distance()) + 0.00528685 * exp(-0.232314 * distance())) * pow((width1() + width2()) / 2 , 0.390804)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0052874 * exp(-0.232398 * distance()) + 0.00528685 * exp(-0.232314 * distance())) * pow((width1() + width2()) / 2 , 0.390804) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0052874 * exp(-0.232398 * distance()) + 0.00528685 * exp(-0.232314 * distance())) * pow((width1() + width2()) / 2 , 0.390804)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00545477 * exp(-0.215659 * distance()) + 0.0054546 * exp(-0.218156 * distance())) * pow((width1() + width2()) / 2 , 0.386085) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00545477 * exp(-0.215659 * distance()) + 0.0054546 * exp(-0.218156 * distance())) * pow((width1() + width2()) / 2 , 0.386085)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00545477 * exp(-0.215659 * distance()) + 0.0054546 * exp(-0.218156 * distance())) * pow((width1() + width2()) / 2 , 0.386085) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00545477 * exp(-0.215659 * distance()) + 0.0054546 * exp(-0.218156 * distance())) * pow((width1() + width2()) / 2 , 0.386085)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00273442 * exp(-0.206215 * distance()) + 0.00273468 * exp(-0.206645 * distance())) * pow((width1() + width2()) / 2 , 0.43507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00273442 * exp(-0.206215 * distance()) + 0.00273468 * exp(-0.206645 * distance())) * pow((width1() + width2()) / 2 , 0.43507)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00273442 * exp(-0.206215 * distance()) + 0.00273468 * exp(-0.206645 * distance())) * pow((width1() + width2()) / 2 , 0.43507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00273442 * exp(-0.206215 * distance()) + 0.00273468 * exp(-0.206645 * distance())) * pow((width1() + width2()) / 2 , 0.43507)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal4,ctm
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00301706 * exp(-0.189641 * distance()) + 0.00301678 * exp(-0.190053 * distance())) * pow((width1() + width2()) / 2 , 0.417455) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00301706 * exp(-0.189641 * distance()) + 0.00301678 * exp(-0.190053 * distance())) * pow((width1() + width2()) / 2 , 0.417455)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00301706 * exp(-0.189641 * distance()) + 0.00301678 * exp(-0.190053 * distance())) * pow((width1() + width2()) / 2 , 0.417455) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00301706 * exp(-0.189641 * distance()) + 0.00301678 * exp(-0.190053 * distance())) * pow((width1() + width2()) / 2 , 0.417455)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00332194 * exp(-0.169425 * distance()) + 0.00332208 * exp(-0.168958 * distance())) * pow((width1() + width2()) / 2 , 0.403065) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00332194 * exp(-0.169425 * distance()) + 0.00332208 * exp(-0.168958 * distance())) * pow((width1() + width2()) / 2 , 0.403065)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00332194 * exp(-0.169425 * distance()) + 0.00332208 * exp(-0.168958 * distance())) * pow((width1() + width2()) / 2 , 0.403065) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00332194 * exp(-0.169425 * distance()) + 0.00332208 * exp(-0.168958 * distance())) * pow((width1() + width2()) / 2 , 0.403065)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal5,ctm
//
CAPACITANCE NEARBODY poly metal5 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00132919 * exp(-0.174345 * distance()) + 0.00131207 * exp(-0.174465 * distance())) * pow((width1() + width2()) / 2 , 0.537717) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00132919 * exp(-0.174345 * distance()) + 0.00131207 * exp(-0.174465 * distance())) * pow((width1() + width2()) / 2 , 0.537717)
    }
]
CAPACITANCE NEARBODY metal5 poly INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00132919 * exp(-0.174345 * distance()) + 0.00131207 * exp(-0.174465 * distance())) * pow((width1() + width2()) / 2 , 0.537717) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00132919 * exp(-0.174345 * distance()) + 0.00131207 * exp(-0.174465 * distance())) * pow((width1() + width2()) / 2 , 0.537717)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE NEARBODY poly metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00190288 * exp(-0.146669 * distance()) + 0.00192113 * exp(-0.14651 * distance())) * pow((width1() + width2()) / 2 , 0.437627) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00190288 * exp(-0.146669 * distance()) + 0.00192113 * exp(-0.14651 * distance())) * pow((width1() + width2()) / 2 , 0.437627)
    }
]
CAPACITANCE NEARBODY metal5 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00190288 * exp(-0.146669 * distance()) + 0.00192113 * exp(-0.14651 * distance())) * pow((width1() + width2()) / 2 , 0.437627) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00190288 * exp(-0.146669 * distance()) + 0.00192113 * exp(-0.14651 * distance())) * pow((width1() + width2()) / 2 , 0.437627)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,ctm,metal6
//
CAPACITANCE NEARBODY poly ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00131654 * exp(-0.135622 * distance()) + 0.00131654 * exp(-0.134896 * distance())) * pow((width1() + width2()) / 2 , 0.511943) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00131654 * exp(-0.135622 * distance()) + 0.00131654 * exp(-0.134896 * distance())) * pow((width1() + width2()) / 2 , 0.511943)
    }
]
CAPACITANCE NEARBODY ctm poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.64 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_poly
    profile = field_profileid
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00131654 * exp(-0.135622 * distance()) + 0.00131654 * exp(-0.134896 * distance())) * pow((width1() + width2()) / 2 , 0.511943) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00131654 * exp(-0.135622 * distance()) + 0.00131654 * exp(-0.134896 * distance())) * pow((width1() + width2()) / 2 , 0.511943)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0223129 * exp(-0.618062 * distance()) + 0.0223129 * exp(-0.61875 * distance())) * pow((width1() + width2()) / 2 , 0.259425) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0223129 * exp(-0.618062 * distance()) + 0.0223129 * exp(-0.61875 * distance())) * pow((width1() + width2()) / 2 , 0.259425)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0223129 * exp(-0.618062 * distance()) + 0.0223129 * exp(-0.61875 * distance())) * pow((width1() + width2()) / 2 , 0.259425) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0223129 * exp(-0.618062 * distance()) + 0.0223129 * exp(-0.61875 * distance())) * pow((width1() + width2()) / 2 , 0.259425)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241666 * exp(-0.52078 * distance()) + 0.0241666 * exp(-0.52094 * distance())) * pow((width1() + width2()) / 2 , 0.253997) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241666 * exp(-0.52078 * distance()) + 0.0241666 * exp(-0.52094 * distance())) * pow((width1() + width2()) / 2 , 0.253997)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241666 * exp(-0.52078 * distance()) + 0.0241666 * exp(-0.52094 * distance())) * pow((width1() + width2()) / 2 , 0.253997) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241666 * exp(-0.52078 * distance()) + 0.0241666 * exp(-0.52094 * distance())) * pow((width1() + width2()) / 2 , 0.253997)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0248374 * exp(-0.478606 * distance()) + 0.0248374 * exp(-0.478607 * distance())) * pow((width1() + width2()) / 2 , 0.254567) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0248374 * exp(-0.478606 * distance()) + 0.0248374 * exp(-0.478607 * distance())) * pow((width1() + width2()) / 2 , 0.254567)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0248374 * exp(-0.478606 * distance()) + 0.0248374 * exp(-0.478607 * distance())) * pow((width1() + width2()) / 2 , 0.254567) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0248374 * exp(-0.478606 * distance()) + 0.0248374 * exp(-0.478607 * distance())) * pow((width1() + width2()) / 2 , 0.254567)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,ctm
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249908 * exp(-0.46775 * distance()) + 0.0249908 * exp(-0.46771 * distance())) * pow((width1() + width2()) / 2 , 0.254882) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249908 * exp(-0.46775 * distance()) + 0.0249908 * exp(-0.46771 * distance())) * pow((width1() + width2()) / 2 , 0.254882)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249908 * exp(-0.46775 * distance()) + 0.0249908 * exp(-0.46771 * distance())) * pow((width1() + width2()) / 2 , 0.254882) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249908 * exp(-0.46775 * distance()) + 0.0249908 * exp(-0.46771 * distance())) * pow((width1() + width2()) / 2 , 0.254882)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251755 * exp(-0.453811 * distance()) + 0.0251756 * exp(-0.453665 * distance())) * pow((width1() + width2()) / 2 , 0.255347) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251755 * exp(-0.453811 * distance()) + 0.0251756 * exp(-0.453665 * distance())) * pow((width1() + width2()) / 2 , 0.255347)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251755 * exp(-0.453811 * distance()) + 0.0251756 * exp(-0.453665 * distance())) * pow((width1() + width2()) / 2 , 0.255347) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251755 * exp(-0.453811 * distance()) + 0.0251756 * exp(-0.453665 * distance())) * pow((width1() + width2()) / 2 , 0.255347)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101283 * exp(-0.337962 * distance()) + 0.0101282 * exp(-0.337668 * distance())) * pow((width1() + width2()) / 2 , 0.332168) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101283 * exp(-0.337962 * distance()) + 0.0101282 * exp(-0.337668 * distance())) * pow((width1() + width2()) / 2 , 0.332168)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101283 * exp(-0.337962 * distance()) + 0.0101282 * exp(-0.337668 * distance())) * pow((width1() + width2()) / 2 , 0.332168) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101283 * exp(-0.337962 * distance()) + 0.0101282 * exp(-0.337668 * distance())) * pow((width1() + width2()) / 2 , 0.332168)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115547 * exp(-0.283229 * distance()) + 0.0115547 * exp(-0.284143 * distance())) * pow((width1() + width2()) / 2 , 0.307214) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115547 * exp(-0.283229 * distance()) + 0.0115547 * exp(-0.284143 * distance())) * pow((width1() + width2()) / 2 , 0.307214)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115547 * exp(-0.283229 * distance()) + 0.0115547 * exp(-0.284143 * distance())) * pow((width1() + width2()) / 2 , 0.307214) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115547 * exp(-0.283229 * distance()) + 0.0115547 * exp(-0.284143 * distance())) * pow((width1() + width2()) / 2 , 0.307214)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,ctm
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0118537 * exp(-0.269803 * distance()) + 0.0118537 * exp(-0.27056 * distance())) * pow((width1() + width2()) / 2 , 0.303862) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0118537 * exp(-0.269803 * distance()) + 0.0118537 * exp(-0.27056 * distance())) * pow((width1() + width2()) / 2 , 0.303862)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0118537 * exp(-0.269803 * distance()) + 0.0118537 * exp(-0.27056 * distance())) * pow((width1() + width2()) / 2 , 0.303862) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0118537 * exp(-0.269803 * distance()) + 0.0118537 * exp(-0.27056 * distance())) * pow((width1() + width2()) / 2 , 0.303862)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122014 * exp(-0.253045 * distance()) + 0.0122012 * exp(-0.253043 * distance())) * pow((width1() + width2()) / 2 , 0.300512) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122014 * exp(-0.253045 * distance()) + 0.0122012 * exp(-0.253043 * distance())) * pow((width1() + width2()) / 2 , 0.300512)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122014 * exp(-0.253045 * distance()) + 0.0122012 * exp(-0.253043 * distance())) * pow((width1() + width2()) / 2 , 0.300512) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122014 * exp(-0.253045 * distance()) + 0.0122012 * exp(-0.253043 * distance())) * pow((width1() + width2()) / 2 , 0.300512)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00600427 * exp(-0.231356 * distance()) + 0.00600427 * exp(-0.230866 * distance())) * pow((width1() + width2()) / 2 , 0.361181) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00600427 * exp(-0.231356 * distance()) + 0.00600427 * exp(-0.230866 * distance())) * pow((width1() + width2()) / 2 , 0.361181)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00600427 * exp(-0.231356 * distance()) + 0.00600427 * exp(-0.230866 * distance())) * pow((width1() + width2()) / 2 , 0.361181) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00600427 * exp(-0.231356 * distance()) + 0.00600427 * exp(-0.230866 * distance())) * pow((width1() + width2()) / 2 , 0.361181)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal4,ctm
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00657556 * exp(-0.212576 * distance()) + 0.00657554 * exp(-0.212821 * distance())) * pow((width1() + width2()) / 2 , 0.340779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00657556 * exp(-0.212576 * distance()) + 0.00657554 * exp(-0.212821 * distance())) * pow((width1() + width2()) / 2 , 0.340779)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00657556 * exp(-0.212576 * distance()) + 0.00657554 * exp(-0.212821 * distance())) * pow((width1() + width2()) / 2 , 0.340779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00657556 * exp(-0.212576 * distance()) + 0.00657554 * exp(-0.212821 * distance())) * pow((width1() + width2()) / 2 , 0.340779)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00719223 * exp(-0.190018 * distance()) + 0.00719223 * exp(-0.190066 * distance())) * pow((width1() + width2()) / 2 , 0.324882) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00719223 * exp(-0.190018 * distance()) + 0.00719223 * exp(-0.190066 * distance())) * pow((width1() + width2()) / 2 , 0.324882)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00719223 * exp(-0.190018 * distance()) + 0.00719223 * exp(-0.190066 * distance())) * pow((width1() + width2()) / 2 , 0.324882) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00719223 * exp(-0.190018 * distance()) + 0.00719223 * exp(-0.190066 * distance())) * pow((width1() + width2()) / 2 , 0.324882)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal5,ctm
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00290586 * exp(-0.19332 * distance()) + 0.00290545 * exp(-0.193661 * distance())) * pow((width1() + width2()) / 2 , 0.483406) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00290586 * exp(-0.19332 * distance()) + 0.00290545 * exp(-0.193661 * distance())) * pow((width1() + width2()) / 2 , 0.483406)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00290586 * exp(-0.19332 * distance()) + 0.00290545 * exp(-0.193661 * distance())) * pow((width1() + width2()) / 2 , 0.483406) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00290586 * exp(-0.19332 * distance()) + 0.00290545 * exp(-0.193661 * distance())) * pow((width1() + width2()) / 2 , 0.483406)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00412028 * exp(-0.163998 * distance()) + 0.00412064 * exp(-0.161419 * distance())) * pow((width1() + width2()) / 2 , 0.369508) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00412028 * exp(-0.163998 * distance()) + 0.00412064 * exp(-0.161419 * distance())) * pow((width1() + width2()) / 2 , 0.369508)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00412028 * exp(-0.163998 * distance()) + 0.00412064 * exp(-0.161419 * distance())) * pow((width1() + width2()) / 2 , 0.369508) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00412028 * exp(-0.163998 * distance()) + 0.00412064 * exp(-0.161419 * distance())) * pow((width1() + width2()) / 2 , 0.369508)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,ctm,metal6
//
CAPACITANCE NEARBODY metal1 ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00285413 * exp(-0.152248 * distance()) + 0.00285419 * exp(-0.151329 * distance())) * pow((width1() + width2()) / 2 , 0.451652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00285413 * exp(-0.152248 * distance()) + 0.00285419 * exp(-0.151329 * distance())) * pow((width1() + width2()) / 2 , 0.451652)
    }
]
CAPACITANCE NEARBODY ctm metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00285413 * exp(-0.152248 * distance()) + 0.00285419 * exp(-0.151329 * distance())) * pow((width1() + width2()) / 2 , 0.451652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00285413 * exp(-0.152248 * distance()) + 0.00285419 * exp(-0.151329 * distance())) * pow((width1() + width2()) / 2 , 0.451652)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0238383 * exp(-0.545389 * distance()) + 0.0238383 * exp(-0.545186 * distance())) * pow((width1() + width2()) / 2 , 0.256255) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0238383 * exp(-0.545389 * distance()) + 0.0238383 * exp(-0.545186 * distance())) * pow((width1() + width2()) / 2 , 0.256255)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0238383 * exp(-0.545389 * distance()) + 0.0238383 * exp(-0.545186 * distance())) * pow((width1() + width2()) / 2 , 0.256255) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0238383 * exp(-0.545389 * distance()) + 0.0238383 * exp(-0.545186 * distance())) * pow((width1() + width2()) / 2 , 0.256255)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0261105 * exp(-0.454646 * distance()) + 0.0261106 * exp(-0.454794 * distance())) * pow((width1() + width2()) / 2 , 0.24661) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0261105 * exp(-0.454646 * distance()) + 0.0261106 * exp(-0.454794 * distance())) * pow((width1() + width2()) / 2 , 0.24661)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0261105 * exp(-0.454646 * distance()) + 0.0261106 * exp(-0.454794 * distance())) * pow((width1() + width2()) / 2 , 0.24661) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0261105 * exp(-0.454646 * distance()) + 0.0261106 * exp(-0.454794 * distance())) * pow((width1() + width2()) / 2 , 0.24661)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,ctm
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0264567 * exp(-0.358319 * distance()) + 0.0267376 * exp(-0.528199 * distance())) * pow((width1() + width2()) / 2 , 0.246047) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0264567 * exp(-0.358319 * distance()) + 0.0267376 * exp(-0.528199 * distance())) * pow((width1() + width2()) / 2 , 0.246047)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0264567 * exp(-0.358319 * distance()) + 0.0267376 * exp(-0.528199 * distance())) * pow((width1() + width2()) / 2 , 0.246047) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0264567 * exp(-0.358319 * distance()) + 0.0267376 * exp(-0.528199 * distance())) * pow((width1() + width2()) / 2 , 0.246047)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0270659 * exp(-0.41038 * distance()) + 0.0270659 * exp(-0.410353 * distance())) * pow((width1() + width2()) / 2 , 0.245951) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0270659 * exp(-0.41038 * distance()) + 0.0270659 * exp(-0.410353 * distance())) * pow((width1() + width2()) / 2 , 0.245951)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0270659 * exp(-0.41038 * distance()) + 0.0270659 * exp(-0.410353 * distance())) * pow((width1() + width2()) / 2 , 0.245951) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0270659 * exp(-0.41038 * distance()) + 0.0270659 * exp(-0.410353 * distance())) * pow((width1() + width2()) / 2 , 0.245951)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114566 * exp(-0.29516 * distance()) + 0.0114568 * exp(-0.295405 * distance())) * pow((width1() + width2()) / 2 , 0.330958) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114566 * exp(-0.29516 * distance()) + 0.0114568 * exp(-0.295405 * distance())) * pow((width1() + width2()) / 2 , 0.330958)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114566 * exp(-0.29516 * distance()) + 0.0114568 * exp(-0.295405 * distance())) * pow((width1() + width2()) / 2 , 0.330958) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114566 * exp(-0.29516 * distance()) + 0.0114568 * exp(-0.295405 * distance())) * pow((width1() + width2()) / 2 , 0.330958)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal4,ctm
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.012384 * exp(-0.271136 * distance()) + 0.0123839 * exp(-0.271298 * distance())) * pow((width1() + width2()) / 2 , 0.312545) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.012384 * exp(-0.271136 * distance()) + 0.0123839 * exp(-0.271298 * distance())) * pow((width1() + width2()) / 2 , 0.312545)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.012384 * exp(-0.271136 * distance()) + 0.0123839 * exp(-0.271298 * distance())) * pow((width1() + width2()) / 2 , 0.312545) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.012384 * exp(-0.271136 * distance()) + 0.0123839 * exp(-0.271298 * distance())) * pow((width1() + width2()) / 2 , 0.312545)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013356 * exp(-0.242678 * distance()) + 0.0133561 * exp(-0.24342 * distance())) * pow((width1() + width2()) / 2 , 0.298923) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013356 * exp(-0.242678 * distance()) + 0.0133561 * exp(-0.24342 * distance())) * pow((width1() + width2()) / 2 , 0.298923)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013356 * exp(-0.242678 * distance()) + 0.0133561 * exp(-0.24342 * distance())) * pow((width1() + width2()) / 2 , 0.298923) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013356 * exp(-0.242678 * distance()) + 0.0133561 * exp(-0.24342 * distance())) * pow((width1() + width2()) / 2 , 0.298923)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal5,ctm
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00525729 * exp(-0.234609 * distance()) + 0.00525721 * exp(-0.234701 * distance())) * pow((width1() + width2()) / 2 , 0.468275) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00525729 * exp(-0.234609 * distance()) + 0.00525721 * exp(-0.234701 * distance())) * pow((width1() + width2()) / 2 , 0.468275)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00525729 * exp(-0.234609 * distance()) + 0.00525721 * exp(-0.234701 * distance())) * pow((width1() + width2()) / 2 , 0.468275) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00525729 * exp(-0.234609 * distance()) + 0.00525721 * exp(-0.234701 * distance())) * pow((width1() + width2()) / 2 , 0.468275)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00727452 * exp(-0.195918 * distance()) + 0.00727454 * exp(-0.196129 * distance())) * pow((width1() + width2()) / 2 , 0.352478) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00727452 * exp(-0.195918 * distance()) + 0.00727454 * exp(-0.196129 * distance())) * pow((width1() + width2()) / 2 , 0.352478)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00727452 * exp(-0.195918 * distance()) + 0.00727454 * exp(-0.196129 * distance())) * pow((width1() + width2()) / 2 , 0.352478) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00727452 * exp(-0.195918 * distance()) + 0.00727454 * exp(-0.196129 * distance())) * pow((width1() + width2()) / 2 , 0.352478)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,ctm,metal6
//
CAPACITANCE NEARBODY metal2 ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00500722 * exp(-0.183243 * distance()) + 0.00500688 * exp(-0.185177 * distance())) * pow((width1() + width2()) / 2 , 0.438657) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00500722 * exp(-0.183243 * distance()) + 0.00500688 * exp(-0.185177 * distance())) * pow((width1() + width2()) / 2 , 0.438657)
    }
]
CAPACITANCE NEARBODY ctm metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00500722 * exp(-0.183243 * distance()) + 0.00500688 * exp(-0.185177 * distance())) * pow((width1() + width2()) / 2 , 0.438657) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00500722 * exp(-0.183243 * distance()) + 0.00500688 * exp(-0.185177 * distance())) * pow((width1() + width2()) / 2 , 0.438657)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0244228 * exp(-0.504761 * distance()) + 0.0244228 * exp(-0.50483 * distance())) * pow((width1() + width2()) / 2 , 0.25907) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0244228 * exp(-0.504761 * distance()) + 0.0244228 * exp(-0.50483 * distance())) * pow((width1() + width2()) / 2 , 0.25907)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0244228 * exp(-0.504761 * distance()) + 0.0244228 * exp(-0.50483 * distance())) * pow((width1() + width2()) / 2 , 0.25907) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0244228 * exp(-0.504761 * distance()) + 0.0244228 * exp(-0.50483 * distance())) * pow((width1() + width2()) / 2 , 0.25907)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal4,ctm
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0257404 * exp(-0.457921 * distance()) + 0.0257404 * exp(-0.460094 * distance())) * pow((width1() + width2()) / 2 , 0.250794) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0257404 * exp(-0.457921 * distance()) + 0.0257404 * exp(-0.460094 * distance())) * pow((width1() + width2()) / 2 , 0.250794)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0257404 * exp(-0.457921 * distance()) + 0.0257404 * exp(-0.460094 * distance())) * pow((width1() + width2()) / 2 , 0.250794) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0257404 * exp(-0.457921 * distance()) + 0.0257404 * exp(-0.460094 * distance())) * pow((width1() + width2()) / 2 , 0.250794)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0270311 * exp(-0.411879 * distance()) + 0.0270311 * exp(-0.411154 * distance())) * pow((width1() + width2()) / 2 , 0.245994) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0270311 * exp(-0.411879 * distance()) + 0.0270311 * exp(-0.411154 * distance())) * pow((width1() + width2()) / 2 , 0.245994)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0270311 * exp(-0.411879 * distance()) + 0.0270311 * exp(-0.411154 * distance())) * pow((width1() + width2()) / 2 , 0.245994) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0270311 * exp(-0.411879 * distance()) + 0.0270311 * exp(-0.411154 * distance())) * pow((width1() + width2()) / 2 , 0.245994)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal5,ctm
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00922641 * exp(-0.320331 * distance()) + 0.0092264 * exp(-0.320324 * distance())) * pow((width1() + width2()) / 2 , 0.422794) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00922641 * exp(-0.320331 * distance()) + 0.0092264 * exp(-0.320324 * distance())) * pow((width1() + width2()) / 2 , 0.422794)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00922641 * exp(-0.320331 * distance()) + 0.0092264 * exp(-0.320324 * distance())) * pow((width1() + width2()) / 2 , 0.422794) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00922641 * exp(-0.320331 * distance()) + 0.0092264 * exp(-0.320324 * distance())) * pow((width1() + width2()) / 2 , 0.422794)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122653 * exp(-0.261273 * distance()) + 0.0122653 * exp(-0.261636 * distance())) * pow((width1() + width2()) / 2 , 0.320356) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122653 * exp(-0.261273 * distance()) + 0.0122653 * exp(-0.261636 * distance())) * pow((width1() + width2()) / 2 , 0.320356)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122653 * exp(-0.261273 * distance()) + 0.0122653 * exp(-0.261636 * distance())) * pow((width1() + width2()) / 2 , 0.320356) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122653 * exp(-0.261273 * distance()) + 0.0122653 * exp(-0.261636 * distance())) * pow((width1() + width2()) / 2 , 0.320356)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,ctm,metal6
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00823905 * exp(-0.243952 * distance()) + 0.00823912 * exp(-0.243695 * distance())) * pow((width1() + width2()) / 2 , 0.406635) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00823905 * exp(-0.243952 * distance()) + 0.00823912 * exp(-0.243695 * distance())) * pow((width1() + width2()) / 2 , 0.406635)
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00823905 * exp(-0.243952 * distance()) + 0.00823912 * exp(-0.243695 * distance())) * pow((width1() + width2()) / 2 , 0.406635) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00823905 * exp(-0.243952 * distance()) + 0.00823912 * exp(-0.243695 * distance())) * pow((width1() + width2()) / 2 , 0.406635)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal5,ctm
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0204297 * exp(-0.616648 * distance()) + 0.0204311 * exp(-0.617507 * distance())) * pow((width1() + width2()) / 2 , 0.317383) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0204297 * exp(-0.616648 * distance()) + 0.0204311 * exp(-0.617507 * distance())) * pow((width1() + width2()) / 2 , 0.317383)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0204297 * exp(-0.616648 * distance()) + 0.0204311 * exp(-0.617507 * distance())) * pow((width1() + width2()) / 2 , 0.317383) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0204297 * exp(-0.616648 * distance()) + 0.0204311 * exp(-0.617507 * distance())) * pow((width1() + width2()) / 2 , 0.317383)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0250956 * exp(-0.470387 * distance()) + 0.0250956 * exp(-0.468547 * distance())) * pow((width1() + width2()) / 2 , 0.257999) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0250956 * exp(-0.470387 * distance()) + 0.0250956 * exp(-0.468547 * distance())) * pow((width1() + width2()) / 2 , 0.257999)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0250956 * exp(-0.470387 * distance()) + 0.0250956 * exp(-0.468547 * distance())) * pow((width1() + width2()) / 2 , 0.257999) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0250956 * exp(-0.470387 * distance()) + 0.0250956 * exp(-0.468547 * distance())) * pow((width1() + width2()) / 2 , 0.257999)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,ctm,metal6
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0152734 * exp(-0.389484 * distance()) + 0.0152734 * exp(-0.389276 * distance())) * pow((width1() + width2()) / 2 , 0.353383) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0152734 * exp(-0.389484 * distance()) + 0.0152734 * exp(-0.389276 * distance())) * pow((width1() + width2()) / 2 , 0.353383)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0152734 * exp(-0.389484 * distance()) + 0.0152734 * exp(-0.389276 * distance())) * pow((width1() + width2()) / 2 , 0.353383) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0152734 * exp(-0.389484 * distance()) + 0.0152734 * exp(-0.389276 * distance())) * pow((width1() + width2()) / 2 , 0.353383)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal5,ctm,metal6
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.113303 * exp(-12.6271 * distance()) + 0.0829325 * exp(-1.08764 * distance())) * pow((width1() + width2()) / 2 , 0.0838266) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.113303 * exp(-12.6271 * distance()) + 0.0829325 * exp(-1.08764 * distance())) * pow((width1() + width2()) / 2 , 0.0838266)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.113303 * exp(-12.6271 * distance()) + 0.0829325 * exp(-1.08764 * distance())) * pow((width1() + width2()) / 2 , 0.0838266) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.113303 * exp(-12.6271 * distance()) + 0.0829325 * exp(-1.08764 * distance())) * pow((width1() + width2()) / 2 , 0.0838266)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0448809 * exp(-0.519566 * distance()) + 0.00350891) * pow((width1() + width2()) / 2 , 0.276218) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0448809 * exp(-0.519566 * distance()) + 0.00350891) * pow((width1() + width2()) / 2 , 0.276218)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0448809 * exp(-0.519566 * distance()) + 0.00350891) * pow((width1() + width2()) / 2 , 0.276218) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0448809 * exp(-0.519566 * distance()) + 0.00350891) * pow((width1() + width2()) / 2 , 0.276218)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120094 * exp(-0.197283 * distance()) + 0.0120094 * exp(-0.195843 * distance())) * pow((width1() + width2()) / 2 , 0.322416) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120094 * exp(-0.197283 * distance()) + 0.0120094 * exp(-0.195843 * distance())) * pow((width1() + width2()) / 2 , 0.322416)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120094 * exp(-0.197283 * distance()) + 0.0120094 * exp(-0.195843 * distance())) * pow((width1() + width2()) / 2 , 0.322416) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120094 * exp(-0.197283 * distance()) + 0.0120094 * exp(-0.195843 * distance())) * pow((width1() + width2()) / 2 , 0.322416)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00797367 * exp(-0.111474 * distance()) + 0.00797366 * exp(-0.112363 * distance())) * pow((width1() + width2()) / 2 , 0.329214) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00797367 * exp(-0.111474 * distance()) + 0.00797366 * exp(-0.112363 * distance())) * pow((width1() + width2()) / 2 , 0.329214)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00797367 * exp(-0.111474 * distance()) + 0.00797366 * exp(-0.112363 * distance())) * pow((width1() + width2()) / 2 , 0.329214) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00797367 * exp(-0.111474 * distance()) + 0.00797366 * exp(-0.112363 * distance())) * pow((width1() + width2()) / 2 , 0.329214)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00600287 * exp(-0.0655242 * distance()) + 0.00600292 * exp(-0.0674849 * distance())) * pow((width1() + width2()) / 2 , 0.32707) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00600287 * exp(-0.0655242 * distance()) + 0.00600292 * exp(-0.0674849 * distance())) * pow((width1() + width2()) / 2 , 0.32707)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00600287 * exp(-0.0655242 * distance()) + 0.00600292 * exp(-0.0674849 * distance())) * pow((width1() + width2()) / 2 , 0.32707) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00600287 * exp(-0.0655242 * distance()) + 0.00600292 * exp(-0.0674849 * distance())) * pow((width1() + width2()) / 2 , 0.32707)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,ctm
//
CAPACITANCE NEARBODY metal1 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00520789 * exp(-0.058133 * distance()) + 0.00520795 * exp(-0.0580698 * distance())) * pow((width1() + width2()) / 2 , 0.356494) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00520789 * exp(-0.058133 * distance()) + 0.00520795 * exp(-0.0580698 * distance())) * pow((width1() + width2()) / 2 , 0.356494)
    }
]
CAPACITANCE NEARBODY ctm metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00520789 * exp(-0.058133 * distance()) + 0.00520795 * exp(-0.0580698 * distance())) * pow((width1() + width2()) / 2 , 0.356494) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00520789 * exp(-0.058133 * distance()) + 0.00520795 * exp(-0.0580698 * distance())) * pow((width1() + width2()) / 2 , 0.356494)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00532081 * exp(-0.0620326 * distance()) + 0.00532051) * pow((width1() + width2()) / 2 , 0.280456) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00532081 * exp(-0.0620326 * distance()) + 0.00532051) * pow((width1() + width2()) / 2 , 0.280456)
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00532081 * exp(-0.0620326 * distance()) + 0.00532051) * pow((width1() + width2()) / 2 , 0.280456) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00532081 * exp(-0.0620326 * distance()) + 0.00532051) * pow((width1() + width2()) / 2 , 0.280456)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00928946 + 0.0478376 * exp(-0.47346 * distance())) * pow((width1() + width2()) / 2 , 0.251273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00928946 + 0.0478376 * exp(-0.47346 * distance())) * pow((width1() + width2()) / 2 , 0.251273)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00928946 + 0.0478376 * exp(-0.47346 * distance())) * pow((width1() + width2()) / 2 , 0.251273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00928946 + 0.0478376 * exp(-0.47346 * distance())) * pow((width1() + width2()) / 2 , 0.251273)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0156391 * exp(-0.148875 * distance()) + 0.0156392 * exp(-0.149069 * distance())) * pow((width1() + width2()) / 2 , 0.282669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0156391 * exp(-0.148875 * distance()) + 0.0156392 * exp(-0.149069 * distance())) * pow((width1() + width2()) / 2 , 0.282669)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0156391 * exp(-0.148875 * distance()) + 0.0156392 * exp(-0.149069 * distance())) * pow((width1() + width2()) / 2 , 0.282669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0156391 * exp(-0.148875 * distance()) + 0.0156392 * exp(-0.149069 * distance())) * pow((width1() + width2()) / 2 , 0.282669)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111017 * exp(-0.0845287 * distance()) + 0.0111015 * exp(-0.0845454 * distance())) * pow((width1() + width2()) / 2 , 0.283451) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111017 * exp(-0.0845287 * distance()) + 0.0111015 * exp(-0.0845454 * distance())) * pow((width1() + width2()) / 2 , 0.283451)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111017 * exp(-0.0845287 * distance()) + 0.0111015 * exp(-0.0845454 * distance())) * pow((width1() + width2()) / 2 , 0.283451) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111017 * exp(-0.0845287 * distance()) + 0.0111015 * exp(-0.0845454 * distance())) * pow((width1() + width2()) / 2 , 0.283451)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,ctm
//
CAPACITANCE NEARBODY metal2 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00948352 * exp(-0.0730408 * distance()) + 0.00948344 * exp(-0.0730482 * distance())) * pow((width1() + width2()) / 2 , 0.313087) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00948352 * exp(-0.0730408 * distance()) + 0.00948344 * exp(-0.0730482 * distance())) * pow((width1() + width2()) / 2 , 0.313087)
    }
]
CAPACITANCE NEARBODY ctm metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00948352 * exp(-0.0730408 * distance()) + 0.00948344 * exp(-0.0730482 * distance())) * pow((width1() + width2()) / 2 , 0.313087) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00948352 * exp(-0.0730408 * distance()) + 0.00948344 * exp(-0.0730482 * distance())) * pow((width1() + width2()) / 2 , 0.313087)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00955849 * exp(-0.0806756 * distance()) + 0.00955769 * exp(-0.000990937 * distance())) * pow((width1() + width2()) / 2 , 0.234874) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00955849 * exp(-0.0806756 * distance()) + 0.00955769 * exp(-0.000990937 * distance())) * pow((width1() + width2()) / 2 , 0.234874)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00955849 * exp(-0.0806756 * distance()) + 0.00955769 * exp(-0.000990937 * distance())) * pow((width1() + width2()) / 2 , 0.234874) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00955849 * exp(-0.0806756 * distance()) + 0.00955769 * exp(-0.000990937 * distance())) * pow((width1() + width2()) / 2 , 0.234874)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141791 + 0.047142 * exp(-0.470642 * distance())) * pow((width1() + width2()) / 2 , 0.242985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141791 + 0.047142 * exp(-0.470642 * distance())) * pow((width1() + width2()) / 2 , 0.242985)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141791 + 0.047142 * exp(-0.470642 * distance())) * pow((width1() + width2()) / 2 , 0.242985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141791 + 0.047142 * exp(-0.470642 * distance())) * pow((width1() + width2()) / 2 , 0.242985)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175907 * exp(-0.121917 * distance()) + 0.0175907 * exp(-0.124612 * distance())) * pow((width1() + width2()) / 2 , 0.262309) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175907 * exp(-0.121917 * distance()) + 0.0175907 * exp(-0.124612 * distance())) * pow((width1() + width2()) / 2 , 0.262309)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175907 * exp(-0.121917 * distance()) + 0.0175907 * exp(-0.124612 * distance())) * pow((width1() + width2()) / 2 , 0.262309) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175907 * exp(-0.121917 * distance()) + 0.0175907 * exp(-0.124612 * distance())) * pow((width1() + width2()) / 2 , 0.262309)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,ctm
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0144972 * exp(-0.103368 * distance()) + 0.0144971 * exp(-0.103713 * distance())) * pow((width1() + width2()) / 2 , 0.293639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0144972 * exp(-0.103368 * distance()) + 0.0144971 * exp(-0.103713 * distance())) * pow((width1() + width2()) / 2 , 0.293639)
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0144972 * exp(-0.103368 * distance()) + 0.0144971 * exp(-0.103713 * distance())) * pow((width1() + width2()) / 2 , 0.293639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0144972 * exp(-0.103368 * distance()) + 0.0144971 * exp(-0.103713 * distance())) * pow((width1() + width2()) / 2 , 0.293639)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141127 * exp(-0.0535494 * distance()) + 0.0141126 * exp(-0.0535494 * distance())) * pow((width1() + width2()) / 2 , 0.214327) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141127 * exp(-0.0535494 * distance()) + 0.0141126 * exp(-0.0535494 * distance())) * pow((width1() + width2()) / 2 , 0.214327)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141127 * exp(-0.0535494 * distance()) + 0.0141126 * exp(-0.0535494 * distance())) * pow((width1() + width2()) / 2 , 0.214327) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141127 * exp(-0.0535494 * distance()) + 0.0141126 * exp(-0.0535494 * distance())) * pow((width1() + width2()) / 2 , 0.214327)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168783 + 0.0472159 * exp(-0.455481 * distance())) * pow((width1() + width2()) / 2 , 0.238742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168783 + 0.0472159 * exp(-0.455481 * distance())) * pow((width1() + width2()) / 2 , 0.238742)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168783 + 0.0472159 * exp(-0.455481 * distance())) * pow((width1() + width2()) / 2 , 0.238742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168783 + 0.0472159 * exp(-0.455481 * distance())) * pow((width1() + width2()) / 2 , 0.238742)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal4,ctm
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230812 * exp(-0.178545 * distance()) + 0.0230812 * exp(-0.178548 * distance())) * pow((width1() + width2()) / 2 , 0.282219) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230812 * exp(-0.178545 * distance()) + 0.0230812 * exp(-0.178548 * distance())) * pow((width1() + width2()) / 2 , 0.282219)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230812 * exp(-0.178545 * distance()) + 0.0230812 * exp(-0.178548 * distance())) * pow((width1() + width2()) / 2 , 0.282219) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230812 * exp(-0.178545 * distance()) + 0.0230812 * exp(-0.178548 * distance())) * pow((width1() + width2()) / 2 , 0.282219)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0205469 * exp(-0.0928772 * distance()) + 0.0205471 * exp(-0.0797897 * distance())) * pow((width1() + width2()) / 2 , 0.209602) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0205469 * exp(-0.0928772 * distance()) + 0.0205471 * exp(-0.0797897 * distance())) * pow((width1() + width2()) / 2 , 0.209602)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0205469 * exp(-0.0928772 * distance()) + 0.0205471 * exp(-0.0797897 * distance())) * pow((width1() + width2()) / 2 , 0.209602) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0205469 * exp(-0.0928772 * distance()) + 0.0205471 * exp(-0.0797897 * distance())) * pow((width1() + width2()) / 2 , 0.209602)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal5,ctm
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0778024 * exp(-0.366628 * distance()) + 0.140025 * exp(-9.34235 * distance())) * pow((width1() + width2()) / 2 , 0.113671) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0778024 * exp(-0.366628 * distance()) + 0.140025 * exp(-9.34235 * distance())) * pow((width1() + width2()) / 2 , 0.113671)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0778024 * exp(-0.366628 * distance()) + 0.140025 * exp(-9.34235 * distance())) * pow((width1() + width2()) / 2 , 0.113671) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0778024 * exp(-0.366628 * distance()) + 0.140025 * exp(-9.34235 * distance())) * pow((width1() + width2()) / 2 , 0.113671)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0339782 * exp(-0.172248 * distance()) + 0.0339782 * exp(-0.172426 * distance())) * pow((width1() + width2()) / 2 , 0.208129) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0339782 * exp(-0.172248 * distance()) + 0.0339782 * exp(-0.172426 * distance())) * pow((width1() + width2()) / 2 , 0.208129)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0339782 * exp(-0.172248 * distance()) + 0.0339782 * exp(-0.172426 * distance())) * pow((width1() + width2()) / 2 , 0.208129) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0339782 * exp(-0.172248 * distance()) + 0.0339782 * exp(-0.172426 * distance())) * pow((width1() + width2()) / 2 , 0.208129)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0456697 * exp(-0.0688967 * distance()) + 0.0465973 * exp(-0.701469 * distance())) * pow((width1() + width2()) / 2 , 0.0665884) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0456697 * exp(-0.0688967 * distance()) + 0.0465973 * exp(-0.701469 * distance())) * pow((width1() + width2()) / 2 , 0.0665884)
    }
]
CAPACITANCE NEARBODY metal6 ctm INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0456697 * exp(-0.0688967 * distance()) + 0.0465973 * exp(-0.701469 * distance())) * pow((width1() + width2()) / 2 , 0.0665884) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0456697 * exp(-0.0688967 * distance()) + 0.0465973 * exp(-0.701469 * distance())) * pow((width1() + width2()) / 2 , 0.0665884)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0461247 * exp(-0.514191 * distance()) + 0.00534433) * pow((width1() + width2()) / 2 , 0.268099) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0461247 * exp(-0.514191 * distance()) + 0.00534433) * pow((width1() + width2()) / 2 , 0.268099)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0461247 * exp(-0.514191 * distance()) + 0.00534433) * pow((width1() + width2()) / 2 , 0.268099) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0461247 * exp(-0.514191 * distance()) + 0.00534433) * pow((width1() + width2()) / 2 , 0.268099)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0132757 * exp(-0.183973 * distance()) + 0.0132757 * exp(-0.183978 * distance())) * pow((width1() + width2()) / 2 , 0.320322) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0132757 * exp(-0.183973 * distance()) + 0.0132757 * exp(-0.183978 * distance())) * pow((width1() + width2()) / 2 , 0.320322)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0132757 * exp(-0.183973 * distance()) + 0.0132757 * exp(-0.183978 * distance())) * pow((width1() + width2()) / 2 , 0.320322) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0132757 * exp(-0.183973 * distance()) + 0.0132757 * exp(-0.183978 * distance())) * pow((width1() + width2()) / 2 , 0.320322)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00903442 * exp(-0.104853 * distance()) + 0.00903437 * exp(-0.10492 * distance())) * pow((width1() + width2()) / 2 , 0.329875) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00903442 * exp(-0.104853 * distance()) + 0.00903437 * exp(-0.10492 * distance())) * pow((width1() + width2()) / 2 , 0.329875)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00903442 * exp(-0.104853 * distance()) + 0.00903437 * exp(-0.10492 * distance())) * pow((width1() + width2()) / 2 , 0.329875) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00903442 * exp(-0.104853 * distance()) + 0.00903437 * exp(-0.10492 * distance())) * pow((width1() + width2()) / 2 , 0.329875)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,ctm
//
CAPACITANCE NEARBODY metal2 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00761158 * exp(-0.0931181 * distance()) + 0.0076116 * exp(-0.0874081 * distance())) * pow((width1() + width2()) / 2 , 0.362171) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00761158 * exp(-0.0931181 * distance()) + 0.0076116 * exp(-0.0874081 * distance())) * pow((width1() + width2()) / 2 , 0.362171)
    }
]
CAPACITANCE NEARBODY ctm metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00761158 * exp(-0.0931181 * distance()) + 0.0076116 * exp(-0.0874081 * distance())) * pow((width1() + width2()) / 2 , 0.362171) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00761158 * exp(-0.0931181 * distance()) + 0.0076116 * exp(-0.0874081 * distance())) * pow((width1() + width2()) / 2 , 0.362171)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00750875 * exp(-0.0466253 * distance()) + 0.00750875 * exp(-0.0466253 * distance())) * pow((width1() + width2()) / 2 , 0.283863) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00750875 * exp(-0.0466253 * distance()) + 0.00750875 * exp(-0.0466253 * distance())) * pow((width1() + width2()) / 2 , 0.283863)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00750875 * exp(-0.0466253 * distance()) + 0.00750875 * exp(-0.0466253 * distance())) * pow((width1() + width2()) / 2 , 0.283863) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00750875 * exp(-0.0466253 * distance()) + 0.00750875 * exp(-0.0466253 * distance())) * pow((width1() + width2()) / 2 , 0.283863)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0102721 + 0.0480379 * exp(-0.465941 * distance())) * pow((width1() + width2()) / 2 , 0.248988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0102721 + 0.0480379 * exp(-0.465941 * distance())) * pow((width1() + width2()) / 2 , 0.248988)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0102721 + 0.0480379 * exp(-0.465941 * distance())) * pow((width1() + width2()) / 2 , 0.248988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0102721 + 0.0480379 * exp(-0.465941 * distance())) * pow((width1() + width2()) / 2 , 0.248988)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162015 * exp(-0.141326 * distance()) + 0.0162016 * exp(-0.140659 * distance())) * pow((width1() + width2()) / 2 , 0.276964) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162015 * exp(-0.141326 * distance()) + 0.0162016 * exp(-0.140659 * distance())) * pow((width1() + width2()) / 2 , 0.276964)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162015 * exp(-0.141326 * distance()) + 0.0162016 * exp(-0.140659 * distance())) * pow((width1() + width2()) / 2 , 0.276964) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162015 * exp(-0.141326 * distance()) + 0.0162016 * exp(-0.140659 * distance())) * pow((width1() + width2()) / 2 , 0.276964)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,ctm
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0132014 * exp(-0.118473 * distance()) + 0.0132014 * exp(-0.118429 * distance())) * pow((width1() + width2()) / 2 , 0.310485) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0132014 * exp(-0.118473 * distance()) + 0.0132014 * exp(-0.118429 * distance())) * pow((width1() + width2()) / 2 , 0.310485)
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0132014 * exp(-0.118473 * distance()) + 0.0132014 * exp(-0.118429 * distance())) * pow((width1() + width2()) / 2 , 0.310485) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0132014 * exp(-0.118473 * distance()) + 0.0132014 * exp(-0.118429 * distance())) * pow((width1() + width2()) / 2 , 0.310485)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0126268 * exp(-0.0617889 * distance()) + 0.0126269 * exp(-0.0605171 * distance())) * pow((width1() + width2()) / 2 , 0.232006) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0126268 * exp(-0.0617889 * distance()) + 0.0126269 * exp(-0.0605171 * distance())) * pow((width1() + width2()) / 2 , 0.232006)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0126268 * exp(-0.0617889 * distance()) + 0.0126269 * exp(-0.0605171 * distance())) * pow((width1() + width2()) / 2 , 0.232006) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0126268 * exp(-0.0617889 * distance()) + 0.0126269 * exp(-0.0605171 * distance())) * pow((width1() + width2()) / 2 , 0.232006)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0153148 + 0.0468353 * exp(-0.471506 * distance())) * pow((width1() + width2()) / 2 , 0.242404) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0153148 + 0.0468353 * exp(-0.471506 * distance())) * pow((width1() + width2()) / 2 , 0.242404)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0153148 + 0.0468353 * exp(-0.471506 * distance())) * pow((width1() + width2()) / 2 , 0.242404) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0153148 + 0.0468353 * exp(-0.471506 * distance())) * pow((width1() + width2()) / 2 , 0.242404)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal4,ctm
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00913132 + 0.0355085 * exp(-0.278882 * distance())) * pow((width1() + width2()) / 2 , 0.288915) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00913132 + 0.0355085 * exp(-0.278882 * distance())) * pow((width1() + width2()) / 2 , 0.288915)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00913132 + 0.0355085 * exp(-0.278882 * distance())) * pow((width1() + width2()) / 2 , 0.288915) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00913132 + 0.0355085 * exp(-0.278882 * distance())) * pow((width1() + width2()) / 2 , 0.288915)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0194198 * exp(-0.0938425 * distance()) + 0.0194198 * exp(-0.0931598 * distance())) * pow((width1() + width2()) / 2 , 0.218143) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0194198 * exp(-0.0938425 * distance()) + 0.0194198 * exp(-0.0931598 * distance())) * pow((width1() + width2()) / 2 , 0.218143)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0194198 * exp(-0.0938425 * distance()) + 0.0194198 * exp(-0.0931598 * distance())) * pow((width1() + width2()) / 2 , 0.218143) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0194198 * exp(-0.0938425 * distance()) + 0.0194198 * exp(-0.0931598 * distance())) * pow((width1() + width2()) / 2 , 0.218143)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal5,ctm
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.138767 * exp(-9.47864 * distance()) + 0.0776693 * exp(-0.39504 * distance())) * pow((width1() + width2()) / 2 , 0.112141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.138767 * exp(-9.47864 * distance()) + 0.0776693 * exp(-0.39504 * distance())) * pow((width1() + width2()) / 2 , 0.112141)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.138767 * exp(-9.47864 * distance()) + 0.0776693 * exp(-0.39504 * distance())) * pow((width1() + width2()) / 2 , 0.112141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.138767 * exp(-9.47864 * distance()) + 0.0776693 * exp(-0.39504 * distance())) * pow((width1() + width2()) / 2 , 0.112141)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0331478 * exp(-0.18082 * distance()) + 0.0331479 * exp(-0.180778 * distance())) * pow((width1() + width2()) / 2 , 0.211826) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0331478 * exp(-0.18082 * distance()) + 0.0331479 * exp(-0.180778 * distance())) * pow((width1() + width2()) / 2 , 0.211826)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0331478 * exp(-0.18082 * distance()) + 0.0331479 * exp(-0.180778 * distance())) * pow((width1() + width2()) / 2 , 0.211826) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0331478 * exp(-0.18082 * distance()) + 0.0331479 * exp(-0.180778 * distance())) * pow((width1() + width2()) / 2 , 0.211826)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514558 * exp(-0.639871 * distance()) + 0.0386587 * exp(-0.0506445 * distance())) * pow((width1() + width2()) / 2 , 0.0691541) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514558 * exp(-0.639871 * distance()) + 0.0386587 * exp(-0.0506445 * distance())) * pow((width1() + width2()) / 2 , 0.0691541)
    }
]
CAPACITANCE NEARBODY metal6 ctm INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514558 * exp(-0.639871 * distance()) + 0.0386587 * exp(-0.0506445 * distance())) * pow((width1() + width2()) / 2 , 0.0691541) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514558 * exp(-0.639871 * distance()) + 0.0386587 * exp(-0.0506445 * distance())) * pow((width1() + width2()) / 2 , 0.0691541)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0462935 * exp(-0.509948 * distance()) + 0.0051741) * pow((width1() + width2()) / 2 , 0.268507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0462935 * exp(-0.509948 * distance()) + 0.0051741) * pow((width1() + width2()) / 2 , 0.268507)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0462935 * exp(-0.509948 * distance()) + 0.0051741) * pow((width1() + width2()) / 2 , 0.268507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0462935 * exp(-0.509948 * distance()) + 0.0051741) * pow((width1() + width2()) / 2 , 0.268507)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133 * exp(-0.182759 * distance()) + 0.0133 * exp(-0.182759 * distance())) * pow((width1() + width2()) / 2 , 0.321231) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133 * exp(-0.182759 * distance()) + 0.0133 * exp(-0.182759 * distance())) * pow((width1() + width2()) / 2 , 0.321231)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133 * exp(-0.182759 * distance()) + 0.0133 * exp(-0.182759 * distance())) * pow((width1() + width2()) / 2 , 0.321231) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133 * exp(-0.182759 * distance()) + 0.0133 * exp(-0.182759 * distance())) * pow((width1() + width2()) / 2 , 0.321231)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,ctm
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0102842 * exp(-0.154333 * distance()) + 0.0109056 * exp(-0.153365 * distance())) * pow((width1() + width2()) / 2 , 0.360579) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0102842 * exp(-0.154333 * distance()) + 0.0109056 * exp(-0.153365 * distance())) * pow((width1() + width2()) / 2 , 0.360579)
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0102842 * exp(-0.154333 * distance()) + 0.0109056 * exp(-0.153365 * distance())) * pow((width1() + width2()) / 2 , 0.360579) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0102842 * exp(-0.154333 * distance()) + 0.0109056 * exp(-0.153365 * distance())) * pow((width1() + width2()) / 2 , 0.360579)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00976531 * exp(-0.0784977 * distance()) + 0.00976532 * exp(-0.0796685 * distance())) * pow((width1() + width2()) / 2 , 0.286492) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00976531 * exp(-0.0784977 * distance()) + 0.00976532 * exp(-0.0796685 * distance())) * pow((width1() + width2()) / 2 , 0.286492)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00976531 * exp(-0.0784977 * distance()) + 0.00976532 * exp(-0.0796685 * distance())) * pow((width1() + width2()) / 2 , 0.286492) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00976531 * exp(-0.0784977 * distance()) + 0.00976532 * exp(-0.0796685 * distance())) * pow((width1() + width2()) / 2 , 0.286492)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0481624 * exp(-0.462171 * distance()) + 0.0102301) * pow((width1() + width2()) / 2 , 0.250048) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0481624 * exp(-0.462171 * distance()) + 0.0102301) * pow((width1() + width2()) / 2 , 0.250048)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0481624 * exp(-0.462171 * distance()) + 0.0102301) * pow((width1() + width2()) / 2 , 0.250048) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0481624 * exp(-0.462171 * distance()) + 0.0102301) * pow((width1() + width2()) / 2 , 0.250048)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal4,ctm
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0205122 * exp(-0.226862 * distance()) + 0.0205122 * exp(-0.227108 * distance())) * pow((width1() + width2()) / 2 , 0.301273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0205122 * exp(-0.226862 * distance()) + 0.0205122 * exp(-0.227108 * distance())) * pow((width1() + width2()) / 2 , 0.301273)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0205122 * exp(-0.226862 * distance()) + 0.0205122 * exp(-0.227108 * distance())) * pow((width1() + width2()) / 2 , 0.301273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0205122 * exp(-0.226862 * distance()) + 0.0205122 * exp(-0.227108 * distance())) * pow((width1() + width2()) / 2 , 0.301273)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174106 * exp(-0.108562 * distance()) + 0.0174106 * exp(-0.108561 * distance())) * pow((width1() + width2()) / 2 , 0.23667) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174106 * exp(-0.108562 * distance()) + 0.0174106 * exp(-0.108561 * distance())) * pow((width1() + width2()) / 2 , 0.23667)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174106 * exp(-0.108562 * distance()) + 0.0174106 * exp(-0.108561 * distance())) * pow((width1() + width2()) / 2 , 0.23667) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174106 * exp(-0.108562 * distance()) + 0.0174106 * exp(-0.108561 * distance())) * pow((width1() + width2()) / 2 , 0.23667)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal5,ctm
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0783621 * exp(-0.462739 * distance()) + 0.135625 * exp(-9.79802 * distance())) * pow((width1() + width2()) / 2 , 0.109047) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0783621 * exp(-0.462739 * distance()) + 0.135625 * exp(-9.79802 * distance())) * pow((width1() + width2()) / 2 , 0.109047)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0783621 * exp(-0.462739 * distance()) + 0.135625 * exp(-9.79802 * distance())) * pow((width1() + width2()) / 2 , 0.109047) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0783621 * exp(-0.462739 * distance()) + 0.135625 * exp(-9.79802 * distance())) * pow((width1() + width2()) / 2 , 0.109047)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0317283 * exp(-0.196819 * distance()) + 0.0317283 * exp(-0.196819 * distance())) * pow((width1() + width2()) / 2 , 0.218512) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0317283 * exp(-0.196819 * distance()) + 0.0317283 * exp(-0.196819 * distance())) * pow((width1() + width2()) / 2 , 0.218512)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0317283 * exp(-0.196819 * distance()) + 0.0317283 * exp(-0.196819 * distance())) * pow((width1() + width2()) / 2 , 0.218512) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0317283 * exp(-0.196819 * distance()) + 0.0317283 * exp(-0.196819 * distance())) * pow((width1() + width2()) / 2 , 0.218512)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0437768 * exp(-0.719078 * distance()) + 0.0426926 * exp(-0.0870358 * distance())) * pow((width1() + width2()) / 2 , 0.0750405) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0437768 * exp(-0.719078 * distance()) + 0.0426926 * exp(-0.0870358 * distance())) * pow((width1() + width2()) / 2 , 0.0750405)
    }
]
CAPACITANCE NEARBODY metal6 ctm INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0437768 * exp(-0.719078 * distance()) + 0.0426926 * exp(-0.0870358 * distance())) * pow((width1() + width2()) / 2 , 0.0750405) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0437768 * exp(-0.719078 * distance()) + 0.0426926 * exp(-0.0870358 * distance())) * pow((width1() + width2()) / 2 , 0.0750405)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0460666 * exp(-0.513718 * distance()) + 0.00547238) * pow((width1() + width2()) / 2 , 0.269493) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0460666 * exp(-0.513718 * distance()) + 0.00547238) * pow((width1() + width2()) / 2 , 0.269493)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0460666 * exp(-0.513718 * distance()) + 0.00547238) * pow((width1() + width2()) / 2 , 0.269493) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0460666 * exp(-0.513718 * distance()) + 0.00547238) * pow((width1() + width2()) / 2 , 0.269493)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal4,ctm
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175632 * exp(-0.294429 * distance()) + 0.0175632 * exp(-0.294452 * distance())) * pow((width1() + width2()) / 2 , 0.332513) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175632 * exp(-0.294429 * distance()) + 0.0175632 * exp(-0.294452 * distance())) * pow((width1() + width2()) / 2 , 0.332513)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175632 * exp(-0.294429 * distance()) + 0.0175632 * exp(-0.294452 * distance())) * pow((width1() + width2()) / 2 , 0.332513) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175632 * exp(-0.294429 * distance()) + 0.0175632 * exp(-0.294452 * distance())) * pow((width1() + width2()) / 2 , 0.332513)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141194 * exp(-0.137747 * distance()) + 0.0141194 * exp(-0.138587 * distance())) * pow((width1() + width2()) / 2 , 0.28428) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141194 * exp(-0.137747 * distance()) + 0.0141194 * exp(-0.138587 * distance())) * pow((width1() + width2()) / 2 , 0.28428)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0141194 * exp(-0.137747 * distance()) + 0.0141194 * exp(-0.138587 * distance())) * pow((width1() + width2()) / 2 , 0.28428) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0141194 * exp(-0.137747 * distance()) + 0.0141194 * exp(-0.138587 * distance())) * pow((width1() + width2()) / 2 , 0.28428)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal5,ctm
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.131117 * exp(-10.355 * distance()) + 0.0791078 * exp(-0.569941 * distance())) * pow((width1() + width2()) / 2 , 0.103436) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.131117 * exp(-10.355 * distance()) + 0.0791078 * exp(-0.569941 * distance())) * pow((width1() + width2()) / 2 , 0.103436)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.131117 * exp(-10.355 * distance()) + 0.0791078 * exp(-0.569941 * distance())) * pow((width1() + width2()) / 2 , 0.103436) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.131117 * exp(-10.355 * distance()) + 0.0791078 * exp(-0.569941 * distance())) * pow((width1() + width2()) / 2 , 0.103436)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0124487 + 0.0473988 * exp(-0.336293 * distance())) * pow((width1() + width2()) / 2 , 0.230115) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0124487 + 0.0473988 * exp(-0.336293 * distance())) * pow((width1() + width2()) / 2 , 0.230115)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0124487 + 0.0473988 * exp(-0.336293 * distance())) * pow((width1() + width2()) / 2 , 0.230115) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0124487 + 0.0473988 * exp(-0.336293 * distance())) * pow((width1() + width2()) / 2 , 0.230115)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0468977 * exp(-0.662714 * distance()) + 0.0340312 * exp(-0.0765993 * distance())) * pow((width1() + width2()) / 2 , 0.0856766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0468977 * exp(-0.662714 * distance()) + 0.0340312 * exp(-0.0765993 * distance())) * pow((width1() + width2()) / 2 , 0.0856766)
    }
]
CAPACITANCE NEARBODY metal6 ctm INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0468977 * exp(-0.662714 * distance()) + 0.0340312 * exp(-0.0765993 * distance())) * pow((width1() + width2()) / 2 , 0.0856766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0468977 * exp(-0.662714 * distance()) + 0.0340312 * exp(-0.0765993 * distance())) * pow((width1() + width2()) / 2 , 0.0856766)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal5,ctm
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0816742 * exp(-0.868455 * distance()) + 0.120405 * exp(-11.6775 * distance())) * pow((width1() + width2()) / 2 , 0.0913947) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0816742 * exp(-0.868455 * distance()) + 0.120405 * exp(-11.6775 * distance())) * pow((width1() + width2()) / 2 , 0.0913947)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0816742 * exp(-0.868455 * distance()) + 0.120405 * exp(-11.6775 * distance())) * pow((width1() + width2()) / 2 , 0.0913947) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0816742 * exp(-0.868455 * distance()) + 0.120405 * exp(-11.6775 * distance())) * pow((width1() + width2()) / 2 , 0.0913947)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00677733 + 0.0451694 * exp(-0.363733 * distance())) * pow((width1() + width2()) / 2 , 0.260476) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00677733 + 0.0451694 * exp(-0.363733 * distance())) * pow((width1() + width2()) / 2 , 0.260476)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00677733 + 0.0451694 * exp(-0.363733 * distance())) * pow((width1() + width2()) / 2 , 0.260476) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00677733 + 0.0451694 * exp(-0.363733 * distance())) * pow((width1() + width2()) / 2 , 0.260476)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0433973 * exp(-0.678527 * distance()) + 0.0286845 * exp(-0.0960841 * distance())) * pow((width1() + width2()) / 2 , 0.106255) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0433973 * exp(-0.678527 * distance()) + 0.0286845 * exp(-0.0960841 * distance())) * pow((width1() + width2()) / 2 , 0.106255)
    }
]
CAPACITANCE NEARBODY metal6 ctm INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0433973 * exp(-0.678527 * distance()) + 0.0286845 * exp(-0.0960841 * distance())) * pow((width1() + width2()) / 2 , 0.106255) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0433973 * exp(-0.678527 * distance()) + 0.0286845 * exp(-0.0960841 * distance())) * pow((width1() + width2()) / 2 , 0.106255)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal5,ctm,metal6
//
CAPACITANCE NEARBODY ctm metal6 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0315065 * exp(-0.792197 * distance()) + 0.0199906 * exp(-0.141811 * distance())) * pow((width1() + width2()) / 2 , 0.167111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0315065 * exp(-0.792197 * distance()) + 0.0199906 * exp(-0.141811 * distance())) * pow((width1() + width2()) / 2 , 0.167111)
    }
]
CAPACITANCE NEARBODY metal6 ctm INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    profile = field_profileid
    max_distance = (MAXDISTANCE_ctm + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0315065 * exp(-0.792197 * distance()) + 0.0199906 * exp(-0.141811 * distance())) * pow((width1() + width2()) / 2 , 0.167111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0315065 * exp(-0.792197 * distance()) + 0.0199906 * exp(-0.141811 * distance())) * pow((width1() + width2()) / 2 , 0.167111)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0209631 * exp(-0.689636 * distance()) + 0.0209631 * exp(-0.689628 * distance())) * pow((width1() + width2()) / 2 , 0.267492) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0209631 * exp(-0.689636 * distance()) + 0.0209631 * exp(-0.689628 * distance())) * pow((width1() + width2()) / 2 , 0.267492)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0209631 * exp(-0.689636 * distance()) + 0.0209631 * exp(-0.689628 * distance())) * pow((width1() + width2()) / 2 , 0.267492) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0209631 * exp(-0.689636 * distance()) + 0.0209631 * exp(-0.689628 * distance())) * pow((width1() + width2()) / 2 , 0.267492)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0225682 * exp(-0.581834 * distance()) + 0.0225682 * exp(-0.581736 * distance())) * pow((width1() + width2()) / 2 , 0.267284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0225682 * exp(-0.581834 * distance()) + 0.0225682 * exp(-0.581736 * distance())) * pow((width1() + width2()) / 2 , 0.267284)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0225682 * exp(-0.581834 * distance()) + 0.0225682 * exp(-0.581736 * distance())) * pow((width1() + width2()) / 2 , 0.267284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0225682 * exp(-0.581834 * distance()) + 0.0225682 * exp(-0.581736 * distance())) * pow((width1() + width2()) / 2 , 0.267284)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0231236 * exp(-0.537205 * distance()) + 0.0231236 * exp(-0.537303 * distance())) * pow((width1() + width2()) / 2 , 0.269661) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0231236 * exp(-0.537205 * distance()) + 0.0231236 * exp(-0.537303 * distance())) * pow((width1() + width2()) / 2 , 0.269661)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0231236 * exp(-0.537205 * distance()) + 0.0231236 * exp(-0.537303 * distance())) * pow((width1() + width2()) / 2 , 0.269661) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0231236 * exp(-0.537205 * distance()) + 0.0231236 * exp(-0.537303 * distance())) * pow((width1() + width2()) / 2 , 0.269661)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,ctm
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0232472 * exp(-0.526095 * distance()) + 0.0232472 * exp(-0.526095 * distance())) * pow((width1() + width2()) / 2 , 0.270371) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0232472 * exp(-0.526095 * distance()) + 0.0232472 * exp(-0.526095 * distance())) * pow((width1() + width2()) / 2 , 0.270371)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0232472 * exp(-0.526095 * distance()) + 0.0232472 * exp(-0.526095 * distance())) * pow((width1() + width2()) / 2 , 0.270371) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0232472 * exp(-0.526095 * distance()) + 0.0232472 * exp(-0.526095 * distance())) * pow((width1() + width2()) / 2 , 0.270371)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0233933 * exp(-0.511965 * distance()) + 0.0233933 * exp(-0.511966 * distance())) * pow((width1() + width2()) / 2 , 0.271302) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0233933 * exp(-0.511965 * distance()) + 0.0233933 * exp(-0.511966 * distance())) * pow((width1() + width2()) / 2 , 0.271302)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0233933 * exp(-0.511965 * distance()) + 0.0233933 * exp(-0.511966 * distance())) * pow((width1() + width2()) / 2 , 0.271302) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0233933 * exp(-0.511965 * distance()) + 0.0233933 * exp(-0.511966 * distance())) * pow((width1() + width2()) / 2 , 0.271302)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00920542 * exp(-0.371111 * distance()) + 0.00920542 * exp(-0.370714 * distance())) * pow((width1() + width2()) / 2 , 0.356579) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00920542 * exp(-0.371111 * distance()) + 0.00920542 * exp(-0.370714 * distance())) * pow((width1() + width2()) / 2 , 0.356579)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00920542 * exp(-0.371111 * distance()) + 0.00920542 * exp(-0.370714 * distance())) * pow((width1() + width2()) / 2 , 0.356579) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00920542 * exp(-0.371111 * distance()) + 0.00920542 * exp(-0.370714 * distance())) * pow((width1() + width2()) / 2 , 0.356579)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0104149 * exp(-0.311814 * distance()) + 0.0104149 * exp(-0.311896 * distance())) * pow((width1() + width2()) / 2 , 0.334916) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0104149 * exp(-0.311814 * distance()) + 0.0104149 * exp(-0.311896 * distance())) * pow((width1() + width2()) / 2 , 0.334916)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0104149 * exp(-0.311814 * distance()) + 0.0104149 * exp(-0.311896 * distance())) * pow((width1() + width2()) / 2 , 0.334916) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0104149 * exp(-0.311814 * distance()) + 0.0104149 * exp(-0.311896 * distance())) * pow((width1() + width2()) / 2 , 0.334916)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,ctm
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106619 * exp(-0.297198 * distance()) + 0.0106619 * exp(-0.297627 * distance())) * pow((width1() + width2()) / 2 , 0.332348) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106619 * exp(-0.297198 * distance()) + 0.0106619 * exp(-0.297627 * distance())) * pow((width1() + width2()) / 2 , 0.332348)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106619 * exp(-0.297198 * distance()) + 0.0106619 * exp(-0.297627 * distance())) * pow((width1() + width2()) / 2 , 0.332348) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106619 * exp(-0.297198 * distance()) + 0.0106619 * exp(-0.297627 * distance())) * pow((width1() + width2()) / 2 , 0.332348)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0109436 * exp(-0.279111 * distance()) + 0.0109436 * exp(-0.279413 * distance())) * pow((width1() + width2()) / 2 , 0.329871) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0109436 * exp(-0.279111 * distance()) + 0.0109436 * exp(-0.279413 * distance())) * pow((width1() + width2()) / 2 , 0.329871)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0109436 * exp(-0.279111 * distance()) + 0.0109436 * exp(-0.279413 * distance())) * pow((width1() + width2()) / 2 , 0.329871) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0109436 * exp(-0.279111 * distance()) + 0.0109436 * exp(-0.279413 * distance())) * pow((width1() + width2()) / 2 , 0.329871)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00533354 * exp(-0.252609 * distance()) + 0.00533349 * exp(-0.25269 * distance())) * pow((width1() + width2()) / 2 , 0.39255) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00533354 * exp(-0.252609 * distance()) + 0.00533349 * exp(-0.25269 * distance())) * pow((width1() + width2()) / 2 , 0.39255)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00533354 * exp(-0.252609 * distance()) + 0.00533349 * exp(-0.25269 * distance())) * pow((width1() + width2()) / 2 , 0.39255) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00533354 * exp(-0.252609 * distance()) + 0.00533349 * exp(-0.25269 * distance())) * pow((width1() + width2()) / 2 , 0.39255)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4,ctm
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00581849 * exp(-0.232538 * distance()) + 0.00581848 * exp(-0.232815 * distance())) * pow((width1() + width2()) / 2 , 0.373195) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00581849 * exp(-0.232538 * distance()) + 0.00581848 * exp(-0.232815 * distance())) * pow((width1() + width2()) / 2 , 0.373195)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00581849 * exp(-0.232538 * distance()) + 0.00581848 * exp(-0.232815 * distance())) * pow((width1() + width2()) / 2 , 0.373195) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00581849 * exp(-0.232538 * distance()) + 0.00581848 * exp(-0.232815 * distance())) * pow((width1() + width2()) / 2 , 0.373195)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0063327 * exp(-0.208158 * distance()) + 0.00633269 * exp(-0.208371 * distance())) * pow((width1() + width2()) / 2 , 0.358487) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0063327 * exp(-0.208158 * distance()) + 0.00633269 * exp(-0.208371 * distance())) * pow((width1() + width2()) / 2 , 0.358487)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0063327 * exp(-0.208158 * distance()) + 0.00633269 * exp(-0.208371 * distance())) * pow((width1() + width2()) / 2 , 0.358487) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0063327 * exp(-0.208158 * distance()) + 0.00633269 * exp(-0.208371 * distance())) * pow((width1() + width2()) / 2 , 0.358487)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5,ctm
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00255705 * exp(-0.211474 * distance()) + 0.00255096 * exp(-0.21148 * distance())) * pow((width1() + width2()) / 2 , 0.516858) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00255705 * exp(-0.211474 * distance()) + 0.00255096 * exp(-0.21148 * distance())) * pow((width1() + width2()) / 2 , 0.516858)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00255705 * exp(-0.211474 * distance()) + 0.00255096 * exp(-0.21148 * distance())) * pow((width1() + width2()) / 2 , 0.516858) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00255705 * exp(-0.211474 * distance()) + 0.00255096 * exp(-0.21148 * distance())) * pow((width1() + width2()) / 2 , 0.516858)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359336 * exp(-0.177907 * distance()) + 0.00359338 * exp(-0.177946 * distance())) * pow((width1() + width2()) / 2 , 0.404562) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359336 * exp(-0.177907 * distance()) + 0.00359338 * exp(-0.177946 * distance())) * pow((width1() + width2()) / 2 , 0.404562)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359336 * exp(-0.177907 * distance()) + 0.00359338 * exp(-0.177946 * distance())) * pow((width1() + width2()) / 2 , 0.404562) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359336 * exp(-0.177907 * distance()) + 0.00359338 * exp(-0.177946 * distance())) * pow((width1() + width2()) / 2 , 0.404562)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,ctm,metal6
//
CAPACITANCE NEARBODY metal1 ctm INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00248485 * exp(-0.166404 * distance()) + 0.0024849 * exp(-0.166801 * distance())) * pow((width1() + width2()) / 2 , 0.487035) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00248485 * exp(-0.166404 * distance()) + 0.0024849 * exp(-0.166801 * distance())) * pow((width1() + width2()) / 2 , 0.487035)
    }
]
CAPACITANCE NEARBODY ctm metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00248485 * exp(-0.166404 * distance()) + 0.0024849 * exp(-0.166801 * distance())) * pow((width1() + width2()) / 2 , 0.487035) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00248485 * exp(-0.166404 * distance()) + 0.0024849 * exp(-0.166801 * distance())) * pow((width1() + width2()) / 2 , 0.487035)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0234381 * exp(-0.570293 * distance()) + 0.0234381 * exp(-0.570401 * distance())) * pow((width1() + width2()) / 2 , 0.255066) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0234381 * exp(-0.570293 * distance()) + 0.0234381 * exp(-0.570401 * distance())) * pow((width1() + width2()) / 2 , 0.255066)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0234381 * exp(-0.570293 * distance()) + 0.0234381 * exp(-0.570401 * distance())) * pow((width1() + width2()) / 2 , 0.255066) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0234381 * exp(-0.570293 * distance()) + 0.0234381 * exp(-0.570401 * distance())) * pow((width1() + width2()) / 2 , 0.255066)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0255976 * exp(-0.477359 * distance()) + 0.0255976 * exp(-0.477543 * distance())) * pow((width1() + width2()) / 2 , 0.24742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0255976 * exp(-0.477359 * distance()) + 0.0255976 * exp(-0.477543 * distance())) * pow((width1() + width2()) / 2 , 0.24742)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0255976 * exp(-0.477359 * distance()) + 0.0255976 * exp(-0.477543 * distance())) * pow((width1() + width2()) / 2 , 0.24742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0255976 * exp(-0.477359 * distance()) + 0.0255976 * exp(-0.477543 * distance())) * pow((width1() + width2()) / 2 , 0.24742)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,ctm
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0260155 * exp(-0.457276 * distance()) + 0.0260155 * exp(-0.456972 * distance())) * pow((width1() + width2()) / 2 , 0.247323) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0260155 * exp(-0.457276 * distance()) + 0.0260155 * exp(-0.456972 * distance())) * pow((width1() + width2()) / 2 , 0.247323)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0260155 * exp(-0.457276 * distance()) + 0.0260155 * exp(-0.456972 * distance())) * pow((width1() + width2()) / 2 , 0.247323) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0260155 * exp(-0.457276 * distance()) + 0.0260155 * exp(-0.456972 * distance())) * pow((width1() + width2()) / 2 , 0.247323)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0264832 * exp(-0.43041 * distance()) + 0.0264832 * exp(-0.434611 * distance())) * pow((width1() + width2()) / 2 , 0.247702) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0264832 * exp(-0.43041 * distance()) + 0.0264832 * exp(-0.434611 * distance())) * pow((width1() + width2()) / 2 , 0.247702)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0264832 * exp(-0.43041 * distance()) + 0.0264832 * exp(-0.434611 * distance())) * pow((width1() + width2()) / 2 , 0.247702) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0264832 * exp(-0.43041 * distance()) + 0.0264832 * exp(-0.434611 * distance())) * pow((width1() + width2()) / 2 , 0.247702)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111256 * exp(-0.311021 * distance()) + 0.0111259 * exp(-0.310981 * distance())) * pow((width1() + width2()) / 2 , 0.33475) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111256 * exp(-0.311021 * distance()) + 0.0111259 * exp(-0.310981 * distance())) * pow((width1() + width2()) / 2 , 0.33475)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111256 * exp(-0.311021 * distance()) + 0.0111259 * exp(-0.310981 * distance())) * pow((width1() + width2()) / 2 , 0.33475) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111256 * exp(-0.311021 * distance()) + 0.0111259 * exp(-0.310981 * distance())) * pow((width1() + width2()) / 2 , 0.33475)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4,ctm
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120013 * exp(-0.285826 * distance()) + 0.0120014 * exp(-0.286059 * distance())) * pow((width1() + width2()) / 2 , 0.317169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120013 * exp(-0.285826 * distance()) + 0.0120014 * exp(-0.286059 * distance())) * pow((width1() + width2()) / 2 , 0.317169)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120013 * exp(-0.285826 * distance()) + 0.0120014 * exp(-0.286059 * distance())) * pow((width1() + width2()) / 2 , 0.317169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120013 * exp(-0.285826 * distance()) + 0.0120014 * exp(-0.286059 * distance())) * pow((width1() + width2()) / 2 , 0.317169)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0129085 * exp(-0.257019 * distance()) + 0.0129087 * exp(-0.25639 * distance())) * pow((width1() + width2()) / 2 , 0.304547) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0129085 * exp(-0.257019 * distance()) + 0.0129087 * exp(-0.25639 * distance())) * pow((width1() + width2()) / 2 , 0.304547)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0129085 * exp(-0.257019 * distance()) + 0.0129087 * exp(-0.25639 * distance())) * pow((width1() + width2()) / 2 , 0.304547) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0129085 * exp(-0.257019 * distance()) + 0.0129087 * exp(-0.25639 * distance())) * pow((width1() + width2()) / 2 , 0.304547)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5,ctm
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00507332 * exp(-0.247746 * distance()) + 0.00507295 * exp(-0.24813 * distance())) * pow((width1() + width2()) / 2 , 0.473776) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00507332 * exp(-0.247746 * distance()) + 0.00507295 * exp(-0.24813 * distance())) * pow((width1() + width2()) / 2 , 0.473776)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00507332 * exp(-0.247746 * distance()) + 0.00507295 * exp(-0.24813 * distance())) * pow((width1() + width2()) / 2 , 0.473776) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00507332 * exp(-0.247746 * distance()) + 0.00507295 * exp(-0.24813 * distance())) * pow((width1() + width2()) / 2 , 0.473776)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00698715 * exp(-0.207519 * distance()) + 0.00698715 * exp(-0.207424 * distance())) * pow((width1() + width2()) / 2 , 0.359536) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00698715 * exp(-0.207519 * distance()) + 0.00698715 * exp(-0.207424 * distance())) * pow((width1() + width2()) / 2 , 0.359536)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00698715 * exp(-0.207519 * distance()) + 0.00698715 * exp(-0.207424 * distance())) * pow((width1() + width2()) / 2 , 0.359536) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00698715 * exp(-0.207519 * distance()) + 0.00698715 * exp(-0.207424 * distance())) * pow((width1() + width2()) / 2 , 0.359536)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,ctm,metal6
//
CAPACITANCE NEARBODY metal2 ctm INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00480425 * exp(-0.195946 * distance()) + 0.00480405 * exp(-0.194922 * distance())) * pow((width1() + width2()) / 2 , 0.446027) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00480425 * exp(-0.195946 * distance()) + 0.00480405 * exp(-0.194922 * distance())) * pow((width1() + width2()) / 2 , 0.446027)
    }
]
CAPACITANCE NEARBODY ctm metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00480425 * exp(-0.195946 * distance()) + 0.00480405 * exp(-0.194922 * distance())) * pow((width1() + width2()) / 2 , 0.446027) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00480425 * exp(-0.195946 * distance()) + 0.00480405 * exp(-0.194922 * distance())) * pow((width1() + width2()) / 2 , 0.446027)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.024253 * exp(-0.517194 * distance()) + 0.0242531 * exp(-0.517302 * distance())) * pow((width1() + width2()) / 2 , 0.258098) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.024253 * exp(-0.517194 * distance()) + 0.0242531 * exp(-0.517302 * distance())) * pow((width1() + width2()) / 2 , 0.258098)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.024253 * exp(-0.517194 * distance()) + 0.0242531 * exp(-0.517302 * distance())) * pow((width1() + width2()) / 2 , 0.258098) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.024253 * exp(-0.517194 * distance()) + 0.0242531 * exp(-0.517302 * distance())) * pow((width1() + width2()) / 2 , 0.258098)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4,ctm
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0255412 * exp(-0.471198 * distance()) + 0.0255412 * exp(-0.471088 * distance())) * pow((width1() + width2()) / 2 , 0.250265) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0255412 * exp(-0.471198 * distance()) + 0.0255412 * exp(-0.471088 * distance())) * pow((width1() + width2()) / 2 , 0.250265)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0255412 * exp(-0.471198 * distance()) + 0.0255412 * exp(-0.471088 * distance())) * pow((width1() + width2()) / 2 , 0.250265) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0255412 * exp(-0.471198 * distance()) + 0.0255412 * exp(-0.471088 * distance())) * pow((width1() + width2()) / 2 , 0.250265)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0269407 * exp(-0.545676 * distance()) + 0.0267764 * exp(-0.329648 * distance())) * pow((width1() + width2()) / 2 , 0.245986) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0269407 * exp(-0.545676 * distance()) + 0.0267764 * exp(-0.329648 * distance())) * pow((width1() + width2()) / 2 , 0.245986)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0269407 * exp(-0.545676 * distance()) + 0.0267764 * exp(-0.329648 * distance())) * pow((width1() + width2()) / 2 , 0.245986) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0269407 * exp(-0.545676 * distance()) + 0.0267764 * exp(-0.329648 * distance())) * pow((width1() + width2()) / 2 , 0.245986)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5,ctm
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00912532 * exp(-0.330542 * distance()) + 0.00912533 * exp(-0.329974 * distance())) * pow((width1() + width2()) / 2 , 0.423012) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00912532 * exp(-0.330542 * distance()) + 0.00912533 * exp(-0.329974 * distance())) * pow((width1() + width2()) / 2 , 0.423012)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00912532 * exp(-0.330542 * distance()) + 0.00912533 * exp(-0.329974 * distance())) * pow((width1() + width2()) / 2 , 0.423012) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00912532 * exp(-0.330542 * distance()) + 0.00912533 * exp(-0.329974 * distance())) * pow((width1() + width2()) / 2 , 0.423012)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.012098 * exp(-0.269663 * distance()) + 0.0120982 * exp(-0.270733 * distance())) * pow((width1() + width2()) / 2 , 0.321617) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.012098 * exp(-0.269663 * distance()) + 0.0120982 * exp(-0.270733 * distance())) * pow((width1() + width2()) / 2 , 0.321617)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.012098 * exp(-0.269663 * distance()) + 0.0120982 * exp(-0.270733 * distance())) * pow((width1() + width2()) / 2 , 0.321617) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.012098 * exp(-0.269663 * distance()) + 0.0120982 * exp(-0.270733 * distance())) * pow((width1() + width2()) / 2 , 0.321617)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,ctm,metal6
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00812574 * exp(-0.252262 * distance()) + 0.00811366 * exp(-0.25236 * distance())) * pow((width1() + width2()) / 2 , 0.408085) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00812574 * exp(-0.252262 * distance()) + 0.00811366 * exp(-0.25236 * distance())) * pow((width1() + width2()) / 2 , 0.408085)
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00812574 * exp(-0.252262 * distance()) + 0.00811366 * exp(-0.25236 * distance())) * pow((width1() + width2()) / 2 , 0.408085) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00812574 * exp(-0.252262 * distance()) + 0.00811366 * exp(-0.25236 * distance())) * pow((width1() + width2()) / 2 , 0.408085)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5,ctm
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0203758 * exp(-0.623843 * distance()) + 0.0203756 * exp(-0.625238 * distance())) * pow((width1() + width2()) / 2 , 0.316268) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0203758 * exp(-0.623843 * distance()) + 0.0203756 * exp(-0.625238 * distance())) * pow((width1() + width2()) / 2 , 0.316268)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF poly ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0203758 * exp(-0.623843 * distance()) + 0.0203756 * exp(-0.625238 * distance())) * pow((width1() + width2()) / 2 , 0.316268) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0203758 * exp(-0.623843 * distance()) + 0.0203756 * exp(-0.625238 * distance())) * pow((width1() + width2()) / 2 , 0.316268)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0250038 * exp(-0.476536 * distance()) + 0.0250038 * exp(-0.476849 * distance())) * pow((width1() + width2()) / 2 , 0.257442) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0250038 * exp(-0.476536 * distance()) + 0.0250038 * exp(-0.476849 * distance())) * pow((width1() + width2()) / 2 , 0.257442)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0250038 * exp(-0.476536 * distance()) + 0.0250038 * exp(-0.476849 * distance())) * pow((width1() + width2()) / 2 , 0.257442) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0250038 * exp(-0.476536 * distance()) + 0.0250038 * exp(-0.476849 * distance())) * pow((width1() + width2()) / 2 , 0.257442)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,ctm,metal6
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0152057 * exp(-0.396031 * distance()) + 0.0152057 * exp(-0.396034 * distance())) * pow((width1() + width2()) / 2 , 0.352935) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0152057 * exp(-0.396031 * distance()) + 0.0152057 * exp(-0.396034 * distance())) * pow((width1() + width2()) / 2 , 0.352935)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0152057 * exp(-0.396031 * distance()) + 0.0152057 * exp(-0.396034 * distance())) * pow((width1() + width2()) / 2 , 0.352935) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0152057 * exp(-0.396031 * distance()) + 0.0152057 * exp(-0.396034 * distance())) * pow((width1() + width2()) / 2 , 0.352935)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal5,ctm,metal6
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.113072 * exp(-12.6483 * distance()) + 0.0830372 * exp(-1.0957 * distance())) * pow((width1() + width2()) / 2 , 0.0833781) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.113072 * exp(-12.6483 * distance()) + 0.0830372 * exp(-1.0957 * distance())) * pow((width1() + width2()) / 2 , 0.0833781)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.113072 * exp(-12.6483 * distance()) + 0.0830372 * exp(-1.0957 * distance())) * pow((width1() + width2()) / 2 , 0.0833781) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.113072 * exp(-12.6483 * distance()) + 0.0830372 * exp(-1.0957 * distance())) * pow((width1() + width2()) / 2 , 0.0833781)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0218677 * exp(-0.659304 * distance()) + 0.0218677 * exp(-0.658405 * distance())) * pow((width1() + width2()) / 2 , 0.256371) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0218677 * exp(-0.659304 * distance()) + 0.0218677 * exp(-0.658405 * distance())) * pow((width1() + width2()) / 2 , 0.256371)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0218677 * exp(-0.659304 * distance()) + 0.0218677 * exp(-0.658405 * distance())) * pow((width1() + width2()) / 2 , 0.256371) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0218677 * exp(-0.659304 * distance()) + 0.0218677 * exp(-0.658405 * distance())) * pow((width1() + width2()) / 2 , 0.256371)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236863 * exp(-0.554743 * distance()) + 0.0236863 * exp(-0.554174 * distance())) * pow((width1() + width2()) / 2 , 0.256276) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236863 * exp(-0.554743 * distance()) + 0.0236863 * exp(-0.554174 * distance())) * pow((width1() + width2()) / 2 , 0.256276)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236863 * exp(-0.554743 * distance()) + 0.0236863 * exp(-0.554174 * distance())) * pow((width1() + width2()) / 2 , 0.256276) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236863 * exp(-0.554743 * distance()) + 0.0236863 * exp(-0.554174 * distance())) * pow((width1() + width2()) / 2 , 0.256276)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,ctm
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0240206 * exp(-0.532626 * distance()) + 0.0240206 * exp(-0.532569 * distance())) * pow((width1() + width2()) / 2 , 0.25761) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0240206 * exp(-0.532626 * distance()) + 0.0240206 * exp(-0.532569 * distance())) * pow((width1() + width2()) / 2 , 0.25761)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0240206 * exp(-0.532626 * distance()) + 0.0240206 * exp(-0.532569 * distance())) * pow((width1() + width2()) / 2 , 0.25761) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0240206 * exp(-0.532626 * distance()) + 0.0240206 * exp(-0.532569 * distance())) * pow((width1() + width2()) / 2 , 0.25761)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0243836 * exp(-0.506718 * distance()) + 0.0243835 * exp(-0.506769 * distance())) * pow((width1() + width2()) / 2 , 0.259571) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0243836 * exp(-0.506718 * distance()) + 0.0243835 * exp(-0.506769 * distance())) * pow((width1() + width2()) / 2 , 0.259571)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0243836 * exp(-0.506718 * distance()) + 0.0243835 * exp(-0.506769 * distance())) * pow((width1() + width2()) / 2 , 0.259571) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0243836 * exp(-0.506718 * distance()) + 0.0243835 * exp(-0.506769 * distance())) * pow((width1() + width2()) / 2 , 0.259571)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0099587 * exp(-0.359407 * distance()) + 0.00995874 * exp(-0.357902 * distance())) * pow((width1() + width2()) / 2 , 0.356431) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0099587 * exp(-0.359407 * distance()) + 0.00995874 * exp(-0.357902 * distance())) * pow((width1() + width2()) / 2 , 0.356431)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0099587 * exp(-0.359407 * distance()) + 0.00995874 * exp(-0.357902 * distance())) * pow((width1() + width2()) / 2 , 0.356431) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0099587 * exp(-0.359407 * distance()) + 0.00995874 * exp(-0.357902 * distance())) * pow((width1() + width2()) / 2 , 0.356431)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4,ctm
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106823 * exp(-0.330065 * distance()) + 0.0106818 * exp(-0.330159 * distance())) * pow((width1() + width2()) / 2 , 0.341758) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106823 * exp(-0.330065 * distance()) + 0.0106818 * exp(-0.330159 * distance())) * pow((width1() + width2()) / 2 , 0.341758)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106823 * exp(-0.330065 * distance()) + 0.0106818 * exp(-0.330159 * distance())) * pow((width1() + width2()) / 2 , 0.341758) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106823 * exp(-0.330065 * distance()) + 0.0106818 * exp(-0.330159 * distance())) * pow((width1() + width2()) / 2 , 0.341758)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114069 * exp(-0.297483 * distance()) + 0.0114064 * exp(-0.297181 * distance())) * pow((width1() + width2()) / 2 , 0.332124) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114069 * exp(-0.297483 * distance()) + 0.0114064 * exp(-0.297181 * distance())) * pow((width1() + width2()) / 2 , 0.332124)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114069 * exp(-0.297483 * distance()) + 0.0114064 * exp(-0.297181 * distance())) * pow((width1() + width2()) / 2 , 0.332124) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114069 * exp(-0.297483 * distance()) + 0.0114064 * exp(-0.297181 * distance())) * pow((width1() + width2()) / 2 , 0.332124)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5,ctm
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00445271 * exp(-0.286307 * distance()) + 0.0044524 * exp(-0.286384 * distance())) * pow((width1() + width2()) / 2 , 0.502036) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00445271 * exp(-0.286307 * distance()) + 0.0044524 * exp(-0.286384 * distance())) * pow((width1() + width2()) / 2 , 0.502036)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00445271 * exp(-0.286307 * distance()) + 0.0044524 * exp(-0.286384 * distance())) * pow((width1() + width2()) / 2 , 0.502036) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00445271 * exp(-0.286307 * distance()) + 0.0044524 * exp(-0.286384 * distance())) * pow((width1() + width2()) / 2 , 0.502036)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0060535 * exp(-0.240105 * distance()) + 0.0060535 * exp(-0.23984 * distance())) * pow((width1() + width2()) / 2 , 0.392503) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0060535 * exp(-0.240105 * distance()) + 0.0060535 * exp(-0.23984 * distance())) * pow((width1() + width2()) / 2 , 0.392503)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0060535 * exp(-0.240105 * distance()) + 0.0060535 * exp(-0.23984 * distance())) * pow((width1() + width2()) / 2 , 0.392503) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0060535 * exp(-0.240105 * distance()) + 0.0060535 * exp(-0.23984 * distance())) * pow((width1() + width2()) / 2 , 0.392503)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,ctm,metal6
//
CAPACITANCE NEARBODY metal2 ctm INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00414842 * exp(-0.227474 * distance()) + 0.0041484 * exp(-0.228043 * distance())) * pow((width1() + width2()) / 2 , 0.479752) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00414842 * exp(-0.227474 * distance()) + 0.0041484 * exp(-0.228043 * distance())) * pow((width1() + width2()) / 2 , 0.479752)
    }
]
CAPACITANCE NEARBODY ctm metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00414842 * exp(-0.227474 * distance()) + 0.0041484 * exp(-0.228043 * distance())) * pow((width1() + width2()) / 2 , 0.479752) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00414842 * exp(-0.227474 * distance()) + 0.0041484 * exp(-0.228043 * distance())) * pow((width1() + width2()) / 2 , 0.479752)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0237013 * exp(-0.554129 * distance()) + 0.0237014 * exp(-0.554037 * distance())) * pow((width1() + width2()) / 2 , 0.255767) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0237013 * exp(-0.554129 * distance()) + 0.0237014 * exp(-0.554037 * distance())) * pow((width1() + width2()) / 2 , 0.255767)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0237013 * exp(-0.554129 * distance()) + 0.0237014 * exp(-0.554037 * distance())) * pow((width1() + width2()) / 2 , 0.255767) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0237013 * exp(-0.554129 * distance()) + 0.0237014 * exp(-0.554037 * distance())) * pow((width1() + width2()) / 2 , 0.255767)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4,ctm
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249061 * exp(-0.506529 * distance()) + 0.0249061 * exp(-0.506306 * distance())) * pow((width1() + width2()) / 2 , 0.249377) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249061 * exp(-0.506529 * distance()) + 0.0249061 * exp(-0.506306 * distance())) * pow((width1() + width2()) / 2 , 0.249377)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249061 * exp(-0.506529 * distance()) + 0.0249061 * exp(-0.506306 * distance())) * pow((width1() + width2()) / 2 , 0.249377) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249061 * exp(-0.506529 * distance()) + 0.0249061 * exp(-0.506306 * distance())) * pow((width1() + width2()) / 2 , 0.249377)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.026052 * exp(-0.457338 * distance()) + 0.026052 * exp(-0.457509 * distance())) * pow((width1() + width2()) / 2 , 0.24665) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.026052 * exp(-0.457338 * distance()) + 0.026052 * exp(-0.457509 * distance())) * pow((width1() + width2()) / 2 , 0.24665)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.026052 * exp(-0.457338 * distance()) + 0.026052 * exp(-0.457509 * distance())) * pow((width1() + width2()) / 2 , 0.24665) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.026052 * exp(-0.457338 * distance()) + 0.026052 * exp(-0.457509 * distance())) * pow((width1() + width2()) / 2 , 0.24665)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5,ctm
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00880968 * exp(-0.358309 * distance()) + 0.00880968 * exp(-0.358239 * distance())) * pow((width1() + width2()) / 2 , 0.42467) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00880968 * exp(-0.358309 * distance()) + 0.00880968 * exp(-0.358239 * distance())) * pow((width1() + width2()) / 2 , 0.42467)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00880968 * exp(-0.358309 * distance()) + 0.00880968 * exp(-0.358239 * distance())) * pow((width1() + width2()) / 2 , 0.42467) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00880968 * exp(-0.358309 * distance()) + 0.00880968 * exp(-0.358239 * distance())) * pow((width1() + width2()) / 2 , 0.42467)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115955 * exp(-0.294451 * distance()) + 0.0115953 * exp(-0.294198 * distance())) * pow((width1() + width2()) / 2 , 0.326468) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115955 * exp(-0.294451 * distance()) + 0.0115953 * exp(-0.294198 * distance())) * pow((width1() + width2()) / 2 , 0.326468)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115955 * exp(-0.294451 * distance()) + 0.0115953 * exp(-0.294198 * distance())) * pow((width1() + width2()) / 2 , 0.326468) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115955 * exp(-0.294451 * distance()) + 0.0115953 * exp(-0.294198 * distance())) * pow((width1() + width2()) / 2 , 0.326468)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,ctm,metal6
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00776373 * exp(-0.276144 * distance()) + 0.00776364 * exp(-0.275905 * distance())) * pow((width1() + width2()) / 2 , 0.413543) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00776373 * exp(-0.276144 * distance()) + 0.00776364 * exp(-0.275905 * distance())) * pow((width1() + width2()) / 2 , 0.413543)
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00776373 * exp(-0.276144 * distance()) + 0.00776364 * exp(-0.275905 * distance())) * pow((width1() + width2()) / 2 , 0.413543) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00776373 * exp(-0.276144 * distance()) + 0.00776364 * exp(-0.275905 * distance())) * pow((width1() + width2()) / 2 , 0.413543)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5,ctm
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0202111 * exp(-0.645336 * distance()) + 0.020211 * exp(-0.645831 * distance())) * pow((width1() + width2()) / 2 , 0.313127) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0202111 * exp(-0.645336 * distance()) + 0.020211 * exp(-0.645831 * distance())) * pow((width1() + width2()) / 2 , 0.313127)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal1 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0202111 * exp(-0.645336 * distance()) + 0.020211 * exp(-0.645831 * distance())) * pow((width1() + width2()) / 2 , 0.313127) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0202111 * exp(-0.645336 * distance()) + 0.020211 * exp(-0.645831 * distance())) * pow((width1() + width2()) / 2 , 0.313127)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0247366 * exp(-0.496348 * distance()) + 0.0247366 * exp(-0.496271 * distance())) * pow((width1() + width2()) / 2 , 0.256004) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0247366 * exp(-0.496348 * distance()) + 0.0247366 * exp(-0.496271 * distance())) * pow((width1() + width2()) / 2 , 0.256004)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0247366 * exp(-0.496348 * distance()) + 0.0247366 * exp(-0.496271 * distance())) * pow((width1() + width2()) / 2 , 0.256004) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0247366 * exp(-0.496348 * distance()) + 0.0247366 * exp(-0.496271 * distance())) * pow((width1() + width2()) / 2 , 0.256004)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,ctm,metal6
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0150101 * exp(-0.413603 * distance()) + 0.0150101 * exp(-0.414711 * distance())) * pow((width1() + width2()) / 2 , 0.351752) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0150101 * exp(-0.413603 * distance()) + 0.0150101 * exp(-0.414711 * distance())) * pow((width1() + width2()) / 2 , 0.351752)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0150101 * exp(-0.413603 * distance()) + 0.0150101 * exp(-0.414711 * distance())) * pow((width1() + width2()) / 2 , 0.351752) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0150101 * exp(-0.413603 * distance()) + 0.0150101 * exp(-0.414711 * distance())) * pow((width1() + width2()) / 2 , 0.351752)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal5,ctm,metal6
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.112102 * exp(-12.7581 * distance()) + 0.083642 * exp(-1.12482 * distance())) * pow((width1() + width2()) / 2 , 0.0820141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.112102 * exp(-12.7581 * distance()) + 0.083642 * exp(-1.12482 * distance())) * pow((width1() + width2()) / 2 , 0.0820141)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.112102 * exp(-12.7581 * distance()) + 0.083642 * exp(-1.12482 * distance())) * pow((width1() + width2()) / 2 , 0.0820141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.112102 * exp(-12.7581 * distance()) + 0.083642 * exp(-1.12482 * distance())) * pow((width1() + width2()) / 2 , 0.0820141)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0218678 * exp(-0.658542 * distance()) + 0.0218677 * exp(-0.659169 * distance())) * pow((width1() + width2()) / 2 , 0.256371) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0218678 * exp(-0.658542 * distance()) + 0.0218677 * exp(-0.659169 * distance())) * pow((width1() + width2()) / 2 , 0.256371)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0218678 * exp(-0.658542 * distance()) + 0.0218677 * exp(-0.659169 * distance())) * pow((width1() + width2()) / 2 , 0.256371) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0218678 * exp(-0.658542 * distance()) + 0.0218677 * exp(-0.659169 * distance())) * pow((width1() + width2()) / 2 , 0.256371)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4,ctm
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0228683 * exp(-0.604236 * distance()) + 0.0228683 * exp(-0.602693 * distance())) * pow((width1() + width2()) / 2 , 0.254779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0228683 * exp(-0.604236 * distance()) + 0.0228683 * exp(-0.602693 * distance())) * pow((width1() + width2()) / 2 , 0.254779)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0228683 * exp(-0.604236 * distance()) + 0.0228683 * exp(-0.602693 * distance())) * pow((width1() + width2()) / 2 , 0.254779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0228683 * exp(-0.604236 * distance()) + 0.0228683 * exp(-0.602693 * distance())) * pow((width1() + width2()) / 2 , 0.254779)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0237783 * exp(-0.548517 * distance()) + 0.0237783 * exp(-0.548706 * distance())) * pow((width1() + width2()) / 2 , 0.256587) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0237783 * exp(-0.548517 * distance()) + 0.0237783 * exp(-0.548706 * distance())) * pow((width1() + width2()) / 2 , 0.256587)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0237783 * exp(-0.548517 * distance()) + 0.0237783 * exp(-0.548706 * distance())) * pow((width1() + width2()) / 2 , 0.256587) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0237783 * exp(-0.548517 * distance()) + 0.0237783 * exp(-0.548706 * distance())) * pow((width1() + width2()) / 2 , 0.256587)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5,ctm
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00784795 * exp(-0.42807 * distance()) + 0.007848 * exp(-0.42791 * distance())) * pow((width1() + width2()) / 2 , 0.440378) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00784795 * exp(-0.42807 * distance()) + 0.007848 * exp(-0.42791 * distance())) * pow((width1() + width2()) / 2 , 0.440378)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00784795 * exp(-0.42807 * distance()) + 0.007848 * exp(-0.42791 * distance())) * pow((width1() + width2()) / 2 , 0.440378) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00784795 * exp(-0.42807 * distance()) + 0.007848 * exp(-0.42791 * distance())) * pow((width1() + width2()) / 2 , 0.440378)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101639 * exp(-0.350406 * distance()) + 0.0101636 * exp(-0.351394 * distance())) * pow((width1() + width2()) / 2 , 0.351741) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101639 * exp(-0.350406 * distance()) + 0.0101636 * exp(-0.351394 * distance())) * pow((width1() + width2()) / 2 , 0.351741)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101639 * exp(-0.350406 * distance()) + 0.0101636 * exp(-0.351394 * distance())) * pow((width1() + width2()) / 2 , 0.351741) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101639 * exp(-0.350406 * distance()) + 0.0101636 * exp(-0.351394 * distance())) * pow((width1() + width2()) / 2 , 0.351741)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,ctm,metal6
//
CAPACITANCE NEARBODY metal3 ctm INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00676716 * exp(-0.33177 * distance()) + 0.00675815 * exp(-0.331995 * distance())) * pow((width1() + width2()) / 2 , 0.440707) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00676716 * exp(-0.33177 * distance()) + 0.00675815 * exp(-0.331995 * distance())) * pow((width1() + width2()) / 2 , 0.440707)
    }
]
CAPACITANCE NEARBODY ctm metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00676716 * exp(-0.33177 * distance()) + 0.00675815 * exp(-0.331995 * distance())) * pow((width1() + width2()) / 2 , 0.440707) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00676716 * exp(-0.33177 * distance()) + 0.00675815 * exp(-0.331995 * distance())) * pow((width1() + width2()) / 2 , 0.440707)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5,ctm
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0197602 * exp(-0.696683 * distance()) + 0.0197601 * exp(-0.696678 * distance())) * pow((width1() + width2()) / 2 , 0.305799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0197602 * exp(-0.696683 * distance()) + 0.0197601 * exp(-0.696678 * distance())) * pow((width1() + width2()) / 2 , 0.305799)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal2 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0197602 * exp(-0.696683 * distance()) + 0.0197601 * exp(-0.696678 * distance())) * pow((width1() + width2()) / 2 , 0.305799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0197602 * exp(-0.696683 * distance()) + 0.0197601 * exp(-0.696678 * distance())) * pow((width1() + width2()) / 2 , 0.305799)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0240482 * exp(-0.540512 * distance()) + 0.0240482 * exp(-0.540667 * distance())) * pow((width1() + width2()) / 2 , 0.253535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0240482 * exp(-0.540512 * distance()) + 0.0240482 * exp(-0.540667 * distance())) * pow((width1() + width2()) / 2 , 0.253535)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0240482 * exp(-0.540512 * distance()) + 0.0240482 * exp(-0.540667 * distance())) * pow((width1() + width2()) / 2 , 0.253535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0240482 * exp(-0.540512 * distance()) + 0.0240482 * exp(-0.540667 * distance())) * pow((width1() + width2()) / 2 , 0.253535)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,ctm,metal6
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014518 * exp(-0.4545 * distance()) + 0.0145179 * exp(-0.455295 * distance())) * pow((width1() + width2()) / 2 , 0.350355) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014518 * exp(-0.4545 * distance()) + 0.0145179 * exp(-0.455295 * distance())) * pow((width1() + width2()) / 2 , 0.350355)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014518 * exp(-0.4545 * distance()) + 0.0145179 * exp(-0.455295 * distance())) * pow((width1() + width2()) / 2 , 0.350355) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014518 * exp(-0.4545 * distance()) + 0.0145179 * exp(-0.455295 * distance())) * pow((width1() + width2()) / 2 , 0.350355)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal5,ctm,metal6
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0845774 * exp(-1.17878 * distance()) + 0.110415 * exp(-12.9817 * distance())) * pow((width1() + width2()) / 2 , 0.0793992) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0845774 * exp(-1.17878 * distance()) + 0.110415 * exp(-12.9817 * distance())) * pow((width1() + width2()) / 2 , 0.0793992)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0845774 * exp(-1.17878 * distance()) + 0.110415 * exp(-12.9817 * distance())) * pow((width1() + width2()) / 2 , 0.0793992) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0845774 * exp(-1.17878 * distance()) + 0.110415 * exp(-12.9817 * distance())) * pow((width1() + width2()) / 2 , 0.0793992)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5,ctm
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal3 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0183975 * exp(-0.828674 * distance()) + 0.0183975 * exp(-0.828653 * distance())) * pow((width1() + width2()) / 2 , 0.292087) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0183975 * exp(-0.828674 * distance()) + 0.0183975 * exp(-0.828653 * distance())) * pow((width1() + width2()) / 2 , 0.292087)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal3 ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0183975 * exp(-0.828674 * distance()) + 0.0183975 * exp(-0.828653 * distance())) * pow((width1() + width2()) / 2 , 0.292087) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0183975 * exp(-0.828674 * distance()) + 0.0183975 * exp(-0.828653 * distance())) * pow((width1() + width2()) / 2 , 0.292087)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0221595 * exp(-0.642904 * distance()) + 0.0221594 * exp(-0.64318 * distance())) * pow((width1() + width2()) / 2 , 0.255514) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0221595 * exp(-0.642904 * distance()) + 0.0221594 * exp(-0.64318 * distance())) * pow((width1() + width2()) / 2 , 0.255514)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0221595 * exp(-0.642904 * distance()) + 0.0221594 * exp(-0.64318 * distance())) * pow((width1() + width2()) / 2 , 0.255514) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0221595 * exp(-0.642904 * distance()) + 0.0221594 * exp(-0.64318 * distance())) * pow((width1() + width2()) / 2 , 0.255514)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,ctm,metal6
//
CAPACITANCE NEARBODY metal4 ctm INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0131913 * exp(-0.544277 * distance()) + 0.0131912 * exp(-0.544841 * distance())) * pow((width1() + width2()) / 2 , 0.355874) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0131913 * exp(-0.544277 * distance()) + 0.0131912 * exp(-0.544841 * distance())) * pow((width1() + width2()) / 2 , 0.355874)
    }
]
CAPACITANCE NEARBODY ctm metal4 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0131913 * exp(-0.544277 * distance()) + 0.0131912 * exp(-0.544841 * distance())) * pow((width1() + width2()) / 2 , 0.355874) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0131913 * exp(-0.544277 * distance()) + 0.0131912 * exp(-0.544841 * distance())) * pow((width1() + width2()) / 2 , 0.355874)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal5,ctm,metal6
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0859306 * exp(-1.28291 * distance()) + 0.107337 * exp(-13.4193 * distance())) * pow((width1() + width2()) / 2 , 0.073567) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0859306 * exp(-1.28291 * distance()) + 0.107337 * exp(-13.4193 * distance())) * pow((width1() + width2()) / 2 , 0.073567)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0859306 * exp(-1.28291 * distance()) + 0.107337 * exp(-13.4193 * distance())) * pow((width1() + width2()) / 2 , 0.073567) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0859306 * exp(-1.28291 * distance()) + 0.107337 * exp(-13.4193 * distance())) * pow((width1() + width2()) / 2 , 0.073567)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal4,metal5,ctm,metal6
//
CAPACITANCE NEARBODY metal5 ctm INSIDE OF metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0986794 * exp(-14.6413 * distance()) + 0.0891516 * exp(-1.62358 * distance())) * pow((width1() + width2()) / 2 , 0.0578675) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0986794 * exp(-14.6413 * distance()) + 0.0891516 * exp(-1.62358 * distance())) * pow((width1() + width2()) / 2 , 0.0578675)
    }
]
CAPACITANCE NEARBODY ctm metal5 INSIDE OF metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    profile = field_profileid
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_ctm) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0986794 * exp(-14.6413 * distance()) + 0.0891516 * exp(-1.62358 * distance())) * pow((width1() + width2()) / 2 , 0.0578675) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0986794 * exp(-14.6413 * distance()) + 0.0891516 * exp(-1.62358 * distance())) * pow((width1() + width2()) / 2 , 0.0578675)
    }
]
//
//  model: 514  target: nearbody514  profile: field  layers: field_base,poly,metal1,metal2,metal3,metal4,metal5,ctm,metal6
//
VARIABLE m514k1 0.854443
VARIABLE m514k2 1.18916
//
//  model: 43  target: nearbody43  profile: field  layers: field_base,poly,metal1,metal2,metal3,metal4,metal5,ctm,metal6
//
VARIABLE m43nbk1 0.586652
VARIABLE m43nbk2 0.0687699
VARIABLE m43nbk3 0.032615
VARIABLE m43nbk4 0.999455
VARIABLE m43nbk5 1
VARIABLE m43nbk6 0
//
//  model: 43  target: intrinsic43  profile: field  layers: field_base,poly,metal1,metal2,metal3,metal4,metal5,ctm,metal6
//
VARIABLE m43ink1 0.162236
VARIABLE m43ink2 0.0621658
VARIABLE m43ink3 0
VARIABLE m43ink4 0.957968
VARIABLE m43ink5 0.129554
VARIABLE m43ink6 0.292955
#endif // $XCAL_EXCL_PROF_field
#ifdef $XCALIBRATE_EXCLUDE_PROFILE_active
    #define $XCAL_EXCL_PROF_active
#endif // ifdef $XCALIBRATE_EXCLUDE_PROFILE_active
#ifdef $XCALIBRATE_INCLUDE_PROFILE_active
    #define $XCAL_INCL_PROF_active
#endif // ifdef $XCALIBRATE_INCLUDE_PROFILE_active
#ifndef $XCAL_EXCL_PROF_active
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal1 tndiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.044351 * area()
]
CAPACITANCE CROSSOVER PLATE metal1 tpdiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.044351 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal2 tndiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.0156387 * area()
]
CAPACITANCE CROSSOVER PLATE metal2 tpdiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.0156387 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal3 tndiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00949546 * area()
]
CAPACITANCE CROSSOVER PLATE metal3 tpdiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00949546 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal4 tndiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00681744 * area()
]
CAPACITANCE CROSSOVER PLATE metal4 tpdiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00681744 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal5 tndiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00531766 * area()
]
CAPACITANCE CROSSOVER PLATE metal5 tpdiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00531766 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE ctm tndiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00488511 * area()
]
CAPACITANCE CROSSOVER PLATE ctm tpdiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00488511 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal6 tndiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00428352 * area()
]
CAPACITANCE CROSSOVER PLATE metal6 tpdiff MASK
[
    PROPERTY C
    profile = active_profileid
    C = 0.00428352 * area()
]
//
//  model: 34  target: intrinsicns  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.150131 * (1 - exp(-0.633748 * (distance() + 0.153031))) * pow(width() , 0.00857048 * distance() + 0.00315488) * (0.0474741 * thickness() + 0.27458) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0423051 * (1 - exp(-0.667551 * (distance() + 0.0625275))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.160447 * pow(width() , 0.0670384) * (0.104682 * thickness() + 0.241499) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0458682 * pow(width() , 0.0247154) * (1 - 0.431455 * exp(-0.262753 * radius_up()))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.150131 * (1 - exp(-0.633748 * (distance() + 0.153031))) * pow(width() , 0.00857048 * distance() + 0.00315488) * (0.0474741 * thickness() + 0.27458) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0423051 * (1 - exp(-0.667551 * (distance() + 0.0625275))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.160447 * pow(width() , 0.0670384) * (0.104682 * thickness() + 0.241499) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0458682 * pow(width() , 0.0247154) * (1 - 0.431455 * exp(-0.262753 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.06298 - 0.988588 * distance()) + 0.21002 / pow(distance() , 1.0347)) * 3.19268 * pow((width1() + width2()) / 2 , 0.0863613) * (0.0378786 * (thickness1() + thickness2()) / 2 + 0.0111878) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.06298 - 0.988588 * distance()) + 0.21002 / pow(distance() , 1.0347)) * 3.19268 * pow((width1() + width2()) / 2 , 0.0863613) * (0.0378786 * (thickness1() + thickness2()) / 2 + 0.0111878) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
]
//
//  model: 34  target: intrinsicns  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.125481 * (1 - exp(-0.313032 * (distance() + 0.452842))) * pow(width() , 0.0146981 * distance() + -0.0326591) * (0.0215744 * thickness() + 0.258673) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0312607 * (1 - exp(-0.343026 * (distance() + 0.171758))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.143999 * pow(width() , 0.0790768) * (0.0712342 * thickness() + 0.213849) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0350774 * pow(width() , 0.0206337) * (1 - 0.528511 * exp(-0.153912 * radius_up()))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.125481 * (1 - exp(-0.313032 * (distance() + 0.452842))) * pow(width() , 0.0146981 * distance() + -0.0326591) * (0.0215744 * thickness() + 0.258673) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0312607 * (1 - exp(-0.343026 * (distance() + 0.171758))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.143999 * pow(width() , 0.0790768) * (0.0712342 * thickness() + 0.213849) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0350774 * pow(width() , 0.0206337) * (1 - 0.528511 * exp(-0.153912 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.2108 - 0.508038 * distance()) + 0.415737 / pow(distance() , 0.889314)) * 3.94119 * pow((width1() + width2()) / 2 , 0.11754) * (0.0195828 * (thickness1() + thickness2()) / 2 + 0.00848526) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.2108 - 0.508038 * distance()) + 0.415737 / pow(distance() , 0.889314)) * 3.94119 * pow((width1() + width2()) / 2 , 0.11754) * (0.0195828 * (thickness1() + thickness2()) / 2 + 0.00848526) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
]
//
//  model: 34  target: intrinsicns  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.104547 * (1 - exp(-0.25199 * (distance() + 0.708877))) * pow(width() , 0.0140472 * distance() + -0.0432877) * (0.0093731 * thickness() + 0.256894) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.025802 * (1 - exp(-0.259721 * (distance() + 0.263392))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.136545 * pow(width() , 0.0800017) * (0.0581287 * thickness() + 0.201077) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0308443 * pow(width() , 0.0139681) * (1 - 0.557104 * exp(-0.10589 * radius_up()))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.104547 * (1 - exp(-0.25199 * (distance() + 0.708877))) * pow(width() , 0.0140472 * distance() + -0.0432877) * (0.0093731 * thickness() + 0.256894) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.025802 * (1 - exp(-0.259721 * (distance() + 0.263392))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.136545 * pow(width() , 0.0800017) * (0.0581287 * thickness() + 0.201077) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0308443 * pow(width() , 0.0139681) * (1 - 0.557104 * exp(-0.10589 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.459085 - 0.307198 * distance()) + 1.30965 / pow(distance() , 0.840082)) * 3.99949 * pow((width1() + width2()) / 2 , 0.127523) * (0.00675294 * (thickness1() + thickness2()) / 2 + 0.00313522) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.459085 - 0.307198 * distance()) + 1.30965 / pow(distance() , 0.840082)) * 3.99949 * pow((width1() + width2()) / 2 , 0.127523) * (0.00675294 * (thickness1() + thickness2()) / 2 + 0.00313522) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
]
//
//  model: 34  target: intrinsicns  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.082827 * (1 - exp(-0.255128 * (distance() + 0.872788))) * pow(width() , 0.0108379 * distance() + -0.0425996) * (0.00116193 * thickness() + 0.261158) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0217602 * (1 - exp(-0.226248 * (distance() + 0.330623))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.132265 * pow(width() , 0.0784975) * (0.0500558 * thickness() + 0.19109) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0284067 * pow(width() , 0.00788438) * (1 - 0.57587 * exp(-0.0779743 * radius_up()))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.082827 * (1 - exp(-0.255128 * (distance() + 0.872788))) * pow(width() , 0.0108379 * distance() + -0.0425996) * (0.00116193 * thickness() + 0.261158) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0217602 * (1 - exp(-0.226248 * (distance() + 0.330623))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.132265 * pow(width() , 0.0784975) * (0.0500558 * thickness() + 0.19109) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0284067 * pow(width() , 0.00788438) * (1 - 0.57587 * exp(-0.0779743 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(0.00444817 - 0.204158 * distance()) + 2.33937 / pow(distance() , 0.825474)) * 4.09587 * pow((width1() + width2()) / 2 , 0.132097) * (0.00378289 * (thickness1() + thickness2()) / 2 + 0.00181478) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(0.00444817 - 0.204158 * distance()) + 2.33937 / pow(distance() , 0.825474)) * 4.09587 * pow((width1() + width2()) / 2 , 0.132097) * (0.00378289 * (thickness1() + thickness2()) / 2 + 0.00181478) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
]
//
//  model: 34  target: intrinsicns  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0656621 * (1 - exp(-0.276357 * (distance() + 0.992393))) * pow(width() , 0.00789135 * distance() + -0.0437273) * (-0.00485035 * thickness() + 0.267478) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0187839 * (1 - exp(-0.205072 * (distance() + 0.385342))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.128946 * pow(width() , 0.0759742) * (0.0438285 * thickness() + 0.183559) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.32))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.32)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0260041 * pow(width() , 0) * (1 - 0.641733 * exp(-0.0887684 * radius_up()))
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0656621 * (1 - exp(-0.276357 * (distance() + 0.992393))) * pow(width() , 0.00789135 * distance() + -0.0437273) * (-0.00485035 * thickness() + 0.267478) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0187839 * (1 - exp(-0.205072 * (distance() + 0.385342))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.128946 * pow(width() , 0.0759742) * (0.0438285 * thickness() + 0.183559) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.32))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.32)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0260041 * pow(width() , 0) * (1 - 0.641733 * exp(-0.0887684 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(1.26294 - 0.148993 * distance()) + 7.77892 / pow(distance() , 0.828075)) * 4.16352 * pow((width1() + width2()) / 2 , 0.136309) * (0.00110254 * (thickness1() + thickness2()) / 2 + 0.00054245) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.32))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.32)))
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(1.26294 - 0.148993 * distance()) + 7.77892 / pow(distance() , 0.828075)) * 4.16352 * pow((width1() + width2()) / 2 , 0.136309) * (0.00110254 * (thickness1() + thickness2()) / 2 + 0.00054245) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.32))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.32)))
]
//
//  model: 34  target: intrinsicns  profile: active  layers: active_base,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE ctm tndiff MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    max_distance = MAXDISTANCE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.118306 * (1 - exp(-0.870335 * (distance() + 0.390435))) * pow(width() , 0.0971377 * distance() + -0.527836) * (0.00416239 * thickness() + 0.109979) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0448372 * (1 - exp(-0.0540701 * (distance() + 0.346144))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.144647 * pow(width() , 0.0500829) * (0.0297778 * thickness() + 0.176111) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.869))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.869)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0299356 * pow(width() , 0.016746) * (1 - 0.875599 * exp(-0.0648327 * radius_up()))
    }
]
CAPACITANCE CROSSOVER FRINGE ctm tpdiff MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    max_distance = MAXDISTANCE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.118306 * (1 - exp(-0.870335 * (distance() + 0.390435))) * pow(width() , 0.0971377 * distance() + -0.527836) * (0.00416239 * thickness() + 0.109979) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0448372 * (1 - exp(-0.0540701 * (distance() + 0.346144))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.144647 * pow(width() , 0.0500829) * (0.0297778 * thickness() + 0.176111) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.869))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.869)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0299356 * pow(width() , 0.016746) * (1 - 0.875599 * exp(-0.0648327 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: active  layers: active_base,ctm,tndiff,tpdiff
//
CAPACITANCE NEARBODY ctm INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.71069 - 0.234917 * distance()) + 0.282967 / pow(distance() , 0.527623)) * 2.62297 * pow((width1() + width2()) / 2 , 0.126753) * (0.0210206 * (thickness1() + thickness2()) / 2 + 0.040258) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.869))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.869)))
]
CAPACITANCE NEARBODY ctm INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    max_distance = MAXDISTANCE_ctm
    C = length() * (exp(-1.71069 - 0.234917 * distance()) + 0.282967 / pow(distance() , 0.527623)) * 2.62297 * pow((width1() + width2()) / 2 , 0.126753) * (0.0210206 * (thickness1() + thickness2()) / 2 + 0.040258) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.869))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.869)))
]
//
//  model: 34  target: intrinsicns  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal6 tndiff MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0678907 * (1 - exp(-0.173351 * (distance() + 1.13918))) * pow(width() , 0.0526918 * distance() + -0.335327) * (0.00257482 * thickness() + 0.260089) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.85))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0472934 * (1 - exp(-0.046317 * (distance() + 0.238471))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.85))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.85)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0892528 * pow(width() , 0.0304076) * (0.0244105 * thickness() + 0.275584) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 7.85))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 7.85)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0299356 * pow(width() , 0.016746) * (1 - 0.875599 * exp(-0.0648327 * radius_up()))
    }
]
CAPACITANCE CROSSOVER FRINGE metal6 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0678907 * (1 - exp(-0.173351 * (distance() + 1.13918))) * pow(width() , 0.0526918 * distance() + -0.335327) * (0.00257482 * thickness() + 0.260089) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.85))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.85)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0472934 * (1 - exp(-0.046317 * (distance() + 0.238471))) * m34IN_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 7.85))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 7.85)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0892528 * pow(width() , 0.0304076) * (0.0244105 * thickness() + 0.275584) * m34IN0_COEF * (1 - m34RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.5 + m43ink3 * 7.85))) * (1 - m34RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.5 + m43ink6 * 7.85)))
    }
    if ((distance() <= 0.0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0299356 * pow(width() , 0.016746) * (1 - 0.875599 * exp(-0.0648327 * radius_up()))
    }
]
//
//  model: 34  target: nearbodyns  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-2.34087 - 0.196164 * distance()) + 1.1843 / pow(distance() , 0.783648)) * 2.73755 * pow((width1() + width2()) / 2 , 0.0988645) * (0.00868636 * (thickness1() + thickness2()) / 2 + 0.0160354) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 7.85))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 7.85)))
]
CAPACITANCE NEARBODY metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 6 // For reference
    max_caldistance = 4.5 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-2.34087 - 0.196164 * distance()) + 1.1843 / pow(distance() , 0.783648)) * 2.73755 * pow((width1() + width2()) / 2 , 0.0988645) * (0.00868636 * (thickness1() + thickness2()) / 2 + 0.0160354) * m34NB_COEF * (1 - m34RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 7.85))) * (1 - m34RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 7.85)))
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.07376 - 1.63978 * distance()) + 0.0212455 / pow(distance() , 1.5694)) * 3.74206 * pow((width1() + width2()) / 2 , 0.0398623) * (0.0874061 * (thickness1() + thickness2()) / 2 + 0.0203347) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-2.78618 - 1.55724 * distance()) + 0.00772432 / pow(distance() , 1.35583)) * 1.12327 * pow((width1() + width2()) / 2 , 0.0292712) * pow(radius_up() , 0.0182838)
    }
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.07376 - 1.63978 * distance()) + 0.0212455 / pow(distance() , 1.5694)) * 3.74206 * pow((width1() + width2()) / 2 , 0.0398623) * (0.0874061 * (thickness1() + thickness2()) / 2 + 0.0203347) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-2.78618 - 1.55724 * distance()) + 0.00772432 / pow(distance() , 1.35583)) * 1.12327 * pow((width1() + width2()) / 2 , 0.0292712) * pow(radius_up() , 0.0182838)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0979362 * (1 - exp(-0.904856 * (distance()))) * pow(width() , 0.0133257 * distance() + -0.0344818) * (0.0367628 * thickness() + 0.239406)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0241909 * (1 - exp(-1.08433 * (distance() + -0.0507942)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119197 * pow(width() , 0.0147374) * (0.106335 * thickness() + 0.146552)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00066853 * distance() + 0.117458) * (0.405198 - exp(-(-0.0438891 * distance() + 0.0403937) * (enclose() + 5.86557 * distance() + 1.5049) - (0.0201182 * distance() + 0.0635223) * (enclose() + 0.676092 * distance() + 3.20209) * (enclose() + 0.676092 * distance() + 3.20209)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0979362 * (1 - exp(-0.904856 * (distance()))) * pow(width() , 0.0133257 * distance() + -0.0344818) * (0.0367628 * thickness() + 0.239406)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0085388 * (1 - exp(-1.57749 * (distance() + 0.38417))) * pow(width() , 0.00285657 * distance() + -0.00243654) * pow(radius_up() , 0.262469)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0570577 * (0.405198 - exp(0.179052 * (enclose() + 30.8327) - 0.164113 * (enclose() + 6.58255) * (enclose() + 6.58255)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0979362 * (1 - exp(-0.904856 * (distance()))) * pow(width() , 0.0133257 * distance() + -0.0344818) * (0.0367628 * thickness() + 0.239406)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0241909 * (1 - exp(-1.08433 * (distance() + -0.0507942)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119197 * pow(width() , 0.0147374) * (0.106335 * thickness() + 0.146552)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00066853 * distance() + 0.117458) * (0.405198 - exp(-(-0.0438891 * distance() + 0.0403937) * (enclose() + 5.86557 * distance() + 1.5049) - (0.0201182 * distance() + 0.0635223) * (enclose() + 0.676092 * distance() + 3.20209) * (enclose() + 0.676092 * distance() + 3.20209)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0979362 * (1 - exp(-0.904856 * (distance()))) * pow(width() , 0.0133257 * distance() + -0.0344818) * (0.0367628 * thickness() + 0.239406)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0085388 * (1 - exp(-1.57749 * (distance() + 0.38417))) * pow(width() , 0.00285657 * distance() + -0.00243654) * pow(radius_up() , 0.262469)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0570577 * (0.405198 - exp(0.179052 * (enclose() + 30.8327) - 0.164113 * (enclose() + 6.58255) * (enclose() + 6.58255)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.106345 * (1 - exp(-0.91782 * (distance()))) * pow(width() , 0.0129071 * distance() + -0.0332039) * (0.0314374 * thickness() + 0.244557) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0265564 * (1 - exp(-1.09002 * (distance() + -0.048685))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0312544 * (1 - exp(-0.937062 * (distance() + 0.784362))) * pow(width() , 0.00079227 * distance() + -0.00122629) * pow(radius_up() , 0.0179992)
    }
    if (distance() <= 0.0) {
        C = length() * 0.124371 * pow(width() , 0.0146856) * (0.10723 * thickness() + 0.156701) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.106345 * (1 - exp(-0.91782 * (distance()))) * pow(width() , 0.0129071 * distance() + -0.0332039) * (0.0314374 * thickness() + 0.244557) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0265564 * (1 - exp(-1.09002 * (distance() + -0.048685))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0312544 * (1 - exp(-0.937062 * (distance() + 0.784362))) * pow(width() , 0.00079227 * distance() + -0.00122629) * pow(radius_up() , 0.0179992)
    }
    if (distance() <= 0.0) {
        C = length() * 0.124371 * pow(width() , 0.0146856) * (0.10723 * thickness() + 0.156701) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.15244 - 1.19749 * distance()) + 0.0489265 / pow(distance() , 1.33225)) * 4.04039 * pow((width1() + width2()) / 2 , 0.06849) * (0.0624986 * (thickness1() + thickness2()) / 2 + 0.0172891) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.19534 - 1.22146 * distance()) + 0.00950185 / pow(distance() , 1.20462)) * 1.38612 * pow((width1() + width2()) / 2 , 0.0558491) * pow(radius_up() , 0.00883486)
    }
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.15244 - 1.19749 * distance()) + 0.0489265 / pow(distance() , 1.33225)) * 4.04039 * pow((width1() + width2()) / 2 , 0.06849) * (0.0624986 * (thickness1() + thickness2()) / 2 + 0.0172891) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.19534 - 1.22146 * distance()) + 0.00950185 / pow(distance() , 1.20462)) * 1.38612 * pow((width1() + width2()) / 2 , 0.0558491) * pow(radius_up() , 0.00883486)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0556604 * (1 - exp(-0.602512 * (distance() + 0.0602345))) * pow(width() , 0.0207184 * distance() + -0.0575526) * (0.106318 * thickness() + 0.201171)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0133638 * (1 - exp(-0.744321 * (distance() + -0.0252451)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0963469 * pow(width() , 0.0374742) * (0.0873654 * thickness() + 0.0951057)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00052724 * distance() + 0.0364412) * (0.724836 - exp(-(0.186312 * distance() + 0.164095) * (enclose() + -0.0588073 * distance() + 1.22075) - (-6.21907e-06 * distance() + 2.63626e-05) * (enclose() + -34.4445 * distance() + 8.29244) * (enclose() + -34.4445 * distance() + 8.29244)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0556604 * (1 - exp(-0.602512 * (distance() + 0.0602345))) * pow(width() , 0.0207184 * distance() + -0.0575526) * (0.106318 * thickness() + 0.201171)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0017425 * (1 - exp(-0.732181 * (distance() + 0.594472))) * pow(width() , 0.0316981 * distance() + -0.157352) * pow(radius_up() , 0.824513)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0169025 * (0.724836 - exp(-1.09566 * (enclose() + 0.926713) - -4.73275e-06 * (enclose() + -163.93) * (enclose() + -163.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0556604 * (1 - exp(-0.602512 * (distance() + 0.0602345))) * pow(width() , 0.0207184 * distance() + -0.0575526) * (0.106318 * thickness() + 0.201171)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0133638 * (1 - exp(-0.744321 * (distance() + -0.0252451)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0963469 * pow(width() , 0.0374742) * (0.0873654 * thickness() + 0.0951057)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00052724 * distance() + 0.0364412) * (0.724836 - exp(-(0.186312 * distance() + 0.164095) * (enclose() + -0.0588073 * distance() + 1.22075) - (-6.21907e-06 * distance() + 2.63626e-05) * (enclose() + -34.4445 * distance() + 8.29244) * (enclose() + -34.4445 * distance() + 8.29244)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0556604 * (1 - exp(-0.602512 * (distance() + 0.0602345))) * pow(width() , 0.0207184 * distance() + -0.0575526) * (0.106318 * thickness() + 0.201171)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0017425 * (1 - exp(-0.732181 * (distance() + 0.594472))) * pow(width() , 0.0316981 * distance() + -0.157352) * pow(radius_up() , 0.824513)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0169025 * (0.724836 - exp(-1.09566 * (enclose() + 0.926713) - -4.73275e-06 * (enclose() + -163.93) * (enclose() + -163.93)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.122212 * (1 - exp(-0.685572 * (distance() + 0.0474891))) * pow(width() , 0.01614 * distance() + -0.0460628) * (0.0617329 * thickness() + 0.248792) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0327653 * (1 - exp(-0.805571 * (distance() + -0.0152566))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0367667 * (1 - exp(-0.764662 * (distance() + 0.946058))) * pow(width() , 0.002339 * distance() + 0.0101591) * pow(radius_up() , 0.00243233)
    }
    if (distance() <= 0.0) {
        C = length() * 0.138526 * pow(width() , 0.0274217) * (0.0891105 * thickness() + 0.19324) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.122212 * (1 - exp(-0.685572 * (distance() + 0.0474891))) * pow(width() , 0.01614 * distance() + -0.0460628) * (0.0617329 * thickness() + 0.248792) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0327653 * (1 - exp(-0.805571 * (distance() + -0.0152566))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0367667 * (1 - exp(-0.764662 * (distance() + 0.946058))) * pow(width() , 0.002339 * distance() + 0.0101591) * pow(radius_up() , 0.00243233)
    }
    if (distance() <= 0.0) {
        C = length() * 0.138526 * pow(width() , 0.0274217) * (0.0891105 * thickness() + 0.19324) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.11589 - 1.0652 * distance()) + 0.078497 / pow(distance() , 1.23016)) * 3.83379 * pow((width1() + width2()) / 2 , 0.07711) * (0.0526964 * (thickness1() + thickness2()) / 2 + 0.0151653) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.29058 - 1.12791 * distance()) + 0.0120967 / pow(distance() , 1.12964)) * 1.33416 * pow((width1() + width2()) / 2 , 0.0667054) * pow(radius_up() , 0.00384396)
    }
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.11589 - 1.0652 * distance()) + 0.078497 / pow(distance() , 1.23016)) * 3.83379 * pow((width1() + width2()) / 2 , 0.07711) * (0.0526964 * (thickness1() + thickness2()) / 2 + 0.0151653) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.29058 - 1.12791 * distance()) + 0.0120967 / pow(distance() , 1.12964)) * 1.33416 * pow((width1() + width2()) / 2 , 0.0667054) * pow(radius_up() , 0.00384396)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0399369 * (1 - exp(-0.533076 * (distance() + 0.112935))) * pow(width() , 0.0213937 * distance() + -0.0566887) * (0.111188 * thickness() + 0.195046)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00935678 * (1 - exp(-0.657748 * (distance() + -9.98096e-06)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0859662 * pow(width() , 0.0496378) * (0.0737374 * thickness() + 0.074098)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00024951 * distance() + 0.0225105) * (0.860222 - exp(-(0.19607 * distance() + 0.150217) * (enclose() + 0.28232 * distance() + 0.72645) - (-0.00516126 * distance() + -0.011297) * (enclose() + 0.971936 * distance() + 2.14173) * (enclose() + 0.971936 * distance() + 2.14173)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0399369 * (1 - exp(-0.533076 * (distance() + 0.112935))) * pow(width() , 0.0213937 * distance() + -0.0566887) * (0.111188 * thickness() + 0.195046)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00026713 * (1 - exp(-0.444157 * (distance() + 0.69206))) * pow(width() , 0.0621118 * distance() + -0.334399) * pow(radius_up() , 1.60232)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0106315 * (0.860222 - exp(-1.13057 * (enclose() + 2.13805) - -0.0371033 * (enclose() + 7.00141) * (enclose() + 7.00141)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0399369 * (1 - exp(-0.533076 * (distance() + 0.112935))) * pow(width() , 0.0213937 * distance() + -0.0566887) * (0.111188 * thickness() + 0.195046)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00935678 * (1 - exp(-0.657748 * (distance() + -9.98096e-06)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0859662 * pow(width() , 0.0496378) * (0.0737374 * thickness() + 0.074098)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00024951 * distance() + 0.0225105) * (0.860222 - exp(-(0.19607 * distance() + 0.150217) * (enclose() + 0.28232 * distance() + 0.72645) - (-0.00516126 * distance() + -0.011297) * (enclose() + 0.971936 * distance() + 2.14173) * (enclose() + 0.971936 * distance() + 2.14173)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0399369 * (1 - exp(-0.533076 * (distance() + 0.112935))) * pow(width() , 0.0213937 * distance() + -0.0566887) * (0.111188 * thickness() + 0.195046)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00026713 * (1 - exp(-0.444157 * (distance() + 0.69206))) * pow(width() , 0.0621118 * distance() + -0.334399) * pow(radius_up() , 1.60232)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0106315 * (0.860222 - exp(-1.13057 * (enclose() + 2.13805) - -0.0371033 * (enclose() + 7.00141) * (enclose() + 7.00141)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.133396 * (1 - exp(-0.62268 * (distance() + 0.0863166))) * pow(width() , 0.0166665 * distance() + -0.0464577) * (0.051997 * thickness() + 0.253374) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0355072 * (1 - exp(-0.726298 * (distance() + 0.00718466))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0402524 * (1 - exp(-0.725303 * (distance() + 1.00806))) * pow(width() , 0.00224293 * distance() + 0.0194557) * pow(radius_up() , -0.0172503)
    }
    if (distance() <= 0.0) {
        C = length() * 0.144816 * pow(width() , 0.0363336) * (0.0875441 * thickness() + 0.207485) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.133396 * (1 - exp(-0.62268 * (distance() + 0.0863166))) * pow(width() , 0.0166665 * distance() + -0.0464577) * (0.051997 * thickness() + 0.253374) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0355072 * (1 - exp(-0.726298 * (distance() + 0.00718466))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0402524 * (1 - exp(-0.725303 * (distance() + 1.00806))) * pow(width() , 0.00224293 * distance() + 0.0194557) * pow(radius_up() , -0.0172503)
    }
    if (distance() <= 0.0) {
        C = length() * 0.144816 * pow(width() , 0.0363336) * (0.0875441 * thickness() + 0.207485) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.14441 - 1.0178 * distance()) + 0.0985654 / pow(distance() , 1.1716)) * 3.8903 * pow((width1() + width2()) / 2 , 0.0805395) * (0.0476446 * (thickness1() + thickness2()) / 2 + 0.0138455) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.45104 - 1.09548 * distance()) + 0.0124189 / pow(distance() , 1.09059)) * 1.44471 * pow((width1() + width2()) / 2 , 0.0715151) * pow(radius_up() , 0.00108946)
    }
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.14441 - 1.0178 * distance()) + 0.0985654 / pow(distance() , 1.1716)) * 3.8903 * pow((width1() + width2()) / 2 , 0.0805395) * (0.0476446 * (thickness1() + thickness2()) / 2 + 0.0138455) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.45104 - 1.09548 * distance()) + 0.0124189 / pow(distance() , 1.09059)) * 1.44471 * pow((width1() + width2()) / 2 , 0.0715151) * pow(radius_up() , 0.00108946)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0309135 * (1 - exp(-0.519081 * (distance() + 0.140128))) * pow(width() , 0.0201868 * distance() + -0.0492488) * (0.114454 * thickness() + 0.191628)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00719271 * (1 - exp(-0.626904 * (distance() + 0.016653)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0790887 * pow(width() , 0.0570655) * (0.0653743 * thickness() + 0.0615522)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00065493 * distance() + 0.0200798) * (0.879743 - exp(-(0.128563 * distance() + 0.147981) * (enclose() + 0.289857 * distance() + 0.943742) - (-0.00219243 * distance() + -0.0136589) * (enclose() + 1.00936 * distance() + 2.49671) * (enclose() + 1.00936 * distance() + 2.49671)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0309135 * (1 - exp(-0.519081 * (distance() + 0.140128))) * pow(width() , 0.0201868 * distance() + -0.0492488) * (0.114454 * thickness() + 0.191628)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00132496 * (1 - exp(-0.003259 * (distance() + 1.20866))) * pow(width() , 0.130274 * distance() + -0.589748) * pow(radius_up() , 2.55487)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00840257 * (0.879743 - exp(-0.790796 * (enclose() + 2.39303) - -0.024621 * (enclose() + 7.54351) * (enclose() + 7.54351)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0309135 * (1 - exp(-0.519081 * (distance() + 0.140128))) * pow(width() , 0.0201868 * distance() + -0.0492488) * (0.114454 * thickness() + 0.191628)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00719271 * (1 - exp(-0.626904 * (distance() + 0.016653)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0790887 * pow(width() , 0.0570655) * (0.0653743 * thickness() + 0.0615522)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00065493 * distance() + 0.0200798) * (0.879743 - exp(-(0.128563 * distance() + 0.147981) * (enclose() + 0.289857 * distance() + 0.943742) - (-0.00219243 * distance() + -0.0136589) * (enclose() + 1.00936 * distance() + 2.49671) * (enclose() + 1.00936 * distance() + 2.49671)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0309135 * (1 - exp(-0.519081 * (distance() + 0.140128))) * pow(width() , 0.0201868 * distance() + -0.0492488) * (0.114454 * thickness() + 0.191628)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00132496 * (1 - exp(-0.003259 * (distance() + 1.20866))) * pow(width() , 0.130274 * distance() + -0.589748) * pow(radius_up() , 2.55487)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00840257 * (0.879743 - exp(-0.790796 * (enclose() + 2.39303) - -0.024621 * (enclose() + 7.54351) * (enclose() + 7.54351)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.136027 * (1 - exp(-0.607029 * (distance() + 0.106505))) * pow(width() , 0.0160701 * distance() + -0.0424546) * (0.0489598 * thickness() + 0.261123) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0369706 * (1 - exp(-0.697006 * (distance() + 0.0206729))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0431946 * (1 - exp(-0.715221 * (distance() + 1.03413))) * pow(width() , 0.00180521 * distance() + 0.0263571) * pow(radius_up() , -0.0364057)
    }
    if (distance() <= 0.0) {
        C = length() * 0.148441 * pow(width() , 0.0421855) * (0.0886026 * thickness() + 0.214985) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.136027 * (1 - exp(-0.607029 * (distance() + 0.106505))) * pow(width() , 0.0160701 * distance() + -0.0424546) * (0.0489598 * thickness() + 0.261123) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0369706 * (1 - exp(-0.697006 * (distance() + 0.0206729))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0431946 * (1 - exp(-0.715221 * (distance() + 1.03413))) * pow(width() , 0.00180521 * distance() + 0.0263571) * pow(radius_up() , -0.0364057)
    }
    if (distance() <= 0.0) {
        C = length() * 0.148441 * pow(width() , 0.0421855) * (0.0886026 * thickness() + 0.214985) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,ctm,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.15681 - 1.00762 * distance()) + 0.10504 / pow(distance() , 1.15493)) * 3.9057 * pow((width1() + width2()) / 2 , 0.0813698) * (0.0463624 * (thickness1() + thickness2()) / 2 + 0.0135038) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.44639 - 1.08179 * distance()) + 0.0129908 / pow(distance() , 1.08369)) * 1.40142 * pow((width1() + width2()) / 2 , 0.0728909) * pow(radius_up() , 0.00412535)
    }
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.15681 - 1.00762 * distance()) + 0.10504 / pow(distance() , 1.15493)) * 3.9057 * pow((width1() + width2()) / 2 , 0.0813698) * (0.0463624 * (thickness1() + thickness2()) / 2 + 0.0135038) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.44639 - 1.08179 * distance()) + 0.0129908 / pow(distance() , 1.08369)) * 1.40142 * pow((width1() + width2()) / 2 , 0.0728909) * pow(radius_up() , 0.00412535)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 ctm INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0283053 * (1 - exp(-0.518235 * (distance() + 0.147219))) * pow(width() , 0.0196055 * distance() + -0.0460541) * (0.115496 * thickness() + 0.19052)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00657557 * (1 - exp(-0.620314 * (distance() + 0.0216263)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0769489 * pow(width() , 0.0592839) * (0.0625838 * thickness() + 0.0577503)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00075807 * distance() + 0.0196264) * (0.863173 - exp(-(0.111534 * distance() + 0.14728) * (enclose() + 0.196933 * distance() + 1.02882) - (-0.0016999 * distance() + -0.0156227) * (enclose() + 0.843482 * distance() + 2.13843) * (enclose() + 0.843482 * distance() + 2.13843)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0283053 * (1 - exp(-0.518235 * (distance() + 0.147219))) * pow(width() , 0.0196055 * distance() + -0.0460541) * (0.115496 * thickness() + 0.19052)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0018191 * (1 - exp(-0.00968286 * (distance() + 1.16287))) * pow(width() , 0.129071 * distance() + -0.558536) * pow(radius_up() , 1.58255)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00791802 * (0.863173 - exp(-0.70495 * (enclose() + 2.01349) - -0.0241222 * (enclose() + 6.35584) * (enclose() + 6.35584)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 ctm INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0283053 * (1 - exp(-0.518235 * (distance() + 0.147219))) * pow(width() , 0.0196055 * distance() + -0.0460541) * (0.115496 * thickness() + 0.19052)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00657557 * (1 - exp(-0.620314 * (distance() + 0.0216263)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0769489 * pow(width() , 0.0592839) * (0.0625838 * thickness() + 0.0577503)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00075807 * distance() + 0.0196264) * (0.863173 - exp(-(0.111534 * distance() + 0.14728) * (enclose() + 0.196933 * distance() + 1.02882) - (-0.0016999 * distance() + -0.0156227) * (enclose() + 0.843482 * distance() + 2.13843) * (enclose() + 0.843482 * distance() + 2.13843)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0283053 * (1 - exp(-0.518235 * (distance() + 0.147219))) * pow(width() , 0.0196055 * distance() + -0.0460541) * (0.115496 * thickness() + 0.19052)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0018191 * (1 - exp(-0.00968286 * (distance() + 1.16287))) * pow(width() , 0.129071 * distance() + -0.558536) * pow(radius_up() , 1.58255)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00791802 * (0.863173 - exp(-0.70495 * (enclose() + 2.01349) - -0.0241222 * (enclose() + 6.35584) * (enclose() + 6.35584)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.137274 * (1 - exp(-0.605459 * (distance() + 0.111602))) * pow(width() , 0.0157201 * distance() + -0.0404474) * (0.0480208 * thickness() + 0.262172) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.037383 * (1 - exp(-0.690692 * (distance() + 0.0245388))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0412588 * (1 - exp(-0.712618 * (distance() + 1.04014))) * pow(width() , 0.00163363 * distance() + 0.0281874) * pow(radius_up() , -0.00158179)
    }
    if (distance() <= 0.0) {
        C = length() * 0.149512 * pow(width() , 0.0439954) * (0.0891807 * thickness() + 0.217096) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.137274 * (1 - exp(-0.605459 * (distance() + 0.111602))) * pow(width() , 0.0157201 * distance() + -0.0404474) * (0.0480208 * thickness() + 0.262172) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.037383 * (1 - exp(-0.690692 * (distance() + 0.0245388))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0412588 * (1 - exp(-0.712618 * (distance() + 1.04014))) * pow(width() , 0.00163363 * distance() + 0.0281874) * pow(radius_up() , -0.00158179)
    }
    if (distance() <= 0.0) {
        C = length() * 0.149512 * pow(width() , 0.0439954) * (0.0891807 * thickness() + 0.217096) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.18499 - 0.996576 * distance()) + 0.113059 / pow(distance() , 1.13307)) * 3.93464 * pow((width1() + width2()) / 2 , 0.0823904) * (0.0450612 * (thickness1() + thickness2()) / 2 + 0.013171) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.4599 - 1.0725 * distance()) + 0.0132828 / pow(distance() , 1.07729)) * 1.4016 * pow((width1() + width2()) / 2 , 0.0742542) * pow(radius_up() , 0.00169697)
    }
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    C = length() * (exp(-1.18499 - 0.996576 * distance()) + 0.113059 / pow(distance() , 1.13307)) * 3.93464 * pow((width1() + width2()) / 2 , 0.0823904) * (0.0450612 * (thickness1() + thickness2()) / 2 + 0.013171) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 0.8))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 0.8)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.4599 - 1.0725 * distance()) + 0.0132828 / pow(distance() , 1.07729)) * 1.4016 * pow((width1() + width2()) / 2 , 0.0742542) * pow(radius_up() , 0.00169697)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0248997 * (1 - exp(-0.51933 * (distance() + 0.156312))) * pow(width() , 0.0186179 * distance() + -0.0407306) * (0.116217 * thickness() + 0.187622)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00573323 * (1 - exp(-0.613002 * (distance() + 0.0286011)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0744033 * pow(width() , 0.0624286) * (0.0580576 * thickness() + 0.0519253)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0005094 * distance() + 0.0158282) * (0.913444 - exp(-(0.0855773 * distance() + 0.105575) * (enclose() + 0.415474 * distance() + 1.1076) - (-0.00140288 * distance() + -0.00477403) * (enclose() + 1.16875 * distance() + 4.531) * (enclose() + 1.16875 * distance() + 4.531)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0248997 * (1 - exp(-0.51933 * (distance() + 0.156312))) * pow(width() , 0.0186179 * distance() + -0.0407306) * (0.116217 * thickness() + 0.187622)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00131699 * (1 - exp(-0.00597996 * (distance() + 1.94262))) * pow(width() , 0.0910458 * distance() + -0.408086) * pow(radius_up() , 1.87734)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0066406 * (0.913444 - exp(-0.533461 * (enclose() + 3.18497) - -0.0117884 * (enclose() + 10.3747) * (enclose() + 10.3747)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0248997 * (1 - exp(-0.51933 * (distance() + 0.156312))) * pow(width() , 0.0186179 * distance() + -0.0407306) * (0.116217 * thickness() + 0.187622)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00573323 * (1 - exp(-0.613002 * (distance() + 0.0286011)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0744033 * pow(width() , 0.0624286) * (0.0580576 * thickness() + 0.0519253)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0005094 * distance() + 0.0158282) * (0.913444 - exp(-(0.0855773 * distance() + 0.105575) * (enclose() + 0.415474 * distance() + 1.1076) - (-0.00140288 * distance() + -0.00477403) * (enclose() + 1.16875 * distance() + 4.531) * (enclose() + 1.16875 * distance() + 4.531)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0248997 * (1 - exp(-0.51933 * (distance() + 0.156312))) * pow(width() , 0.0186179 * distance() + -0.0407306) * (0.116217 * thickness() + 0.187622)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00131699 * (1 - exp(-0.00597996 * (distance() + 1.94262))) * pow(width() , 0.0910458 * distance() + -0.408086) * pow(radius_up() , 1.87734)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0066406 * (0.913444 - exp(-0.533461 * (enclose() + 3.18497) - -0.0117884 * (enclose() + 10.3747) * (enclose() + 10.3747)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.139432 * (1 - exp(-0.605987 * (distance() + 0.117669))) * pow(width() , 0.0150304 * distance() + -0.0367475) * (0.046656 * thickness() + 0.262596) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.037946 * (1 - exp(-0.683556 * (distance() + 0.0297779))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0428184 * (1 - exp(-0.709114 * (distance() + 1.04332))) * pow(width() , 0.00158093 * distance() + 0.0327378) * pow(radius_up() , -0.0200408)
    }
    if (distance() <= 0.0) {
        C = length() * 0.150999 * pow(width() , 0.0466007) * (0.0901851 * thickness() + 0.219975) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.92 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    profile = active_profileid
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.139432 * (1 - exp(-0.605987 * (distance() + 0.117669))) * pow(width() , 0.0150304 * distance() + -0.0367475) * (0.046656 * thickness() + 0.262596) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.037946 * (1 - exp(-0.683556 * (distance() + 0.0297779))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 0.8)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0428184 * (1 - exp(-0.709114 * (distance() + 1.04332))) * pow(width() , 0.00158093 * distance() + 0.0327378) * pow(radius_up() , -0.0200408)
    }
    if (distance() <= 0.0) {
        C = length() * 0.150999 * pow(width() , 0.0466007) * (0.0901851 * thickness() + 0.219975) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 0.8))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 0.8)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.20748 - 1.12309 * distance()) + 0.0428229 / pow(distance() , 1.39124)) * 3.63458 * pow((width1() + width2()) / 2 , 0.0674116) * (0.0702863 * (thickness1() + thickness2()) / 2 + 0.0225944) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.52407 - 1.07627 * distance()) + 0.0101209 / pow(distance() , 1.13386)) * 1.59919 * pow((width1() + width2()) / 2 , 0.0490676) * pow(radius_up() , 0.0240254)
    }
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.20748 - 1.12309 * distance()) + 0.0428229 / pow(distance() , 1.39124)) * 3.63458 * pow((width1() + width2()) / 2 , 0.0674116) * (0.0702863 * (thickness1() + thickness2()) / 2 + 0.0225944) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.52407 - 1.07627 * distance()) + 0.0101209 / pow(distance() , 1.13386)) * 1.59919 * pow((width1() + width2()) / 2 , 0.0490676) * pow(radius_up() , 0.0240254)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.11478 * (1 - exp(-0.671951 * (distance() + 0.0315606))) * pow(width() , 0.0161067 * distance() + -0.0488326) * (0.0599807 * thickness() + 0.248745)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0309501 * (1 - exp(-0.771498 * (distance() + -0.0256592)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133152 * pow(width() , 0.0241795) * (0.0846584 * thickness() + 0.189191)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0107726 * distance() + 0.665137) * (0.0808644 - exp(-(-0.185675 * distance() + 0.401208) * (enclose() + 1.36753 * distance() + 5.53602) - (0.0221554 * distance() + 0.00245291) * (enclose() + 0.483361 * distance() + 6.82462) * (enclose() + 0.483361 * distance() + 6.82462)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.11478 * (1 - exp(-0.671951 * (distance() + 0.0315606))) * pow(width() , 0.0161067 * distance() + -0.0488326) * (0.0599807 * thickness() + 0.248745)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0104679 * (1 - exp(-1.36679 * (distance() + 0.506766))) * pow(width() , -0.00147428 * distance() + 0.0278424) * pow(radius_up() , 0.252235)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.3595 * (0.0808644 - exp(0.527167 * (enclose() + 12.3737) - 0.11323 * (enclose() + 9.24142) * (enclose() + 9.24142)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.11478 * (1 - exp(-0.671951 * (distance() + 0.0315606))) * pow(width() , 0.0161067 * distance() + -0.0488326) * (0.0599807 * thickness() + 0.248745)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0309501 * (1 - exp(-0.771498 * (distance() + -0.0256592)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133152 * pow(width() , 0.0241795) * (0.0846584 * thickness() + 0.189191)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0107726 * distance() + 0.665137) * (0.0808644 - exp(-(-0.185675 * distance() + 0.401208) * (enclose() + 1.36753 * distance() + 5.53602) - (0.0221554 * distance() + 0.00245291) * (enclose() + 0.483361 * distance() + 6.82462) * (enclose() + 0.483361 * distance() + 6.82462)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.11478 * (1 - exp(-0.671951 * (distance() + 0.0315606))) * pow(width() , 0.0161067 * distance() + -0.0488326) * (0.0599807 * thickness() + 0.248745)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0104679 * (1 - exp(-1.36679 * (distance() + 0.506766))) * pow(width() , -0.00147428 * distance() + 0.0278424) * pow(radius_up() , 0.252235)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.3595 * (0.0808644 - exp(0.527167 * (enclose() + 12.3737) - 0.11323 * (enclose() + 9.24142) * (enclose() + 9.24142)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0592938 * (1 - exp(-0.599723 * (distance() + 0.0387996))) * pow(width() , 0.0207089 * distance() + -0.0611772) * (0.0984311 * thickness() + 0.20571) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.014447 * (1 - exp(-0.719088 * (distance() + -0.0385138))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0200018 * (1 - exp(-0.5778 * (distance() + 1.46186))) * pow(width() , -7.83931e-05 * distance() + 0.00091622) * pow(radius_up() , 0.0134028)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0988926 * pow(width() , 0.03348) * (0.0851855 * thickness() + 0.102025) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0592938 * (1 - exp(-0.599723 * (distance() + 0.0387996))) * pow(width() , 0.0207089 * distance() + -0.0611772) * (0.0984311 * thickness() + 0.20571) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.014447 * (1 - exp(-0.719088 * (distance() + -0.0385138))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0200018 * (1 - exp(-0.5778 * (distance() + 1.46186))) * pow(width() , -7.83931e-05 * distance() + 0.00091622) * pow(radius_up() , 0.0134028)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0988926 * pow(width() , 0.03348) * (0.0851855 * thickness() + 0.102025) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.09943 - 0.786179 * distance()) + 0.082793 / pow(distance() , 1.23816)) * 3.57229 * pow((width1() + width2()) / 2 , 0.0945608) * (0.0500425 * (thickness1() + thickness2()) / 2 + 0.019699) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.54142 - 0.803311 * distance()) + 0.0140348 / pow(distance() , 1.04296)) * 1.4674 * pow((width1() + width2()) / 2 , 0.0708326) * pow(radius_up() , 0.011616)
    }
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.09943 - 0.786179 * distance()) + 0.082793 / pow(distance() , 1.23816)) * 3.57229 * pow((width1() + width2()) / 2 , 0.0945608) * (0.0500425 * (thickness1() + thickness2()) / 2 + 0.019699) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.54142 - 0.803311 * distance()) + 0.0140348 / pow(distance() , 1.04296)) * 1.4674 * pow((width1() + width2()) / 2 , 0.0708326) * pow(radius_up() , 0.011616)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0845522 * (1 - exp(-0.349463 * (distance() + 0.163869))) * pow(width() , 0.0293768 * distance() + -0.103761) * (0.0564345 * thickness() + 0.226263)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0193528 * (1 - exp(-0.467072 * (distance() + 0.00647149)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.111693 * pow(width() , 0.0403513) * (0.0653189 * thickness() + 0.136736)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0008291 * distance() + 0.0388452) * (0.835102 - exp(-(0.283738 * distance() + 0.356569) * (enclose() + 0.299463 * distance() + 1.16231) - (-0.00661322 * distance() + -0.0311976) * (enclose() + 0.919088 * distance() + 3.32674) * (enclose() + 0.919088 * distance() + 3.32674)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0845522 * (1 - exp(-0.349463 * (distance() + 0.163869))) * pow(width() , 0.0293768 * distance() + -0.103761) * (0.0564345 * thickness() + 0.226263)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00277932 * (1 - exp(-0.649378 * (distance() + 0.835259))) * pow(width() , 0.0177757 * distance() + -0.0644445) * pow(radius_up() , 0.733623)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0214954 * (0.835102 - exp(-1.77526 * (enclose() + 2.65963) - -0.0642637 * (enclose() + 7.92218) * (enclose() + 7.92218)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0845522 * (1 - exp(-0.349463 * (distance() + 0.163869))) * pow(width() , 0.0293768 * distance() + -0.103761) * (0.0564345 * thickness() + 0.226263)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0193528 * (1 - exp(-0.467072 * (distance() + 0.00647149)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.111693 * pow(width() , 0.0403513) * (0.0653189 * thickness() + 0.136736)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0008291 * distance() + 0.0388452) * (0.835102 - exp(-(0.283738 * distance() + 0.356569) * (enclose() + 0.299463 * distance() + 1.16231) - (-0.00661322 * distance() + -0.0311976) * (enclose() + 0.919088 * distance() + 3.32674) * (enclose() + 0.919088 * distance() + 3.32674)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0845522 * (1 - exp(-0.349463 * (distance() + 0.163869))) * pow(width() , 0.0293768 * distance() + -0.103761) * (0.0564345 * thickness() + 0.226263)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00277932 * (1 - exp(-0.649378 * (distance() + 0.835259))) * pow(width() , 0.0177757 * distance() + -0.0644445) * pow(radius_up() , 0.733623)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0214954 * (0.835102 - exp(-1.77526 * (enclose() + 2.65963) - -0.0642637 * (enclose() + 7.92218) * (enclose() + 7.92218)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0878598 * (1 - exp(-0.350532 * (distance() + 0.160796))) * pow(width() , 0.0293366 * distance() + -0.103931) * (0.055659 * thickness() + 0.226865) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0201411 * (1 - exp(-0.467898 * (distance() + 0.00514268))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0247363 * (1 - exp(-0.438814 * (distance() + 1.76608))) * pow(width() , 0.00146459 * distance() + 0.0096294) * pow(radius_up() , -0.00884236)
    }
    if (distance() <= 0.0) {
        C = length() * 0.113454 * pow(width() , 0.0398676) * (0.0662695 * thickness() + 0.140409) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0878598 * (1 - exp(-0.350532 * (distance() + 0.160796))) * pow(width() , 0.0293366 * distance() + -0.103931) * (0.055659 * thickness() + 0.226865) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0201411 * (1 - exp(-0.467898 * (distance() + 0.00514268))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0247363 * (1 - exp(-0.438814 * (distance() + 1.76608))) * pow(width() , 0.00146459 * distance() + 0.0096294) * pow(radius_up() , -0.00884236)
    }
    if (distance() <= 0.0) {
        C = length() * 0.113454 * pow(width() , 0.0398676) * (0.0662695 * thickness() + 0.140409) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.09584 - 0.671801 * distance()) + 0.127605 / pow(distance() , 1.12783)) * 3.60109 * pow((width1() + width2()) / 2 , 0.104669) * (0.040811 * (thickness1() + thickness2()) / 2 + 0.0167715) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.50461 - 0.707559 * distance()) + 0.0188709 / pow(distance() , 0.988209)) * 1.26602 * pow((width1() + width2()) / 2 , 0.0829802) * pow(radius_up() , 0.00428924)
    }
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.09584 - 0.671801 * distance()) + 0.127605 / pow(distance() , 1.12783)) * 3.60109 * pow((width1() + width2()) / 2 , 0.104669) * (0.040811 * (thickness1() + thickness2()) / 2 + 0.0167715) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.50461 - 0.707559 * distance()) + 0.0188709 / pow(distance() , 0.988209)) * 1.26602 * pow((width1() + width2()) / 2 , 0.0829802) * pow(radius_up() , 0.00428924)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0662513 * (1 - exp(-0.274157 * (distance() + 0.268497))) * pow(width() , 0.0323679 * distance() + -0.117014) * (0.0553355 * thickness() + 0.224105)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0144537 * (1 - exp(-0.384316 * (distance() + 0.0442535)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.100231 * pow(width() , 0.0500188) * (0.0567976 * thickness() + 0.112759)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00015754 * distance() + 0.029836) * (0.879549 - exp(-(0.175167 * distance() + 0.211152) * (enclose() + 0.412053 * distance() + 0.923496) - (-0.0023056 * distance() + -0.0224117) * (enclose() + 1.27382 * distance() + 2.54388) * (enclose() + 1.27382 * distance() + 2.54388)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0662513 * (1 - exp(-0.274157 * (distance() + 0.268497))) * pow(width() , 0.0323679 * distance() + -0.117014) * (0.0553355 * thickness() + 0.224105)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00058869 * (1 - exp(-0.409688 * (distance() + 1.06501))) * pow(width() , 0.0365814 * distance() + -0.170018) * pow(radius_up() , 1.38408)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0153119 * (0.879549 - exp(-1.08699 * (enclose() + 2.98376) - -0.0339397 * (enclose() + 8.91298) * (enclose() + 8.91298)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0662513 * (1 - exp(-0.274157 * (distance() + 0.268497))) * pow(width() , 0.0323679 * distance() + -0.117014) * (0.0553355 * thickness() + 0.224105)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0144537 * (1 - exp(-0.384316 * (distance() + 0.0442535)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.100231 * pow(width() , 0.0500188) * (0.0567976 * thickness() + 0.112759)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00015754 * distance() + 0.029836) * (0.879549 - exp(-(0.175167 * distance() + 0.211152) * (enclose() + 0.412053 * distance() + 0.923496) - (-0.0023056 * distance() + -0.0224117) * (enclose() + 1.27382 * distance() + 2.54388) * (enclose() + 1.27382 * distance() + 2.54388)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0662513 * (1 - exp(-0.274157 * (distance() + 0.268497))) * pow(width() , 0.0323679 * distance() + -0.117014) * (0.0553355 * thickness() + 0.224105)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00058869 * (1 - exp(-0.409688 * (distance() + 1.06501))) * pow(width() , 0.0365814 * distance() + -0.170018) * pow(radius_up() , 1.38408)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0153119 * (0.879549 - exp(-1.08699 * (enclose() + 2.98376) - -0.0339397 * (enclose() + 8.91298) * (enclose() + 8.91298)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.103234 * (1 - exp(-0.285191 * (distance() + 0.24975))) * pow(width() , 0.0320277 * distance() + -0.117317) * (0.0409981 * thickness() + 0.237178) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0232247 * (1 - exp(-0.39418 * (distance() + 0.0409866))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0283505 * (1 - exp(-0.399683 * (distance() + 1.91292))) * pow(width() , 0.00150044 * distance() + 0.018586) * pow(radius_up() , -0.0400851)
    }
    if (distance() <= 0.0) {
        C = length() * 0.120823 * pow(width() , 0.0467685) * (0.0620241 * thickness() + 0.158138) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.103234 * (1 - exp(-0.285191 * (distance() + 0.24975))) * pow(width() , 0.0320277 * distance() + -0.117317) * (0.0409981 * thickness() + 0.237178) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0232247 * (1 - exp(-0.39418 * (distance() + 0.0409866))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0283505 * (1 - exp(-0.399683 * (distance() + 1.91292))) * pow(width() , 0.00150044 * distance() + 0.018586) * pow(radius_up() , -0.0400851)
    }
    if (distance() <= 0.0) {
        C = length() * 0.120823 * pow(width() , 0.0467685) * (0.0620241 * thickness() + 0.158138) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,ctm,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.10322 - 0.646474 * distance()) + 0.146335 / pow(distance() , 1.09331)) * 3.62778 * pow((width1() + width2()) / 2 , 0.106847) * (0.0380641 * (thickness1() + thickness2()) / 2 + 0.0157907) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.53457 - 0.681691 * distance()) + 0.0191996 / pow(distance() , 0.978959)) * 1.26484 * pow((width1() + width2()) / 2 , 0.0862986) * pow(radius_up() , 0.00986811)
    }
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.10322 - 0.646474 * distance()) + 0.146335 / pow(distance() , 1.09331)) * 3.62778 * pow((width1() + width2()) / 2 , 0.106847) * (0.0380641 * (thickness1() + thickness2()) / 2 + 0.0157907) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.53457 - 0.681691 * distance()) + 0.0191996 / pow(distance() , 0.978959)) * 1.26484 * pow((width1() + width2()) / 2 , 0.0862986) * pow(radius_up() , 0.00986811)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 ctm INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0603682 * (1 - exp(-0.263849 * (distance() + 0.29757))) * pow(width() , 0.0321378 * distance() + -0.116312) * (0.0551356 * thickness() + 0.22327)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.013098 * (1 - exp(-0.368363 * (distance() + 0.0564579)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0967901 * pow(width() , 0.0528859) * (0.054153 * thickness() + 0.106036)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00042224 * distance() + 0.0301191) * (0.88988 - exp(-(0.155555 * distance() + 0.213263) * (enclose() + 0.406485 * distance() + 1.03399) - (-0.00126828 * distance() + -0.0243154) * (enclose() + 1.31151 * distance() + 2.61999) * (enclose() + 1.31151 * distance() + 2.61999)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0603682 * (1 - exp(-0.263849 * (distance() + 0.29757))) * pow(width() , 0.0321378 * distance() + -0.116312) * (0.0551356 * thickness() + 0.22327)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00099321 * (1 - exp(-0.348703 * (distance() + 1.12436))) * pow(width() , 0.041819 * distance() + -0.162175) * pow(radius_up() , 0.889082)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0140039 * (0.88988 - exp(-0.991038 * (enclose() + 3.06642) - -0.0306568 * (enclose() + 9.17754) * (enclose() + 9.17754)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 ctm INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0603682 * (1 - exp(-0.263849 * (distance() + 0.29757))) * pow(width() , 0.0321378 * distance() + -0.116312) * (0.0551356 * thickness() + 0.22327)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.013098 * (1 - exp(-0.368363 * (distance() + 0.0564579)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0967901 * pow(width() , 0.0528859) * (0.054153 * thickness() + 0.106036)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00042224 * distance() + 0.0301191) * (0.88988 - exp(-(0.155555 * distance() + 0.213263) * (enclose() + 0.406485 * distance() + 1.03399) - (-0.00126828 * distance() + -0.0243154) * (enclose() + 1.31151 * distance() + 2.61999) * (enclose() + 1.31151 * distance() + 2.61999)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0603682 * (1 - exp(-0.263849 * (distance() + 0.29757))) * pow(width() , 0.0321378 * distance() + -0.116312) * (0.0551356 * thickness() + 0.22327)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00099321 * (1 - exp(-0.348703 * (distance() + 1.12436))) * pow(width() , 0.041819 * distance() + -0.162175) * pow(radius_up() , 0.889082)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0140039 * (0.88988 - exp(-0.991038 * (enclose() + 3.06642) - -0.0306568 * (enclose() + 9.17754) * (enclose() + 9.17754)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.106917 * (1 - exp(-0.275953 * (distance() + 0.274388))) * pow(width() , 0.0319408 * distance() + -0.117487) * (0.0375036 * thickness() + 0.239245) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0240454 * (1 - exp(-0.37972 * (distance() + 0.052184))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0274073 * (1 - exp(-0.394186 * (distance() + 1.94651))) * pow(width() , 0.00116791 * distance() + 0.0219398) * pow(radius_up() , 0.0035941)
    }
    if (distance() <= 0.0) {
        C = length() * 0.122892 * pow(width() , 0.0490644) * (0.0613987 * thickness() + 0.16302) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.106917 * (1 - exp(-0.275953 * (distance() + 0.274388))) * pow(width() , 0.0319408 * distance() + -0.117487) * (0.0375036 * thickness() + 0.239245) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0240454 * (1 - exp(-0.37972 * (distance() + 0.052184))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0274073 * (1 - exp(-0.394186 * (distance() + 1.94651))) * pow(width() , 0.00116791 * distance() + 0.0219398) * pow(radius_up() , 0.0035941)
    }
    if (distance() <= 0.0) {
        C = length() * 0.122892 * pow(width() , 0.0490644) * (0.0613987 * thickness() + 0.16302) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.10611 - 0.612818 * distance()) + 0.171397 / pow(distance() , 1.05878)) * 3.66788 * pow((width1() + width2()) / 2 , 0.109381) * (0.0347097 * (thickness1() + thickness2()) / 2 + 0.0145643) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.57013 - 0.661802 * distance()) + 0.0193546 / pow(distance() , 0.970953)) * 1.29383 * pow((width1() + width2()) / 2 , 0.0900849) * pow(radius_up() , 0.00461021)
    }
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    C = length() * (exp(-1.10611 - 0.612818 * distance()) + 0.171397 / pow(distance() , 1.05878)) * 3.66788 * pow((width1() + width2()) / 2 , 0.109381) * (0.0347097 * (thickness1() + thickness2()) / 2 + 0.0145643) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 2.18))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 2.18)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.57013 - 0.661802 * distance()) + 0.0193546 / pow(distance() , 0.970953)) * 1.29383 * pow((width1() + width2()) / 2 , 0.0900849) * pow(radius_up() , 0.00461021)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0518688 * (1 - exp(-0.256794 * (distance() + 0.334848))) * pow(width() , 0.03099 * distance() + -0.111641) * (0.055071 * thickness() + 0.222235)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0112667 * (1 - exp(-0.35137 * (distance() + 0.0739733)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0922722 * pow(width() , 0.0569314) * (0.0503794 * thickness() + 0.096194)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00021151 * distance() + 0.0240487) * (0.941861 - exp(-(0.104742 * distance() + 0.158469) * (enclose() + 0.570625 * distance() + 1.1912) - (-0.00095774 * distance() + -0.0106563) * (enclose() + 1.59158 * distance() + 4.27389) * (enclose() + 1.59158 * distance() + 4.27389)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0518688 * (1 - exp(-0.256794 * (distance() + 0.334848))) * pow(width() , 0.03099 * distance() + -0.111641) * (0.055071 * thickness() + 0.222235)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00519763 * (1 - exp(-0.0146698 * (distance() + 3.49396))) * pow(width() , 0.041031 * distance() + -0.161131) * pow(radius_up() , 1.10884)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0114956 * (0.941861 - exp(-0.682179 * (enclose() + 4.04433) - -0.015445 * (enclose() + 12.2318) * (enclose() + 12.2318)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0518688 * (1 - exp(-0.256794 * (distance() + 0.334848))) * pow(width() , 0.03099 * distance() + -0.111641) * (0.055071 * thickness() + 0.222235)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0112667 * (1 - exp(-0.35137 * (distance() + 0.0739733)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0922722 * pow(width() , 0.0569314) * (0.0503794 * thickness() + 0.096194)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00021151 * distance() + 0.0240487) * (0.941861 - exp(-(0.104742 * distance() + 0.158469) * (enclose() + 0.570625 * distance() + 1.1912) - (-0.00095774 * distance() + -0.0106563) * (enclose() + 1.59158 * distance() + 4.27389) * (enclose() + 1.59158 * distance() + 4.27389)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0518688 * (1 - exp(-0.256794 * (distance() + 0.334848))) * pow(width() , 0.03099 * distance() + -0.111641) * (0.055071 * thickness() + 0.222235)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00519763 * (1 - exp(-0.0146698 * (distance() + 3.49396))) * pow(width() , 0.041031 * distance() + -0.161131) * pow(radius_up() , 1.10884)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0114956 * (0.941861 - exp(-0.682179 * (enclose() + 4.04433) - -0.015445 * (enclose() + 12.2318) * (enclose() + 12.2318)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.110951 * (1 - exp(-0.269421 * (distance() + 0.306039))) * pow(width() , 0.0311207 * distance() + -0.114576) * (0.0331925 * thickness() + 0.241926) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0251021 * (1 - exp(-0.364226 * (distance() + 0.0680232))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0289827 * (1 - exp(-0.384209 * (distance() + 1.97113))) * pow(width() , 0.00118225 * distance() + 0.0320979) * pow(radius_up() , -0.0264692)
    }
    if (distance() <= 0.0) {
        C = length() * 0.125207 * pow(width() , 0.052395) * (0.0615135 * thickness() + 0.170097) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    profile = active_profileid
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.110951 * (1 - exp(-0.269421 * (distance() + 0.306039))) * pow(width() , 0.0311207 * distance() + -0.114576) * (0.0331925 * thickness() + 0.241926) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0251021 * (1 - exp(-0.364226 * (distance() + 0.0680232))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 2.18)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0289827 * (1 - exp(-0.384209 * (distance() + 1.97113))) * pow(width() , 0.00118225 * distance() + 0.0320979) * pow(radius_up() , -0.0264692)
    }
    if (distance() <= 0.0) {
        C = length() * 0.125207 * pow(width() , 0.052395) * (0.0615135 * thickness() + 0.170097) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 2.18))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 2.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.23716 - 0.979698 * distance()) + 0.0673662 / pow(distance() , 1.26541)) * 3.67194 * pow((width1() + width2()) / 2 , 0.0800434) * (0.0589014 * (thickness1() + thickness2()) / 2 + 0.019791) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.8889 - 0.963319 * distance()) + 0.0126544 / pow(distance() , 1.01157)) * 1.73447 * pow((width1() + width2()) / 2 , 0.0599992) * pow(radius_up() , 0.0230822)
    }
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.23716 - 0.979698 * distance()) + 0.0673662 / pow(distance() , 1.26541)) * 3.67194 * pow((width1() + width2()) / 2 , 0.0800434) * (0.0589014 * (thickness1() + thickness2()) / 2 + 0.019791) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.8889 - 0.963319 * distance()) + 0.0126544 / pow(distance() , 1.01157)) * 1.73447 * pow((width1() + width2()) / 2 , 0.0599992) * pow(radius_up() , 0.0230822)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.126583 * (1 - exp(-0.592085 * (distance() + 0.0796598))) * pow(width() , 0.018002 * distance() + -0.0547211) * (0.0490001 * thickness() + 0.254993)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0339464 * (1 - exp(-0.681184 * (distance() + 0.00152324)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.138967 * pow(width() , 0.0343317) * (0.0829364 * thickness() + 0.206171)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0345058 * distance() + 1.11714) * (0.0486026 - exp(-(-0.154099 * distance() + 0.312522) * (enclose() + 1.73297 * distance() + 7.48289) - (0.0152231 * distance() + 0.00705581) * (enclose() + 0.603675 * distance() + 8.50257) * (enclose() + 0.603675 * distance() + 8.50257)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.126583 * (1 - exp(-0.592085 * (distance() + 0.0796598))) * pow(width() , 0.018002 * distance() + -0.0547211) * (0.0490001 * thickness() + 0.254993)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.011315 * (1 - exp(-1.29926 * (distance() + 0.562145))) * pow(width() , -0.00289168 * distance() + 0.0402907) * pow(radius_up() , 0.246634)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.644834 * (0.0486026 - exp(0.457973 * (enclose() + 16.1477) - 0.0831713 * (enclose() + 11.5209) * (enclose() + 11.5209)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.126583 * (1 - exp(-0.592085 * (distance() + 0.0796598))) * pow(width() , 0.018002 * distance() + -0.0547211) * (0.0490001 * thickness() + 0.254993)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0339464 * (1 - exp(-0.681184 * (distance() + 0.00152324)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.138967 * pow(width() , 0.0343317) * (0.0829364 * thickness() + 0.206171)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0345058 * distance() + 1.11714) * (0.0486026 - exp(-(-0.154099 * distance() + 0.312522) * (enclose() + 1.73297 * distance() + 7.48289) - (0.0152231 * distance() + 0.00705581) * (enclose() + 0.603675 * distance() + 8.50257) * (enclose() + 0.603675 * distance() + 8.50257)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.126583 * (1 - exp(-0.592085 * (distance() + 0.0796598))) * pow(width() , 0.018002 * distance() + -0.0547211) * (0.0490001 * thickness() + 0.254993)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.011315 * (1 - exp(-1.29926 * (distance() + 0.562145))) * pow(width() , -0.00289168 * distance() + 0.0402907) * pow(radius_up() , 0.246634)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.644834 * (0.0486026 - exp(0.457973 * (enclose() + 16.1477) - 0.0831713 * (enclose() + 11.5209) * (enclose() + 11.5209)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0428991 * (1 - exp(-0.512146 * (distance() + 0.100961))) * pow(width() , 0.0231522 * distance() + -0.0676166) * (0.101762 * thickness() + 0.1986) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0100983 * (1 - exp(-0.621799 * (distance() + -0.00983961))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0156735 * (1 - exp(-0.528451 * (distance() + 1.81714))) * pow(width() , -0.00189357 * distance() + 0.0073335) * pow(radius_up() , 0.00456001)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0879183 * pow(width() , 0.0470764) * (0.0720548 * thickness() + 0.0794933) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0428991 * (1 - exp(-0.512146 * (distance() + 0.100961))) * pow(width() , 0.0231522 * distance() + -0.0676166) * (0.101762 * thickness() + 0.1986) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0100983 * (1 - exp(-0.621799 * (distance() + -0.00983961))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0156735 * (1 - exp(-0.528451 * (distance() + 1.81714))) * pow(width() , -0.00189357 * distance() + 0.0073335) * pow(radius_up() , 0.00456001)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0879183 * pow(width() , 0.0470764) * (0.0720548 * thickness() + 0.0794933) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.08969 - 0.665069 * distance()) + 0.129584 / pow(distance() , 1.12549)) * 3.60006 * pow((width1() + width2()) / 2 , 0.105207) * (0.0403671 * (thickness1() + thickness2()) / 2 + 0.0166556) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.8923 - 0.684105 * distance()) + 0.0160408 / pow(distance() , 0.94504)) * 1.64016 * pow((width1() + width2()) / 2 , 0.079068) * pow(radius_up() , 0.0100368)
    }
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.08969 - 0.665069 * distance()) + 0.129584 / pow(distance() , 1.12549)) * 3.60006 * pow((width1() + width2()) / 2 , 0.105207) * (0.0403671 * (thickness1() + thickness2()) / 2 + 0.0166556) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.8923 - 0.684105 * distance()) + 0.0160408 / pow(distance() , 0.94504)) * 1.64016 * pow((width1() + width2()) / 2 , 0.079068) * pow(radius_up() , 0.0100368)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.101106 * (1 - exp(-0.277708 * (distance() + 0.259146))) * pow(width() , 0.0324907 * distance() + -0.119426) * (0.0407003 * thickness() + 0.23681)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.022567 * (1 - exp(-0.387758 * (distance() + 0.0437116)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119383 * pow(width() , 0.0470814) * (0.0607672 * thickness() + 0.155228)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00202782 * distance() + 0.04449) * (0.672939 - exp(-(0.125967 * distance() + 0.186548) * (enclose() + 0.00967952 * distance() + 1.56192) - (-0.00014246 * distance() + -0.00010898) * (enclose() + -6.53131 * distance() + 11.4012) * (enclose() + -6.53131 * distance() + 11.4012)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.101106 * (1 - exp(-0.277708 * (distance() + 0.259146))) * pow(width() , 0.0324907 * distance() + -0.119426) * (0.0407003 * thickness() + 0.23681)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00338015 * (1 - exp(-0.614514 * (distance() + 0.976906))) * pow(width() , 0.0123617 * distance() + -0.0260242) * pow(radius_up() , 0.68765)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0273145 * (0.672939 - exp(-0.816383 * (enclose() + 1.61032) - -0.00082128 * (enclose() + -21.2554) * (enclose() + -21.2554)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.101106 * (1 - exp(-0.277708 * (distance() + 0.259146))) * pow(width() , 0.0324907 * distance() + -0.119426) * (0.0407003 * thickness() + 0.23681)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.022567 * (1 - exp(-0.387758 * (distance() + 0.0437116)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119383 * pow(width() , 0.0470814) * (0.0607672 * thickness() + 0.155228)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00202782 * distance() + 0.04449) * (0.672939 - exp(-(0.125967 * distance() + 0.186548) * (enclose() + 0.00967952 * distance() + 1.56192) - (-0.00014246 * distance() + -0.00010898) * (enclose() + -6.53131 * distance() + 11.4012) * (enclose() + -6.53131 * distance() + 11.4012)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.101106 * (1 - exp(-0.277708 * (distance() + 0.259146))) * pow(width() , 0.0324907 * distance() + -0.119426) * (0.0407003 * thickness() + 0.23681)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00338015 * (1 - exp(-0.614514 * (distance() + 0.976906))) * pow(width() , 0.0123617 * distance() + -0.0260242) * pow(radius_up() , 0.68765)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0273145 * (0.672939 - exp(-0.816383 * (enclose() + 1.61032) - -0.00082128 * (enclose() + -21.2554) * (enclose() + -21.2554)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0691358 * (1 - exp(-0.268038 * (distance() + 0.271409))) * pow(width() , 0.0328798 * distance() + -0.119971) * (0.0539666 * thickness() + 0.224582) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0149919 * (1 - exp(-0.379154 * (distance() + 0.0439732))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0196513 * (1 - exp(-0.386264 * (distance() + 2.21581))) * pow(width() , -0.00056862 * distance() + 0.0143496) * pow(radius_up() , -0.0210369)
    }
    if (distance() <= 0.0) {
        C = length() * 0.101591 * pow(width() , 0.0494491) * (0.0571023 * thickness() + 0.115557) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0691358 * (1 - exp(-0.268038 * (distance() + 0.271409))) * pow(width() , 0.0328798 * distance() + -0.119971) * (0.0539666 * thickness() + 0.224582) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0149919 * (1 - exp(-0.379154 * (distance() + 0.0439732))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0196513 * (1 - exp(-0.386264 * (distance() + 2.21581))) * pow(width() , -0.00056862 * distance() + 0.0143496) * pow(radius_up() , -0.0210369)
    }
    if (distance() <= 0.0) {
        C = length() * 0.101591 * pow(width() , 0.0494491) * (0.0571023 * thickness() + 0.115557) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,ctm,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.04443 - 0.605947 * distance()) + 0.158389 / pow(distance() , 1.08968)) * 3.61704 * pow((width1() + width2()) / 2 , 0.110121) * (0.0354247 * (thickness1() + thickness2()) / 2 + 0.0150294) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.80692 - 0.623759 * distance()) + 0.0180243 / pow(distance() , 0.936534)) * 1.48564 * pow((width1() + width2()) / 2 , 0.0854954) * pow(radius_up() , 0.0154248)
    }
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.04443 - 0.605947 * distance()) + 0.158389 / pow(distance() , 1.08968)) * 3.61704 * pow((width1() + width2()) / 2 , 0.110121) * (0.0354247 * (thickness1() + thickness2()) / 2 + 0.0150294) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.80692 - 0.623759 * distance()) + 0.0180243 / pow(distance() , 0.936534)) * 1.48564 * pow((width1() + width2()) / 2 , 0.0854954) * pow(radius_up() , 0.0154248)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 ctm INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0947479 * (1 - exp(-0.235385 * (distance() + 0.325643))) * pow(width() , 0.0346539 * distance() + -0.130594) * (0.0386317 * thickness() + 0.234427)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0201944 * (1 - exp(-0.344561 * (distance() + 0.0634118)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.11404 * pow(width() , 0.050446) * (0.0566079 * thickness() + 0.144417)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00093787 * distance() + 0.0361275) * (0.869562 - exp(-(0.216008 * distance() + 0.35049) * (enclose() + 0.298036 * distance() + 1.26386) - (-0.00374492 * distance() + -0.0316804) * (enclose() + 0.944834 * distance() + 3.48957) * (enclose() + 0.944834 * distance() + 3.48957)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0947479 * (1 - exp(-0.235385 * (distance() + 0.325643))) * pow(width() , 0.0346539 * distance() + -0.130594) * (0.0386317 * thickness() + 0.234427)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00360517 * (1 - exp(-0.470438 * (distance() + 1.09751))) * pow(width() , 0.0210837 * distance() + -0.0309809) * pow(radius_up() , 0.456075)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0204084 * (0.869562 - exp(-1.43053 * (enclose() + 2.75404) - -0.050405 * (enclose() + 8.21374) * (enclose() + 8.21374)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 ctm INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0947479 * (1 - exp(-0.235385 * (distance() + 0.325643))) * pow(width() , 0.0346539 * distance() + -0.130594) * (0.0386317 * thickness() + 0.234427)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0201944 * (1 - exp(-0.344561 * (distance() + 0.0634118)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.11404 * pow(width() , 0.050446) * (0.0566079 * thickness() + 0.144417)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00093787 * distance() + 0.0361275) * (0.869562 - exp(-(0.216008 * distance() + 0.35049) * (enclose() + 0.298036 * distance() + 1.26386) - (-0.00374492 * distance() + -0.0316804) * (enclose() + 0.944834 * distance() + 3.48957) * (enclose() + 0.944834 * distance() + 3.48957)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0947479 * (1 - exp(-0.235385 * (distance() + 0.325643))) * pow(width() , 0.0346539 * distance() + -0.130594) * (0.0386317 * thickness() + 0.234427)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00360517 * (1 - exp(-0.470438 * (distance() + 1.09751))) * pow(width() , 0.0210837 * distance() + -0.0309809) * pow(radius_up() , 0.456075)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0204084 * (0.869562 - exp(-1.43053 * (enclose() + 2.75404) - -0.050405 * (enclose() + 8.21374) * (enclose() + 8.21374)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0777835 * (1 - exp(-0.231756 * (distance() + 0.330864))) * pow(width() , 0.0347176 * distance() + -0.130464) * (0.0451497 * thickness() + 0.228798) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0163756 * (1 - exp(-0.341016 * (distance() + 0.0632258))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0200701 * (1 - exp(-0.367778 * (distance() + 2.30075))) * pow(width() , -0.00070918 * distance() + 0.0178954) * pow(radius_up() , 0.0121165)
    }
    if (distance() <= 0.0) {
        C = length() * 0.105011 * pow(width() , 0.0513812) * (0.0550481 * thickness() + 0.124093) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0777835 * (1 - exp(-0.231756 * (distance() + 0.330864))) * pow(width() , 0.0347176 * distance() + -0.130464) * (0.0451497 * thickness() + 0.228798) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0163756 * (1 - exp(-0.341016 * (distance() + 0.0632258))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0200701 * (1 - exp(-0.367778 * (distance() + 2.30075))) * pow(width() , -0.00070918 * distance() + 0.0178954) * pow(radius_up() , 0.0121165)
    }
    if (distance() <= 0.0) {
        C = length() * 0.105011 * pow(width() , 0.0513812) * (0.0550481 * thickness() + 0.124093) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.00527 - 0.540338 * distance()) + 0.210497 / pow(distance() , 1.03589)) * 3.63975 * pow((width1() + width2()) / 2 , 0.11526) * (0.029728 * (thickness1() + thickness2()) / 2 + 0.012948) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.62143 - 0.580864 * distance()) + 0.0222459 / pow(distance() , 0.93097)) * 1.23941 * pow((width1() + width2()) / 2 , 0.0927428) * pow(radius_up() , 0.00782255)
    }
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    C = length() * (exp(-1.00527 - 0.540338 * distance()) + 0.210497 / pow(distance() , 1.03589)) * 3.63975 * pow((width1() + width2()) / 2 , 0.11526) * (0.029728 * (thickness1() + thickness2()) / 2 + 0.012948) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 3.56))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 3.56)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.62143 - 0.580864 * distance()) + 0.0222459 / pow(distance() , 0.93097)) * 1.23941 * pow((width1() + width2()) / 2 , 0.0927428) * pow(radius_up() , 0.00782255)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0831626 * (1 - exp(-0.201358 * (distance() + 0.414541))) * pow(width() , 0.0354126 * distance() + -0.136486) * (0.036445 * thickness() + 0.23288)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0171158 * (1 - exp(-0.302592 * (distance() + 0.0925801)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107262 * pow(width() , 0.0549889) * (0.0513655 * thickness() + 0.129851)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00105823 * distance() + 0.0279272) * (0.876398 - exp(-(0.144454 * distance() + 0.169784) * (enclose() + 0.470924 * distance() + 0.775742) - (-0.0021738 * distance() + -0.0144968) * (enclose() + 1.37664 * distance() + 2.37774) * (enclose() + 1.37664 * distance() + 2.37774)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0831626 * (1 - exp(-0.201358 * (distance() + 0.414541))) * pow(width() , 0.0354126 * distance() + -0.136486) * (0.036445 * thickness() + 0.23288)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00249006 * (1 - exp(-0.435334 * (distance() + 1.44239))) * pow(width() , 0.0142123 * distance() + -0.0417967) * pow(radius_up() , 0.637946)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0166092 * (0.876398 - exp(-0.892054 * (enclose() + 3.13036) - -0.0253658 * (enclose() + 9.26094) * (enclose() + 9.26094)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0831626 * (1 - exp(-0.201358 * (distance() + 0.414541))) * pow(width() , 0.0354126 * distance() + -0.136486) * (0.036445 * thickness() + 0.23288)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0171158 * (1 - exp(-0.302592 * (distance() + 0.0925801)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107262 * pow(width() , 0.0549889) * (0.0513655 * thickness() + 0.129851)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00105823 * distance() + 0.0279272) * (0.876398 - exp(-(0.144454 * distance() + 0.169784) * (enclose() + 0.470924 * distance() + 0.775742) - (-0.0021738 * distance() + -0.0144968) * (enclose() + 1.37664 * distance() + 2.37774) * (enclose() + 1.37664 * distance() + 2.37774)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0831626 * (1 - exp(-0.201358 * (distance() + 0.414541))) * pow(width() , 0.0354126 * distance() + -0.136486) * (0.036445 * thickness() + 0.23288)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00249006 * (1 - exp(-0.435334 * (distance() + 1.44239))) * pow(width() , 0.0142123 * distance() + -0.0417967) * pow(radius_up() , 0.637946)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0166092 * (0.876398 - exp(-0.892054 * (enclose() + 3.13036) - -0.0253658 * (enclose() + 9.26094) * (enclose() + 9.26094)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0880643 * (1 - exp(-0.202013 * (distance() + 0.409809))) * pow(width() , 0.0355151 * distance() + -0.137247) * (0.0353428 * thickness() + 0.233749) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0181561 * (1 - exp(-0.303354 * (distance() + 0.0908398))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0213344 * (1 - exp(-0.339824 * (distance() + 2.38738))) * pow(width() , -0.00037595 * distance() + 0.0334187) * pow(radius_up() , -0.0206843)
    }
    if (distance() <= 0.0) {
        C = length() * 0.10949 * pow(width() , 0.054499) * (0.0526144 * thickness() + 0.135372) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    profile = active_profileid
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0880643 * (1 - exp(-0.202013 * (distance() + 0.409809))) * pow(width() , 0.0355151 * distance() + -0.137247) * (0.0353428 * thickness() + 0.233749) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0181561 * (1 - exp(-0.303354 * (distance() + 0.0908398))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 3.56)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0213344 * (1 - exp(-0.339824 * (distance() + 2.38738))) * pow(width() , -0.00037595 * distance() + 0.0334187) * pow(radius_up() , -0.0206843)
    }
    if (distance() <= 0.0) {
        C = length() * 0.10949 * pow(width() , 0.054499) * (0.0526144 * thickness() + 0.135372) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 3.56))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 3.56)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.27142 - 0.92784 * distance()) + 0.0878406 / pow(distance() , 1.18919)) * 3.72279 * pow((width1() + width2()) / 2 , 0.0852618) * (0.052722 * (thickness1() + thickness2()) / 2 + 0.018055) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-4.17018 - 0.926634 * distance()) + 0.0138374 / pow(distance() , 0.948508)) * 1.85985 * pow((width1() + width2()) / 2 , 0.0652133) * pow(radius_up() , 0.0220715)
    }
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.27142 - 0.92784 * distance()) + 0.0878406 / pow(distance() , 1.18919)) * 3.72279 * pow((width1() + width2()) / 2 , 0.0852618) * (0.052722 * (thickness1() + thickness2()) / 2 + 0.018055) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-4.17018 - 0.926634 * distance()) + 0.0138374 / pow(distance() , 0.948508)) * 1.85985 * pow((width1() + width2()) / 2 , 0.0652133) * pow(radius_up() , 0.0220715)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.132337 * (1 - exp(-0.571302 * (distance() + 0.104979))) * pow(width() , 0.0177212 * distance() + -0.0519898) * (0.0442511 * thickness() + 0.258149)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0355367 * (1 - exp(-0.647912 * (distance() + 0.0182162)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142751 * pow(width() , 0.0412071) * (0.0832691 * thickness() + 0.214874)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0117841 * distance() + 0.280946) * (0.190536 - exp(-(-0.0953116 * distance() + 0.170993) * (enclose() + 2.36432 * distance() + 4.4671) - (0.0150485 * distance() + 0.025532) * (enclose() + 0.637142 * distance() + 5.29626) * (enclose() + 0.637142 * distance() + 5.29626)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.132337 * (1 - exp(-0.571302 * (distance() + 0.104979))) * pow(width() , 0.0177212 * distance() + -0.0519898) * (0.0442511 * thickness() + 0.258149)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0118345 * (1 - exp(-1.26598 * (distance() + 0.595853))) * pow(width() , -0.00372729 * distance() + 0.0482461) * pow(radius_up() , 0.243012)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.169933 * (0.190536 - exp(0.305565 * (enclose() + 16.2887) - 0.100774 * (enclose() + 8.48197) * (enclose() + 8.48197)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.132337 * (1 - exp(-0.571302 * (distance() + 0.104979))) * pow(width() , 0.0177212 * distance() + -0.0519898) * (0.0442511 * thickness() + 0.258149)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0355367 * (1 - exp(-0.647912 * (distance() + 0.0182162)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142751 * pow(width() , 0.0412071) * (0.0832691 * thickness() + 0.214874)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0117841 * distance() + 0.280946) * (0.190536 - exp(-(-0.0953116 * distance() + 0.170993) * (enclose() + 2.36432 * distance() + 4.4671) - (0.0150485 * distance() + 0.025532) * (enclose() + 0.637142 * distance() + 5.29626) * (enclose() + 0.637142 * distance() + 5.29626)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.132337 * (1 - exp(-0.571302 * (distance() + 0.104979))) * pow(width() , 0.0177212 * distance() + -0.0519898) * (0.0442511 * thickness() + 0.258149)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0118345 * (1 - exp(-1.26598 * (distance() + 0.595853))) * pow(width() , -0.00372729 * distance() + 0.0482461) * pow(radius_up() , 0.243012)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.169933 * (0.190536 - exp(0.305565 * (enclose() + 16.2887) - 0.100774 * (enclose() + 8.48197) * (enclose() + 8.48197)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0335513 * (1 - exp(-0.491443 * (distance() + 0.134824))) * pow(width() , 0.0224263 * distance() + -0.0621635) * (0.103565 * thickness() + 0.193391) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00775561 * (1 - exp(-0.5868 * (distance() + 0.0102948))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0131728 * (1 - exp(-0.544493 * (distance() + 1.95325))) * pow(width() , -0.0036368 * distance() + 0.0133688) * pow(radius_up() , -0.00316255)
    }
    if (distance() <= 0.0) {
        C = length() * 0.081202 * pow(width() , 0.0556236) * (0.063661 * thickness() + 0.0657669) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0335513 * (1 - exp(-0.491443 * (distance() + 0.134824))) * pow(width() , 0.0224263 * distance() + -0.0621635) * (0.103565 * thickness() + 0.193391) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00775561 * (1 - exp(-0.5868 * (distance() + 0.0102948))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0131728 * (1 - exp(-0.544493 * (distance() + 1.95325))) * pow(width() , -0.0036368 * distance() + 0.0133688) * pow(radius_up() , -0.00316255)
    }
    if (distance() <= 0.0) {
        C = length() * 0.081202 * pow(width() , 0.0556236) * (0.063661 * thickness() + 0.0657669) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,ctm,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.20572 - 0.751134 * distance()) + 0.117815 / pow(distance() , 1.12335)) * 3.69116 * pow((width1() + width2()) / 2 , 0.0978402) * (0.0442596 * (thickness1() + thickness2()) / 2 + 0.0171631) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.99312 - 0.716034 * distance()) + 0.0161258 / pow(distance() , 0.932647)) * 1.65388 * pow((width1() + width2()) / 2 , 0.074962) * pow(radius_up() , 0.0227623)
    }
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.20572 - 0.751134 * distance()) + 0.117815 / pow(distance() , 1.12335)) * 3.69116 * pow((width1() + width2()) / 2 , 0.0978402) * (0.0442596 * (thickness1() + thickness2()) / 2 + 0.0171631) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.99312 - 0.716034 * distance()) + 0.0161258 / pow(distance() , 0.932647)) * 1.65388 * pow((width1() + width2()) / 2 , 0.074962) * pow(radius_up() , 0.0227623)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 ctm INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.118444 * (1 - exp(-0.38718 * (distance() + 0.184638))) * pow(width() , 0.025205 * distance() + -0.0854025) * (0.0389212 * thickness() + 0.248941)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0293757 * (1 - exp(-0.47648 * (distance() + 0.034615)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133212 * pow(width() , 0.0465797) * (0.0701202 * thickness() + 0.189235)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00183681 * distance() + 0.0809207) * (0.746186 - exp(-(0.0582546 * distance() + 1.10546) * (enclose() + -0.146301 * distance() + 1.73353) - (0.00578451 * distance() + -0.114472) * (enclose() + -0.230446 * distance() + 3.88174) * (enclose() + -0.230446 * distance() + 3.88174)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.118444 * (1 - exp(-0.38718 * (distance() + 0.184638))) * pow(width() , 0.025205 * distance() + -0.0854025) * (0.0389212 * thickness() + 0.248941)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00907497 * (1 - exp(-0.804621 * (distance() + 0.741902))) * pow(width() , 0.00618706 * distance() + 0.0433704) * pow(radius_up() , 0.176435)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0358683 * (0.746186 - exp(-1.39673 * (enclose() + 1.00203) - -0.0855494 * (enclose() + 2.72951) * (enclose() + 2.72951)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 ctm INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.118444 * (1 - exp(-0.38718 * (distance() + 0.184638))) * pow(width() , 0.025205 * distance() + -0.0854025) * (0.0389212 * thickness() + 0.248941)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0293757 * (1 - exp(-0.47648 * (distance() + 0.034615)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133212 * pow(width() , 0.0465797) * (0.0701202 * thickness() + 0.189235)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00183681 * distance() + 0.0809207) * (0.746186 - exp(-(0.0582546 * distance() + 1.10546) * (enclose() + -0.146301 * distance() + 1.73353) - (0.00578451 * distance() + -0.114472) * (enclose() + -0.230446 * distance() + 3.88174) * (enclose() + -0.230446 * distance() + 3.88174)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.118444 * (1 - exp(-0.38718 * (distance() + 0.184638))) * pow(width() , 0.025205 * distance() + -0.0854025) * (0.0389212 * thickness() + 0.248941)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00907497 * (1 - exp(-0.804621 * (distance() + 0.741902))) * pow(width() , 0.00618706 * distance() + 0.0433704) * pow(radius_up() , 0.176435)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0358683 * (0.746186 - exp(-1.39673 * (enclose() + 1.00203) - -0.0855494 * (enclose() + 2.72951) * (enclose() + 2.72951)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0421543 * (1 - exp(-0.353716 * (distance() + 0.215774))) * pow(width() , 0.0267738 * distance() + -0.0875897) * (0.0764939 * thickness() + 0.212333) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00964254 * (1 - exp(-0.448409 * (distance() + 0.0357068))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0147789 * (1 - exp(-0.464512 * (distance() + 2.12136))) * pow(width() , -0.00232732 * distance() + 0.011942) * pow(radius_up() , 0.0145442)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0874948 * pow(width() , 0.0545276) * (0.0567623 * thickness() + 0.0825924) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0421543 * (1 - exp(-0.353716 * (distance() + 0.215774))) * pow(width() , 0.0267738 * distance() + -0.0875897) * (0.0764939 * thickness() + 0.212333) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00964254 * (1 - exp(-0.448409 * (distance() + 0.0357068))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0147789 * (1 - exp(-0.464512 * (distance() + 2.12136))) * pow(width() , -0.00232732 * distance() + 0.011942) * pow(radius_up() , 0.0145442)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0874948 * pow(width() , 0.0545276) * (0.0567623 * thickness() + 0.0825924) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.07799 - 0.590455 * distance()) + 0.181867 / pow(distance() , 1.05185)) * 3.66094 * pow((width1() + width2()) / 2 , 0.111112) * (0.0331972 * (thickness1() + thickness2()) / 2 + 0.0140924) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.79475 - 0.6127 * distance()) + 0.0185342 / pow(distance() , 0.934395)) * 1.46417 * pow((width1() + width2()) / 2 , 0.0881724) * pow(radius_up() , 0.0120372)
    }
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    C = length() * (exp(-1.07799 - 0.590455 * distance()) + 0.181867 / pow(distance() , 1.05185)) * 3.66094 * pow((width1() + width2()) / 2 , 0.111112) * (0.0331972 * (thickness1() + thickness2()) / 2 + 0.0140924) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 4.94))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 4.94)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.79475 - 0.6127 * distance()) + 0.0185342 / pow(distance() , 0.934395)) * 1.46417 * pow((width1() + width2()) / 2 , 0.0881724) * pow(radius_up() , 0.0120372)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107628 * (1 - exp(-0.24846 * (distance() + 0.335218))) * pow(width() , 0.0324683 * distance() + -0.121426) * (0.0322187 * thickness() + 0.240524)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0237695 * (1 - exp(-0.345586 * (distance() + 0.0752698)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.12275 * pow(width() , 0.0529119) * (0.0584129 * thickness() + 0.163517)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00305973 * distance() + 0.0351134) * (0.787119 - exp(-(0.237977 * distance() + 0.252043) * (enclose() + 0.375816 * distance() + 1.14354) - (-0.00670374 * distance() + -0.0109781) * (enclose() + 0.992754 * distance() + 3.92935) * (enclose() + 0.992754 * distance() + 3.92935)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107628 * (1 - exp(-0.24846 * (distance() + 0.335218))) * pow(width() , 0.0324683 * distance() + -0.121426) * (0.0322187 * thickness() + 0.240524)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00670301 * (1 - exp(-0.581048 * (distance() + 1.1968))) * pow(width() , 0.00679609 * distance() + 0.00624088) * pow(radius_up() , 0.326667)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.025206 * (0.787119 - exp(-1.44193 * (enclose() + 3.02262) - -0.0444968 * (enclose() + 8.89312) * (enclose() + 8.89312)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107628 * (1 - exp(-0.24846 * (distance() + 0.335218))) * pow(width() , 0.0324683 * distance() + -0.121426) * (0.0322187 * thickness() + 0.240524)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0237695 * (1 - exp(-0.345586 * (distance() + 0.0752698)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.12275 * pow(width() , 0.0529119) * (0.0584129 * thickness() + 0.163517)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00305973 * distance() + 0.0351134) * (0.787119 - exp(-(0.237977 * distance() + 0.252043) * (enclose() + 0.375816 * distance() + 1.14354) - (-0.00670374 * distance() + -0.0109781) * (enclose() + 0.992754 * distance() + 3.92935) * (enclose() + 0.992754 * distance() + 3.92935)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107628 * (1 - exp(-0.24846 * (distance() + 0.335218))) * pow(width() , 0.0324683 * distance() + -0.121426) * (0.0322187 * thickness() + 0.240524)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00670301 * (1 - exp(-0.581048 * (distance() + 1.1968))) * pow(width() , 0.00679609 * distance() + 0.00624088) * pow(radius_up() , 0.326667)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.025206 * (0.787119 - exp(-1.44193 * (enclose() + 3.02262) - -0.0444968 * (enclose() + 8.89312) * (enclose() + 8.89312)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0571966 * (1 - exp(-0.238678 * (distance() + 0.356048))) * pow(width() , 0.0323278 * distance() + -0.119453) * (0.0510834 * thickness() + 0.224053) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0122193 * (1 - exp(-0.33547 * (distance() + 0.077997))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0156678 * (1 - exp(-0.383034 * (distance() + 2.34686))) * pow(width() , -0.00229326 * distance() + 0.0371993) * pow(radius_up() , -0.0117905)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0947157 * pow(width() , 0.0561079) * (0.0505784 * thickness() + 0.102119) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    profile = active_profileid
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0571966 * (1 - exp(-0.238678 * (distance() + 0.356048))) * pow(width() , 0.0323278 * distance() + -0.119453) * (0.0510834 * thickness() + 0.224053) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0122193 * (1 - exp(-0.33547 * (distance() + 0.077997))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 4.94)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0156678 * (1 - exp(-0.383034 * (distance() + 2.34686))) * pow(width() , -0.00229326 * distance() + 0.0371993) * pow(radius_up() , -0.0117905)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0947157 * pow(width() , 0.0561079) * (0.0505784 * thickness() + 0.102119) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 4.94))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 4.94)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal5,ctm,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    C = length() * (exp(-1.51714 - 1.45804 * distance()) + 0.0683233 / pow(distance() , 1.31119)) * 3.65506 * pow((width1() + width2()) / 2 , 0.106305) * (0.0695386 * (thickness1() + thickness2()) / 2 + 0.00506565) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.32))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.32)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-4.24745 - 0.847569 * distance()) + 0.0135098 / pow(distance() , 0.949721)) * 1.60609 * pow((width1() + width2()) / 2 , 0.0692664) * pow(radius_up() , 0.0384934)
    }
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    C = length() * (exp(-1.51714 - 1.45804 * distance()) + 0.0683233 / pow(distance() , 1.31119)) * 3.65506 * pow((width1() + width2()) / 2 , 0.106305) * (0.0695386 * (thickness1() + thickness2()) / 2 + 0.00506565) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.32))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.32)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-4.24745 - 0.847569 * distance()) + 0.0135098 / pow(distance() , 0.949721)) * 1.60609 * pow((width1() + width2()) / 2 , 0.0692664) * pow(radius_up() , 0.0384934)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal5,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 ctm INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0435077 * (1 - exp(-1.84058 * (distance() + 0.349941))) * pow(width() , -0.00343426 * distance() + 0.0105206) * (0.275313 * thickness() + 2.38814)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.108678 * (1 - exp(-1.58771 * (distance() + 0.440297)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.242611 * pow(width() , 0.0180218) * (0.311283 * thickness() + 0.316606)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00882798 * distance() + 0.12002) * (1.27191 - exp(-(-66.6433 * distance() + -36.7701) * (enclose() + 32.0349 * distance() + 100) - (97.074 * distance() + 46.8384) * (enclose() + -42.065 * distance() + -34.7472) * (enclose() + -42.065 * distance() + -34.7472)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0435077 * (1 - exp(-1.84058 * (distance() + 0.349941))) * pow(width() , -0.00343426 * distance() + 0.0105206) * (0.275313 * thickness() + 2.38814)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.060673 * (1 - exp(-1.99203 * (distance() + 0.800224))) * pow(width() , -0.0007159 * distance() + 0.0247238) * pow(radius_up() , 0.00718359)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0820799 * (1.27191 - exp(369.987 * (enclose() + 260.174) - 532.208 * (enclose() + -245.072) * (enclose() + -245.072)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 ctm INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0435077 * (1 - exp(-1.84058 * (distance() + 0.349941))) * pow(width() , -0.00343426 * distance() + 0.0105206) * (0.275313 * thickness() + 2.38814)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.108678 * (1 - exp(-1.58771 * (distance() + 0.440297)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.242611 * pow(width() , 0.0180218) * (0.311283 * thickness() + 0.316606)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00882798 * distance() + 0.12002) * (1.27191 - exp(-(-66.6433 * distance() + -36.7701) * (enclose() + 32.0349 * distance() + 100) - (97.074 * distance() + 46.8384) * (enclose() + -42.065 * distance() + -34.7472) * (enclose() + -42.065 * distance() + -34.7472)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0435077 * (1 - exp(-1.84058 * (distance() + 0.349941))) * pow(width() , -0.00343426 * distance() + 0.0105206) * (0.275313 * thickness() + 2.38814)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.060673 * (1 - exp(-1.99203 * (distance() + 0.800224))) * pow(width() , -0.0007159 * distance() + 0.0247238) * pow(radius_up() , 0.00718359)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0820799 * (1.27191 - exp(369.987 * (enclose() + 260.174) - 532.208 * (enclose() + -245.072) * (enclose() + -245.072)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal5,ctm,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0146637 * (1 - exp(-1.00446 * (distance() + 0.0832814))) * pow(width() , 0.0144222 * distance() + -0.00740978) * (0.246649 * thickness() + 0.107119) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00327272 * (1 - exp(-1.15745 * (distance() + 0.00957874))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0105091 * (1 - exp(-0.994838 * (distance() + 1.05483))) * pow(width() , -0.00403078 * distance() + 0.00979322) * pow(radius_up() , 0.0112691)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0643128 * pow(width() , 0.0796905) * (0.0792828 * thickness() + 0.0132389) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.32)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff INSIDE OF ctm MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0146637 * (1 - exp(-1.00446 * (distance() + 0.0832814))) * pow(width() , 0.0144222 * distance() + -0.00740978) * (0.246649 * thickness() + 0.107119) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00327272 * (1 - exp(-1.15745 * (distance() + 0.00957874))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0105091 * (1 - exp(-0.994838 * (distance() + 1.05483))) * pow(width() , -0.00403078 * distance() + 0.00979322) * pow(radius_up() , 0.0112691)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0643128 * pow(width() , 0.0796905) * (0.0792828 * thickness() + 0.0132389) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.32)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    C = length() * (exp(-1.35565 - 0.852177 * distance()) + 0.103615 / pow(distance() , 1.1257)) * 3.97586 * pow((width1() + width2()) / 2 , 0.0911281) * (0.0476444 * (thickness1() + thickness2()) / 2 + 0.0170901) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.32))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.32)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.81601 - 0.771716 * distance()) + 0.0148767 / pow(distance() , 0.992059)) * 1.5688 * pow((width1() + width2()) / 2 , 0.0763706) * pow(radius_up() , 0.016389)
    }
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    C = length() * (exp(-1.35565 - 0.852177 * distance()) + 0.103615 / pow(distance() , 1.1257)) * 3.97586 * pow((width1() + width2()) / 2 , 0.0911281) * (0.0476444 * (thickness1() + thickness2()) / 2 + 0.0170901) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.32))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.32)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.81601 - 0.771716 * distance()) + 0.0148767 / pow(distance() , 0.992059)) * 1.5688 * pow((width1() + width2()) / 2 , 0.0763706) * pow(radius_up() , 0.016389)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.130939 * (1 - exp(-0.510296 * (distance() + 0.137901))) * pow(width() , 0.0188001 * distance() + -0.0556157) * (0.0402341 * thickness() + 0.257879)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0346424 * (1 - exp(-0.580414 * (distance() + 0.0320583)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142604 * pow(width() , 0.0474389) * (0.0800959 * thickness() + 0.213054)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00594012 * distance() + 0.0727341) * (0.611196 - exp(-(0.00594281 * distance() + 0.460944) * (enclose() + 0.00121809 * distance() + 1.01285) - (0.0222115 * distance() + -0.00725146) * (enclose() + -0.423862 * distance() + 4.82932) * (enclose() + -0.423862 * distance() + 4.82932)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.130939 * (1 - exp(-0.510296 * (distance() + 0.137901))) * pow(width() , 0.0188001 * distance() + -0.0556157) * (0.0402341 * thickness() + 0.257879)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0155088 * (1 - exp(-0.962125 * (distance() + 0.74162))) * pow(width() , 0.00141241 * distance() + 0.0274996) * pow(radius_up() , 0.123211)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0512173 * (0.611196 - exp(-0.490658 * (enclose() + 1.01894) - 0.103806 * (enclose() + 2.71001) * (enclose() + 2.71001)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.130939 * (1 - exp(-0.510296 * (distance() + 0.137901))) * pow(width() , 0.0188001 * distance() + -0.0556157) * (0.0402341 * thickness() + 0.257879)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0346424 * (1 - exp(-0.580414 * (distance() + 0.0320583)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142604 * pow(width() , 0.0474389) * (0.0800959 * thickness() + 0.213054)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00594012 * distance() + 0.0727341) * (0.611196 - exp(-(0.00594281 * distance() + 0.460944) * (enclose() + 0.00121809 * distance() + 1.01285) - (0.0222115 * distance() + -0.00725146) * (enclose() + -0.423862 * distance() + 4.82932) * (enclose() + -0.423862 * distance() + 4.82932)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.130939 * (1 - exp(-0.510296 * (distance() + 0.137901))) * pow(width() , 0.0188001 * distance() + -0.0556157) * (0.0402341 * thickness() + 0.257879)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0155088 * (1 - exp(-0.962125 * (distance() + 0.74162))) * pow(width() , 0.00141241 * distance() + 0.0274996) * pow(radius_up() , 0.123211)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0512173 * (0.611196 - exp(-0.490658 * (enclose() + 1.01894) - 0.103806 * (enclose() + 2.71001) * (enclose() + 2.71001)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0286767 * (1 - exp(-0.448195 * (distance() + 0.174192))) * pow(width() , 0.0219128 * distance() + -0.0603189) * (0.0984079 * thickness() + 0.199321) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00669566 * (1 - exp(-0.530345 * (distance() + 0.030004))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0110133 * (1 - exp(-0.555681 * (distance() + 1.8386))) * pow(width() , -0.00312298 * distance() + 0.0422668) * pow(radius_up() , -0.00703306)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0779788 * pow(width() , 0.0607186) * (0.0555144 * thickness() + 0.0609135) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.32)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.12 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    profile = active_profileid
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0286767 * (1 - exp(-0.448195 * (distance() + 0.174192))) * pow(width() , 0.0219128 * distance() + -0.0603189) * (0.0984079 * thickness() + 0.199321) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00669566 * (1 - exp(-0.530345 * (distance() + 0.030004))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.32)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0110133 * (1 - exp(-0.555681 * (distance() + 1.8386))) * pow(width() , -0.00312298 * distance() + 0.0422668) * pow(radius_up() , -0.00703306)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0779788 * pow(width() , 0.0607186) * (0.0555144 * thickness() + 0.0609135) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 5 + m43ink3 * 6.32))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 5 + m43ink6 * 6.32)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,ctm,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY ctm INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = active_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    C = length() * (exp(-1.88602 - 0.596877 * distance()) + 0.0668433 / pow(distance() , 1.19949)) * 2.76896 * pow((width1() + width2()) / 2 , 0.0552481) * (0.0984006 * (thickness1() + thickness2()) / 2 + 0.0468331) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.869))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.869)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.24569 - 0.523166 * distance()) + 0.00942279 / pow(distance() , 1.31572)) * 0.850676 * pow((width1() + width2()) / 2 , 0.0421343) * pow(radius_up() , 0.130035)
    }
]
CAPACITANCE NEARBODY ctm INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = active_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    C = length() * (exp(-1.88602 - 0.596877 * distance()) + 0.0668433 / pow(distance() , 1.19949)) * 2.76896 * pow((width1() + width2()) / 2 , 0.0552481) * (0.0984006 * (thickness1() + thickness2()) / 2 + 0.0468331) * m36NB_COEF * (1 - m36RS * exp((-m43nbk1 * radius_down()) / (m43nbk2 * distance() + m43nbk3 * 6.869))) * (1 - m36RS * exp((-m43nbk4 * radius_up()) / (m43nbk5 * distance() + m43nbk6 * 6.869)))
    if ((inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * (exp(-3.24569 - 0.523166 * distance()) + 0.00942279 / pow(distance() , 1.31572)) * 0.850676 * pow((width1() + width2()) / 2 , 0.0421343) * pow(radius_up() , 0.130035)
    }
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,ctm,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = active_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0610501 * (1 - exp(-0.69942 * (distance()))) * pow(width() , 0.0142223 * distance() + -0.122885) * (-4.07821 * thickness() + 1.44574)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0383683 * (1 - exp(-0.599591 * (distance() + 0.0972668)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154627 * pow(width() , 0) * (0.325955 * thickness() + 0.17104)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00180665 * distance() + 0.0715617) * (0.86722 - exp(-(-0.0678421 * distance() + 1.32971) * (enclose() + 0.146144 * distance() + 0.263186) - (0.0887739 * distance() + -0.431707) * (enclose() + -0.317501 * distance() + 0.942028) * (enclose() + -0.317501 * distance() + 0.942028)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0610501 * (1 - exp(-0.69942 * (distance()))) * pow(width() , 0.0142223 * distance() + -0.122885) * (-4.07821 * thickness() + 1.44574)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0167487 * (1 - exp(-1.1697 * (distance() + 0.632398))) * pow(width() , 7.78653e-05 * distance() + 0.0136059) * pow(radius_up() , 0.106882)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0395748 * (0.86722 - exp(-1.04477 * (enclose() + 0.876991) - -0.0588566 * (enclose() + -0.391476) * (enclose() + -0.391476)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE ctm metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = active_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0610501 * (1 - exp(-0.69942 * (distance()))) * pow(width() , 0.0142223 * distance() + -0.122885) * (-4.07821 * thickness() + 1.44574)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0383683 * (1 - exp(-0.599591 * (distance() + 0.0972668)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154627 * pow(width() , 0) * (0.325955 * thickness() + 0.17104)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00180665 * distance() + 0.0715617) * (0.86722 - exp(-(-0.0678421 * distance() + 1.32971) * (enclose() + 0.146144 * distance() + 0.263186) - (0.0887739 * distance() + -0.431707) * (enclose() + -0.317501 * distance() + 0.942028) * (enclose() + -0.317501 * distance() + 0.942028)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0610501 * (1 - exp(-0.69942 * (distance()))) * pow(width() , 0.0142223 * distance() + -0.122885) * (-4.07821 * thickness() + 1.44574)
    }
    if ((distance() > 0.0) && (enclose() < EPS_ENC) && (enclose() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.0167487 * (1 - exp(-1.1697 * (distance() + 0.632398))) * pow(width() , 7.78653e-05 * distance() + 0.0136059) * pow(radius_up() , 0.106882)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0395748 * (0.86722 - exp(-1.04477 * (enclose() + 0.876991) - -0.0588566 * (enclose() + -0.391476) * (enclose() + -0.391476)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,ctm,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE ctm tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = active_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0239006 * (1 - exp(-0.669546 * (distance()))) * pow(width() , 0.0104875 * distance() + -0.0416042) * (0.195604 * thickness() + 0.157035) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0115324 * (1 - exp(-0.0798402 * (distance() + 1.87799))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00857469 * (1 - exp(-0.558186 * (distance() + 2.00003))) * pow(width() , 0.00148591 * distance() + 0.00615304) * pow(radius_up() , 0.0213406)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0606791 * pow(width() , 0.0167212) * (0.128176 * thickness() + 0.0544548) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.869))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.869)))
    }
]
CAPACITANCE CROSSOVER FRINGE ctm tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 16 // For reference
    max_caldistance = 4.2 // For reference
    max_width = MAXWIDTH_ctm
    max_radius = MAXRADIUS_ctm
    profile = active_profileid
    max_distance = MAXDISTANCE_ctm
    max_enclose = MAXENCLOSE_ctm
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0239006 * (1 - exp(-0.669546 * (distance()))) * pow(width() , 0.0104875 * distance() + -0.0416042) * (0.195604 * thickness() + 0.157035) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0115324 * (1 - exp(-0.0798402 * (distance() + 1.87799))) * m36IN_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * distance() + m43ink3 * 6.869))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * distance() + m43ink6 * 6.869)))
    }
    if ((distance() > 0.0) && (inside_up() < EPS_ENC) && (inside_up() >= 0) && (radius_up() < max_radius) && (radius_down() >= max_radius)) {
        C = length() * 0.00857469 * (1 - exp(-0.558186 * (distance() + 2.00003))) * pow(width() , 0.00148591 * distance() + 0.00615304) * pow(radius_up() , 0.0213406)
    }
    if (distance() <= 0.0) {
        C = length() * 0.0606791 * pow(width() , 0.0167212) * (0.128176 * thickness() + 0.0544548) * m36IN0_COEF * (1 - m36RS * exp((-m43ink1 * radius_down()) / (m43ink2 * 4.2 + m43ink3 * 6.869))) * (1 - m36RS * exp((-m43ink4 * radius_up()) / (m43ink5 * 4.2 + m43ink6 * 6.869)))
    }
]
#endif // $XCAL_EXCL_PROF_active
//
// Completed on Thu Jun 12 19:24:37 2008 by xCalibrate v2007.4_21.19 Thu Dec  6 22:10:39 PST 2007
//
//attach the following commands to support both xCalibre and XRC tools.
CAPACITANCE CROSSOVER PLATE gate poly
[
    PROPERTY C
      C = 0
]
CAPACITANCE CROSSOVER FRINGE poly tndiff
[
    PROPERTY C
      C = 0
]
CAPACITANCE CROSSOVER FRINGE poly tpdiff
[
    PROPERTY C
      C = 0
]
CAPACITANCE CROSSOVER PLATE mim_block ctm
[ PROPERTY C
C=0
]
CAPACITANCE CROSSOVER FRINGE ctm metal5 INSIDE OF mim_block
[ PROPERTY C
C=0
]

